# TODO

## Coverage Ledger Refresh

- [ ] **Release gate — Reconcile the coverage ledger.** Refresh `coverage/COVERAGE_COMPLETENESS_REPORT.md` and the coverage inventory assignments; `./scripts/coverage-inventory.sh --consistency-checks` currently reports unassigned source paths and exact patch gaps, including recent music, albino vomit, and contamination storage surfaces. Do not claim release readiness from the existing PASS text until the ledger is current and the consistency checks pass.

## Review Follow-Ups (2026-07-10)

- [ ] **P1 — Contamination effects require a drafter.** In `Source/ContaminationEffect.cs`, `ContaminationEffect.ApplyJob` unconditionally writes `pawn.drafter.Drafted = false`, but contaminated animals, hostile/prisoner pawns, and other non-draftable pawns can have no drafter. Guard eligibility/null before starting job-based effects so one pawn cannot throw out `ContaminationEffectManager.Tick` and terminate that map's recurring Zombieland tasks. Cover at least one contaminated non-draftable pawn and confirm later tick tasks still run.
- [ ] **P1 — Make reachable-region caching map-local.** `Tools.PlayerReachableRegions` in `Source/Tools.cs` uses one static region list and refresh deadline for every map. Key both the cached regions and expiry by `Map`, and clear them during load/reset, so multi-map spawn selection never consumes `Region` objects from another map.
- [ ] **P1 — Apply the complete vanilla target predicate before ranking fallback zombies.** The fallback in `Source/Patches_Hostility.cs` currently filters only zombie state, range, hitability, and LOS, then applies `validator` only to the selected candidate. Re-check RimWorld 1.6 `AttackTargetFinder.BestAttackTarget` with the decompiler and apply its full per-candidate constraints (including the caller validator and relevant `TargetScanFlags`) before custom zombie scoring; if the highest-scored zombie is invalid, the next valid one must still be selectable. While touching this scan, replace `OrderBy(...).FirstOrDefault()` with a stable one-pass best-candidate selection that preserves the exact existing score ordering and first-candidate tie behavior.
- [ ] **P1 — Limit Symbiant breaches to actual constructed walls.** `ZombieSymbiant.BreakableConstructedWall` in `Source/ZombieSymbiant.cs` accepts any player-owned, non-door edifice with hit points. Require the vanilla wall property/definition as well, while continuing to exclude natural rock and mineables, so coolers, nutrient paste dispensers, and other edifices cannot be destroyed as walls. Extend the Symbiant expansion contract with a non-wall edifice regression case.
- [ ] **P1 — Stop and drain avoid-grid workers before game-load disposal.** The `GameDataSaveLoader.LoadGame` prefix in `Source/Patches_Startup.cs` must quiesce/recreate `Tools.avoider` before vanilla clears the old maps. Ensure queued and running `ZombieAvoider` work cannot concurrently read disposed map/pathing state, and verify loading while a request is queued or active.
- [ ] **P2 — Clear all static map-owned state on in-process load.** The same load reset must remove old-game entries from `Tools.gridCache`, `TargetCachePatches.playerHostilesWithoutZombies`, the reachable-region cache/deadlines, and all avoider queues/results. Prefer explicit reset methods owned by each subsystem; verify a save with a lower `TicksGame` cannot reuse regions or objects from the previous game.
- [ ] **P2 — Use stat offsets for contamination resistance.** In `1.6/Defs/Zombie_Hediffs.xml`, move all four stage values for `ToxicEnvironmentResistance` (`0.25`, `0.75`, `0.5`, `1`) from `statFactors` into the corresponding `statOffsets` blocks. Biotech's base value is zero, so factors currently grant no resistance to an ordinary pawn and incorrectly scale other resistance sources. Validate the effective stat values with and without another resistance source.
- [ ] **P3 — Correct serum purity probability.** `Recipe_CureZombieInfection.ApplyOnPawn` in `Source/Recipe_CureZombieInfection.cs` uses inclusive `0..100`, producing 101 outcomes and making `purity + 1` outcomes succeed. Use an exact 100-outcome or normalized probability roll so serum labels/XML values (10%, 50%, etc.) match runtime behavior and 100% remains guaranteed.

## Verified Performance Follow-Ups (2026-07-10)

The impact report was a static review, not a profiler capture. Keep these as separate behavior-preserving changes, measure the nontrivial ones against the existing large-zombie fixtures/contracts, and do not broaden them into cadence changes or incremental caches without evidence.

- [ ] **High impact, lifecycle-sensitive — Stop scanning and copying the full avoid grid for every path request.** `TryCreateZombieAvoidGridCustomizer` in `Source/Patches.cs` first scans every cell for a nonzero cost, and `ZombieAvoidGridPathCustomizer` then allocates and fills a map-sized persistent `NativeArray<ushort>` for each request that has avoidance costs. Record whether a completed grid generation has any costs so the zero-cost check is O(1), then design an immutable generation-keyed native snapshot that requests can share. RimWorld's `PathFinderMapData.ParameterizeGridJob` gives scheduled jobs a read-only view of this array, so ownership must be reference-counted (or otherwise proven) across synchronous, asynchronous, resolved, cancelled, and disposed requests; never dispose a generation while a job can still read it. Preserve every clamped cost exactly, add allocation/copy counters before changing ownership, and validate path results plus load and multi-map teardown with the existing avoid-grid path contracts.
- [ ] **Low risk — Do not advance `ZombieWanderer` while the game is paused.** The `TickManager.TickManagerUpdate` prefix in `Source/Patches.cs` calls `ZombieWanderer.processor.MoveNext()` before its pause guard, whereas vanilla returns before ticking when paused. Move the guard ahead of the processor call and verify that pause performs no wander-grid work and that resume immediately continues normal updates. Do not change the unpaused recalculation cadence in the same change.
- [ ] **Low risk — Materialize nearby zombies once during colonist repositioning.** `RepositionColonists` in `Source/TickManager.cs` builds `zombiesNearby` as a lazy enumeration, then re-runs its radius/thing-grid traversal from every flood-fill candidate while scoring cells. Snapshot the nearby zombie positions once before the bounded flood fill and reuse that snapshot for all scores, preserving the current distance formula, candidate traversal, threshold, and tie behavior. Compare the selected destination before and after on the same fixture.
- [ ] **UI — Restrict detailed contamination-overlay thing discovery to the visible cells.** The detailed overlay postfix in `Source/Patches.cs` runs on every `MapInterfaceUpdate` and scans `map.listerThings.AllThings` even though it draws only things whose position is in the current camera rectangle. Enumerate the visible rectangle's `thingGrid` entries instead, deduplicate multi-cell things, and retain the existing origin-in-rectangle, fog, mineable, and contamination filters so the rendered result does not change. Validate multi-cell buildings and things crossing the camera boundary as well as normal overlay screenshots.
- [ ] **Low risk — Remove redundant healer radial scans.** The healer branch in `Source/Zombie.cs` counts injured zombies into an unused local, then immediately enumerates the same radius again to choose healing targets; it also scans even when the pending-heal queue is already full. Delete the unused first traversal and return before target discovery when `healInfo` has reached its existing limit, while preserving the second traversal's descending hediff-count ordering and first-enumerated tie behavior. Re-run the existing healer behavior contract.
- [ ] **Low risk — Remove the thumper wave's per-cell LINQ set allocation.** `ZombieThumper.DamageThings` in `Source/ZombieThumper.cs` calls `ThingsAt(cell).Except(seenThings)` for every cell in an expanding shell. Iterate `ThingsAt(cell)` directly and damage only when `seenThings.Add(thing)` succeeds; this preserves first-seen order and one hit per thing while eliminating a temporary set/enumerator per cell. Re-run the thumper visual-wave damage contract, including a multi-cell target.
