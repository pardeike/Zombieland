# TODO

## Coverage Ledger Refresh

- Refresh `coverage/COVERAGE_COMPLETENESS_REPORT.md` and the coverage inventory assignments before release readiness is claimed again. The current review reported that `./scripts/coverage-inventory.sh --consistency-checks` is failing with unassigned source paths and exact patch gaps, including recent music, albino vomit, and contamination storage surfaces. Do not treat the existing PASS text as release-ready until this ledger is reconciled.
