# Zombieland 1.6 Patch Target Audit

This file records durable source/decompiler dispositions for Harmony patch targets. It is not a work log. Use it when `S-Source-Patch-Audit` needs target-level evidence that is too detailed for `TEST_COVERAGE.md`.

Regenerate the current inventory with:

```bash
scripts/coverage-inventory.sh --patch-groups
scripts/coverage-inventory.sh --dynamic-patches
```

Status meanings:

- `resolved/source`: RimWorld 1.6 target exists and the source-level target expression is stable enough for scenario testing.
- `fanout/source`: the base target shape exists, but runtime should record the concrete generated target list because the patch expands over subclasses or overloads.
- `semantic/source`: the target is compiler-generated or instruction-shape based, but the current lookup is semantic and decompiler evidence shows the expected 1.6 call site.
- `semantic/runtime`: the target exists only through compiler-generated closure names or IL-shape searches; runtime patch-application evidence is required.
- `obsolete/disposed`: the old target is absent in RimWorld 1.6 and needs an explicit replacement/vanilla-behavior disposition.
- `deleted/obsolete`: the obsolete source patch class was removed after replacement evidence or negative-space disposition was recorded.
- `dependency/unavailable`: the target belongs to an optional mod, DLC, inactive configuration surface, or content setup that is not currently exercised; keep the source audit and require content-specific runtime evidence later.
- `pending`: not audited yet.

## Symbiant Map-Removal Targets

| Location | Patch class | Status | RimWorld 1.6 evidence | Coverage disposition |
| --- | --- | --- | --- | --- |
| `Source/Patches.cs:138` | `Game_DeinitAndRemoveMap_Patch` | `resolved/runtime` | Current Steam RimWorld 1.6 MVID `967ddb80559449f0a776dafa26a855d1` exposes the single nonvirtual `Verse.Game.DeinitAndRemoveMap(Map, bool)` overload. After null and `maps.Contains` guards it calls `MapDeiniter.Deinit`, whose `PassPawnsToWorld` path snapshots every `map.mapPawns.AllPawns`, despawns each, and passes it to `WorldPawns`. Direct callers include `MapParent.CheckRemoveMapNow`, `MapParent.PostRemove`, `PocketMapUtility.DestroyPocketMap`, colony movement, and debug removal. Automatic `CheckRemoveMapNow` covers Camp, CaravansBattlefield, DestroyedSettlement, Settlement, Site, and SpaceMapParent predicate overrides. The prefix mirrors both vanilla guards, enumerates spawned and held map-bound Symbiants, and uses the safe-severance destroy path before vanilla snapshots pawns. A non-relocation `DeSpawn` is a final self-destruct boundary for other extraction paths, but explicitly stands down during `Pawn.Kill` and the Symbiant's internal room reseed. The cleanup removes the destroyed pawn from `WorldPawns` before discarding it. | Historical operation `op_b474f364760444fcb2a3a71723497a9a` proved the common pre-deinit boundary with a remote host. Corrected `zombieland/symbiant_unsafe_damage_contract` operation `op_437ba7510405436591d10e0102b1d8a4` kept both the linked host and Symbiant spawned on a disposable Site when the real `Current.Game.DeinitAndRemoveMap(map, false)` began. The host survived with unchanged injuries, its hediff/link cleared, vanilla transferred it alive to `WorldPawns`, the Symbiant was destroyed/discarded and never entered `WorldPawns`, map count changed `2 -> 1`, and forced cleanup removed all residue. The 2026-07-12 rerun `op_01016aeca2cd4df284b2ebc0c012de69` additionally proved that real `Pawn.Kill` still produces a valid corpse containing the Symbiant and that a genuine non-relocation despawn leaves the Symbiant destroyed, discarded, and absent from `WorldPawns`. Runtime patch ownership was present and operation-correlated warning-or-higher logs were empty (`op_c5041a1ccf9b411291b21b44c8dd65c3`). Automatic caller predicates remain source/decompiler-backed rather than separately forced. |
| `Source/Patches.cs:150` | `GravshipUtility_AbandonMap_Patch` | `resolved/runtime` | Current Steam RimWorld 1.6 MVID `967ddb80559449f0a776dafa26a855d1` exposes `RimWorld.GravshipUtility.AbandonMap(Map)` as token `0601361E`. Its body snapshots `map.mapPawns.AllPawnsSpawned`, despawns each pawn, calls `ThingUtility.DestroyOrPassToWorld`, and only then calls `map.Parent.Abandon(true)`. `ThingDef` defaults `bringAlongOnGravship=true`; `Gravship.ShouldBringOnGravship(Thing, IntVec3)` admits things with that flag, and `GenerateGravship` can despawn accepted pawns before `AbandonMap`. The Symbiant def therefore explicitly sets `bringAlongOnGravship=false`, and the prefix destroys, removes from `WorldPawns`, and discards the map-bound Symbiant before vanilla's snapshot. | The same refreshed operation kept Odyssey active with `skipped=false`: the real `GravshipUtility.AbandonMap` route ran on a disposable Site map containing only the Symbiant; the loaded def flag and reflected vanilla `ShouldBringOnGravship` both returned false. The remote host survived spawned and uninjured, its hediff/link cleared, the Symbiant was destroyed and absent from `WorldPawns`, the disposable map/parent were removed, the original current map remained intact, and all fixture/world-pawn residue arrays were empty. This invokes the exact destructive abandonment method, not a full constructed-gravship takeoff/cutscene. |
| `Source/Patches.cs:162` | `VoidAwakeningUtility_CloseMetalHell_Patch` | `resolved/runtime` | Current Steam RimWorld 1.6 `VoidAwakeningUtility.CloseMetalHell(Pawn)` first reads `pawn.MapHeld` and returns when it is null or not a pocket map. Only then does it snapshot `mapPawns.AllPawns`, despawn every entry, append them to `GameComponent_Anomaly.metalHellPawns`, and destroy the pocket map. The prefix mirrors that pocket-map guard before safely destroying map-bound Symbiants, so calls where vanilla intentionally does nothing cannot sever an ordinary-map bond. | Operation `op_799175b3bb604228ad18e3c034b9800b` confirmed runtime patch ownership and directly invoked the prefix with a linked host on ordinary map `0`: `isPocketMap=false`, both hediff states stayed true, the host survived, the Symbiant stayed spawned/undestroyed, and the link remained exact. A complete Metal Hell enter/close/return story remains source/decompiler-backed rather than claimed as live behavior coverage. |
| `Source/Patches.cs:182` | `MapParent_Abandon_Patch` | `fanout/runtime` | The same assembly exposes virtual base `MapParent.Abandon(bool)` (`0601592E`) plus exactly two true overrides: `Settlement.Abandon(bool)` (`060159F3`) and `ArchotechSettlement.Abandon(bool)` (`060159B9`). `Settlement` calls the base and then can create `AbandonedSettlement`, so canceling only the base would let the override continue; `ArchotechSettlement` bypasses the base entirely. `TargetMethods()` now patches the base and every concrete method whose `GetBaseDefinition()` is that base method, so confirmation cancellation happens at the most-derived entry point and confirmed re-entry destroys/discards the Symbiant once before the original override runs. Its warning gate covers every live map-bound Symbiant, including one held by another thing rather than spawned. | The same live operation reported Zombieland Harmony ownership on all three installed implementations. The disposable Site gravship route exercised the base method after the gravship prefix had already removed the Symbiant. Settlement/Archotech confirmation-dialog behavior, including the broader held-Symbiant warning gate, is source/decompiler-backed plus runtime target-ownership evidence; a direct disposable Settlement confirmation-click scenario remains additive rather than claimed as live behavior coverage. |

## Symbiant Damage-Facade Targets

| Location | Patch class | Status | RimWorld 1.6 evidence | Coverage disposition |
| --- | --- | --- | --- | --- |
| `Source/Patches_SymbiantHealth.cs` | `Thing_TakeDamage_Symbiant_Patch` | `resolved/runtime` | Current Steam RimWorld 1.6 MVID `967ddb80559449f0a776dafa26a855d1` exposes nonvirtual `Verse.Thing.TakeDamage(DamageInfo)` as `06003960`. It applies def multipliers, calls `PreApplyDamage`, runs `dinfo.Def.Worker.Apply`, sends watchers/instigator hediff callbacks, then calls `PostApplyDamage` and returns the final `DamageResult`. The type-guarded Prefix snapshots Symbiant hediff state and the `Priority.Last` Postfix drains the custom pool from final `totalDamageDealt`, after vanilla/CE armor and effects. | Final `zombieland/symbiant_unsafe_damage_contract` operation `op_f9e1a90c825d4729b76d00a948086b20` passed actual Cut/Flame accounting, zero-health Stun, anatomy pruning, inert echoes, recovery, dormancy, lethal collapse, and zero residue. Real projectile/melee/explosion delivery passed in `zombieland/symbiant_combat_isolation_contract` operation `op_6c0ad2f2027f4ceaa9fdca08cc876b1d`. |
| `Source/Patches_SymbiantHealth.cs` | `HediffSet_GetPartHealth_Symbiant_Patch` | `resolved/source` | Installed `HediffSet.GetPartHealth(BodyPartRecord)` is `06002F0C`; it directly subtracts every `Hediff_Injury.Severity` and otherwise has no opt-out for an effect-only pawn. The prefix returns the current part's normal maximum only for a live `ZombieSymbiant`. | The damage facade records part/max equality after real Cut and Flame workers while exact anatomy-only injuries are pruned. |
| `Source/Patches_SymbiantHealth.cs` | `Pawn_HealthTracker_ShouldBeDead_Symbiant_Patch` | `resolved/source` | Installed `Pawn_HealthTracker.ShouldBeDead()` is `0600332D`; it checks lethal hediffs, required capacities, core efficiency, and the lethal-injury threshold. The prefix prevents only ordinary health evaluation from bypassing a live Symbiant's custom pool. | Pool exhaustion still invokes Zombieland's explicit collapse; explicit `Pawn.Kill` remains separately covered as a valid corpse-producing boundary. |
| `Source/Patches_SymbiantHealth.cs` | `Pawn_HealthTracker_ShouldBeDowned_Symbiant_Patch` | `resolved/runtime` | Installed `Pawn_HealthTracker.ShouldBeDowned()` is `0600332C`; pain shock, inability to remain awake, or loss of Moving returns true. The prefix returns false only for a live Symbiant, complementing the existing `MakeDowned` guard. | The unsafe-damage facade proved Cut/Flame/Stun never leave the Symbiant downed while the real stunner remains available. |
| `Source/Patches_SymbiantHealth.cs` | `SummaryHealthHandler_SummaryHealthPercent_Symbiant_Patch` | `resolved/source` | Installed `SummaryHealthHandler.SummaryHealthPercent` is property `1700089D`; vanilla multiplies every hediff's `SummaryHealthPercentImpact` and caches the result. The prefix exposes the authoritative shared-pool fraction for a live Symbiant. | The facade snapshot requires summary health to equal `SharedHealthCurrent / SharedHealthMax` independently of retained effect hediffs. |

## Dynamic Patch Summary

Current `scripts/coverage-inventory.sh --dynamic-patches` summary:

| Owner | Rows | Lookup classes |
| --- | ---: | --- |
| Core | 21 | 7 multi-target, 4 closure-search, 7 typed-reflection, 2 dynamic-other, 1 external-string |
| Specialized | 4 | 2 typed-reflection, 1 multi-target, 1 dynamic-other |
| Contamination | 18 | 12 closure-search, 6 multi-target |
| Optional integrations | 13 | 9 external type-name, 3 external string, 1 multi-target |

## Core And Specialized Dynamic Targets

| Location | Patch class | Status | RimWorld 1.6 evidence | Coverage disposition |
| --- | --- | --- | --- | --- |
| `Source/CustomPawnState.cs:16` | `Pawn_DrawTracker_Constructor_Patch` | `resolved/runtime` | `Verse.Pawn_DrawTracker..ctor(Pawn pawn)` exists: `88df42e0e943404ba42043aebd37585c:06002675:M`. Runtime `zombieland/area_workflow_state actionMode:visual-support` records the installed Zombieland postfix on the constructor. | Covered for custom draw-tracker state: a spawned human had `CustomPawnState`, its extension-backed infection state round-tripped `None -> Infecting -> None`, and a spawned normal zombie used `ZombieLeaner` while `InfectionState()` returned `Infected`. No warning-or-higher logs. |
| `Source/Patches.cs:700` | `Pawn_EquipmentTracker_YieldGizmos_Patch` | `resolved/runtime` | Generated local function exists as `Verse.Pawn_EquipmentTracker.<GetGizmos>g__YieldGizmos\|30_0(ThingWithComps eq, KeyBindingDef preferredHotKey = null)`: `88df42e0e943404ba42043aebd37585c:060032E9:M`. RimWorld 1.6 `Pawn_EquipmentTracker.GetGizmos()` kept the outer `IEnumerable GetGizmos()` signature but added an early `PrimaryEq.CompGetEquippedGizmosExtra()` pass before `PawnAttackGizmoUtility.CanShowEquipmentGizmos()`. That does not replace the Zombieland hook: `CompEquippable.CompGetEquippedGizmosExtra()` is empty in Core, its only 1.6 override is the dev-only ability-reloadable command, and `Chainsaw.GetGizmos()` still emits the drafted on/off command and fuel gizmo through the patched verb-yield path. Runtime `S-Defense-Room` evidence selected drafted colonist `Thing_Human6920` from `ZL_Defense_Room_base.rws` and listed RimWorld's selection-scoped gizmos through `rimworld/list_selected_gizmos`; the equipped running chainsaw contributed an enabled `Command_Action` with hotkey `Misc6` and description `Turn chainsaw off to conserve fuel`. | Covered for the generated equipment-gizmo yield path exposing chainsaw controls on selected drafted pawns. The new 1.6 equipped-extra prelude is recorded as negative-space evidence, not a new Zombieland behavior gap. Direct chainsaw gizmo behavior remains covered by defense-room action runs. |
| `Source/Patches.cs:1173` | `JobDriver_AttackStatic_MakeNewToils_b__1_Patch` | `semantic/runtime-covered` | `Verse.AI.JobDriver_AttackStatic.MakeNewToils()` exists: `88df42e0e943404ba42043aebd37585c:06005E29:M`. Decompiler comparison showed 1.4 used `<MakeNewToils>b__1` for the tick delegate, while installed 1.6 uses `<MakeNewToils>b__2(int delta)`: `88df42e0e943404ba42043aebd37585c:06005E2E:M`. The target lookup now selects the one-`int` generated delegate instead of a stale closure name. | Covered 2026-05-30 in `S-Defense-Room`: `actionMode:wallDoorPressure` confirmed Harmony owner `net.pardeike.zombieland` patched the installed 1.6 tick delegate and a zombie left real `AttackStatic` one tick after the target door opened. 2026-05-31 `ZL_Core_RoomCorridor_Gauntlet_after_runtime_00.rws` added scenario co-location evidence: a normal zombie kept a real `AttackStatic` job against a wooden door through save-load and post-reload runtime with no warning-or-higher logs. |
| `Source/Patches.cs:1634` | `Building_Door_PawnCanOpen_Patch` | `fanout/runtime` | Base virtual method exists as `RimWorld.Building_Door.PawnCanOpen(Pawn p)`: `88df42e0e943404ba42043aebd37585c:0600EDA4:M`. Patch fans out over `Building_Door` subclasses. Crash bisection proved the old `FreePassage` read inside this postfix was unsafe: RimWorld 1.6 `Building_Door.FreePassage` (`88df42e0e943404ba42043aebd37585c:170024B2:P`) can call `WillCloseSoon` (`88df42e0e943404ba42043aebd37585c:170024B4:P`), which scans nearby pawns and calls `PawnCanOpen` recursively. | Fixed by removing the `FreePassage` read. `zombieland/contamination_hoard_pather_failure_contract` plus 360 real follow-up ticks survived with the full path patch surface enabled; focused avoidance coverage proved avoid-grid door blocking. 2026-05-31 room/corridor gauntlet co-located door pressure with wall barrier, corpse, downed pawn, fire attachment, pheromones, save-load, and post-reload continuation: the door took real damage (`90/104 -> 86/104`) and warning-or-higher logs stayed empty. |
| `Source/Patches.cs:1841` | `WorkGiver_Scanner_HasJobOnCell_Patches` | `fanout/runtime` | Base virtual method exists as `RimWorld.WorkGiver_Scanner.HasJobOnCell(Pawn pawn, IntVec3 c, Boolean forced)`: `88df42e0e943404ba42043aebd37585c:0600818E:M`; the 1.6 body still returns `JobOnCell(pawn, c, forced) != null`. Runtime `S-Settings-Persistence actionMode:behavior` recorded 12 installed concrete targets, including `WorkGiver_BuildRoof`, construct/remove/smooth floor/wall, remove roof, clear pollution, and clear snow/sand overrides. | Covered for installed fanout and avoid predicate behavior. With `autoAvoidZombies=true`, `betterZombieAvoidance=true`, a tracking zombie, and avoid cost `972` at target cell `(100,100)`, the private patch `ShouldAvoid` returned true for non-forced work; forced work and `autoAvoidZombies=false` returned false. No warning-or-higher logs. |
| `Source/Patches.cs:1885` | `WorkGiver_Scanner_JobOnCell_Patches` | `fanout/runtime` | Base virtual method exists as `RimWorld.WorkGiver_Scanner.JobOnCell(Pawn pawn, IntVec3 cell, Boolean forced)`: `88df42e0e943404ba42043aebd37585c:0600818F:M`; the 1.6 base body still returns null. Runtime `S-Settings-Persistence actionMode:behavior` recorded 12 installed concrete targets, including `WorkGiver_BuildRoof`, construct/remove/smooth floor/wall, remove roof, clear pollution, and clear snow/sand overrides. | Covered for installed fanout and null-job guard predicate behavior. The same avoided-cell fixture made the private patch `ShouldAvoid` return true only for non-forced auto-avoid-enabled work and false for forced work or `autoAvoidZombies=false`. No warning-or-higher logs. |
| `Source/Patches.cs:1929` | `WorkGiver_Scanner_HasJobOnThing_Patches` | `fanout/runtime` | Base virtual method exists as `RimWorld.WorkGiver_Scanner.HasJobOnThing(Pawn pawn, Thing t, Boolean forced)`: `88df42e0e943404ba42043aebd37585c:0600818B:M`; the 1.6 body still returns `JobOnThing(pawn, t, forced) != null`. Runtime `S-Settings-Persistence actionMode:behavior` recorded 73 installed concrete targets, including animal-resource/slaughter, deconstruct/remove/uninstall, warden chat, and many other thing workgiver overrides. | Covered for installed fanout and avoid predicate behavior. A spawned `Steel` marker in the avoided cell made the private patch `ShouldAvoid` return true for non-forced auto-avoid-enabled thing work and false for forced work or `autoAvoidZombies=false`. Existing behavior coverage in the same run also proved Zombieland's own serum extraction and double-tap `HasJobOnThing` setting gates against real corpses. No warning-or-higher logs. |
| `Source/Patches.cs:1973` | `WorkGiver_Scanner_JobOnThing_Patches` | `fanout/runtime` | Base virtual method exists as `RimWorld.WorkGiver_Scanner.JobOnThing(Pawn pawn, Thing t, Boolean forced)`: `88df42e0e943404ba42043aebd37585c:0600818D:M`; the 1.6 base body still returns null. Runtime `S-Settings-Persistence actionMode:behavior` recorded 97 installed concrete targets, including animal, construction-delivery, tame/train, slaughter, and many other thing workgiver overrides. The active Vehicle Framework pass then proved this fanout must skip open generic scanner targets: `Vehicles.WorkGiver_CarryToVehicle<T>.JobOnThing` caused Harmony wrapper generation to fail until the broad WorkGiver fanout was filtered to concrete non-generic methods. | Covered for installed fanout and null-job guard predicate behavior. The same `Steel` marker in the avoided cell made the private patch `ShouldAvoid` return true only for non-forced auto-avoid-enabled thing work and false for forced work or `autoAvoidZombies=false`. Existing behavior coverage in the same run also proved Zombieland's own `JobOnThing` returns `ExtractZombieSerum` and `DoubleTap` under persisted colonist toggles. The Vehicle Framework follow-up rebuilt and relaunched cleanly with the open generic target skipped and no warning-or-higher logs. |
| `Source/Patches.cs:2946` | `Toils_Combat_FollowAndMeleeAttack_KillIncappedTarget_Patch` | `resolved/runtime` | Compiler-generated closure exists as `Verse.AI.Toils_Combat.<>c__DisplayClass6_0.<FollowAndMeleeAttack>b__0`: `88df42e0e943404ba42043aebd37585c:0600606A:M`. Runtime `S-Hostility-Area-Workflow actionMode:targeting` recorded the installed Harmony target as `Verse.AI.<>c__DisplayClass6_0::<FollowAndMeleeAttack>b__0(System.Int32 delta)` with owner `net.pardeike.zombieland` and the Zombieland transpiler. | Covered for installed patch ownership and behavior: a drafted colonist kept a real `AttackMelee` job against a health/public-downed normal zombie with `job.killIncappedTarget=false`, entered attack/cooldown by tick 10, and raised zombie injury from `0` to `21.5750351`. |
| `Source/Patches.cs:2995` | `JobDriver_AttackStatic_TickAction_Patch` | `resolved/runtime` | Candidate members exist: `JobDriver_AttackStatic.MakeNewToils()` (`88df42e0e943404ba42043aebd37585c:06005E29:M`), `Toils_Jump.<>c__DisplayClass5_0.<JumpIfTargetDowned>b__0` (`88df42e0e943404ba42043aebd37585c:060060C1:M`), `TargetingParameters.CanTarget` (`88df42e0e943404ba42043aebd37585c:06013B21:M`), `VerbUtility.AllowAdjacentShot` (`88df42e0e943404ba42043aebd37585c:06005806:M`), `Stance_Warmup.StanceTick` (`88df42e0e943404ba42043aebd37585c:060033ED:M`), and `Verb_Shoot.WarmupComplete` (`88df42e0e943404ba42043aebd37585c:06005841:M`). Runtime recorded seven installed Zombieland-owned transpiler targets: the two `JobDriver_AttackStatic` generated delegates, `Toils_Jump.<JumpIfTargetDowned>b__0`, `TargetingParameters.CanTarget`, `VerbUtility.AllowAdjacentShot`, `Stance_Warmup.StanceTick`, and `Verb_Shoot.WarmupComplete`. Installed `Pawn_MindState.MeleeThreatStillThreat` (`88df42e0e943404ba42043aebd37585c:17000FB1:P`) no longer reads `Pawn.Downed`, so the instruction-dependent filter correctly did not patch it. | Covered for final runtime fanout and behavior: a drafted rifle colonist kept a real `AttackStatic` job against a health/public-downed normal zombie with `job.killIncappedTarget=false` and entered firing cooldown by tick 129. Damage is not asserted for the ranged case because projectile travel can outlive the branch proof; job continuation and cooldown prove the downed replacement path. |
| `deleted from Source/Patches.cs` | `PawnGraphicSet_HeadMatAt_Patch` | `deleted/obsolete` | `Verse.PawnGraphicSet.HeadMatAt` exists in RimWorld 1.4 (`0f44124915ec469b8a0ef6daf05b1c19:06001336:M`) but not RimWorld 1.6. RimWorld 1.6 uses `Verse.PawnRenderNode_Head.GraphicFor(Pawn pawn)`: `88df42e0e943404ba42043aebd37585c:060027BD:M`, which returns `null` when `pawn.health.hediffSet.HasHead` is false. | Disposed as a removed 1.4 target with replacement runtime evidence, then deleted from current source. Zombieland patches `PawnRenderNode_Head.GraphicFor`, and `zombieland/area_workflow_state actionMode:render-node-graphics` proved custom head routing plus the headless-zombie null result with no warning-or-higher logs. |
| `Source/Patches.cs:3689` | `PawnApparelGenerator_PossibleApparelSet_PairOverlapsAnything_Patch` | `resolved/runtime` | `RimWorld.PawnApparelGenerator.PossibleApparelSet.PairOverlapsAnything(ThingStuffPair pair)` exists in 1.6 as `88df42e0e943404ba42043aebd37585c:0600C436:M`, and its body matches the 1.4 target role. Runtime `zombieland/settings_state actionMode:apparel-generation` recorded the installed Zombieland prefix on this target. | Covered for the generated-apparel exclusion contract: from entry scene after play-data load, an ordinary `Apparel_PlateArmor`/`Gold` pair still returned non-overlap on an empty set while Zombieland `Apparel_BombVest` was identified as a zombie def and forced to overlap. Runtime `S-Settings-Persistence actionMode:modal` separately proves apparel blacklist modal persistence. Warning-or-higher logs were empty. |
| `Source/Patches.cs:5563` | `GenClamor_DoClamor_Patch` | `resolved/runtime` | Four declared `Verse.GenClamor.DoClamor` overloads exist in RimWorld 1.6: `88df42e0e943404ba42043aebd37585c:0600546B:M`, `0600546C:M`, `0600546D:M`, and `0600546E:M`. A 2026-06-01 negative-space pass compared RimWorld 1.4 and 1.6 and found the four-overload surface still present; the simple `DoClamor(Thing, float, ClamorDef)` body is unchanged. Runtime `S-Core-Horde-Loop` sub-contract evidence reran `zombieland/zombie_clamor_suppression` from `EMPTY.rws`. | Covered for zombie clamor emission suppression: an ordinary human clamor reached the listener (`humanEffectCount=1`), while normal zombie, spitter, and symbiant sources all produced zero listener effects. The separate `Pawn_HearClamor_Patch` row owns the 1.6 hearer-side semantic fix. |
| `Source/Patches.cs:5591` | `Pawn_RecordsTracker_Increment_Patch` | `resolved/runtime` | Targets exist: `Pawn_RecordsTracker.AddTo` (`88df42e0e943404ba42043aebd37585c:0600CDCB:M`), `RecordsTickUpdate(Int32 interval)` (`88df42e0e943404ba42043aebd37585c:0600CDC9:M`), and `Increment(RecordDef def)` (`88df42e0e943404ba42043aebd37585c:0600CDCA:M`). A 2026-06-01 negative-space pass found the 1.4 wrapper `RecordsTick()` became 1.6 `RecordsTickInterval(int delta)`, but both wrappers still call `RecordsTickUpdate(80)`, and `RecordsTickUpdate` itself has unchanged signature/body across 1.4/1.6. `AddTo` and `Increment` retain signatures, with only log-message string formatting changed. Runtime `S-Core-Horde-Loop` sub-contract evidence ran `zombieland/zombie_records_suppression` from `EMPTY.rws`. | Covered for record mutation suppression: human `Kills` record mutated `0 -> 3` through real `Increment` plus `AddTo`, while the normal zombie raw/public/int values all stayed `0`. The renamed tick wrapper does not create a new runtime obligation because the patched mutation sink remains `RecordsTickUpdate`. |
| `Source/Patches_Hostility.cs:450` | `AttackTargetFinder_FriendlyFire_Patch` | `resolved/runtime` | Targets exist: `AttackTargetFinder.FriendlyFireConeTargetScoreOffset(IAttackTarget target, IAttackTargetSearcher searcher, Verb verb)` (`88df42e0e943404ba42043aebd37585c:06006559:M`) and `FriendlyFireBlastRadiusTargetScoreOffset(IAttackTarget target, IAttackTargetSearcher searcher, Verb verb)` (`88df42e0e943404ba42043aebd37585c:06006558:M`). A 2026-06-01 negative-space pass compared RimWorld 1.4 and 1.6 and found both signatures and bodies unchanged; both still read `IntVec3.GetThingList(map)`/`item.GetThingList(map)`, so the `RemoveZombies` insertion point remains semantically valid. Runtime `S-Hostility-Area-Workflow actionMode:targeting` confirmed both installed methods have `net.pardeike.zombieland` owners and the transpiler helper removes zombies while preserving non-zombie pawns. A focused rerun from `EMPTY` invoked both real private score helpers with the same armed player searcher and nearby map targets: zombie blast/cone offsets were positive (`10.8`/`1.08907568`), while a nearby colonist produced friendly-fire penalties (`-7.2`/`-0.7260504`). | Covered for installed patch ownership, helper behavior, and real private score-method semantics in the mixed-targeting fixture. The current proof is score-level rather than a full projectile target-choice UI scenario, which remains additive. |

## Contamination Dynamic Targets

| Location | Patch class | Status | RimWorld 1.6 evidence | Coverage disposition |
| --- | --- | --- | --- | --- |
| `Source/ContaminationPatchesBuilding.cs:344` | `Misc_Building_Patch` | `resolved/runtime` | Targets exist and were recorded as installed at runtime. A 2026-06-01 rw14/rw16 decompiler surface compare confirmed unchanged signatures for `Building_FermentingBarrel.TakeOutBeer()` (`40ca93f8c2cf4459bd8a72c25d1bfb81:0600EE3A:M`) and `Building_GeneAssembler.Finish()` (`40ca93f8c2cf4459bd8a72c25d1bfb81:0600EE7A:M`). Existing semantic evidence records `TakeOutBeer()` as the beer `ThingMaker.MakeThing` product path and `Finish()` as the Xenogerm product path. | Covered by `zombieland/contamination_building_products_contract` for both current targets. Fermenting barrel at contamination `0.66` returned unspawned `Beer x17` at contamination `0.109560013`, matching `fermentingBarrelTransfer=0.166000009`, and reset to `0.55044`. With active Biotech, the GeneAssembler branch used real `GeneAssembler`/`Genepack`/`Xenogerm` defs: assembler contamination moved `0.62 -> 0.5208`, spawned `Xenogerm` contamination was `0.0991999954`, reset was true, and warning-or-higher logs contained only the known old `EMPTY` hidden-ritual migration warning. |
| `Source/ContaminationPatchesBuilding.cs:371` | `JobDriver_Repair_MakeNewToils_Patch` | `semantic/runtime` | `ListerBuildingsRepairable.Notify_BuildingRepaired(Building b)` exists (`88df42e0e943404ba42043aebd37585c:0600B1FB:M`). RimWorld 1.6 `JobDriver_Repair.<>c__DisplayClass8_0.<MakeNewToils>b__1` (`88df42e0e943404ba42043aebd37585c:06006DBB:M`) calls it, and runtime recorded the installed Zombieland transpiler on that generated tick delegate. | Covered by `zombieland/contamination_repair_contract`: a real `Repair` job on a damaged player wall reached the patched closure at tick 24, HP moved `194 -> 195`, wall contamination moved `0.75 -> 0.72504`, and worker contamination moved `0.15 -> 0.17496`, matching `repairTransfer=0.0416`; warning-or-higher logs were empty. |
| `Source/ContaminationPatchesBuilding.cs:405` | `JobDriver_RepairMech_MakeNewToils_Patch` | `resolved/runtime` | RimWorld `1.6.4850` (`832a8ce3293a45e386bdef129cf24a57`) keeps `JobDriver_RepairMech.MakeNewToils()` behind `ModLister.CheckBiotech("Mech repair")`; its generated closure `JobDriver_RepairMech.<MakeNewToils>b__10_2(Int32 delta)` now calls the current one-argument `MechRepairUtility.RepairTick(Pawn)` (`832a8ce3293a45e386bdef129cf24a57:06007787:M`). The older two-argument overload remains obsolete and is no longer the closure call target. Runtime recorded the installed Zombieland transpiler on the generated tick delegate after retargeting. | Covered by `zombieland/contamination_repair_contract` under active Biotech after the `1.6.4850` retarget: a real `RepairMech` job on a wounded Scyther reached the patched closure at tick 44, repaired the wound `1 -> 0`, moved worker contamination `0.15 -> 0.169020012`, moved mech contamination `0.75 -> 0.73098`, and warning-or-higher logs were empty from the resaved `EMPTY` fixture. |
| `Source/ContaminationPatchesFilth.cs:33` | `JobDriver_CleanFilth_MakeNewToils_Patch` | `semantic/runtime` | `JobDriver_CleanFilth.MakeNewToils()` exists (`88df42e0e943404ba42043aebd37585c:0600725D:M`). RimWorld 1.6 changed the clean toil from `tickAction` to `tickIntervalAction(int delta)`, scales cleaning work by `delta`, and added a destroyed-target queue jump, but the generated closure still calls `Filth.ThinFilth()`: `JobDriver_CleanFilth.<>c__DisplayClass7_0.<MakeNewToils>b__1(int delta)` (`88df42e0e943404ba42043aebd37585c:06007262:M`). Runtime C4 recorded the installed Zombieland transpiler on that closure and cleaned real `Filth_Dirt` through `WorkGiver_CleanFilth`/`Clean`, destroying the filth after 26 ticks. | Covered by `zombieland/contamination_clean_filth_contract`: contaminated filth `0.64` transferred `0.026624` to the worker, matching `filthTransfer=0.0416`; `TicksSinceThickened=601` satisfied the vanilla clean-job gate, and warning-or-higher logs were empty. |
| `Source/ContaminationPatchesFilth.cs:168` | `JobDriver_Vomit_MakeNewToils_Patch` | `semantic/runtime` | `JobDriver_Vomit.MakeNewToils()` exists (`88df42e0e943404ba42043aebd37585c:06007978:M`). RimWorld 1.6 changed the vomit toil from `tickAction` to `tickIntervalAction(int delta)`, replaced the modulo check with `pawn.IsHashIntervalTick(150, delta)`, and made food-need access nullable; the generated closure still calls `FilthMaker.TryMakeFilth`: `JobDriver_Vomit.<MakeNewToils>b__4_1(int delta)` (`88df42e0e943404ba42043aebd37585c:0600797B:M`). Runtime C3 recorded the installed Zombieland prefix/postfix on that generated tick delegate. | Covered by `zombieland/contamination_filth_leavings_contract`: a pawn with contamination `0.58` ran real `JobDefOf.Vomit`, produced `Filth_Vomit` at target cell `(116,100)` after 94 ticks, and the filth contamination `0.023896` matched `filthEqualize=0.0412`; warning-or-higher logs were empty. |
| `Source/ContaminationPatchesFilth.cs:225` | `Filth_MakeThing_Patch` | `semantic/runtime` | Targets exist: `Pawn_FilthTracker.GainFilth` two-argument overload (`88df42e0e943404ba42043aebd37585c:0600CCAF:M`) and `FilthMaker.TryMakeFilth` overloads, including the out-filth overload `88df42e0e943404ba42043aebd37585c:0600F4E3:M` and the private core spawn path `88df42e0e943404ba42043aebd37585c:0600F4E7:M`. Runtime C3 `zombieland/contamination_filth_leavings_contract` recorded installed Zombieland transpilers on both `Pawn_FilthTracker.GainFilth(ThingDef, IEnumerable<string>)` and `FilthMaker.TryMakeFilth(..., out Filth, ...)`, drove `TryMakeFilth` through blood, vomit, birth, flame ash, liquid projectile, tunnel hive, explosion, comp-spawned, damage, execution, and butcher blood paths, and drove `GainFilth` directly through a spawned pawn's carried-filth tracker. | Covered for both target-method families. A pawn at contamination `0.58` gained carried `Filth_Dirt` at contamination `0.023896`, matching `filthEqualize=0.0412`; source-setter branches remain tracked by their owning patch rows. Warning-or-higher logs were empty. |
| `Source/ContaminationPatchesIncidents.cs:11` | `Skyfaller_SpawnThings_Patch` | `semantic/runtime` | `Skyfaller.SpawnThings()` exists (`88df42e0e943404ba42043aebd37585c:0600F866:M`) and still places inner things through `GenPlace.TryPlaceThing`; the 1.6 generated callback `Skyfaller.<SpawnThings>b__59_0` (`88df42e0e943404ba42043aebd37585c:0600F870:M`) is installed with the Zombieland postfix. | Covered by `zombieland/contamination_generation_handoffs_contract`: direct `SpawnThings()` probes created `ShipChunkIncoming -> ShipChunk` at `(172,108)` and `MeteoriteIncoming -> MineableSteel` at `(177,108)`, and both products received `meteoriteAdd=0.6`; warning-or-higher logs were empty. |
| `Source/ContaminationPatchesMisc.cs:51` | `Verb_MeleeAttack_ApplyMeleeDamageToTarget_Patch` | `resolved/runtime` | Base abstract target exists (`Verb_MeleeAttack.ApplyMeleeDamageToTarget`, `88df42e0e943404ba42043aebd37585c:06013BC1:M`) and 1.6 implementations include `Verb_MeleeApplyHediff` (`06013BB9:M`) and `Verb_MeleeAttackDamage` (`06013BCF:M`). `zombieland/contamination_melee_equalize_contract` applied real melee damage and matched expected attacker/target contamination `0.132800013`/`0.667199969`. | Covered for `Verb_MeleeAttackDamage`; combat phases should still verify special zombie/melee verb interactions outside contamination transfer. |
| `Source/ContaminationPatchesMisc.cs:119` | `Pawn_CarryTracker_TryStartCarry_Patch_Patch` | `semantic/runtime` | Two overloads exist: `TryStartCarry(Thing item)` (`88df42e0e943404ba42043aebd37585c:060032B6:M`) and `TryStartCarry(Thing item, Int32 count, Boolean reserve)` (`88df42e0e943404ba42043aebd37585c:060032B7:M`). Runtime C1 `zombieland/contamination_carry_tracker_contract` recorded installed Zombieland prefix/postfix owners on both overloads. It called the one-argument path for contaminated `Steel x20`, verified the same item became the carried thing, and then proved real carry tick equalization changed pawn/item contamination to `0.060200002`/`0.6398` while the item remained held. The same contract called the count/reserve overload with `Steel x20`, requested `5`, and received an unspawned carried `Steel x5` plus spawned leftover `Steel x15`; both retained contamination `0.7`. | Covered for both direct carry overloads and the downstream carried-item contamination read. Full haul-job pickup flow remains a broader C1/player-flow smoke, not a separate patch-target gap. Warning-or-higher logs were empty. |
| `Source/ContaminationPatchesMisc.cs:298` | `ThingComp_MakeThing_Patch` | `fanout/runtime` | Candidate methods exist for removable shells, eggs, gathered resources, mech carrier spawn setup, plantable items, pollution pump, refuelable destroy, spawners, tree connection, and waste producer. Representative ids: `CompChangeableProjectile.RemoveShell` (`0600FE89:M`), `CompEggLayer.ProduceEgg` (`0600FF85:M`), `CompHasGatherableBodyResource.Gathered` (`06010282:M`), `CompMechCarrier.PostSpawnSetup` (`0601046F:M`), `CompPlantable.DoPlant` (`06010739:M`), `CompPollutionPump.Pump` (`0601079C:M`), `CompRefuelable.PostDestroy` (`060108BE:M`), `CompSpawnerItems.SpawnItems` (`06010B14:M`), `CompSpawner.TryDoSpawn` (`06010AC1:M`), `CompTreeConnection.CompTick` (`06010E18:M`), and `CompWasteProducer.ProduceWaste` (`06010F63:M`). Decompiler check showed 1.6 `CompMechCarrier.PostSpawnSetup` no longer calls `ThingMaker.MakeThing`; current decompiler evidence also confirms `CompPollutionPump.Pump()` and `CompWasteProducer.ProduceWaste(int)` are Wastepack-producing Biotech tails. Runtime `C0-state-serializer` recorded 10 installed Zombieland transpiler targets from the remaining call-bearing subset. | Covered for installed fanout plus one real component branch. `zombieland/contamination_core_contract` sheared a contaminated player-faction alpaca through real `CompHasGatherableBodyResource.Gathered`: parent contamination moved `0.72 -> 0.60048`, and produced `WoolAlpaca x45` received `0.119520009`, matching `generalTransfer`. Pollution-pump and waste-producer runtime behavior remains dependency-bound by active Biotech/Wastepack content, while other component branches are additive under the owning C3 product row. No warning-or-higher logs. |
| `Source/ContaminationPatchesMisc.cs:466` | `Jobdriver_ClearPollution_Spawn_Patch` | `resolved/runtime` | `JobDriver_ClearPollution.MakeNewToils()` exists (`88df42e0e943404ba42043aebd37585c:06007270:M`) and directly calls `ClearPollutionAt(base.TargetLocA, pawn.Map)` followed by `GenSpawn.Spawn(ThingDefOf.Wastepack, ...)`. Active-DLC decompiler refresh also confirms `PollutionGrid.SetPolluted` is still Biotech-gated, and Biotech is active in the final loadout. | Covered by `zombieland/contamination_clear_pollution_contract` in the final DLC stretch. The fixed contract waited `sourceDerivedLaborTicks + 120`; a real `ClearPollution` job cleared polluted ground `0.66` after `5604` ticks and spawned exactly one `Wastepack` at contamination `0.264000028`, matching `wastePackAdd=0.4`. The only warning present after loading `EMPTY` was the pre-existing DLC migration warning `A hidden ritual precept was missing, adding: Funeral (no corpse)`. |
| `Source/ContaminationPatchesMisc.cs:490` | `MedicalRecipesUtility_GenSpawn_Spawn_Patches` | `semantic/runtime` | Targets exist and were recorded as installed at runtime: `MedicalRecipesUtility.SpawnNaturalPartIfClean` (`88df42e0e943404ba42043aebd37585c:0600C2AD:M`) and `SpawnThingsFromHediffs` (`88df42e0e943404ba42043aebd37585c:0600C2AE:M`). Runtime C3 exercised both branches through `zombieland/contamination_misc_spawn_handoffs_contract`: the hediff branch added an `ArchotechArm` hediff to a contaminated pawn arm, while the natural-part branch temporarily set a clean arm's `spawnThingOnRemoved` to `ComponentIndustrial`, called the real `SpawnNaturalPartIfClean`, and restored the def immediately. | Covered for both installed transpiler targets. `SpawnThingsFromHediffs` moved pawn contamination `0.69 -> 0.65182` and spawned one `ArchotechArm` at `0.03818`, matching measured holder-aware source loss `0.0381799936`; `SpawnNaturalPartIfClean` moved pawn contamination `0.64 -> 0.604586661` and spawned one `ComponentIndustrial` at `0.035413336`, matching measured source loss `0.0354133248`. Warning-or-higher logs were empty. |
| `Source/ContaminationPatchesMisc.cs:561` | `JobDriver_AffectFloor_MakeNewToils_Patch` | `semantic/runtime` | `JobDriver_AffectFloor.MakeNewToils()` exists (`88df42e0e943404ba42043aebd37585c:06006CED:M`). RimWorld 1.6 changed the work toil from `tickAction` to `tickIntervalAction(int delta)`, scales work and construction learning by `delta`, and now clears optional sand when `clearSnow` is true; the generated tick delegate still calls `DoEffect(base.TargetLocA)` when work completes. Runtime recorded the installed Zombieland transpiler on that generated tick delegate. | Covered by `zombieland/contamination_terrain_construction_contract`: a real `RemoveFloor` job removed contaminated `WoodPlankFloor` at `(112,100)` on tick 45, and worker contamination moved `0 -> 0.0172800012`, matching ground contamination `0.8 * floorAdd=0.0216`; warning-or-higher logs were empty. |
| `Source/ContaminationPatchesMisc.cs:588` | `JobDriver_DisassembleMech_MakeNewToils_Patch` | `resolved/runtime` | `JobDriver_DisassembleMech.MakeNewToils()` exists (`88df42e0e943404ba42043aebd37585c:060072D2:M`), starts with `ModLister.CheckBiotech("Disassemble mech")`, then creates each `MechanitorUtility.IngredientsFromDisassembly(Mech.def)` product with `ThingMaker.MakeThing`, assigns `stackCount`, and places it through `GenPlace.TryPlaceThing(...)`. | Covered by `zombieland/contamination_biotech_product_handoffs_contract`: a real Scyther disassembly finished after about 313 ticks and produced Steel x30, Plasteel x30, ComponentIndustrial x2, and SubcoreRegular x1. Product contamination totaled `0.1909947`, worker gain was `0.19099471`, matching `transferFactor=0.166000009`, and warning-or-higher logs contained only the known old `EMPTY` hidden-ritual migration warning. |
| `Source/ContaminationPatchesPlants.cs:13` | `GenSpawn_Spawn_Replacement_Patch` | `resolved/runtime` | `TunnelJellySpawner.Spawn(Map map, IntVec3 loc)` exists (`88df42e0e943404ba42043aebd37585c:0600F477:M`) and still creates `ThingDefOf.InsectJelly`, splits the remaining `jellyCount` into spawned stacks, and places each stack through `GenSpawn.Spawn(thing, CellFinder.RandomClosewalkCellNear(...), map)`. A 2026-06-01 runtime verifier exposed that the old expression-based target installed on `GroundSpawner.Spawn`; the patch now binds `TunnelJellySpawner.Spawn(Map, IntVec3)` directly. | Covered by `zombieland/contamination_misc_spawn_handoffs_contract`: from `ZL_CE_EMPTY_00`, the installed target was `TunnelJellySpawner.Spawn`, two `InsectJelly` stacks totaling 18 spawned at contamination `0.2552`, matching `0.58 * jellyAdd=0.44`, and warning-or-higher logs were empty. |
| `Source/ContaminationPatchesPlants.cs:57` | `JobDriver_PlantWork_MakeNewToils_Patch` | `semantic/runtime` | `JobDriver_PlantWork.MakeNewToils()` exists (`88df42e0e943404ba42043aebd37585c:060079EC:M`) and its 1.6 generated tick delegate `JobDriver_PlantWork.<>c__DisplayClass11_0.<MakeNewToils>b__1` (`88df42e0e943404ba42043aebd37585c:060079F6:M`) still creates the main harvested product through `ThingMaker.MakeThing(...)`. A 2026-06-01 1.4/1.6 decompiler pass found the 1.6 branch added `ThingComp.GetAdditionalHarvestYield()` products through a second `ThingMaker.MakeThing(...)` call, so the patch was changed from a single-call transpiler to a scoped harvest-plant context around the closure. | Runtime regression from `EMPTY` with Core/Zombieland/RimBridgeServer harvested real rice through `zombieland/contamination_plant_harvest_contract`: source plant `0.64`, `plantTransfer=0.4`, product `RawRice x6` at `0.256`, and warning-or-higher logs empty. A follow-up run against the local `ZLTestSupport` fixture plant `ZLTestAdditionalHarvestPlant` loaded from `ZL_CE_EMPTY_00` proved the 1.6 additional-yield branch: the same contract produced main `RawPotatoes x1` at `0.256` and additional `WoodLog x2` at `0.153599992`, matching sequential `TransferContamination` semantics from the remaining plant contamination. |
| `Source/ContaminationPatchesPlants.cs:88` | `ThingMaker_MakeThing_PlantHarvestContext_Patch` | `semantic/runtime` | `ThingMaker.MakeThing(ThingDef, ThingDef)` exists (`88df42e0e943404ba42043aebd37585c:060039BB:M`). The patch is intentionally inert except while `JobDriver_PlantWork_MakeNewToils_Patch` has set an active harvested-plant context for the real harvest closure. | Supporting patch for `JobDriver_PlantWork_MakeNewToils_Patch`; the fixture run above proves both main and additional products created through `ThingMaker.MakeThing` inside the harvest context receive contamination, while the patch remains inert outside an active harvest context. Do not add a separate generic `ThingMaker.MakeThing` behavior claim. |
| `Source/ContaminationPatchesPlants.cs:132` | `JobDriver_PlantSow_MakeNewToils_Patch` | `semantic/runtime` | `JobDriver_PlantSow.MakeNewToils()` exists (`88df42e0e943404ba42043aebd37585c:060079D1:M`) and the init action calls `GenSpawn.Spawn(job.plantDefToSow, ...)`. Runtime C3 sowed rice through the real Sow job and reloaded `Thing_Plant_Rice6926` with contamination `0.207860008`. | Covered in `ZL_Contamination_Persistence_20_world_products.rws`; worker equalization was proved before save. |
| `Source/ContaminationPatchesPollutionSnow.cs:55` | `JobDriver_ClearSnow_MakeNewToils_Patch` | `semantic/runtime` | RimWorld 1.4 used `JobDriver_ClearSnow.MakeNewToils()` (`0f44124915ec469b8a0ef6daf05b1c19:06003A48:M`) with a `tickAction` that only called `snowGrid.SetDepth(...)`. RimWorld 1.6 replaced it with `JobDriver_ClearSnowAndSand.MakeNewToils()` (`88df42e0e943404ba42043aebd37585c:0600728B:M`), uses `tickIntervalAction`, selects a snow/sand effect, and clears `snowGrid.SetDepth(...)` plus `sandGrid?.SetDepth(...)`. The Zombieland target still selects the generated delegate by the `SnowGrid.SetDepth` call-site, so both snow and sand clearing pass through the same worker-contamination transfer before vanilla depth clearing. Runtime C4 cleared snow through the real job, transferred contamination `0.0443520024` to worker `Thing_Human6903`, and reloaded the cleared snow cell `(101,100)` at ground contamination `0.72`. The first 2026-06-01 `mode:sand` pass correctly skipped under the pre-DLC loadout because usable `SandGrid` depth storage is Odyssey-gated. The active-Odyssey final stretch then fixed the contract setup to stage sand on `Soil` rather than rejected `TerrainDefOf.Sand`, widened the wait for interval ticks, and proved `mode:sand` cleared sand depth `1 -> 0` after `248` ticks with worker contamination `0 -> 0.0443520024`; the snow control rerun cleared depth `1 -> 0` after `388` ticks with the same transfer. | Covered in `ZL_Contamination_Persistence_25_cleaning_environment.rws`, `EV.RUNTIME.C4.SANDGRID_ABSENT`, and `EV.RUNTIME.C4.ODYSSEY_SAND`; no open snow/sand clearing gap remains for the current official-DLC loadout. |

## Optional Integration Dynamic Targets

Combat Extended is installed locally from upstream `CombatExtended-Continued/CombatExtended` commit `6380aeedeb24c49d20309fe233f268003c5f436d` and active before Zombieland in the normal `ModsConfig.xml`. Save Our Ship 2 is installed locally from `Bqr1s/SaveOurShip2` commit `7c9e1ec3`; source/decompiler evidence resolves the current SoS2 targets, and active runtime now uses local `ZLTestSoS2Content` plus CE `SOS2Compat.dll` to prove the SoS2 space-floater path, current ship-generation conversion/event-zombie path, and hologram exclusion path. Earlier CE SOS2Compat and `PlantPot_Bonsai` startup failures are retained as packaging history, not current Zombieland blockers. RimConnect is installed locally from `Better-Scenes/RimConnect-mod` commit `6b9c0a0` and manually built into `Mods/RimConnect/1.6/Assemblies`. Active startup resolves and patches the current action-list and settings-window targets. A connected follow-up temporarily rebuilt the external RimConnect DLL with DEBUG localhost `BASE_URL` and used `scripts/rimconnect-local-backend.py`; RimConnect showed Connected, the Items command-option window opened, and warning-or-higher logs stayed empty. The external release DLL and normal `ModsConfig.xml` were restored afterward.

| Location | Patch class | Status | Target expression | Coverage disposition |
| --- | --- | --- | --- | --- |
| `Source/CETools.cs:35` | `CETools_Patch1` | `resolved/runtime` | `CombatExtended.HarmonyCE.Harmony_DamageWorker_AddInjury_ApplyDamageToPart.ArmorReroute` with legacy fallback to `CombatExtended.Harmony...ArmorReroute` | Current CE moved the Harmony patch namespace to `CombatExtended.HarmonyCE`. Active `startup_support_state` resolved the current ArmorReroute target and installed `CETools_Patch1::Prefix(DamageInfo& dinfo)`. `zombieland/area_workflow_state actionMode:ce-compat` invoked the patched CE ArmorReroute with `SurgicalCut` and a null pawn; it did not throw and returned `SurgicalCut` amount `7` with deflected/diminished false. |
| `Source/CETools.cs:62` | `CETools_Patch2` | `resolved/runtime` | `CombatExtended.ProjectileCE.Launch(Thing, Vector2, float shotAngle, float shotRotation, float shotHeight, float shotSpeed, Thing equipment, float distance)` plus declared CE overrides with the same signature | Current CE still matches the typed eight-argument base lookup, and a source audit found `ProjectileCE_Bursting.Launch(...)` declares the same physics-launch override without calling the patched base eight-argument method. Zombieland now installs the postfix on the base launch plus declared CE overrides; the postfix binds the named `shotAngle`, `shotHeight`, and `shotSpeed` arguments and ignores `shotRotation`. `actionMode:ce-compat` records installed postfix targets on both `ProjectileCE.Launch` and `ProjectileCE_Bursting.Launch`, launches `Bullet_303British_FMJ` through `CombatExtended.BulletCE` with a human shooter changing `797` pheromone cells and a spitter changing `0`, then launches `Bullet_25x40mmGrenade_HE_TFuzed` through `ProjectileCE_Bursting` with `797` changed pheromone cells. `actionMode:ce-ranged-projectiles` now also runs a real `CombatExtended.Verb_ShootCE.TryCastShot` with `Gun_BoltActionRifle`: the CE verb boundary does not enter Zombieland's vanilla `Verb_LaunchProjectile` forced-miss helper, but it spawns `CombatExtended.BulletCE` with intended target set to the zombie, consumes ammo `8 -> 7`, hits the Zombieland CE launch postfix, changes `797` pheromone cells, and leaves warning-or-higher logs empty. |
| `Source/CETools.cs:115` | `CETools_Patch3` | `resolved/runtime` | `CombatExtended.ArmorUtilityCE.GetAfterArmorDamage(DamageInfo, Pawn, BodyPartRecord, bool& armorDeflected, bool& armorReduced, bool& shieldAbsorbed)` | Active startup support resolved and installed the CE after-armor prefix. Active `zombieland/damage_tanky_armor` under CE reduced tanky shield durability without pawn injury, proving the tanky branch through the CE damage stack. `actionMode:ce-compat` also invoked CE after-armor damage on a spitter with `Bullet` amount `22`; Zombieland reduced the returned amount to `2` with `armorReduced=true`, proving the fixed reduced-`DamageInfo` propagation. |
| `Source/CETools.cs:176` | `CETools_Patch4` | `resolved/runtime` | `CombatExtended.CompAmmoUser.Notify_ShotFired(int ammoConsumedPerShot)` | Current CE removed the old `CompAmmoUser.TryReduceAmmoCount` target; ammo use now flows through `Notify_ShotFired(int)`, which active startup support resolved and installed. `actionMode:ce-compat` invoked `Notify_ShotFired(int)` on the current CE turret ammo comp: with `reducedTurretConsumption=1`, ammo stayed `8 -> 8`; with the setting disabled, the same path consumed `8 -> 7`. |
| `Source/RimConnectSupport.cs:209` | `RimConnection_Settings_CommandOptionSettings_Patch` | `resolved/runtime` | `RimConnection.Settings.CommandOptionSettings.DoWindowContents` | Local RimConnect commit `6b9c0a0` source/decompiler resolves `CommandOptionSettings.DoWindowContents(Rect)`, and the method still uses the `180f`/`198f`/height-subtraction layout constants shifted by the Zombieland transpiler. Active startup support reports the Zombieland transpiler installed on this method. The connected local-backend pass rendered RimConnect as Connected, made the Items button available, opened `RimConnection.Settings.CommandOptionSettings`, and `get_ui_layout` saw the Loyalty Store command-option window with category tabs, shifted header row, scroll view, item labels, and numeric cost/cooldown fields. Warning-or-higher logs stayed empty. |
| `Source/SoSTools.cs:138` | `RimWorld_ShipCombatManager_GenerateShip_Patch` | `resolved/runtime` | Current `SaveOurShip2.ShipInteriorMod2.GenerateShip(...)` with legacy fallback to `RimWorld.ShipCombatManager.GenerateShip` | Local SoS2 commit `7c9e1ec3` decompiler resolves `ShipInteriorMod2.GenerateShip(ShipDef, Map, PassingShip, Faction, Lord, out List<Building>, ...)`; the old Core `RimWorld.ShipCombatManager` target is absent. Zombieland now prefers the current SoS2 target and parses arguments without compile-referencing SoS2 types. Active runtime `zombieland/sos2_ship_hologram_state` loaded `ZL_SOS2_SPACE_00`, reflected this current GenerateShip target, spawned 8 hostile ship pawn shapes, and proved the postfix produced 3 converted ship zombies plus 15 generated event zombies with warning-or-higher logs limited to the known external English translation warning. |
| `Source/SoSTools.cs:208` | `SaveOurShip2_GenerateSpaceSubMesh_GenerateMesh_Patch` | `resolved/runtime` | Current `SaveOurShip2.GenerateSpaceSubMesh.Prefix(SectionLayer, MeshParts)` with legacy fallback to `SaveOurShip2.GenerateSpaceSubMesh.GenerateMesh` | Local SoS2 decompiler shows the current `Prefix` owns the `Printer_Mesh.PrintMesh` call for the `EmptySpace` planet material, so the existing altitude-replacement transpiler still targets the right semantic call site. Active runtime `EV.RUNTIME.SOS2.SPACE_FLOATERS` proves the visual mesh-order path: a real SoS2 `EmptySpaceMap` had `OuterSpaceBiome`, 100/10 floaters ready after stepped ticks, screenshot evidence, and only the known external translation warning. |

## Pending Dynamic Targets

No dynamic Harmony patch rows remain pending. The remaining dynamic risks are runtime-only: core/specialized `semantic/runtime` rows, fanout target-list recording, and optional integration runtime checks when their dependencies are installed.

## Class-Level Context Rows

`scripts/coverage-inventory.sh --patch-groups` also reports class-level `base` rows. These are Harmony type-context declarations only; they are not independent method patches and do not create separate runtime obligations beyond their concrete child rows.

| Location | Patch class | Status | Disposition |
| --- | --- | --- | --- |
| `Source/Assets.cs:10` | `Assets` | `base/context` | Container for `Assets.LoadAssetBundle`; the concrete `Source/Assets.cs:19` row covers `UIRoot_Entry.Init` runtime asset loading. |
| `Source/Patches.cs:533` | `InfestationCellFinder_CalculateLocationCandidates_Patch` | `base/context` | Container for `CalculateLocationCandidates_Prefix` and `GetScoreAt_Prefix`; both concrete rows are covered by the defense-room infestation-thumper scenario. |
| `Source/ZombieAreaManager.cs:256` | `AreaManager_Patches` | `base/context` | Container for `AreaManager.CanMakeNewAllowed` and `AreaManager.SortAreas`; both concrete rows are covered by the area workflow scenario. |
| `Source/ZombieAreaManager.cs:272` | `Dialog_ManageAreas_Patches` | `base/context` | Container for `Dialog_ManageAreas` constructor and `DoWindowContents`; both concrete rows are covered by the area workflow dialog evidence. |

## 2026-07-13 Release-Delta Exact-Class Reconciliation

This table expands the release delta's aggregate/range descriptions into exact current patch class names. It is an inventory and ownership reconciliation, not a claim of runtime evidence beyond the detailed rows elsewhere in this audit. `scripts/coverage-inventory.sh --patch-row-gaps` and `--consistency-checks` use these exact names together with the semantic owners in `coverage/ZL_COVERAGE_INDEX.tsv`.

| Location | Exact patch class | Semantic owner | Evidence disposition |
| --- | --- | --- | --- |
| `Source/AlbinoScreamVomit.cs:59` | `JobDriver_Vomit_AlbinoScreamDuration_Patch` | `C.SPECIAL.ZOMBIES` | Dynamic generated-delegate adapter for extending the albino scream-vomit init duration; source ownership is explicit, while runtime behavior remains with the special-zombie scenario. |
| `Source/CETools.cs:308` | `CETools_Patch5_SymbiantCanHit` | `INT.CombatExtended`; `C.SPECIAL.SYMBIANT_INFESTATION` | Dynamic CE `CanHitTargetFrom(IntVec3, LocalTargetInfo)` adapter; detailed active-CE evidence remains in the aggregate Symbiant CE row below. |
| `Source/CETools.cs:338` | `CETools_Patch6_SymbiantCanHitWithReport` | `INT.CombatExtended`; `C.SPECIAL.SYMBIANT_INFESTATION` | Dynamic CE `CanHitTargetFrom` report-overload adapter; detailed active-CE evidence remains in the aggregate Symbiant CE row below. |
| `Source/CETools.cs:373` | `CETools_Patch7_SymbiantShootLine` | `INT.CombatExtended`; `C.SPECIAL.SYMBIANT_INFESTATION` | Dynamic CE `TryFindCEShootLineFromTo` adapter; detailed active-CE evidence remains in the aggregate Symbiant CE row below. |
| `Source/CETools.cs:423` | `CETools_Patch8_SymbiantProjectileImpact` | `INT.CombatExtended`; `C.SPECIAL.SYMBIANT_INFESTATION` | Dynamic CE `ProjectileCE.ImpactSomething` logical-cell adapter; detailed active-CE evidence remains in the aggregate Symbiant CE row below. |
| `Source/CETools.cs:490` | `CETools_Patch9_SymbiantCollisionBounds` | `INT.CombatExtended`; `C.SPECIAL.SYMBIANT_INFESTATION` | Dynamic CE `CE_Utility.GetBoundsFor(Thing)` recentering adapter; detailed active-CE evidence remains in the aggregate Symbiant CE row below. |
| `Source/CETools.cs:520` | `CETools_Patch10_SymbiantRayCast` | `INT.CombatExtended`; `C.SPECIAL.SYMBIANT_INFESTATION` | Dynamic CE `ProjectileCE.RayCast`/`CheckCellForCollision` adapter; detailed active-CE evidence remains in the aggregate Symbiant CE row below. |
| `Source/ContaminationStorageRange.cs:151` | `ContaminationStorageSettings_DefaultConstructor_Patch` | `G.CONTAMINATION.C0_STATE` | Static registration hook for default storage settings; source-owned by contamination range persistence. |
| `Source/ContaminationStorageRange.cs:161` | `ContaminationStorageSettings_OwnerConstructor_Patch` | `G.CONTAMINATION.C0_STATE` | Static registration hook for owner-backed storage settings; source-owned by contamination range persistence. |
| `Source/ContaminationStorageRange.cs:170` | `ContaminationStorageSettings_ExposeData_Patch` | `G.CONTAMINATION.C0_STATE` | Static save/load hook for the storage contamination range. |
| `Source/ContaminationStorageRange.cs:179` | `ContaminationStorageSettings_CopyFrom_Patch` | `G.CONTAMINATION.C0_STATE` | Static copy hook preserving the custom range across `StorageSettings.CopyFrom`. |
| `Source/ContaminationStorageRange.cs:188` | `ContaminationStorageSettings_AllowedToAccept_Patch` | `G.CONTAMINATION.C1_STACKS` | Static acceptance gate applying the configured range after vanilla accepts a thing. |
| `Source/ContaminationStorageRange.cs:200` | `ContaminationThingFilterUI_DoThingFilterConfigWindow_Patch` | `G.CONTAMINATION.C6_UI_QUEST` | Static UI transpiler inserting the contamination range control into the thing-filter window. |
| `Source/ContaminationStorageRange.cs:252` | `ContaminationITabStorage_Constructor_Patch` | `G.CONTAMINATION.C6_UI_QUEST` | Static storage-tab sizing hook for the added range control. |
| `Source/Patches.cs:2005` | `PathFinder_ForceCompleteScheduledJobs_ZombieAvoidGrid_Patch` | `D.PATHING.DOOR_AVOIDANCE` | Static lease-flush hook at the documented scheduled-job completion boundary; focused avoidance evidence owns behavior. |
| `Source/Patches.cs:2015` | `PathFinder_Dispose_ZombieAvoidGrid_Patch` | `D.PATHING.DOOR_AVOIDANCE` | Static owner-disposal hook after vanilla completes outstanding path jobs; focused avoidance evidence owns behavior. |
| `Source/Patches.cs:7379` | `Pawn_PathFollower_CostToPayThisTick_Patch` | `C.CORE.ZOMBIE_LOOP` | Static adaptive-ticking movement-payment adapter; detailed runtime evidence is recorded in the adaptive ticking re-audit below. |
| `Source/Patches_MainMenuBackground.cs:84` | `UIRoot_Entry_DoMainMenu_Patch` | `A.STARTUP.LOAD` | Static main-menu transpiler that inserts the Zombieland background/title post-effect after vanilla background drawing. |
| `Source/ZombieSymbiantCombat.cs:527` | `ShootLeanUtility_CalcShootableCellsOf_Symbiant_Patch` | `C.SPECIAL.SYMBIANT_INFESTATION` | Static logical-cell shootable-cell adapter; detailed normal-profile evidence remains in the aggregate Symbiant combat row below. |
| `Source/ZombieSymbiantCombat.cs:540` | `Verb_TryFindShootLineFromTo_Symbiant_Patch` | `C.SPECIAL.SYMBIANT_INFESTATION` | Static logical-cell ranged/melee shoot-line adapter; detailed normal-profile evidence remains in the aggregate Symbiant combat row below. |
| `Source/ZombieSymbiantCombat.cs:593` | `ShotReport_HitReportFor_SymbiantCell_Patch` | `C.SPECIAL.SYMBIANT_INFESTATION` | Static bound-cell shot-report adapter preserving owner-dependent report fields. |
| `Source/ZombieSymbiantCombat.cs:645` | `ReachabilityUtility_CanReach_Symbiant_Patch` | `C.SPECIAL.SYMBIANT_INFESTATION` | Static logical melee reachability adapter. |
| `Source/ZombieSymbiantCombat.cs:661` | `ReachabilityImmediate_CanReachImmediate_Symbiant_Patch` | `C.SPECIAL.SYMBIANT_INFESTATION` | Static immediate-reachability adapter for stored melee target cells. |
| `Source/ZombieSymbiantCombat.cs:677` | `Pawn_PathFollower_StartPath_SymbiantMelee_Patch` | `C.SPECIAL.SYMBIANT_INFESTATION` | Static melee path-start repair for invalidated stored stand/target pairs; this is the post-review addition not present in the earlier aggregate count. |
| `Source/ZombieSymbiantCombat.cs:697` | `JobGiver_AIFightEnemy_MeleeAttackJob_Symbiant_Patch` | `C.SPECIAL.SYMBIANT_INFESTATION` | Static melee-job B/C preparation adapter. |
| `Source/ZombieSymbiantCombat.cs:707` | `AttackTargetFinder_BestAttackTarget_SymbiantContext_Patch` | `C.SPECIAL.SYMBIANT_INFESTATION` | Static target-scan context lifecycle adapter. |
| `Source/ZombieSymbiantCombat.cs:746` | `AttackTargetFinder_GetRandomShootingTargetByScore_Symbiant_Patch` | `C.SPECIAL.SYMBIANT_INFESTATION` | Static weighted-shooting-pool injection adapter. |
| `Source/ZombieSymbiantCombat.cs:763` | `Verb_LaunchProjectile_TryCastShot_SymbiantContext_Patch` | `C.SPECIAL.SYMBIANT_INFESTATION` | Static projectile-cast binding context adapter. |
| `Source/ZombieSymbiantCombat.cs:777` | `Projectile_Launch_SymbiantCell_Patch` | `C.SPECIAL.SYMBIANT_INFESTATION` | Static logical used-target launch adapter. |
| `Source/ZombieSymbiantCombat.cs:790` | `Projectile_ImpactSomething_SymbiantCell_Patch` | `C.SPECIAL.SYMBIANT_INFESTATION` | Static owner restoration at logical-cell projectile impact. |
| `Source/ZombieSymbiantCombat.cs:805` | `Pawn_DrawTracker_Notify_MeleeAttackOn_Symbiant_Patch` | `C.SPECIAL.SYMBIANT_INFESTATION` | Static logical-cell melee draw/jitter adapter. |

## Static Patch Audit Slices

Use `scripts/coverage-inventory.sh --static-summary` to regenerate this grouping. The static inventory currently has 323 rows. Audit these by slice and scenario evidence, not as one giant file pass. The scanner ignores commented-out Harmony blocks and the deleted obsolete patch classes do not become test obligations.

| Static slice | Rows | Primary scenario ownership |
| --- | ---: | --- |
| `Source/ContaminationStorageRange.cs` | 7 | `S-Source-Patch-Audit`; `C0-state-serializer`; `C1-stack-container`; `C6-ui-quest` |
| `Source/Patches_MainMenuBackground.cs` | 1 | `S-Source-Patch-Audit`; `S-Startup-ColdLoad` |
| `Source/Patches_Music.cs` | 3 | `S-Source-Patch-Audit`; settings music contract |
| `Source/Patches_Startup.cs` | 7 | `S-Source-Patch-Audit`; `S-Startup-ColdLoad` |
| `Source/Patches_SymbiantHealth.cs` | 5 | `S-Source-Patch-Audit`; `S-Symbiant-Symbiosis` |
| `Source/ThingIngestionPatches.cs` | 1 | `S-Source-Patch-Audit`; contamination ingestion |
| `Source/ZombieSymbiantCombat.cs` | 13 | `S-Source-Patch-Audit`; `S-Symbiant-Symbiosis` |
| `contamination-static` | 68 | `S-Contamination-Persistence` |
| `patches-records-clamor-misc` | 101 | `S-Core-Horde-Loop`, `S-Incident-Threat` |
| `patches-damage-infection-hostility` | 16 | `S-Core-Horde-Loop`, `S-Special-Gauntlet` |
| `patches-chainsaw-equipment-jobs` | 21 | `S-Defense-Room` |
| `patches-zombie-lifecycle-combat` | 9 | `S-Core-Horde-Loop`, `S-Special-Gauntlet` |
| `patches-render-specials-apparel` | 24 | `S-Special-Gauntlet`, `S-Settings-Persistence` |
| `patches-avoidance-doors-workgivers` | 15 | `S-Settings-Persistence`, `S-Core-Horde-Loop`, `S-Defense-Room` |
| `patches-startup-ui-tick` | 7 | `S-Source-Patch-Audit`, `S-Settings-Persistence`, `S-Core-Horde-Loop` |
| `hostility-static` | 11 | `S-Special-Gauntlet`, `S-Defense-Room` |
| `area-manager-static` | 6 | `S-Settings-Persistence`, `S-Defense-Room` |
| `damage-flasher-static` | 3 | `S-Special-Gauntlet` |
| `service-static` | 2 | `S-Source-Patch-Audit` |
| `zombie-leaner-static` | 2 | `S-Core-Horde-Loop`, `S-Special-Gauntlet` |
| `assets-static` | 1 | `S-Source-Patch-Audit` |

Next static audit priority:

- `patches-startup-ui-tick`, `assets-static`, and `service-static` are source-audited below because they gate cold load, startup logs, and asset availability.
- `patches-avoidance-doors-workgivers` is source-audited below because it has high player-visible risk and overlaps multiple scenarios.
- `patches-zombie-lifecycle-combat` is source-audited below because it ties world/faction setup, movement side effects, infection gates, incidents, and interaction suppression into the horde loop.
- `patches-damage-infection-hostility` is source-audited below because it covers damage scaling, infection persistence, tar/fire behavior, targeting, death cleanup, and fire attachment state.
- `patches-records-clamor-misc` is source-audited below. It has 101 live static rows after excluding disabled block-commented Harmony code from the inventory.
- `patches-chainsaw-equipment-jobs` is source-audited below because it covers chainsaw equipment state, smart melee/ranged combat, faction goodwill, flee/danger behavior, animal response, and zombie job gating.
- `patches-render-specials-apparel` is source-audited below because it covers name labels, downed zombie visuals/state, special zombie render overlays, generated apparel/graphics, SoS floating zombies, warmup scaling, and zombie stats.
- `hostility-static` is source-audited below because it covers attack-target choice, hostility predicates, active-threat filtering, drafted auto-attacks, and target-cache cleanup.
- `area-manager-static`, `damage-flasher-static`, and `zombie-leaner-static` are source-audited below because they are small specialized support groups for area-risk UI, bite damage flash coloring, and zombie draw jitter.
- `contamination-static` is source-audited below. The current C0-C6 runtime/save-load suite now covers the active Core branches for persistence, transfer, UI overlays, map generation, cleanup, player effects, and quest flow; remaining contamination rows are dependency-gated Biotech/Odyssey branches or explicitly documented partial parent markers.
- Runtime patch-application checks should prioritize only changed contamination code, dependency-enabled DLC branches, or fanout rows where a future source change creates a new semantic branch not already represented by the C0-C6 suite.

## Music Playback Static Targets

| Location | Patch class | Status | RimWorld 1.6 evidence | Coverage disposition |
| --- | --- | --- | --- | --- |
| `Source/Patches_Music.cs:10` | `MusicManagerPlay_ChooseNextSong_Patch` | `resolved/source` | `RimWorld.MusicManagerPlay.ChooseNextSong()` exists as `967ddb80559449f0a776dafa26a855d1:0600E643:M`. The current 1.6 body still filters `SongDef` values through `AppropriateNow`, excludes the recent-song queue, and weights the result by commonality. | The Zombieland prefix keeps vanilla eligibility checks while applying the configured Zombieland/non-Zombieland share and mixed-mode candidate pool. The reusable `zombieland/settings_state actionMode:music` contract covers target installation and recent-song exhaustion; mixed-mode playback duration still needs a restarted-process runtime check after the 2026-07-12 fade fix. |
| `Source/Patches_Music.cs:23` | `MusicManagerPlay_PlaySong_Patch` | `resolved/source` | Private `RimWorld.MusicManagerPlay.PlaySong(SongDef song, Boolean forced, Boolean ignorePrefsVolume)` exists as `967ddb80559449f0a776dafa26a855d1:0600E63F:M`. `MusicUpdate()` (`0600E62E:M`) starts a 10-second fade whenever the current song's `tense` flag differs from `DangerMusicMode`; this remains an independent check after `ChooseNextSong()` returns. | The prefix records the actual playback mode on a mixed Zombieland song until the next song starts. This prevents an opposite-mode selection from immediately failing `MusicUpdate` while preserving vanilla fade-and-reselect behavior if danger mode changes during playback. Release build and deployed-DLL decompilation passed; restarted-process duration evidence remains pending. |
| `Source/Patches_Music.cs:30` | `MusicManagerEntry_StartPlaying_Patch` | `resolved/source` | `RimWorld.MusicManagerEntry.StartPlaying()` exists as `967ddb80559449f0a776dafa26a855d1:0600E616:M`; it still assigns `SongDefOf.EntrySong.clip` to the entry audio source before starting looped playback. | The prefix remains attached to the correct point for installing or restoring Zombieland's entry song from default settings. This path is independent of map-song mixed-mode playback. |

## Startup, UI, Tick, Asset, And Service Static Targets

This slice covers the generated `assets-static`, `patches-startup-ui-tick`, `Source/Patches_Startup.cs`, `Source/Patches_MainMenuBackground.cs`, and `service-static` rows. The `Resources/MacOS/zombieland`, `Resources/Linux/zombieland`, and `Resources/Win64/zombieland` bundles exist in the local workspace.

| Location | Patch class | Status | RimWorld 1.6 evidence | Coverage disposition |
| --- | --- | --- | --- | --- |
| `Source/Assets.cs:19` | `Assets.LoadAssetBundle` | `resolved/runtime` | `Verse.UIRoot_Entry.Init()` exists: `88df42e0e943404ba42043aebd37585c:06004B22:M`; decompiler evidence shows entry-root init still runs before main-menu setup. Runtime `zombieland/startup_support_state` records the installed Zombieland postfix on `UIRoot_Entry.Init`. | Covered for MacOS asset bundle load from cold start: `Assets.initialized=true`, `Metaballs` resolved as shader `Custom/Metaballs`, and `Dust(Clone)` instantiated with `ParticleSystem` plus `ParticleSystemRenderer`. No warning-or-higher logs. |
| `Source/Patches.cs:104` | `ParseHelper_FromString_Patch` | `resolved/runtime` | `Verse.ParseHelper.FromString(String str, Type itemType)` exists: `88df42e0e943404ba42043aebd37585c:06000920:M`; decompiler evidence shows enum parsing still goes through `Enum.Parse` after RimWorld back-compat lookup. Runtime `zombieland/startup_support_state` records the installed Zombieland prefix on this overload. | Covered for legacy `AreaRiskMode` value migration: live parsing mapped `IfInside -> ColonistInside` and `IfOutside -> ColonistOutside`, while current `ZombieInside` still parsed unchanged. No warning-or-higher logs. |
| `Source/Patches.cs:121` | `MainMenuDrawer_Init_Patch` | `resolved/runtime` | `RimWorld.MainMenuDrawer.Init()` exists: `88df42e0e943404ba42043aebd37585c:06012336:M`; decompiler evidence shows main-menu init still runs save detection, background setup, and countdown resets. Runtime `zombieland/startup_support_state` records the installed Zombieland postfix. | Covered for the normal cold-load path: the internal Zombieland startup patch-error list was empty (`errorCount=0`) and no `Dialog_ErrorMessage` window was open. The injected-error dialog branch remains a source-only failure path because deliberately dirtying the patch-error list would pollute startup diagnostics. |
| `Source/Patches.cs:141` | `SelectionDrawer_DrawSelectionOverlays_Patch` | `resolved/source` | `RimWorld.SelectionDrawer.DrawSelectionOverlays()` exists: `88df42e0e943404ba42043aebd37585c:06012B2E:M`. 2026-06-01 decompiler compare found the 1.4 and 1.6 signatures and bodies unchanged. | Debug-only pheromone overlay; source-resolved, runtime visual evidence only needed if debug-grid tooling is promoted into a supported player-facing scenario. |
| `Source/Patches.cs:187` | `MapInterface_MapInterfaceUpdate_Patch` | `resolved/source` | `RimWorld.MapInterface.MapInterfaceUpdate()` exists: `88df42e0e943404ba42043aebd37585c:06012D43:M`. 2026-06-01 1.4/1.6 body compare found the signature unchanged; the map guard changed from the old world-renderer negative check to `WorldRendererUtility.DrawingMap`, and 1.6 inserts map-component draw/pathfinder/Odyssey update calls before the existing vanilla grid/debug draw tail. | Source audit confirms Zombieland's postfix remains after the current map-interface draw/update pass, so contamination, player-reachable-region, avoidance, and wander debug overlays are still attached to the intended phase. Runtime visual evidence is only needed if those debug overlays become a named supported scenario. |
| `Source/Patches.cs:276` | `MapInterface_MapInterfaceOnGUI_AfterMainTabs_Patch` | `resolved/source` | `RimWorld.MapInterface.MapInterfaceOnGUI_AfterMainTabs()` exists: `88df42e0e943404ba42043aebd37585c:06012D40:M`. 2026-06-01 1.4/1.6 compare found the signature unchanged and only the world-renderer guard updated to `WorldRendererUtility.DrawingMap`; the environment stats, deep-resource GUI, and debug drawer call order is unchanged. | Debug pathing label overlay remains attached to the same after-main-tabs GUI phase; runtime visual evidence is only needed for an explicitly supported pathing-debug scenario. |
| `Source/Patches.cs:323` | `GlobalControlsUtility_DoDate_Patch` | `resolved/runtime` | `RimWorld.GlobalControlsUtility.DoDate(Single leftX, Single width, Single& curBaseY)` exists: `88df42e0e943404ba42043aebd37585c:0601282C:M`; decompiler evidence shows vanilla still only draws the date readout and decrements `curBaseY`. Runtime `S-Incident-Threat` evidence forced one zombie and dynamic threat, then verified the patch's right-aligned `1 Zombies` rect, `0% threat level` rect, and 720x320 tooltip rect were on-screen and non-overlapping with the vanilla date rect. The tooltip drawer rendered successfully and was captured at `/Users/ap/Library/Application Support/RimWorld/Screenshots/zl_forecast_tooltip_preview__clip.png`. | Covered for zombie count, dynamic threat label, tooltip placement, and forecast tooltip rendering. |
| `Source/Patches.cs:430` | `Verse_TickManager_TickManagerUpdate_Patch` | `resolved/runtime` | `Verse.TickManager.TickManagerUpdate()` exists: `88df42e0e943404ba42043aebd37585c:06001585:M`. Runtime `S-Core-Horde-Loop` sub-contract evidence ran `zombieland/zombie_ticking_feedback_contract` from `EMPTY.rws`: the reflected prefix reset `ZombieTicker.zombiesTicked`, sized `maxTicking=40` and `currentTicking=10` for 20 live zombies at `PercentTicking=0.25`, and the reflected postfix fed back `100/400` as slot `0.25` with averaged `PercentTicking=0.90625`. The mixed horde-loop scenario then proved normal `TickManagerUpdate` runtime is required for over-time evidence; paused `rimworld/step_game_ticks` alone bypasses the prefix and does not prepare `ZombieTicker.managers`. The dense smoke fixture `ZL_Dense_Performance_base.rws` then ran 80 normal zombies for a normal-speed window and recorded one ticker manager, `currentTicking=80`, `maxTicking=80`, `zombiesTicked=80`, `PercentTicking=1`, 80 live/cached zombies, and clean warning-or-higher logs. | Covered for adaptive zombie tick-budget accounting, real-time horde-loop preparation, and repeatable dense-horde smoke baseline. |
| `Source/Patches.cs:480` | `TickManager_DoSingleTick_Patch` | `resolved/runtime` | `Verse.TickManager.DoSingleTick()` exists: `88df42e0e943404ba42043aebd37585c:06001586:M`. Runtime `S-Core-Horde-Loop` sub-contract evidence ran `zombieland/zombie_ticking_feedback_contract` from `EMPTY.rws`: after prefix setup with 20 live zombies at `PercentTicking=0.25`, one real `DoSingleTick()` through the Harmony postfix selected five live zombies and counted exactly five zombie production ticks. The `ZL_Core_HordeLoop_over_time_00.rws` scenario then saved and reloaded a mixed active horde and proved post-reload normal-speed runtime resumed tracking/pathing/sabotage with clean logs. | Covered for the real one-tick bridge from RimWorld's tick manager into `ZombieTicker.DoSingleTick` and for longer save-load horde progression. |
| `Source/Patches.cs:489` | `Verse_TickManager_NothingHappeningInGame_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is private `Verse.TickManager.NothingHappeningInGame()` (`88df42e0e943404ba42043aebd37585c:06001580:M`) and vanilla returns a cached no-activity state after scanning player pawns and home-map danger. Runtime `S-Core-Horde-Loop` sub-contract evidence ran `zombieland/zombie_ticking_budget_contract` from `EMPTY.rws`. | Covered for pending-spawn idleness gating: with `ZombieGenerator.ZombiesSpawning=0`, reflected `NothingHappeningInGame()` returned true in the quiet fixture; with `ZombiesSpawning=1`, the Zombieland postfix forced the result false. |
| `Source/Patches.cs:503` | `WorldPawns_ShouldMothball_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is private `RimWorld.Planet.WorldPawns.ShouldMothball(Pawn p)` (`88df42e0e943404ba42043aebd37585c:06015DB1:M`), and vanilla returns true when no hediff/caravan/transport-pod condition prevents mothballing. Runtime `S-Core-Horde-Loop` sub-contract evidence ran `zombieland/zombie_records_suppression` from `EMPTY.rws`. | Covered for direct mothball gating: an ordinary human control returned `ShouldMothball=true`, while a normal zombie returned `false` through the Zombieland prefix. |
| `Source/Patches.cs:526` | `InfestationCellFinder_CalculateLocationCandidates_Patch.CalculateLocationCandidates_Prefix` | `resolved/runtime` | `RimWorld.InfestationCellFinder.CalculateLocationCandidates(Map map)` exists: `88df42e0e943404ba42043aebd37585c:06013276:M`; decompiler evidence shows it recalculates supporting grids and then calls `GetScoreAt` for every map cell. Runtime `S-Defense-Room actionMode:infestationThumper` loaded `ZL_Defense_Room_base.rws`, temporarily thick-roofed a valid candidate around saved thumper `Thing_Thumper6918`, and compared `CalculateLocationCandidates` results with the thumper inactive vs active. Inactive candidate count inside radius was `148`; active thumper candidate count inside radius was `0`. | Covered for active-thumper candidate-list suppression around the saved defense fixture; full storyteller infestation firing remains additive. |
| `Source/Patches.cs:534` | `InfestationCellFinder_CalculateLocationCandidates_Patch.GetScoreAt_Prefix` | `resolved/runtime` | `RimWorld.InfestationCellFinder.GetScoreAt(IntVec3 cell, Map map)` exists: `88df42e0e943404ba42043aebd37585c:06013274:M`; decompiler evidence shows a positive vanilla score requires nearby colony building, walkable unfogged thick-roofed cell, sufficient closed area, temperature, mountainousness, and final score above `7.5`. Runtime `S-Defense-Room actionMode:infestationThumper` used cell `(110,99)`, which scored `13.9857321` with the thumper inactive and `0` with active thumper `Thing_Thumper6918` at radius `12`. | Covered for the score-prefix zeroing branch on a vanilla-positive infestation candidate inside active thumper radius. |
| `Source/Patches.cs:550` | `Pawn_Tick_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is protected `Verse.Pawn.Tick()` (`88df42e0e943404ba42043aebd37585c:06003057:M`); decompiler evidence shows vanilla calls `base.Tick()` before rare ticks, pather/verbs/jobs/health, equipment, inventory, genes, ambience, and renderer effecters. Runtime `zombieland/infection_medical_state` from `EMPTY.rws` now invokes that protected method by reflection. | Covered for both patch-owned branches: a final-stage bitten human went from custom `InfectionState=None` to `Infecting` after one real `Pawn.Tick()`, and a contaminated non-downed pawn at effectiveness `0.335` consumed `0.0266113281` chainsaw fuel over 40 real `Pawn.Tick()` calls versus `0.06652832` for the clean effectiveness-`1` control, proving the contamination gate reduces post-base pawn tick execution. Clean warning-or-higher logs. |
| `Source/Service.cs:12` | `TimeControlService` | `resolved/runtime` | `RimWorld.TimeControls.DoTimeControlsGUI(Rect timerRect)` exists: `88df42e0e943404ba42043aebd37585c:06012834:M`; decompiler evidence shows it still reads and updates `Find.TickManager.CurTimeSpeed` from the time-control UI. Runtime `zombieland/startup_support_state` records the installed Zombieland postfix. | Covered for shared subscriber dispatch without UI clicking: a temporary subscriber received exactly one `Paused` notification from the postfix and did not receive a duplicate when the speed was unchanged. `ZombieThumper` remains the player-facing subscriber exercised by defense-room/thumper scenarios. |
| `Source/Service.cs:40` | `ClearMapsService` | `resolved/runtime` | `Verse.Profile.MemoryUtility.ClearAllMapsAndWorld()` exists: `88df42e0e943404ba42043aebd37585c:06005A3A:M`; decompiler evidence shows the vanilla method clears live maps/world state, so runtime proof must avoid calling the destructive vanilla body during a loaded fixture. Runtime `zombieland/startup_support_state` records the installed Zombieland prefix. | Covered for shared cleanup dispatch: a temporary subscriber received one callback by invoking the Zombieland prefix directly, intentionally not the vanilla clear method. Player-facing cleanup remains represented by thumper unsubscribe/cleanup paths and reload smoke. |

### Adaptive ticking re-audit (2026-07-11)

`Source/Patches.cs:566` now patches `Verse.TickManager.TickManagerUpdate()` using the current RimWorld 1.6 member `967ddb80559449f0a776dafa26a855d1:06001585:M`. Decompilation proves the native loop resets `ticksThisFrame`, computes the same demand from `CurTimePerTick`, caps at `TickRateMultiplier * 2`, and breaks above 45.4545 ms. The Zombieland stopwatch starts before `ZombieWanderer.ProcessNext`, so simulation pressure includes the bounded wanderer recalculation slice as well as manager preparation and zombie ticks. The controller measures raw demand and completion against the native semantics, normalizes global pressure by effective multiplier, derives simulation pressure from the complete Zombieland elapsed time/shortfall, and resets state on game/load/entry transitions. Follow-up feedback operation `op_ac4f5c656f9a480a88195972f2f052ad` recorded `wandererTimingIncluded=true` with the wanderer start inside the prefix window and otherwise retained all demand, feedback, hysteresis, and live one-tick assertions. Earlier operations `op_317cd88ec6ad441580072bea64bbfe34` and `op_bf8c38ac6c00478cb17c812875cce330` cover the same patched boundary, speed normalization, fair remote service, churn, priority service, and no-RNG selection.

`Source/Patches.cs:7074` adds the narrowly scoped postfix on `Pawn_PathFollower.CostToPayThisTick()` (`967ddb80559449f0a776dafa26a855d1:06006343:M`, private no-argument float). It applies only to ordinary `Zombie` instances and multiplies path payment by bounded `1 / simulationTickRate` (minimum rate .05, maximum multiplier 20). This removes the prior artificial game/sampling-rate multiplication from `StatExtension.GetStatValue(MoveSpeed)`: final live stats operation `op_d14875992fd142478ad9922d2049f892` kept the idle normal zombie at `0.804999948` for simulation rates 1 and .05; tanky and roped-down values remained `0.0184` and `0.4`. The ticking budget contract independently observed remote `MoveSpeed` ratio 1 while real payment became 4.75.

## Avoidance, Doors, And Workgiver Static Targets

This slice covers the 15 static rows in `patches-avoidance-doors-workgivers`. The dynamic door/workgiver fanout rows are already recorded in the core dynamic table above; this section records the remaining source-level static targets that drive the same runtime scenarios. Relevant post-baseline commit evidence includes `3981f82 Fix zombie movement with async path requests`, `f43506a Add path avoidance recalculation smoke`, `36915af Restore avoid-grid path costs`, `2bce190 Add zombie door close smoke`, `0114625 Add albino door hold-open smoke`, `0c6dacb Add avoid grid door and danger smoke`, `0416696 Add workgiver avoidance smoke`, `f651efa Add colonist avoidance interrupt smoke`, and `391c041 Document avoidance contract recheck`.

| Location | Patch class | Status | RimWorld 1.6 evidence | Coverage disposition |
| --- | --- | --- | --- | --- |
| `Source/Patches.cs:1423` | `PathFinder_CreateRequest_Patch` | `resolved/runtime` | Exact overload exists: `Verse.PathFinder.CreateRequest(IntVec3, LocalTargetInfo, IntVec3?, TraverseParms, PathFinderCostTuning?, PathEndMode, Pawn, PathRequest.IPathGridCustomizer)` (`88df42e0e943404ba42043aebd37585c:060025A1:M`). Decompiler evidence shows it constructs `new PathRequest(..., customizer)`. Runtime `zombieland/avoid_grid_costs_route_new_path` now also creates async requests after seeding avoid costs. | Covered for async request injection and non-replacement: when no customizer was supplied, the request contained `ZombieAvoidGridPathCustomizer`; when a caller-supplied probe customizer was passed, it was preserved and not disposed by Zombieland. Warning-or-higher logs were empty. |
| `Source/Patches.cs:1434` | `PathFinder_FindPathNow_Patch` | `resolved/runtime` | Exact overload exists: `PathFinder.FindPathNow(IntVec3, LocalTargetInfo, TraverseParms, PathFinderCostTuning?, PathEndMode, PathRequest.IPathGridCustomizer)` (`88df42e0e943404ba42043aebd37585c:060025A5:M`). It creates a `PathRequest` with `traverseParms.pawn` and `customizer`. Runtime `S-Hostility-Area-Workflow` evidence from `zombieland/avoid_grid_costs_route_new_path` routed a real synchronous colonist path away from a tracking zombie: baseline had `11` avoid cells and avoid cost `7940`, while the Zombieland-costed path had `0` avoid cells and avoid cost `0`. | Covered for synchronous path requests using avoid-grid costs; async request injection and cleanup are now covered by the surrounding `CreateRequest`/`PathRequest` rows in the same contract. |
| `Source/Patches.cs:1456` | `PathRequest_Resolve_Patch` | `resolved/runtime` | `PathRequest.Resolve(PawnPath p)` exists (`88df42e0e943404ba42043aebd37585c:06002601:M`) and stores success/failure without disposing the customizer itself. Runtime `zombieland/avoid_grid_costs_route_new_path` reflected the request customizer, called real `Resolve(null)`, and rechecked the Zombieland customizer's native offset grid. | Covered for async completion cleanup: injected customizer grid was created before `Resolve` and disposed afterward (`true -> false`). Warning-or-higher logs were empty. |
| `Source/Patches.cs:1466` | `PathRequest_Dispose_Patch` | `resolved/runtime` | `PathRequest.Dispose()` exists (`88df42e0e943404ba42043aebd37585c:06002605:M`) and only cancels/disposes the path in vanilla, not the customizer. Runtime `zombieland/avoid_grid_costs_route_new_path` reflected the request customizer, called real `Dispose()`, and rechecked the Zombieland customizer's native offset grid. | Covered for async cancellation/failure cleanup: injected customizer grid was created before `Dispose` and disposed afterward (`true -> false`), while a caller-supplied customizer remained created after request disposal and was disposed manually by the probe. Warning-or-higher logs were empty. |
| `Source/Patches.cs:1476` | `Pawn_PathFollower_NeedNewPath_Patch` | `resolved/runtime` | `Pawn_PathFollower.NeedNewPath()` exists (`88df42e0e943404ba42043aebd37585c:06006339:M`) and still evaluates current path validity, reachability, and path node state. Runtime `S-Hostility-Area-Workflow` evidence from `zombieland/avoid_grid_interrupts_existing_path` started a real non-forced `Goto`, waited for a path with `18` nodes, spawned a tracking zombie beside the source-derived fourth lookahead cell, and observed `NeedNewPath` change from false to true with avoid cost `972`. | Covered for danger appearing ahead of an already-started path; broader horde-loop path persistence remains additive. |
| `Source/Patches.cs:1527` | `GenSpawn_SpawningWipes_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `GenSpawn.SpawningWipes(BuildableDef newEntDef, BuildableDef oldEntDef, Boolean ignoreDestroyable = false)` (`88df42e0e943404ba42043aebd37585c:060055BC:M`); decompiler evidence shows vanilla would wipe when a new edifice is placed over an old edifice. Runtime `zombieland/defense_room_state setupFixture:true` records the installed Zombieland prefix. | Covered for shocker placement over existing buildings without wiping support: the fixture cell `(100,100)` contained both `Wall` and `ZombieShocker`, `shocker.OnWall=true`, direct patched `SpawningWipes(ZombieShocker, Wall, true)=false`, `SpawningWipes(ZombieShocker, Door, true)=false`, and a control `SpawningWipes(Wall, Wall, true)=true`. No warning-or-higher logs. |
| `Source/Patches.cs:1600` | `Building_Door_Tick_Patch` | `semantic/runtime` | `Building_Door.Tick()` exists (`88df42e0e943404ba42043aebd37585c:0600EDA0:M`) and the auto-close loop still calls `base.Map.thingGrid.CellContains(item2, ThingCategory.Pawn)`. Runtime `S-Hostility-Area-Workflow` evidence from `zombieland/albino_does_not_hold_door_open` placed normal and albino zombies in open door cells: the normal zombie reset auto-close from `10` to `109`, while the albino door ticked down from `10` to `9`. | Covered for the occupied-rect auto-close branch and albino exemption; visual door animation evidence remains optional. |
| `Source/Patches.cs:1620` | `Building_Door_StartManualCloseBy_Patch` | `resolved/runtime` | `Building_Door.StartManualCloseBy(Pawn closer)` exists (`88df42e0e943404ba42043aebd37585c:0600EDA9:M`). Runtime `S-Hostility-Area-Workflow` evidence from `zombieland/zombie_manual_door_close_ignored` proved a normal zombie left `ticksUntilClose` at the sentinel `12345`, while an ordinary colonist call changed it to `110`. | Covered for zombie manual-close suppression and ordinary pawn fallback. |
| `Source/Patches.cs:1632` | `JobDriver_DriverTick_Patch` | `resolved/runtime` | `JobDriver.DriverTick()` exists (`88df42e0e943404ba42043aebd37585c:06005FA3:M`) and still runs per-toil tick actions for the driver pawn. Runtime `S-Hostility-Area-Workflow` evidence put a colonist inside avoid danger with `autoAvoidZombies=true`: drafted `Wait_Combat` did not convert to flee after 5 ticks, undrafted `Wait_Combat` converted to a player-forced `Flee` on tick 1, and undrafted non-forced `AttackMelee` also converted to a player-forced `Flee` on tick 1 with a safe destination. Runtime `S-Settings-Persistence actionMode:behavior` then proved the negative setting gate: with persisted `autoAvoidZombies=false`, active avoid danger (`avoidCost=972`, `shouldAvoid=true`) did not convert a non-forced `Goto` job into `Flee`. | Covered for ordinary undrafted job interruption, melee job interruption, drafted exemption, and the persisted auto-avoid false gate; mental-state/downed/vomit edge exemptions remain lower-priority special cases. |
| `Source/Patches.cs:1702` | `JobGiver_ConfigurableHostilityResponse_TryGetAttackNearbyEnemyJob_Patch` | `semantic/runtime` | `JobGiver_ConfigurableHostilityResponse.TryGetAttackNearbyEnemyJob(Pawn pawn)` exists (`88df42e0e943404ba42043aebd37585c:06007C69:M`) and still contains `pawn.CanReachImmediate(thing, PathEndMode.Touch)` before creating `AttackMelee` or `AttackStatic` jobs. Runtime `zombieland/area_workflow_state setupFixture:true actionMode:behavior` confirmed the installed Zombieland prefix and transpiler on this private method. | Covered for forced-flee attack suppression and active-electric reach override: while a colonist was in player-forced `Flee` and a hostile nearby zombie existed, the prefix returned `false` with null result; for a far active electrifier at squared distance `144`, vanilla `CanReachImmediate` returned false while the transpiled helper returned true, and a far normal-zombie control stayed false/false. Warning-or-higher logs were empty. |
| `Source/Patches.cs:1731` | `DangerUtility_GetDangerFor_Patch` | `resolved/runtime` | `DangerUtility.GetDangerFor(IntVec3 c, Pawn p, Map map)` exists (`88df42e0e943404ba42043aebd37585c:06000C2D:M`) and returns region danger for the pawn/map. Runtime `S-Hostility-Area-Workflow` evidence built a real avoid grid around a tracking zombie: the colonist's cell had avoid cost `972`, normal `GetDangerFor` returned `Deadly`, and the same cell returned `None` while the current job was player-forced. | Covered for active colony pawn danger escalation and player-forced exemption in `ZL_Area_Workflow_behavior.rws`; zombie/non-colony negatives can be sampled in the mixed-faction combat pass. |
| `Source/Patches.cs:1934` | `Pawn_PathFollower_StartPath_Patch` | `resolved/runtime` | `Pawn_PathFollower.StartPath(LocalTargetInfo dest, PathEndMode peMode)` exists (`88df42e0e943404ba42043aebd37585c:06006317:M`) and still has the destroyed-thing error branch plus the `pawn.jobs.posture = PawnPosture.Standing` assignment. The 2026-05-31 static/runtime pass found the old transpiler's 1.4-era `FindLastIndex(LoadsConstant(0))` posture anchor now hit a later 1.6 zero constant, then fixed it to anchor on the `Pawn_JobTracker.posture` store. | Covered in `zombieland/avoid_grid_costs_route_new_path`: installed patch owner was present, reflected helper returned false for a destroyed zombie target and true for a destroyed colonist target, a real `StartPath` call to the destroyed zombie target emitted no warning-or-higher logs, and a health-downed zombie with public `Pawn.Downed=false` started a path with posture `LayingMask` instead of vanilla `Standing`. |
| `Source/Patches.cs:2078` | `EditWindow_DebugInspector_CurrentDebugString_Patch` | `resolved/source` | `EditWindow_DebugInspector.CurrentDebugString()` exists (`88df42e0e943404ba42043aebd37585c:06000340:M`). 2026-06-01 decompiler compare confirmed the 1.4/1.6 surface still returns `string CurrentDebugString()`; the current 1.6 declaring type is `LudeonTK.EditWindow_DebugInspector`, matching the compiled source import. | Debug inspector evidence is optional; source audit shows the transpiler still appends Zombieland colony points, threat, contamination, avoidance, pheromone, wander, and local zombie-grid diagnostics to the vanilla debug string. Runtime evidence should be added only if debug-inspector support becomes a named scenario. |

## Zombie Lifecycle, Combat, And Incident Static Targets

This slice covers the 9 static rows in `patches-zombie-lifecycle-combat`. It spans world/faction setup, zombie movement side effects, infection/mental-state gates, incident/debug spawning, and player interaction suppression. Relevant post-baseline and historical commit evidence includes `787156f Fix zombie render tree initialization on load`, `3981f82 Fix zombie movement with async path requests`, `623822f Add corpse conversion bridge smoke`, `9c46bc0 Add zombie corpse management smoke`, `fd1c12f Fix double tap corpse job for RimWorld 1.6`, `b7d560e Cover incident infection and ticking budget`, `d7f1a04 Cover zombie hostility rules`, `6beb859 Cover zombie target cache filtering`, `bac414e Cover zombie corpse eating`, and older downed/eating/targeting fixes such as `239a4bc`, `72c8af5`, `100f809`, `a2adea9`, and `fe80efa`.

| Location | Patch class | Status | RimWorld 1.6 evidence | Coverage disposition |
| --- | --- | --- | --- | --- |
| `Source/Patches.cs:2175` | `FactionGenerator_GenerateFactionsIntoWorldLayer_Patch` | `resolved/runtime` | `FactionGenerator.GenerateFactionsIntoWorldLayer(PlanetLayer layer, List<FactionDef> factions = null)` exists (`88df42e0e943404ba42043aebd37585c:0600D29F:M`) and still passes the supplied faction-def list into `InitializeFactions`, which creates factions from that list. Runtime `zombieland/zombie_faction_world_contract` now records the installed Zombieland owner on the target and reflects the prefix against synthetic generation lists. | Covered for the new-world list mutation boundary: an empty faction-def list became `["Zombies"]`, a preexisting `["Zombies"]` list stayed single, and a null list was accepted. The same run also proved the loaded `EMPTY` world has exactly one Zombies faction and warning-or-higher logs were empty. |
| `Source/Patches.cs:2189` | `FactionManager_ExposeData_Patch` | `resolved/runtime` | `FactionManager.ExposeData()` exists (`88df42e0e943404ba42043aebd37585c:0600D2F9:M`) and still scribes `allFactions` plus post-load recache. Runtime `zombieland/zombie_faction_world_contract` from `EMPTY.rws` proved the loaded world has exactly one `Zombies` faction, hidden true, temporary false, and mutual hostile relations with all 11 non-zombie factions including the player. | Covered for existing-save faction availability and relation symmetry: `zombieland/zombie_faction_pawn_generation_contract` also generated a normal zombie through the Zombies faction while symbiant/spitter generation remained unspawned, and warning-or-higher logs were empty. Save-after-load duplicate prevention remains additive if we create a faction-specific save fixture. |
| `Source/Patches.cs:2231` | `Thing_Position_Patch` | `resolved/runtime` | `Thing.Position` exists (`88df42e0e943404ba42043aebd37585c:17000A93:P`) and its setter is semantically unchanged from RimWorld 1.4: early same-cell return, spawned deregistration, `positionInt` assignment, spawned reregistration, gas notification, region update, mesh dirtying, and reachability cache clear. Runtime `zombieland/position_side_effects_contract` records the installed owner on `Verse.Thing::set_Position(Verse.IntVec3 value)`. The `ZL_Core_Pheromone_Senses_00.rws` fixture used the same timestamp semantics to prove a fresh radius-4 pheromone source behind a wall remains fresh on wall cells and walkable cells on both sides of the wall, while an otherwise equivalent source older than `Tools.PheromoneFadeoff()` has zero fresh cells. | Covered for the base-mod movement side effects from `EMPTY.rws`: prior contracts proved tanky pheromones, dark-slimer `TarSlime`, and StickyGoo toxic buildup; the row-level contract added spitter trail (`changedCount=280`), ordinary zombie clogging (`timestampBefore=320038000`, `timestampAfter=320008000`, expected 3 * `ZOMBIE_CLOGGING_FACTOR`), colonist avoid-danger contact (`avoidCost=937`, and `lastZombieContact=5038`), and `OnlyColonists` attraction gating (colonist trail `changedCount=49`, hostile non-colonist and manhunter non-colonist `changedCount=0`). The live pheromone/senses fixture then proved the scent field is not line-of-sight gated and that stale timestamps are rejected by the tracking threshold: the fresh-through-wall zombie entered `Tracking`, while the stale-lane zombie remained `Wandering`. Source/runtime evidence notes the manhunter branch skips only the direct early-return gate; the final `Customization.DoesAttractsZombies` gate still applies without an external `ZombielandSupport` evaluator. Active Vehicle Framework startup now resolves the vehicle type, and the same position-side-effects contract spawned concrete `ZLTestScoutVehicle` from the local `ZLTestVehicle` fixture and proved `VehicleTools.BumpTimestamps` on a live `Vehicles.VehiclePawn` (`moveSpeed=10`, `changedCount=709`, `timestampAfter=320039000`). Warning-or-higher logs were empty before and after reloading `EMPTY`, and after the active Vehicle Framework passes. |
| `Source/Patches.cs:2384` | `CompRefuelable_ConsumeFuel_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `CompRefuelable.ConsumeFuel(Single amount)` (`88df42e0e943404ba42043aebd37585c:060108C2:M`); decompiler evidence shows vanilla subtracts the input amount and clamps fuel to zero before notifying ran-out-of-fuel. Runtime `zombieland/defense_room_state actionMode:turretFuel` records the installed Zombieland prefix. | Covered for turret-only fuel reduction and vanilla clamp preservation: with `reducedTurretConsumption=0.5` and input amount `4`, a real `Turret_MiniTurret` consumed `2` fuel, a non-turret `ZombieThumper` control consumed the full `4`, and a low-fuel turret clamped from `1` to `0` without negative fuel. No warning-or-higher logs. |
| `Source/Patches.cs:2398` | `DamageWorker_Scratch_ChooseHitPart_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `DamageWorker_Scratch.ChooseHitPart(DamageInfo dinfo, Pawn pawn)` (`88df42e0e943404ba42043aebd37585c:06002957:M`); decompiler evidence shows vanilla selects a part from `dinfo.Height` and `BodyPartDepth.Outside`. Runtime `zombieland/area_workflow_state actionMode:smart-melee` records the installed Zombieland prefix. | Covered for downed-zombie scratch targeting: a standing-zombie control left `DamageInfo.Height=Undefined`, while a health-downed normal zombie scratch under deterministic seed `7101` returned `left leg` with `height=Bottom`, `depth=Outside`, groups `Legs`, and tag `MovingLimbCore`. No warning-or-higher logs. |
| `Source/Patches.cs:2409` | `DamageWorker_Bite_ChooseHitPart_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `DamageWorker_Bite.ChooseHitPart(DamageInfo dinfo, Pawn pawn)` (`88df42e0e943404ba42043aebd37585c:0600292E:M`); decompiler evidence shows vanilla selects a part from `dinfo.Height` and `BodyPartDepth.Outside`. Runtime `zombieland/area_workflow_state actionMode:smart-melee` records the installed Zombieland prefix. | Covered for downed-zombie bite targeting: a standing-zombie control left `DamageInfo.Height=Undefined`, while a health-downed normal zombie bite under deterministic seed `7102` returned `right leg` with `height=Bottom`, `depth=Outside`, groups `Legs`, and tag `MovingLimbCore`. No warning-or-higher logs. |
| `Source/Patches.cs:2423` | `Need_CurLevel_Patch` | `semantic/runtime` | Installed RimWorld 1.6 target is `RimWorld.Need.CurLevel` (`40ca93f8c2cf4459bd8a72c25d1bfb81:17001EBF:P`). Runtime `zombieland/zombie_health_needs_upkeep_suppression` from `EMPTY.rws` added final-stage zombie bites to alert-tracked colonists and set `Food.CurLevel` through the real property setter. | Covered for tracked infected-colonist need clamping: assignments `0.13` and `0.93` both read back as `0.5`, while the untracked control kept `0.13`. |
| `Source/Patches.cs:2472` | `Pawn_JobTracker_ShouldStartJobFromThinkTree_Patch` | `resolved/runtime` | Private `Pawn_JobTracker.ShouldStartJobFromThinkTree(ThinkResult thinkResult)` exists (`88df42e0e943404ba42043aebd37585c:0600638D:M`). Sleepwalk, breakdown, and hoard driver-flow jobs survived the 30-tick window and save-load. Focused reruns also proved hallucination survived its 30-tick update, mimic survived the think-tree window after fixture correction, and hoard pather-failure continued through pickup/return-to-storage after forced failure. Final combined C5 save-load kept hoard, hallucination, mimic, sleepwalk, and breakdown active together after reload and 270 post-reload ticks. | Covered for contamination mental-job contracts and the combined `C5-player-effects` persistence scenario. |
| `Source/Patches.cs:2520` | `MentalStateHandler_TryStartMentalState_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `Verse.AI.MentalStateHandler.TryStartMentalState(...)` (`40ca93f8c2cf4459bd8a72c25d1bfb81:060061EB:M`). Runtime `zombieland/zombie_health_needs_upkeep_suppression` called `TryStartMentalState(Berserk, forced:true, forceWake:true)` on a control pawn and an alert-tracked final-stage infected colonist, with the control pawn equipped with real chainsaw `Thing_Chainsaw6921`. | Covered for both branches: the forced control call returned true and dropped/spawned the equipped chainsaw at the pawn cell, while the tracked infected call returned false and did not start the mental state. |
| `Source/Patches.cs:2560` | `HediffSet_CalculatePain_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `Verse.HediffSet.PainTotal` (`40ca93f8c2cf4459bd8a72c25d1bfb81:1700088E:P`). Runtime `zombieland/zombie_health_needs_upkeep_suppression` applied real `Cut` hediffs to a control pawn and an alert-tracked final-stage infected colonist. | Covered for tracked infected-colonist pain suppression: the control reported `PainTotal=0.1` and the tracked infected pawn reported `PainTotal=0`. |
| `Source/Patches.cs:2577` | `PawnCapacitiesHandler_GetLevel_Patch` | `semantic/runtime` | Installed RimWorld 1.6 target is `Verse.PawnCapacitiesHandler.GetLevel(PawnCapacityDef)` (`40ca93f8c2cf4459bd8a72c25d1bfb81:06002FD5:M`). Runtime `zombieland/zombie_health_needs_upkeep_suppression` used anesthetic controls and a killed tracked pawn. | Covered for tracked infected-colonist capacity override and dead fallback: anesthetized control `Consciousness=0.01`, anesthetized tracked infected pawn `Consciousness=1` and `Moving=1`, and dead tracked pawn `Consciousness=0`. |
| `Source/Patches.cs:2613` | `IncidentWorker_TryExecute_Patch` | `resolved/runtime` | `IncidentWorker.TryExecute(IncidentParms parms)` exists (`88df42e0e943404ba42043aebd37585c:060097D0:M`) and still calls `TryExecuteWorker` before storyteller/codex side effects. Runtime `S-Incident-Threat` evidence ran the incident infection hook path: the generated human incident pawn had a zombie bite before the incident postfix and the bite became harmless after it, while generated animal pawns remained unbitten. | Covered for incident-generated bite reduction in `ZL_Incident_Threat_base.rws`; broader storyteller raid execution remains on the raid-worker row. |
| `Source/Patches.cs:2635` | `IncidentWorker_Raid_TryExecuteWorker_Patch` | `resolved/runtime` | `IncidentWorker_Raid.TryExecuteWorker(IncidentParms parms)` exists (`88df42e0e943404ba42043aebd37585c:0600AB82:M`). Runtime `S-Incident-Threat` evidence invoked the installed raid worker method with zombie-faction parms while a temporary bridge probe intercepted `ZombiesRising.TryExecute`: `EdgeWalkIn` observed `FromTheEdges`, `CenterDrop` observed `AllOverTheMap`, both raid-worker invocations returned `false`, and both restored the previous sentinel spawn mode afterward. | Covered for zombie-faction raid debug/storyteller route at the patch boundary in `ZL_Incident_Threat_base.rws`; the probe prevents coroutine side effects because `ZombiesRising` spawn execution is already covered by scheduler/alert-wave contracts. |
| `Source/Patches.cs:2657` | `IncidentWorker_Patches` | `resolved/runtime` | `PawnGroupKindWorker.GeneratePawns(PawnGroupMakerParms parms, PawnGroupMaker groupMaker, Boolean errorOnZeroResults = true)` exists (`88df42e0e943404ba42043aebd37585c:0600991F:M`). Runtime `S-Incident-Threat` evidence generated an incident human pawn and animal pawn under infected-raid settings: the human received one zombie bite and the animal received none. | Covered for humanlike-only incident infection generation in `ZL_Incident_Threat_base.rws`; ship-reactor launch state is still a separate incident edge case. |
| `Source/Patches.cs:2682` | `PawnGenerator_GenerateNewPawnInternal_Patch` | `resolved/runtime` | Private static `PawnGenerator.GenerateNewPawnInternal(ref PawnGenerationRequest request)` exists (`88df42e0e943404ba42043aebd37585c:0600316B:M`). Runtime `S-Incident-Threat` evidence generated zombie-faction pawn kinds: normal `Zombie` returned a spawned Zombieland zombie through the patch, while `ZombieSymbiant` and `ZombieSpitter` stayed on the vanilla generation path. | Covered for zombie-faction normal/symbiant/spitter generation routing in `ZL_Incident_Threat_base.rws`. |
| `Source/Patches.cs:2731` | `WorkGiver_Haul_JobOnThing_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `RimWorld.WorkGiver_Haul.JobOnThing(Pawn, Thing, Boolean)` (`40ca93f8c2cf4459bd8a72c25d1bfb81:0600846E:M`). Runtime `zombieland/zombie_corpse_alert_forbid_contract` used concrete `WorkGiver_HaulCorpses` against a real human corpse, real zombie corpse, and a corpse stockpile. | Covered for vanilla corpse-haul boundary: the stockpile accepted the human corpse and `JobOnThing(..., forced:false)` returned `HaulToCell`; the stockpile rejected the zombie corpse, unforced zombie corpse haul returned null, and forced zombie corpse haul also returned null through the storage/filter layer. |
| `Source/Patches.cs:2750` | `ReservationManager_CanReserve_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `Verse.AI.ReservationManager.CanReserve(Pawn, LocalTargetInfo, Int32, Int32, ReservationLayerDef, Boolean)` (`40ca93f8c2cf4459bd8a72c25d1bfb81:06006444:M`). Runtime `zombieland/zombie_corpse_alert_forbid_contract` called the real reservation manager against a steel control, a non-former zombie, and a former-map-pawn zombie. | Covered for CanReserve gating: steel control returned true, non-former zombie returned false, and former-map-pawn zombie returned true. |
| `Source/Patches.cs:2765` | `ReservationManager_Reserve_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `Verse.AI.ReservationManager.Reserve(Pawn, Job, LocalTargetInfo, Int32, Int32, ReservationLayerDef, Boolean, Boolean, Boolean)` (`40ca93f8c2cf4459bd8a72c25d1bfb81:06006447:M`). Runtime `zombieland/zombie_corpse_alert_forbid_contract` called `Reserve(..., errorOnFailed:false)` with `Wait_Combat` jobs, then released worker reservations after each probe. | Covered for hard live-zombie reserve blocking: steel control reserved successfully, while both non-former and former-map-pawn zombie reserve calls returned false with clean warning-or-higher logs. |
| `Source/Patches.cs:2787` | `Pawn_AnythingToStrip_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `Verse.Pawn.AnythingToStrip()` (`40ca93f8c2cf4459bd8a72c25d1bfb81:060030C9:M`). Runtime `zombieland/zombie_corpse_alert_forbid_contract` added `Steel` inventory payloads to a human, normal zombie, spitter, and symbiant before calling the real method. | Covered for strip suppression: the payloaded human returned true, while payloaded normal zombie, spitter, and symbiant all returned false. |

## Damage, Infection, Hostility, Fire, And Death Static Targets

This slice covers the 16 static rows in `patches-damage-infection-hostility`. It overlaps `S-Core-Horde-Loop`, `S-Special-Gauntlet`, `S-Contamination-Persistence`, and `S-Incident-Threat` because damage, infection, fire, tar smoke, and death state cross several systems. Relevant post-baseline and historical commit evidence includes `b7d560e Cover incident infection and ticking budget`, `22b45b3 Cover zombie death contamination`, `2bee432 Cover contamination ground and fire hooks`, `d7f1a04 Cover zombie hostility rules`, `6beb859 Cover zombie target cache filtering`, `62343fa Cover albino melee bite filtering`, `d093385 Cover tar smoke human targeting`, `665ed0c Cover special zombie fire danger`, `1233bb1 Extend burn-longer fire hooks`, `7ccd5e4 Verify zombie fire rain vulnerability`, `8d4e084 Cover zombie fire attachment state`, `5da3f96 Restore tar fire smoke contracts`, `2524c39 Add tar slime fire spread smoke`, `b9dd918 Add zombie damage memory smoke`, and `6c84b92 Add zombie death thought smoke`.

| Location | Patch class | Status | RimWorld 1.6 evidence | Coverage disposition |
| --- | --- | --- | --- | --- |
| `Source/Patches.cs:3880` | `Verb_GetDamageFactorFor_Patch` | `resolved/runtime` | Exact overload exists: `VerbProperties.GetDamageFactorFor(Tool tool, Pawn attacker, HediffComp_VerbGiver hediffCompSource)` (`88df42e0e943404ba42043aebd37585c:06000F72:M`) and RimWorld 1.6 still multiplies linked-part efficiency, life-stage melee factor, and `MeleeDamageFactor` before returning the factor. Runtime `zombieland/area_workflow_state setupFixture:true actionMode:zombie-stats` confirmed the installed Zombieland postfix on the real target. | Covered for vanilla control, body-type scaling, former-pawn scaling, and tanky-equipment scaling: human stayed `1`, Fat zombie with `damageFactor=1.25` returned `5`, Thin returned `0.625`, former-map-pawn Hulk returned `18.75`, and fully armored tanky shield+helmet+suit returned `60`. Warning-or-higher logs were empty. |
| `Source/Patches.cs:3944` | `Pawn_GeneTracker_AddGene_Gene_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is private `Pawn_GeneTracker.AddGene(Gene gene, Boolean addAsXenogene)` (`40ca93f8c2cf4459bd8a72c25d1bfb81:0600CD1E:M`). `Pawn_GeneTracker.pawn` is public (`40ca93f8c2cf4459bd8a72c25d1bfb81:04008BCC:F`), and `Pawn_GeneTracker(Pawn)` exists (`40ca93f8c2cf4459bd8a72c25d1bfb81:0600CD0C:M`). Runtime `S-Special-Gauntlet` sub-contract evidence ran `zombieland/zombie_gene_rejection_contract` from `EMPTY.rws`. | Covered at the patch boundary: human control accepted the private `AddGene(Gene, bool)` path with a temporary in-memory `Gene`, while `Zombie`, `ZombieSpitter`, and `ZombieSymbiant` returned null and kept gene counts unchanged. Spitter/symbiant needed temporary gene trackers in this earlier Core-only fixture. Full Biotech DLC gameplay remains additive and should be content-specific, not a full retest. |
| `Source/Patches.cs:3960` | `Pawn_GeneTracker_AddGene_GeneDef_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is public `Pawn_GeneTracker.AddGene(GeneDef geneDef, Boolean xenogene)` (`40ca93f8c2cf4459bd8a72c25d1bfb81:0600CD1D:M`). Runtime `S-Special-Gauntlet` sub-contract evidence ran `zombieland/zombie_gene_rejection_contract` from `EMPTY.rws` with `biotechActive=false` and temporary in-memory `GeneDef` probes. | Covered at the patch boundary: human control accepted the public `AddGene(GeneDef, bool)` path, while `Zombie`, `ZombieSpitter`, and `ZombieSymbiant` returned null and kept gene counts unchanged. The skin-color/melanin fallback path is separately bounded by `Pawn_StoryTracker_SkinColorBase_Patch`; full Biotech DLC gameplay remains additive. |
| `Source/Patches.cs:3975` | `Pawn_StoryTracker_SkinColorBase_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `RimWorld.Pawn_StoryTracker.SkinColorBase` (`40ca93f8c2cf4459bd8a72c25d1bfb81:17001E6D:P`). `Pawn_StoryTracker(Pawn)` exists (`40ca93f8c2cf4459bd8a72c25d1bfb81:0600C945:M`) and `Verse.Pawn.story` is public (`40ca93f8c2cf4459bd8a72c25d1bfb81:04002589:F`). Runtime `S-Special-Gauntlet` sub-contract evidence ran `zombieland/zombie_skin_color_contract` from `EMPTY.rws`. | Covered at the patch boundary: ordinary human retained a manually set non-white story color, naturally generated `Zombie` returned `Color.white`, and spawned `ZombieSpitter`/`ZombieSymbiant` returned `Color.white` when temporary story trackers were attached for the probe. Visual rendering remains additive because live spitter/symbiant instances in this fixture did not naturally have story trackers. |
| `Source/Patches.cs:3969` | `Thing_AmbientTemperature_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `Verse.Thing.AmbientTemperature` (`40ca93f8c2cf4459bd8a72c25d1bfb81:17000ABF:P`). Runtime `S-Core-Horde-Loop` sub-contract evidence ran `zombieland/ambient_temperature_contract` on an outdoor map with cell temperature `4.5`. | Covered for temperature normalization: ordinary human and human corpse matched vanilla cell temperature (`4.5`), while `Zombie`, `ZombieCorpse`, `ZombieSpitter`, and `ZombieSymbiant` all returned forced normal temperature (`21`). |
| `Source/Patches.cs:3988` | `PriorityWork_GetGizmos_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `Verse.PriorityWork.GetGizmos()` (`40ca93f8c2cf4459bd8a72c25d1bfb81:06000C94:M`). Runtime `S-Settings-Persistence actionMode:gizmos` seeded a vanilla prioritized work command and enumerated real `PriorityWork.GetGizmos()` output on a capable temporary colonist. | Covered for direct gizmo enumeration: vanilla clear-priority command remained present, Zombieland avoid/extract/double-tap commands appeared enabled under their global gates and pawn capabilities, and invoking their `Command_Action` delegates flipped all three `ColonistConfig` booleans. Visible mouse-click UI interaction remains additive. |
| `Source/Patches.cs:4021` | `HediffUtility_CanHealNaturally_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `Verse.HediffUtility.CanHealNaturally(Hediff_Injury)` (`40ca93f8c2cf4459bd8a72c25d1bfb81:06002CCE:M`). Runtime `S-Infection-Medical` evidence checked hidden, harmless, infectable, and infecting zombie bites, an ordinary cut, and an animal zombie bite with infection state `None`. | Covered for natural-heal gating: hidden/infectable/infecting zombie bites did not heal naturally, harmless bite/ordinary cut healed, and the animal/non-infecting bite kept vanilla healing. |
| `Source/Patches.cs:4051` | `Recipe_RemoveBodyPart_GetPartsToApplyOn_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `RimWorld.Recipe_RemoveBodyPart.GetPartsToApplyOn(Pawn pawn, RecipeDef recipe)` (`40ca93f8c2cf4459bd8a72c25d1bfb81:0600C371:M`). Runtime `S-Infection-Medical` evidence called the real recipe worker after applying four zombie bites. | Covered for bitten-part surgery targets: all four distinct bitten body parts were offered, with no missing or duplicate bitten parts. The postfix now de-duplicates vanilla plus bite-added parts while preserving vanilla outputs. |
| `Source/Patches.cs:4075` | `Hediff_ShouldRemove_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `Verse.Hediff.ShouldRemove` (`40ca93f8c2cf4459bd8a72c25d1bfb81:1700074D:P`). Runtime `S-Infection-Medical` evidence forced bite/cut severities to zero and read `ShouldRemove`. | Covered for bite persistence at zero severity: hidden/infectable/infecting zombie bites stayed, while harmless bite and ordinary cut remained removable. Dead-pawn zombie hediff persistence remains part of the corpse conversion/save-load workflow. |
| `Source/Patches.cs:4113` | `Fire_VulnerableToRain_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is private `Fire.VulnerableToRain()` (`88df42e0e943404ba42043aebd37585c:0600F503:M`). Runtime `S-Core-Horde-Loop` sub-contract evidence ran `zombieland/zombie_fire_rain_vulnerability_contract` from `EMPTY.rws`. | Covered for rain vulnerability gating: human attached fire stayed vanilla-vulnerable (`100/100` true), while `Zombie`, `ZombieSpitter`, and `ZombieSymbiant` each returned a true/false mix while `zombiesBurnLonger=true` (`79/21`, `86/14`, `82/18`) and returned `100/100` true when the setting was disabled. |
| `Source/Patches.cs:4129` | `Fire_DoFireDamage_Patch` | `semantic/runtime` | Installed RimWorld 1.6 target is private `Fire.DoFireDamage(Thing targ)` (`88df42e0e943404ba42043aebd37585c:0600F504:M`) and still contains the `GenMath.RoundRandom(...)` fire-damage calculation before applying flame damage. Runtime `S-Core-Horde-Loop` sub-contracts ran `zombieland/zombie_fire_damage_reduction_contract` and `zombieland/tar_slime_fire_spread_contract` from `EMPTY.rws`. | Covered for both branches: the burn-longer setting reduced total sampled flame injury for normal zombies (`17 -> 12`), spitters (`235 -> 175`), and symbiants (`8 -> 6`) with a damaged human control and no sample errors; real `Fire.DoFireDamage` on burning `TarSlime` raised fire size from `0.1` to `0.5` and ignited adjacent `TarSlime`. The final warning-log check was clean, so the transpiler did not emit runtime patch errors. |
| `Source/Patches.cs:4185` | `Fire_SpawnSmokeParticles_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is private `Fire.SpawnSmokeParticles()` (`88df42e0e943404ba42043aebd37585c:0600F500:M`) and vanilla still throws smoke plus fire glow. Runtime `S-Core-Horde-Loop` sub-contract evidence ran `zombieland/tar_slime_fire_smoke_contract` from `EMPTY.rws`. | Covered for tar-fire smoke substitution: after 15 real ticks on burning `TarSlime`, target-cell `TarSmoke` count increased `0 -> 1` and the cell gas became `TarSmoke`. Source still provides the exact vanilla-smoke suppression proof because the prefix returns `false` after spawning tar smoke/fire glow. |
| `Source/Patches.cs:4205` | `FireWatcher_UpdateObservations_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is private `FireWatcher.UpdateObservations()` (`88df42e0e943404ba42043aebd37585c:0600AE56:M`) and vanilla sums every `ThingDefOf.Fire` as `0.5 + fireSize`. Runtime `S-Core-Horde-Loop` sub-contract evidence ran `zombieland/zombie_fire_watcher_excludes_attached_fires` from `EMPTY.rws`. | Covered for fire-danger accounting: a normal map fire plus attached human fire produced `FireDanger=4.25`, exactly matching the expected map-plus-human value, while including normal zombie, spitter, and symbiant attached fires would have produced `12`. The fixture now clears pre-existing map fires before measurement and cleans attached fire pawns after returning. |
| `Source/Patches.cs:4227` | `DamageWorker_DamageResult_AssociateWithLog_Patch` | `resolved/runtime` | `DamageWorker.DamageResult.AssociateWithLog(LogEntry_DamageResult log)` exists (`88df42e0e943404ba42043aebd37585c:06000E9E:M`) and vanilla writes combat-log references into damaged hediffs. Runtime `zombieland/zombie_damage_log_association_suppression` from `EMPTY.rws` proved ordinary human damage associated with the combat log (`resultHediffCount=1`, `combatTextDelta=1`), while normal zombie, spitter, and symbiant damage each produced a result hediff without combat-log text (`combatTextDelta=0`). The tool now destroys its spawned pawns before returning, and the follow-up warning log check was clean. | Covered for ordinary-human control and all three Zombieland pawn classes. Broader battle-log UI behavior remains additive. |
| `Source/Patches.cs:4239` | `Alert_ColonistLeftUnburied_IsCorpseOfColonist_Patch` | `resolved/runtime` | `Alert_ColonistLeftUnburied.IsCorpseOfColonist(Corpse corpse)` exists (`88df42e0e943404ba42043aebd37585c:06011556:M`) and dereferences `corpse.InnerPawn`. Runtime `zombieland/zombie_corpse_alert_forbid_contract` from `EMPTY.rws` proved a human corpse still counts as a colonist corpse, while normal and former zombie corpses do not. | Covered for human control plus normal/former zombie corpse alert suppression. The empty-corpse null guard remains a source-evidenced edge unless a future fixture creates a corpse with no `InnerPawn`. |
| `Source/Patches.cs:4256` | `ShotReport_HitReportFor_Patch` | `resolved/runtime` | `Verb.CanHitTargetFrom(IntVec3 root, LocalTargetInfo targ)` exists (`88df42e0e943404ba42043aebd37585c:060057CF:M`). Runtime `S-Hostility-Area-Workflow actionMode:targeting` spawned real `TarSmoke` on an adjacent hostile target and proved a melee verb stayed able to hit (`CanHitTargetFrom=true`) before and after smoke; earlier focused tar-smoke tools cover ranged/human non-melee blocking. | Covered for the melee early-return branch and linked to existing ranged tar-smoke evidence; keep future fire/tar visuals separate from this targeting row. |
| `Source/Patches.cs:4278` | `ShotReport_AimOnTargetChance_StandardTarget_Patch` | `resolved/runtime` | `ShotReport.AimOnTargetChance_StandardTarget` exists (`88df42e0e943404ba42043aebd37585c:17000DAC:P`) and still combines shooter, distance, equipment, weather, gas, cover, and execution factors, then clamps ordinary results to at least `0.0201`. Runtime `S-Hostility-Area-Workflow actionMode:targeting` spawned a real rifle shooter/hostile target pair: aim chance was `0.612535536` before smoke, real target-cell `TarSmoke` made `AimOnTargetChance_StandardTarget` return `0`, and ranged `CanHitTargetFrom` became false. The same probe set a synthetic `ShotReport.covers` list with a `TarSmoke` `CoverInfo` and verified the cover branch also returns `0`; RimWorld 1.6 cover calculation itself is source-known to use `GetCover`, while `TarSmoke` is a gas. | Covered for the end-to-end target-gas branch and direct cover-list branch. Keep future tar work focused on fire/visual emission rather than retesting the shot-report aim gate. |
| `Source/Patches.cs:4413` | `DamageFlasher_Notify_DamageApplied_Patch` | `resolved/runtime` | `StunHandler.Notify_DamageApplied(DamageInfo dinfo)` exists (`88df42e0e943404ba42043aebd37585c:0600F6D7:M`). A 2026-06-01 decompiler reread confirms RimWorld 1.6 kept the signature but changed stun semantics to `CanBeStunnedByDamage`, damage-def constant stun duration, per-damage adaptation ticks, optional resist stat scaling, EMP `stunFromEMP`, then `StunFor`; Zombieland's priority-first prefix still runs before those vanilla gates and only reads `dinfo.Def`, `dinfo.Amount`, and the zombie electric state. Runtime `zombieland/emp_electrifier` confirmed the installed Zombieland prefix on this target. | Covered for active electrifier EMP disable: a spawned active electrifier took real EMP damage amount `5`, `IsActiveElectric` changed `true -> false`, and `electricDisabledUntil` moved from `0` to `5338` (`tickBefore=5038`, matching the expected 300-tick disable window). The spawned fixture was cleaned up and warning-or-higher logs were empty. |
| `Source/Patches.cs:4317` | `DamageWorker_AddInjury_ApplyDamageToPart_Patch` | `resolved/runtime` | `DamageWorker_AddInjury.ApplyDamageToPart(DamageInfo dinfo, Pawn pawn, DamageResult result)` exists (`88df42e0e943404ba42043aebd37585c:0600291B:M`) and RimWorld 1.6 vanilla still resolves the hit part, applies armor, applies external-violence incoming-damage factors, records deflection/headshot state, and finalizes or propagates injury. Runtime `zombieland/area_workflow_state actionMode:special-damage` confirmed the installed Zombieland prefix on this private target after removing the stale spitter `IncomingDamageFactor` stat override. | Covered for the special-damage branch matrix: with `spitterThreat=1`, final runtime `op_494c283d92374003a9f52527edb58248` measured spitter non-ranged `Cut` input `2 -> 5` direct core injury while ranged `Bullet` input `10 -> 1`; the focused bug-report probe measured propagated `Cut` input `2 -> 7.05882359`. Former-map-pawn zombie bullet input `12` dealt `1.84822178` versus normal zombie `3.43253374`; albino bullet attempts produced `8` blocked and `1` hit while explosive `Bomb` dealt `1`; dark-slimer bullet damage spawned `37` `TarSmoke` things and set cell gas to `TarSmoke`; active electrifier bullet input `4` dealt `0` and added `4` absorb markers, while active electrifier `Cut` dealt `1.41176474`. The 2026-06-07 refresh kept `special-damage` (`op_a4793903b6db4488bdb3ce1090fcb3d6`) and the focused spitter-health `bug-reports` probe (`op_03d8a60e8d674635a7dec19de39966f5`) green, and added `spitter-readiness` (`op_14106eb9e3d94cb58cef8319df2666a8`): weak-colony durability factor `0.43` versus armed `1`, scaled Bullet/Cut amounts `2.32558131x` higher, and real Cut injury `38.054966` versus `5`. Warning-or-higher logs were empty. |
| `Source/Patches.cs:4389` | `ArmorUtility_GetPostArmorDamage_Patch` | `resolved/runtime` | `ArmorUtility.GetPostArmorDamage(Pawn pawn, Single amount, Single armorPenetration, BodyPartRecord part, DamageDef& damageDef, Boolean& deflectedByMetalArmor, Boolean& diminishedByMetalArmor)` exists (`88df42e0e943404ba42043aebd37585c:0600296A:M`) and RimWorld 1.6 still runs apparel armor, then pawn armor, and returns remaining damage. Runtime `zombieland/area_workflow_state actionMode:special-damage` confirmed the installed Zombieland prefix on the real target. | Covered for tanky armor and vanilla control in final runtime `op_494c283d92374003a9f52527edb58248`: unarmored human torso `Bullet` input `10` returned `10` with no deflect/diminish flags; shield-only tanky torso reduced shield `1 -> 0.9600951`; helmet-only head reduced helmet `1 -> 0.807393`; suit-only torso reduced suit `1 -> 0.9205075`, all with real `ArmorUtility` deflect/diminish flags. The `diminished` flag is the `Tools.PlayTink` branch trigger in the prefix, and warning-or-higher logs were empty. |
| `Source/Patches.cs:4491` | `Verb_CausesTimeSlowdown_Patch` | `resolved/runtime` | Private `Verb.CausesTimeSlowdown(LocalTargetInfo castTarg)` exists (`88df42e0e943404ba42043aebd37585c:060057C9:M`) and vanilla returns true for player-vs-hostile pawn/turret slowdown cases when the verb property allows slowdown and the target is not downed. Runtime `zombieland/active_electrifier_attack_verb_contract` confirmed the installed Zombieland postfix on this target. | Covered for the distance gate and vanilla controls: a player bolt-action rifle against a far normal zombie at squared distance `49` returned `false`, a close normal zombie at squared distance `10` stayed `true`, and a far hostile human at squared distance `49` stayed `true` with threshold `16`. Warning-or-higher logs were empty. |
| `Source/Patches.cs:4515` | `Pawn_TryGetAttackVerb_Patch` | `resolved/runtime` | `Pawn.TryGetAttackVerb(Thing target, Boolean allowManualCastWeapons = false, Boolean allowTurrets = false)` exists (`88df42e0e943404ba42043aebd37585c:0600307E:M`) and RimWorld 1.6 vanilla checks equipped primary verbs first, then optional apparel/turret verbs, then falls back to melee. Runtime `zombieland/active_electrifier_attack_verb_contract` confirmed the installed Zombieland prefix on this target. | Covered for all prefix branches in a focused live fixture: a spitter attacker returned no attack verb, a bolt-action rifle stayed ranged against a normal zombie, the same rifle user fell back to safe melee against an active electrifier, and `Weapon_GrenadeFrag` passed through with a non-melee location-targeting verb. Warning-or-higher logs were empty. |
| `Source/Patches.cs:4542` | `Pawn_TryStartAttack_Patch` | `resolved/runtime` | `Pawn.TryStartAttack(LocalTargetInfo targ)` exists (`88df42e0e943404ba42043aebd37585c:0600307F:M`) and RimWorld 1.6 vanilla first returns false for `stances.FullBodyBusy` or `WorkTagIsDisabled(WorkTags.Violent)`, then starts the selected attack verb. Runtime `zombieland/active_electrifier_attack_verb_contract` confirmed the installed Zombieland prefix on this target. | Covered for the zombie-specific simplified attack path and work-tag bypass: the verifier temporarily marked the normal zombie kind as `Violent`-disabled, confirmed the spawned normal zombie reported `WorkTagIsDisabled(Violent)=true`, then `TryStartAttack` still returned true with a `ZombieBite` melee verb, entered `Stance_Cooldown`, and damaged the adjacent colonist target by `3.69211769`. The kind def was restored in `finally` and warning-or-higher logs were empty. |
| `Source/Patches.cs:4560` | `Pawn_Kill_Patch` | `resolved/runtime` | `Pawn.Kill(DamageInfo? dinfo, Hediff exactCulprit = null)` exists (`88df42e0e943404ba42043aebd37585c:0600306B:M`) and RimWorld 1.6 vanilla still caches held map/position state, runs death side effects and pre-death modifications, drops pawn contents, sets the pawn dead, creates/places the corpse, calls `base.Kill`, removes killed components, and notifies hediff/faction/quest systems. Runtime `zombieland/infection_medical_state actionMode:pawn-kill` confirmed the installed Zombieland prefix on this target. | Covered for all prefix branches in live death fixtures: normal-zombie job/apparel behavior, bitten-human death infection, animal control, and aggressive-spitter loot. The 2026-07-11 non-tribal rerun `op_cd6e231366d940dd87b0c20ee6a7542f` proved an aggressive spitter drops one forbidden `Zombie100Serum`, with `makeshiftSerumAvailable=false`; tribal runs select the functionally equivalent `ZombieSerumSimple` through the same faction-aware helper. |
| `Source/Patches.cs:4624` | `Pawn_Destroy_Patch` | `resolved/runtime` | `Pawn.Destroy(DestroyMode mode = DestroyMode.Vanish)` exists (`88df42e0e943404ba42043aebd37585c:0600306C:M`) and RimWorld 1.6 vanilla still performs world-pawn, lord, equipment/inventory/apparel, relation, ideology, and render-tree cleanup after `base.Destroy`. Runtime `S-Hostility-Area-Workflow actionMode:behavior` confirmed the installed Zombieland prefix on this target. | Covered for Zombieland cleanup state: a spawned colonist and normal zombie were inserted into `ZombieAreaManager.pawnsInDanger`, the colonist received a real `ColonistSettings` config, and real `Pawn.Destroy(DestroyMode.Vanish)` removed both danger entries while also removing the humanlike non-zombie colonist config (`true -> false`). Warning-or-higher logs were empty. |
| `Source/Patches.cs:4639` | `DamageWorker_AddInjury_IsHeadshot_Patch` | `resolved/runtime` | Private `DamageWorker_AddInjury.IsHeadshot(DamageInfo dinfo, Pawn pawn)` exists (`88df42e0e943404ba42043aebd37585c:06002920:M`) and vanilla returns true only for non-instant-permanent ranged damage whose forced hit part is in `FullHead`. Runtime `S-Hostility-Area-Workflow actionMode:ranged-projectiles` confirmed the installed Zombieland postfix on this target. | Covered for positive and negative state transitions: direct patched `IsHeadshot` returned true for `Bullet` damage forced to a normal zombie's `Head` and changed state to `ShouldDie`; the same bullet damage forced to `Torso` returned false and left state `Tracking`. Warning-or-higher logs were empty. |
| `Source/Patches.cs:4652` | `HediffSet_AddDirect_Patch` | `resolved/runtime` | `HediffSet.AddDirect(Hediff hediff, DamageInfo? dinfo = null, DamageWorker.DamageResult damageResult = null)` exists (`88df42e0e943404ba42043aebd37585c:06002EFA:M`) and RimWorld 1.6 vanilla still validates the part, sets `hediff.pawn`, merges or adds the hediff, calls `PostAdd`, and may add missing-part fallout. Runtime `S-Hostility-Area-Workflow actionMode:ranged-projectiles` confirmed the installed Zombieland postfix on this target. | Covered for consciousness-source gating: adding bad `Cut` to a normal zombie `Brain` part tagged `ConsciousnessSource` changed state to `ShouldDie`; adding bad `Cut` to non-consciousness `Torso` left state `Tracking`. Warning-or-higher logs were empty. |
| `Source/Patches.cs:4669` | `CompAttachBase_AddAttachment_Patch` | `resolved/runtime` | `CompAttachBase.AddAttachment(AttachableThing t)` exists (`88df42e0e943404ba42043aebd37585c:06003B4C:M`) and vanilla appends to the attachment list. Runtime `zombieland/zombie_fire_attachment_state_contract` confirmed the installed Zombieland postfix on this target. | Covered for immediate fire-state synchronization: a real `FireUtility.TryAttachFire` call attached one fire to a normal zombie and changed `isOnFire` from false to true; a direct second `Fire.AttachTo` raised the fire attachment count to `2` while keeping `isOnFire=true`. |
| `Source/Patches.cs:4682` | `CompAttachBase_RemoveAttachment_Patch` | `resolved/runtime` | `CompAttachBase.RemoveAttachment(AttachableThing t)` exists (`88df42e0e943404ba42043aebd37585c:06003B4D:M`) and vanilla removes the exact attachment from the list. Runtime `zombieland/zombie_fire_attachment_state_contract` confirmed the installed Zombieland postfix on this target. | Covered for multi-fire removal semantics: removing one of two attached fires left one fire attached and `isOnFire=true`; removing the final fire left zero fire attachments and `isOnFire=false`, with clean warning-or-higher logs. |

## Records, Clamor, Social, UI, Corpse, And Misc Static Targets

This slice covers the 101 live static rows in `patches-records-clamor-misc`. The scanner excludes disabled block-commented Harmony code so it does not create false test obligations. This section includes social/thought suppression, former-colonist selection, corpse conversion, needs/records/clamor suppression, gunshot pheromones, tar slime movement, startup/menu hooks, fog-room zombie spawning, float-menu actions, area warnings, and Ideology load cleanup. Relevant commit evidence includes `6d2afda Restore zombie social suppression`, `2ca0e7b Add former-colonist selection smoke`, `38f4943 Suppress all zombie clamors`, `5c15373 Restore zombie record suppression`, `3eb246c Restore zombie health needs suppression`, `635be71 Restore zombie blood filth contract`, `9c46bc0 Add zombie corpse management smoke`, `623822f Add corpse conversion bridge smoke`, `3045b9d Add fogged door room spawn smoke`, `ce0d6a5 Add fog blocker room spawn smoke`, `81a2198 Add fog blocker replacement smoke`, `13f3b89 Restore Zombieland menu texture`, and `24e55cc Harden corpse bridge diagnostics`.

| Location | Patch class | Status | RimWorld 1.6 evidence | Coverage disposition |
| --- | --- | --- | --- | --- |
| `Source/Patches.cs:4707` | `RelationsUtility_HasAnySocialMemoryWith_Patch` | `resolved/runtime` | `RelationsUtility.HasAnySocialMemoryWith` exists (`88df42e0e943404ba42043aebd37585c:0600D06F:M`) and vanilla scans social memories for `OtherPawn() == otherPawn`. Runtime `zombieland/zombie_social_thought_suppression` from `EMPTY.rws` proved `hasAnySocialMemoryWithZombie=false` for an ordinary colonist actor and a normal zombie. | Covered for a normal zombie social-memory partner. |
| `Source/Patches.cs:4722` | `Pawn_RelationsTracker_OpinionOf_Patch` | `resolved/runtime` | `Pawn_RelationsTracker.OpinionOf` exists (`88df42e0e943404ba42043aebd37585c:0600D012:M`) and vanilla sums relations plus mood-thought opinion offsets. Runtime `zombieland/zombie_social_thought_suppression` proved both actor opinion of zombie and zombie opinion of actor returned `0`. | Covered for bidirectional ordinary-colonist/normal-zombie opinion suppression. |
| `Source/Patches.cs:4748` | `RelationsUtility_PawnsKnowEachOther_Patch` | `resolved/runtime` | `RelationsUtility.PawnsKnowEachOther` exists (`88df42e0e943404ba42043aebd37585c:0600D063:M`) and vanilla can return true for same faction, direct relations, or social memories. Runtime `zombieland/zombie_social_thought_suppression` proved both directions returned false for a colonist and normal zombie. | Covered for normal zombie acquaintance suppression. |
| `Source/Patches.cs:4764` | `ThoughtHandler_GetSocialThoughts_Patch` | `resolved/runtime` | `ThoughtHandler.GetSocialThoughts(Pawn otherPawn, List outThoughts)` exists (`88df42e0e943404ba42043aebd37585c:0600D0B1:M`) and vanilla appends memory and situational social thoughts. Runtime `zombieland/zombie_social_thought_suppression` proved `GetSocialThoughts(zombie, outThoughts)` returned an empty list for the colonist actor. | Covered for social-thought list clearing with a normal zombie participant. |
| `Source/Patches.cs:4779` | `SituationalThoughtHandler_AppendSocialThoughts_Patch` | `resolved/runtime` | `SituationalThoughtHandler.AppendSocialThoughts(Pawn otherPawn, List outThoughts)` exists (`88df42e0e943404ba42043aebd37585c:0600D09B:M`) and vanilla recalculates cached social thoughts, updates `lastQueryTick`, and appends active situational social thoughts. Runtime `zombieland/zombie_social_thought_suppression` confirmed the installed Zombieland prefix on this target. | Covered for situational social-thought suppression: the verifier directly invoked the prefix with an ordinary human target and a normal zombie target, proving `humanOtherResult=true` while `zombieOtherResult=false`, alongside the public `GetSocialThoughts(zombie)` empty-list check. |
| `Source/Patches.cs:4789` | `Corpse_GiveObservedThought_Patch` | `resolved/runtime` | `Corpse.GiveObservedThought()` exists (`88df42e0e943404ba42043aebd37585c:060036B9:M`). Runtime `zombieland/zombie_social_thought_suppression` killed the test zombie into a `ZombieCorpse` and proved `GiveObservedThought(actor)` returned null. | Covered for observed zombie-corpse thought suppression. |
| `Source/Patches.cs:4799` | `Corpse_GiveObservedHistoryEvent_Patch` | `resolved/runtime` | `Corpse.GiveObservedHistoryEvent(Pawn observer)` exists (`88df42e0e943404ba42043aebd37585c:060036BA:M`) and vanilla dereferences `InnerPawn.RaceProps.Humanlike` before returning observed-corpse history events. Runtime `zombieland/zombie_social_thought_suppression` proved `GiveObservedHistoryEvent(actor)` returned null for the zombie corpse. | Covered for observed zombie-corpse history-event suppression. |
| `Source/Patches.cs:4812` | `ThoughtUtility_CanGetThought_Patch` | `resolved/runtime` | `ThoughtUtility.CanGetThought(Pawn pawn, ThoughtDef def, Boolean checkIfNullified)` exists (`88df42e0e943404ba42043aebd37585c:06013B4D:M`) and vanilla walks developmental stage, gender, quest lodger, expectation, trait, hediff, and nullification rules. Runtime `zombieland/zombie_social_thought_suppression` proved an ordinary actor could receive `DebugBad` while the normal zombie could not. | Covered for normal zombie thought eligibility suppression with an ordinary-pawn control. |
| `Source/Patches.cs:4830` | `ForbidUtility_SetForbiddenIfOutsideHomeArea_Patch` | `resolved/runtime` | `ForbidUtility.SetForbiddenIfOutsideHomeArea(Thing t)` exists (`88df42e0e943404ba42043aebd37585c:06010135:M`) and vanilla forbids spawned things outside home. Runtime `zombieland/zombie_corpse_alert_forbid_contract` from `EMPTY.rws` proved a human corpse outside home became forbidden, while normal and former zombie corpses stayed unforbidden. | Covered for ordinary-corpse control plus normal/former zombie corpse auto-forbid suppression. |
| `Source/Patches.cs:4843` | `Pawn_InteractionsTracker_TryInteractWith_Patch` | `resolved/runtime` | `Pawn_InteractionsTracker.TryInteractWith(Pawn recipient, InteractionDef intDef = null)` exists (`88df42e0e943404ba42043aebd37585c:0600CFE6:M`) and vanilla can add interaction thoughts, skill XP, exposure, social fights, play logs, and motes. Runtime `zombieland/zombie_social_thought_suppression` proved `actor.interactions.TryInteractWith(zombie, Chitchat)` and `zombie.interactions.TryInteractWith(actor, Chitchat)` both returned false. | Covered for bidirectional colonist/normal-zombie interaction suppression. |
| `Source/Patches.cs:4858` | `Pawn_InteractionsTracker_InteractionsTrackerTickInterval_Patch` | `resolved/runtime` | `Pawn_InteractionsTracker.InteractionsTrackerTickInterval(Int32 delta)` exists (`88df42e0e943404ba42043aebd37585c:0600CFE3:M`) and vanilla clears `wantsRandomInteract` immediately when `CurrentSocialMode` is `Off`. Runtime `zombieland/zombie_social_thought_suppression` temporarily forced the actor and zombie into no-faction `Off` mode, set both `wantsRandomInteract=true`, ticked both trackers, and proved the actor flag cleared while the zombie flag stayed true. | Covered for direct zombie interaction-tick suppression with an ordinary pawn control. |
| `Source/Patches.cs:4871` | `PawnNameColorUtility_PawnNameColorOf_Patch` | `resolved/runtime` | `PawnNameColorUtility.PawnNameColorOf(Pawn pawn)` exists (`88df42e0e943404ba42043aebd37585c:0600265C:M`) and vanilla chooses colors from mental state, prisoner/slave/wildman/player/hostile/neutral state. Runtime `zombieland/zombie_selection_respects_former_colonist` proved a former-colonist zombie gets Zombieland label color `{r:0.7,g:1,b:0.7,a:1}` while an ordinary zombie does not. | Covered for live normal/former zombie label-color split. |
| `Source/Patches.cs:4891` | `Selector_SelectInternal_Patch` | `resolved/runtime` | `Selector.SelectInternal(Object obj, Boolean playSound, Boolean forceDesignatorDeselect)` exists (`88df42e0e943404ba42043aebd37585c:06012B76:M`) and vanilla selects valid non-destroyed things/zones/plans. Runtime `zombieland/zombie_selection_respects_former_colonist` proved the selector rejects an ordinary zombie corpse and accepts a former-colonist zombie corpse. | Covered for normal/former zombie corpse selection through the real selector. |
| `Source/Patches.cs:4906` | `ThingSelectionUtility_SelectableByMapClick_Patch` | `resolved/runtime` | `ThingSelectionUtility.SelectableByMapClick(Thing t)` exists (`88df42e0e943404ba42043aebd37585c:06012B37:M`) and vanilla checks selectable defs, hidden pawns, spawned parent, and fogged occupied cells. Runtime `zombieland/zombie_selection_respects_former_colonist` proved ordinary live zombies and corpses are not map-click selectable, while former-colonist live zombies and corpses are selectable. | Covered for live and corpse map-click selection split between ordinary and former-colonist zombies. |
| `Source/Patches.cs:6029` | `ThingFilter_SetAllow_Patch` | `resolved/runtime` | Exact overload `ThingFilter.SetAllow(ThingDef thingDef, Boolean allow)` exists (`88df42e0e943404ba42043aebd37585c:06005764:M`) and vanilla mutates `allowedDefs` plus virtual defs. The allowlist now identifies `ZombieExtract` and `ZombieSerum` by their Zombieland types rather than English words in localized descriptions. French runtime operation `op_ea7c44ff718a4a7fad305921e29ec8a1` proved extract and serum remain allowed while `Corpse_Zombie` and `Zombie` are blocked. | Covered for language-independent serum/extract allowlisting and zombie pawn/corpse def blocking in `ThingFilter.SetAllow`. |
| `Source/Patches.cs:6069` | `Listing_TreeThingFilter_Visible_Patch` | `resolved/runtime` | Exact private `Listing_TreeThingFilter.Visible(ThingDef td)` exists (`88df42e0e943404ba42043aebd37585c:06004CFB:M`) and vanilla gates player-acquirable/filter-visible defs. Runtime operation `op_c158a531d7ab473f87e44a8d6c1c6d0a` proved a normal `PlayerColony` sees extract and ordinary 100% serum but not makeshift serum, while zombie pawn/corpse defs remain hidden. The visibility expectation is tied to the same `MakeZombieSerum.AvailableNow` faction gate used for acquisition. | Covered for language-independent extract/serum visibility and faction-dependent makeshift-serum visibility. Tribal runs retain both makeshift and ordinary 100% entries. |
| `Source/Patches.cs:6313` | `ThingMaker_MakeThing_Patch` | `resolved/runtime` | `ThingMaker.MakeThing(ThingDef def, ThingDef stuff = null)` exists (`88df42e0e943404ba42043aebd37585c:060039BB:M`) and vanilla constructs `def.thingClass` before `PostMake`. Existing runtime covers zombie corpse-class conversion. Operation `op_c158a531d7ab473f87e44a8d6c1c6d0a` additionally proved that an attempted `ZombieSerumSimple` creation in active non-tribal `PlayerColony` resolves to `Zombie100Serum`; tribal games leave the makeshift def unchanged. | Covered for normal/spitter zombie corpse conversion and the non-tribal makeshift-serum safety boundary. Generic makeshift acquisition is also disabled in XML through tradeability/generation/scatter/debug flags. |
| `Source/Patches.cs:5032` | `PawnCapacitiesHandler_CanBeAwake_Patch` | `resolved/runtime` | `PawnCapacitiesHandler.CanBeAwake` exists (`88df42e0e943404ba42043aebd37585c:17000899:P`) and vanilla returns false when consciousness is below `0.3` unless the race is always-awake, then still rejects deactivated pawns. Runtime `zombieland/zombie_ticking_budget_contract` from `EMPTY.rws` anesthetized a normal zombie and human control to `Consciousness=0.01`; the zombie returned `CanBeAwake=true`, while the human control returned false. | Covered for low-consciousness ordinary zombie awake override with vanilla human control and clean warning-or-higher logs. |
| `Source/Patches.cs:5045` | `Pawn_HealthTracker_MakeDowned_Patch` | `resolved/runtime` | `Pawn_HealthTracker.MakeDowned(DamageInfo? dinfo, Hediff hediff)` exists (`88df42e0e943404ba42043aebd37585c:06003335:M`) and vanilla sets `healthState=Down`, drops/clears jobs, notifies lords/storyteller/records/hediffs, and fires pawn downed notifications. Runtime `zombieland/zombie_ticking_budget_contract` from `EMPTY.rws` seeded 197 pheromone timestamps around a human with kill-circle multiplier enabled, invoked `MakeDowned`, and proved all seeded timestamps cleared while the human became health-downed. The same reflected invocation left `ZombieSpitter` and `ZombieSymbiant` not health-downed. | Covered for non-zombie pheromone cleanup and special zombie downing suppression with clean warning-or-higher logs. |
| `Source/Patches.cs:6431` | `GlobalTextureAtlasManager_BakeStaticAtlases_Patch` | `resolved/runtime` | `GlobalTextureAtlasManager.BakeStaticAtlases()` exists (`967ddb80559449f0a776dafa26a855d1:06003407:M`). RimWorld calls it from the main-thread `ExecuteWhenFinished` phase of `PlayDataLoader.DoPlayLoad`, after static constructors and before atlas bake. The prefix rebuilds the cached Twinkie graphic at that safe lifecycle boundary, avoiding a texture object retained from an earlier play-data/language load. Two French quick-test maps in one process reported `twinkieCachedGraphicApplied=true`, both Twinkie and survival-meal textures present, and zero meal-print, null-GUI-texture, off-thread-Unity, XML, or language errors in `Player.log`. | Covered for repeated same-process play-data initialization and French atlas baking. The graphic refresh deliberately does not run from worker-thread `Game.FinalizeInit`. |
| `Source/Patches.cs:6441` | `Game_FinalizeInit_Patch` | `resolved/runtime` | `Game.FinalizeInit()` exists (`88df42e0e943404ba42043aebd37585c:0600148C:M`) and vanilla finalizes components, sets `ProgramState.Playing`, notifies ideology, resets recipes, and schedules dev palette UI. This is late enough for loaded `WorldPawns` and host references to be available, so the postfix now also purges legacy world-pawn Symbiants through the trauma-free destroy/remove/discard path. French runtime `zombieland/get_status` operations `op_c2219ad79be34e4b9b6ff9eb073fde0a` and `op_5ef45415434d4c25850ee1a14a07c35c` retain the existing Twinkie proof. | `op_799175b3bb604228ad18e3c034b9800b` staged a live linked Symbiant in `WorldPawns` and invoked the same idempotent migration: migrated count `1`, host alive, hediff/link cleared, Symbiant destroyed/discarded/not in `WorldPawns`. The Release build also loaded `EMPTY` through real game finalization before the contract. Existing saved-settings evidence covers the other postfix effects. |
| `Source/Patches.cs:5094` | `Map_FinalizeLoading_Patch` | `resolved/runtime` | `Map.FinalizeLoading()` exists (`88df42e0e943404ba42043aebd37585c:06001CE7:M`) and vanilla merges compressed things, spawns non-buildings/buildings, runs backcompat checks, and calls `FinalizeInit()`. Runtime `ZL_Core_HordeLoop_grid_reset.rws` was created with three ordinary zombies and deliberately primed stale zombie-count grid values `2`, `3`, and `4` (`zombieGrid.totalZombieCount=9`) before save. After `rimworld/load_game_ready`, the same three zombies were loaded/cached, while `zombieGrid.nonZeroCells=0`, `totalZombieCount=0`, and `maxZombieCount=0`. The 2026-05-31 `zombieland/zombie_ticking_budget_contract` rerun then simulated the post-load transient state by clearing the sample cells and setting `lastGotoPosition` invalid, used the real `JobDriver_Stumble.ExecuteMove` path, and proved the destination grid count changed `0 -> 1` while the source cell stayed `0`, `lastGotoPosition` became the destination, and the pather targeted that same destination. | Covered for save-load stale zombie-count grid reset before loaded gameplay resumes and for first movement-driven zombie-count grid repopulation after the reset, with clean warning-or-higher logs before and after reloading `EMPTY`. |
| `Source/Patches.cs:5107` | `Root_Update_Patch` | `resolved/runtime` | `Root.Update()` exists (`88df42e0e943404ba42043aebd37585c:060015B9:M`) and vanilla runs per-frame root update, long-event handling, UI update, and sound update. Runtime `zombieland/startup_support_state` records the installed Zombieland prefix. The dense smoke fixture captured `ZombielandMod.frameWatch` as running after the normal-speed window with `frameWatchElapsedMilliseconds=118`. | Covered for the Zombieland frame-watch boundary and for a repeatable dense-horde smoke read of the live frame-watch state, with clean warning-or-higher logs. |
| `Source/Patches.cs:5226` | `Root_Shutdown_Patch` | `resolved/runtime` | `Root.Shutdown()` exists (`88df42e0e943404ba42043aebd37585c:060015BB:M`) and vanilla shuts down Steam, deletes temp files, and quits the application. Runtime `zombieland/startup_support_state` records the installed Zombieland prefix. | Covered for the Zombieland shutdown cleanup boundary without invoking destructive vanilla shutdown: the contract set `Tools.avoider.running=true`, invoked only the Zombieland prefix, and proved it became false with clean warning-or-higher logs. The prefix now also releases tracked `ZombieSymbiant` render resources from this main-thread shutdown path; the related load-game path is covered by `S-Special-Gauntlet` save-load visual evidence. |
| `Source/Patches.cs:5244` | `GameDataSaveLoader_LoadGame_Patch` | `resolved/runtime` | `GameDataSaveLoader.LoadGame(string saveFileName)` exists (`88df42e0e943404ba42043aebd37585c:0600358A:M`) and queues an asynchronous `PreLoadAct` that calls `MemoryUtility.ClearAllMapsAndWorld()` before creating the replacement `Game`, then disposes the current game on the caller thread. | Covered by `S-Special-Gauntlet` save-load evidence: the visual gauntlet first proved disposing a symbiant `ComputeBuffer` from `MemoryUtility.ClearAllMapsAndWorld`'s long-event thread can native-crash after `Graphics device is null`; the new prefix releases tracked `ZombieSymbiant` render resources before the asynchronous load event. Reloading `ZL_Special_Visual_Gauntlet_00.rws` after the fix produced no crash, no `ComputeBuffer` warning, no warning-or-higher logs, and a clean post-reload screenshot. |
| `Source/Patches.cs:5138` | `Corpse_RotStageChanged_Patch` | `resolved/runtime` | `Corpse.RotStageChanged()` exists (`88df42e0e943404ba42043aebd37585c:060036BD:M`) and vanilla dirties corpse graphics/wound overlays and notifies the colonist bar. Runtime `zombieland/convert_infected_corpse_to_zombie conversionTrigger=rotStage` from `ZL_Settings_Persistence_00.rws` applied a final-stage zombie bite, killed the pawn, moved the corpse from `Fresh` to `Rotting`, queued the corpse (`queueCountBeforeRun=1`), and converted it into one normal zombie. | Covered for infected humanlike corpse conversion on rot-stage transition past fresh with clean warning-or-higher logs. |
| `Source/Patches.cs:5168` | `Corpse_TickRare_Patch` | `resolved/runtime` | `Corpse.TickRare()` exists (`88df42e0e943404ba42043aebd37585c:060036B2:M`) and vanilla runs `TickRareInt`, handles bugged corpses, death refusal, dessicated anomaly flesh beasts, and vanish checks. Runtime `zombieland/convert_infected_corpse_to_zombie conversionTrigger=tickRare` from `ZL_Settings_Persistence_00.rws` forced `Hediff_ZombieInfection.ticksWhenBecomingZombie` from `25039` to `5038` at `GenTicks.TicksGame=5039`, called `Corpse.TickRare()` while the corpse remained `Fresh`, queued the corpse, and converted it into one normal zombie. | Covered for expired dead-pawn zombie infection queuing during rare corpse ticks with clean warning-or-higher logs. |
| `Source/Patches.cs:5198` | `TaleRecorder_RecordTale_Patch` | `resolved/runtime` | `TaleRecorder.RecordTale(TaleDef def, Object[] args)` exists (`832a8ce3293a45e386bdef129cf24a57:0600E70A:M`), and decompiler evidence shows `RecordTale` dereferences `def.ignoreChance` at method entry. `TaleUtility.Notify_PawnDied` (`832a8ce3293a45e386bdef129cf24a57:0600E7BB:M`) reaches `TaleDefOf.KilledChild` at `IL_00ED` for non-adult humanlike victims; in Core-only loadouts that DefOf can be null while Zombieland can produce child-aged humanlike zombies. Runtime `zombieland/zombie_death_thought_suppression` on `RightClickAndPhase2Breaking.rws` confirmed the installed prefix and `TaleRecorder.RecordTale(null, Array.Empty<object>())` returned null without exception. | Covered for null `TaleDef` hardening while preserving all non-null vanilla tale recording. This fixes player log D913C0A for Core-only child-aged zombie deaths such as Bene/Maviz. |
| `Source/Patches.cs:5311` | `HealthCardUtility_DrawOverviewTab_Patch` | `resolved/runtime` | `HealthCardUtility.DrawOverviewTab(Rect rect, Pawn pawn, Single curY)` exists (`88df42e0e943404ba42043aebd37585c:06012108:M`) and RimWorld 1.6 still draws the overview tab first, returns the final `curY`, and uses the same left overview column that Zombieland extends. Runtime `zombieland/infection_medical_state actionMode:health-card-living` and `actionMode:health-card-dead` recorded the installed Zombieland postfix on this target. | Covered for living and dead infected health-card UI. From `EMPTY`, the living fixture selected an infecting colonist, opened real `ITab_Pawn_Health`, and `rimworld/get_ui_layout` captured the overview label `This body is infected with the zombie virus!` below vanilla capacity rows. After reloading `EMPTY`, the dead fixture selected an infected human corpse, opened the same Health tab, and captured the same label below the dead-pawn pain row plus the `Zombie infection` and `Zombie bite (36.0h to become a zombie)` hediff rows. Warning-or-higher logs were empty before and after reloading `EMPTY`. |
| `Source/Patches.cs:5244` | `PawnComponentsUtility_RemoveComponentsOnKilled_Patch` | `resolved/runtime` | `PawnComponentsUtility.RemoveComponentsOnKilled(Pawn pawn)` exists (`88df42e0e943404ba42043aebd37585c:0600C3E6:M`) and vanilla nulls killed-pawn components. RimWorld 1.6 `Pawn.Kill` (`88df42e0e943404ba42043aebd37585c:0600306B:M`) stores `prevMap` before destruction and calls `RemoveComponentsOnKilled(this)` after the pawn is no longer spawned. Runtime `zombieland/zombie_ticking_budget_contract` confirmed the installed Zombieland postfix. | Covered for real killed-pawn kill-circle cleanup after fixing the map source to use the kill-time map fallback: the contract killed a colonist through `Pawn.Kill`, observed `pawn.Map == null` and `pawn.MapHeld != null` after kill, verified carry/needs/jobs/stances removal, and proved all 197 seeded pheromone cells cleared with clean warning-or-higher logs. |
| `Source/Patches.cs:5373` | `PawnDiedOrDownedThoughtsUtility_TryGiveThoughts_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `PawnDiedOrDownedThoughtsUtility.TryGiveThoughts(Pawn victim, DamageInfo? dinfo, PawnDiedOrDownedThoughtsKind thoughtsKind)` (`88df42e0e943404ba42043aebd37585c:0600C3F0:M`). A 2026-06-01 rw14/rw16 decompiler compare confirmed the signature is unchanged from 1.4; 1.6 only adds `wasLeftBehindStartingPawn` and transporters naming. Runtime `zombieland/zombie_death_thought_suppression` confirmed the installed Zombieland prefix. | Covered by `zombieland/zombie_death_thought_suppression` under active Biotech. Adult control behavior stayed intact: a human death added one `KnowColonistDied` memory, while adult normal zombie death added zero memories. The child exception now has real active-Biotech evidence: a generated child zombie had `childZombieIsChild=true`, `TryGiveThoughts` added one `KilledChild` memory, and the known old `EMPTY` hidden-ritual migration warning was the only warning-or-higher log entry. |
| `Source/Patches.cs:5386` | `IndividualThoughtToAdd_Constructor_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `IndividualThoughtToAdd..ctor(ThoughtDef thoughtDef, Pawn addTo, Pawn otherPawn, Single moodPowerFactor, Single opinionOffsetFactor)` (`88df42e0e943404ba42043aebd37585c:0600C3EE:M`), and runtime `zombieland/zombie_death_thought_suppression` confirmed the Zombieland prefix is installed. The 1.4 and 1.6 constructor bodies are identical and still apply `moodPowerFactor` directly to the created `Thought_Memory`. | Covered by the active-Biotech child branch in `zombieland/zombie_death_thought_suppression`: the child zombie death-thought path added one `KilledChild` memory to the adult observer, and the Zombieland prefix reduced `zombieKilledChildMoodFactor` to `0.5`. This closes the earlier dependency gate that could not be reached under the Core-only loadout. |
| `Source/Patches.cs:5396` | `Thought_Tale_OpinionOffset_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `Thought_Tale.OpinionOffset()` (`88df42e0e943404ba42043aebd37585c:06009006:M`), and runtime `zombieland/zombie_death_thought_suppression` confirmed the Zombieland postfix is installed. The 1.4 and 1.6 `OpinionOffset()` bodies are identical: they get the latest tale for `def.taleDef` and `otherPawn`, then return `baseOpinionOffset` scaled by tale age. | Covered by the active-Biotech child branch in `zombieland/zombie_death_thought_suppression`: the contract used real `KilledChild_Opinion`/`TaleDefOf.KilledChild` defs and a zombie-faction child tale, then measured `zombieKilledChildOpinionOffset=-5` from `killedChildBaseOpinion=-20`, matching the Zombieland `0.25` reduction. The direct synthetic opinion probe had to force the first situational stage after `RecalculateState()` left the hand-built `Thought_Tale` inactive; this is fixture setup only and does not bypass the postfix target or real Biotech defs. |
| `Source/Patches.cs:5314` | `ImmunityHandler_ImmunityHandlerTickInterval_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `Verse.ImmunityHandler.ImmunityHandlerTickInterval(Int32 delta)` (`40ca93f8c2cf4459bd8a72c25d1bfb81:06002FBA:M`). Runtime `zombieland/zombie_health_needs_upkeep_suppression` applied `Plague` to a human control and normal zombie, then ticked both immunity handlers for `60000` ticks. | Covered for Zombieland-pawn immunity suppression: human immunity advanced from `0` to `0.5890898` with one record, while zombie immunity stayed `0` with zero records. |
| `Source/Patches.cs:5328` | `Projectile_Launch_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `Verse.Projectile.Launch(Thing launcher, Vector3 origin, LocalTargetInfo usedTarget, LocalTargetInfo intendedTarget, ProjectileHitFlags hitFlags, Boolean preventFriendlyFire, Thing equipment, ThingDef targetCoverDef)` (`88df42e0e943404ba42043aebd37585c:060038D2:M`). Runtime `S-Core-Horde-Loop` sub-contract evidence ran `zombieland/zombie_ticking_budget_contract` from `EMPTY.rws`. | Covered for the projectile-launch pheromone boundary: a human bolt-action projectile launch changed 341 pheromone cells around the launcher, while a `ZombieSpitter` launcher using the same projectile/target changed zero cells, proving the non-spitter pawn gate and spitter exclusion. Scaling by exact muzzle flash/range remains source-evidenced by the target/body formula. |
| `Source/Patches.cs:5351` | `Pawn_PathFollower_WillCollideWithPawnAt_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is private `Pawn_PathFollower.WillCollideWithPawnAt(IntVec3 c, Boolean forceOnlyStanding, Boolean useId)` (`88df42e0e943404ba42043aebd37585c:06006323:M`). Runtime `S-Core-Horde-Loop` sub-contract evidence ran `zombieland/zombie_ticking_budget_contract` from `EMPTY.rws`. | Covered for direct zombie path-collision suppression: reflected `WillCollideWithPawnAt` on a spawned normal zombie returned false for an adjacent probe cell. Dense moving-horde overlap remains broader scenario work. |
| `Source/Patches.cs:7078` | `PawnCollisionTweenerUtility_PawnCollisionPosOffsetFor_Patch` | `resolved/runtime` | Current Steam RimWorld 1.6 target is `PawnCollisionTweenerUtility.PawnCollisionPosOffsetFor(Pawn pawn)` (`967ddb80559449f0a776dafa26a855d1:0600266F:M`). Vanilla includes this value in `PawnTweener.TweenedPosRoot`, offsetting a stationary standing Pawn by up to 0.32 cells when another Pawn stands on or approaches its root cell. The prefix returns zero only for ordinary Zombieland zombies and the Pawn-backed Symbiant; the separate `JitterHandler.CurrentOffset` damage-feedback component remains untouched. | Existing `S-Core-Horde-Loop` evidence covers direct normal-zombie collision suppression. The player save `symb-zombie-pathing` established the Symbiant regression: 900 ticks kept shared health `14967/14967`, 14 render elements, and zero active cell motions while passing zombies visibly shifted the full mesh. The Symbiant-specific zero-offset follow-up removes only that visual collision response. |
| `Source/Patches.cs:5499` | `Pawn_NeedsTracker_AllNeeds_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `Pawn_NeedsTracker.AllNeeds` (`88df42e0e943404ba42043aebd37585c:17001F05:P`). Runtime `zombieland/zombie_health_needs_upkeep_suppression` confirmed the installed prefix on `RimWorld.Pawn_NeedsTracker::get_AllNeeds()`. | Covered for Zombieland needs hiding: a forced zombie `Food` need remained visible in the reflected internal list (`internalCount=1`) while `AllNeeds` still returned `allCount=0`; the human control exposed ten needs through both internal and public paths. |
| `Source/Patches.cs:5513` | `Pawn_NeedsTracker_AddOrRemoveNeedsAsAppropriate_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `Pawn_NeedsTracker.AddOrRemoveNeedsAsAppropriate()` (`88df42e0e943404ba42043aebd37585c:0600CB84:M`). Runtime `zombieland/zombie_health_needs_upkeep_suppression` confirmed the installed prefix on `RimWorld.Pawn_NeedsTracker::AddOrRemoveNeedsAsAppropriate()`. | Covered for zombie need reconciliation: after forcing `Food`, real reconciliation cleared the internal and public lists (`internalCount=0`, `allCount=0`) and rebound direct fields (`hasFoodField=false`, `hasMoodField=false`) while human needs remained populated. |
| `Source/Patches.cs:5527` | `Pawn_NeedsTracker_NeedsTrackerTickInterval_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `Pawn_NeedsTracker.NeedsTrackerTickInterval(Int32 delta)` (`88df42e0e943404ba42043aebd37585c:0600CB7E:M`). Runtime `zombieland/zombie_health_needs_upkeep_suppression` confirmed the installed prefix on `RimWorld.Pawn_NeedsTracker::NeedsTrackerTickInterval(System.Int32 delta)`. | Covered for zombie need tick suppression: a forced zombie `Food` need stayed at `0.8 -> 0.8` through a real `NeedsTrackerTickInterval(150)`, then was cleared by final reconciliation; the run completed with clean warning-or-higher logs. |
| `Source/Patches.cs:5579` | `Pawn_HearClamor_Patch` | `resolved/runtime` | `Pawn.HearClamor(Thing source, ClamorDef type)` exists (`ca21103994f9441fa83eb29a9c8af0b6:0600308F:M`). Runtime `S-Core-Horde-Loop` sub-contract evidence reran `zombieland/zombie_clamor_suppression` from `EMPTY.rws` after decompiling `GenClamor.DoClamor` and `Pawn_MindState.Notify_ClamorImpact`. | Covered at the clamor boundary together with the `GenClamor` fanout: ordinary human source reached the listener once, while normal zombie, spitter, and symbiant clamor sources produced zero listener effects. The patch now suppresses when either source or hearer is a Zombieland pawn; human impact clamor advanced the human listener `canSleepTick 4038 -> 6038`, while the zombie hearer stayed at `4038`. |
| `Source/Patches.cs:5485` | `Pawn_RecordsTracker_GetValue_Patch` | `resolved/runtime` | `Pawn_RecordsTracker.GetValue(RecordDef def)` exists (`88df42e0e943404ba42043aebd37585c:0600CDCC:M`). Runtime `S-Core-Horde-Loop` sub-contract evidence ran `zombieland/zombie_records_suppression` from `EMPTY.rws`. | Covered for public float record reads: after attempted zombie `Increment` and `AddTo`, the selected zombie `Kills` record still returned `0` while the human control returned `3`. |
| `Source/Patches.cs:5499` | `Pawn_RecordsTracker_GetAsInt_Patch` | `resolved/runtime` | `Pawn_RecordsTracker.GetAsInt(RecordDef def)` exists (`88df42e0e943404ba42043aebd37585c:0600CDCD:M`). Runtime `S-Core-Horde-Loop` sub-contract evidence ran `zombieland/zombie_records_suppression` from `EMPTY.rws`. | Covered for public integer record reads: after attempted zombie `Increment` and `AddTo`, the selected zombie `Kills` int value still returned `0` while the human control returned `3`. |
| `Source/Patches.cs:5617` | `Pawn_PathFollower_CostToMoveIntoCell_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is static `Pawn_PathFollower.CostToMoveIntoCell(Pawn pawn, IntVec3 c)` (`88df42e0e943404ba42043aebd37585c:06006331:M`). Runtime `zombieland/tar_slime_move_cost_contract` confirmed the installed Zombieland postfix on `Verse.AI.Pawn_PathFollower::CostToMoveIntoCell(Pawn, IntVec3)`. | Covered for tar-slime movement cost: at difficulty `1`, human tar cost matched the non-zombie formula (`91.2000046`), normal zombie and spitter tar cost matched the Zombieland-pawn formula (`122.8`), and all three clear-cell costs differed from tar-cell costs. The fixture now snapshots evidence and cleans up spawned pawns/tar before returning; warning-or-higher logs were empty after rerun. |
| `Source/Patches.cs:5634` | `Pawn_HealthTracker_DropBloodFilth_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `Pawn_HealthTracker.DropBloodFilth()` (`88df42e0e943404ba42043aebd37585c:0600333D:M`). Runtime `zombieland/zombie_blood_filth_contract` confirmed installed Zombieland patches on `Verse.Pawn_HealthTracker::DropBloodFilth()`. | Covered for blood-filth gating: with `zombiesDropBlood=true`, human, normal zombie, and spitter each dropped one `Filth_Blood`, while tanky with active shield/suit/helmet dropped none; with `zombiesDropBlood=false`, human still dropped one blood filth while normal zombie and spitter dropped none. The fixture now snapshots evidence and cleans up spawned pawns/filth before returning; warning-or-higher logs were empty. |
| `Source/Patches.cs:5590` | `Page_SelectScenario_BeginScenarioConfiguration_Patch` | `semantic/runtime` | `Page_SelectScenario.BeginScenarioConfiguration(Scenario scen, Page originPage)` exists (`88df42e0e943404ba42043aebd37585c:06012471:M`) and still creates `Current.Game`, assigns the scenario, calls `Scenario.PreConfigure()`, and links the first scenario config page before `InitGameStart`. Runtime `zombieland/settings_state actionMode:new-game` records the installed Zombieland prefix on the target. | Covered at the patch boundary for default reset before scenario configuration: the verifier seeded sentinel game settings (`threatScale=9.25`, `maximumNumberOfZombies=925`, `attackMode=OnlyColonists`, scroll `(17,23)`), invoked the installed prefix body, and observed default day-0 values (`threatScale=1`, `maximumNumberOfZombies=2000`, `attackMode=OnlyHumans`) plus scroll reset `(0,0)`. Operation `op_5f0011f54cbe4dd7bb7568f9bdfa5702` also invoked the installed prefix against eight valid/malformed timeline shapes; all repaired without exception while preserving complete frames and day-zero fallback semantics. Full mouse-driven new-colony wizard evidence remains additive. |
| `Source/Patches.cs:5602` | `PageUtility_StitchedPages_Patch` | `resolved/runtime` | `PageUtility.StitchedPages(IEnumerable<Page> pages)` exists (`88df42e0e943404ba42043aebd37585c:06012367:M`) and still converts the input pages to a list, assigns `prev`/`next`, and returns the first page. Runtime `zombieland/settings_state actionMode:new-game` called the real patched method with a two-page list. | Covered for the stitched page flow: the patched call transformed `[Page_SelectScenario, Page_SelectStoryteller]` into `[Page_SelectScenario, Dialog_Settings, Page_SelectStoryteller]`, and the `prev`/`next` links were exactly `SelectScenario -> Dialog_Settings -> SelectStoryteller`. |
| `Source/Patches.cs:5614` | `Game_InitNewGame_Patch` | `semantic/runtime` | `Game.InitNewGame()` exists (`88df42e0e943404ba42043aebd37585c:06001486:M`) and still generates the starting map, finalizes init, assigns `CurrentMap`, applies scenario/research/new-game components, and clears `initData`. Runtime `zombieland/settings_state actionMode:new-game` records the installed Zombieland postfix on the target. | Covered at the postfix boundary for the intended side effect: a temporary player colonist spawned on `EMPTY` was set to `HostilityResponseMode.Ignore`; invoking the installed postfix changed it to `Attack`. Warning-or-higher logs were empty before and after reloading `EMPTY`. Full quick-start new-game evidence remains additive. |
| `Source/Patches.cs:5627` | `Pawn_HealthTracker_PreApplyDamage_Patch` | `semantic/runtime` | `Pawn_HealthTracker.PreApplyDamage(DamageInfo dinfo, out Boolean absorbed)` exists (`88df42e0e943404ba42043aebd37585c:06003320:M`) and still contains the `MemoryThoughtHandler.TryGainMemory(ThoughtDefOf.HarmedMe, pawn2)` path the transpiler searches for. Runtime `zombieland/zombie_damage_memory_suppression` confirmed the installed Zombieland transpiler. | Covered for zombie-inflicted damage memory suppression with a human attacker control: human-on-human damage added one `HarmedMe` memory, while normal-zombie-on-human damage dealt real damage and added zero memories. The fixture now cleans up spawned pawns, and warning-or-higher logs were empty. |
| `Source/Patches.cs:5671` | `MainTabWindow_Menu_RequestedTabSize_Path` | `resolved/runtime` | `MainTabWindow_Menu.RequestedTabSize` exists (`88df42e0e943404ba42043aebd37585c:17003132:P`) and vanilla returns `450x390`. Runtime `rimworld/open_main_tab mainTabId=Menu` showed the in-game menu opened at `450x442`, matching the Zombieland `addedHeight=52`. | Covered for the in-game menu height expansion; the injected controls fit inside the opened menu window and warning-or-higher logs were empty. |
| `Source/Patches.cs:5680` | `MainTabWindow_Menu_DoWindowContents_Path` | `resolved/runtime` | `MainTabWindow_Menu.DoWindowContents(Rect rect)` exists (`88df42e0e943404ba42043aebd37585c:06012C53:M`) and still delegates to `MainMenuDrawer.DoMainMenuControls(rect, anyGameFiles)`. Runtime `rimworld/get_ui_layout surfaceId=main-tab:Menu` showed the controls were drawn inside the expanded menu surface. | Covered with the same in-game menu UI pass: `Zombieland` appeared between `Review scenario` and vanilla `Options`, and the web-link column included `Brrainz`; warning-or-higher logs were empty. |
| `Source/Patches.cs:5689` | `MainMenuDrawer_DoMainMenuControls_Path` | `resolved/runtime` | `MainMenuDrawer.DoMainMenuControls(Rect rect, Boolean anyMapFiles)` exists (`88df42e0e943404ba42043aebd37585c:06012338:M`) and still contains exactly two `OptionListingUtility.DrawOptionListing` calls: one for the main option list and one for the web-link list. Runtime layout in playing state recorded the injected `Zombieland` option and `Brrainz` link. | Covered for in-game settings entry and link insertion: clicking the `Zombieland` invisible-button target closed `MainTabWindow_Menu` and opened real `RimWorld.Dialog_ModSettings` for package `brrainz.zombieland` with `settingsCategory="Zombieland settings"`; `rimbridge/list_logs minimumLevel=warning` returned no entries after the UI pass. |
| `Source/Patches.cs:5748` | `RegionAndRoomUpdater_CreateOrUpdateRooms_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `RegionAndRoomUpdater.CreateOrUpdateRooms()` (`88df42e0e943404ba42043aebd37585c:060020AB:M`); the 1.4 and 1.6 signatures still match, and 1.6 `TryRebuildDirtyRegionsAndRooms()` still regenerates dirty regions before calling `CreateOrUpdateRooms()`. Runtime `zombieland/incident_threat_state actionMode:pathingRegions` records the installed Zombieland postfix on this target. | Covered for zombie smart-wander region refresh after room/region rebuild: the verifier built a proper 25-cell room, observed an initial `zombiePathing` graph with 291 indexed valid regions, cleared both pathing caches to zero, then called real `RegionAndRoomUpdater.RebuildAllRegionsAndRooms()`. The installed postfix repopulated 291 indexed valid regions, including one root, 290 child/backpoint regions, and valid `GetWanderDestination` samples through the door/normal-region parent chain. No warning-or-higher logs before or after reloading `EMPTY`. |
| `Source/Patches.cs:5759` | `Building_DeSpawn_Patch` | `resolved/runtime` | `Building.DeSpawn(DestroyMode mode = Vanish)` exists (`88df42e0e943404ba42043aebd37585c:06003662:M`). Runtime `S-Incident-Threat` evidence destroyed fog-making walls both with ordinary deconstruction and `DestroyMode.WillReplace`: deconstruction spawned two room zombies and unfogged the room, while replacement spawned none and preserved fog. | Covered for fog-blocker removal and replacement suppression in `ZL_Incident_Threat_base.rws`. |
| `Source/Patches.cs:5800` | `FogGrid_Notify_PawnEnteringDoor_Patch` | `resolved/runtime` | `FogGrid.Notify_PawnEnteringDoor(Building_Door door, Pawn pawn)` exists (`88df42e0e943404ba42043aebd37585c:06001A2B:M`). Runtime `S-Incident-Threat` evidence called `Notify_PawnEnteringDoor` on a sealed fogged room: hostile entry preserved fog and spawned no zombies; player entry unfogged the room and spawned two room zombies. | Covered for fogged-door player entry spawning in `ZL_Incident_Threat_base.rws`. |
| `Source/Patches.cs:5935` | `FloatMenuMakerMap_GetOptions_Patch` | `resolved/runtime` | `FloatMenuMakerMap.GetOptions(List<Pawn> selectedPawns, Vector3 clickPos, out FloatMenuContext context)` exists (`88df42e0e943404ba42043aebd37585c:06012504:M`) and RimWorld 1.6 still builds a `FloatMenuContext`, validates selected pawns/clicked cell, calls `GetProviderOptions(context, list)`, and returns the mutable `List<FloatMenuOption>` that the Zombieland postfix appends to. Runtime `zombieland/defense_room_state actionMode:floatMenu` records the installed Zombieland postfix on this target. | Covered for both custom menu branches through real `FloatMenuMakerMap.GetOptions`: from `ZL_Defense_Room_base.rws`, a reachable/reservable colonist clicking powered valid-room shocker `Thing_ZombieShocker6914` received one enabled `Start zombie shocker` option; clicking a confused/paralyzed zombie received one enabled `Capture this zombie` option; clicking an ordinary non-confused zombie produced no rope option; and an empty selected-pawn list produced no Zombieland custom options. Warning-or-higher logs were empty before and after reloading the fixture. |
| `Source/Patches.cs:5865` | `Messages_MessagesDoGUI_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `Messages.MessagesDoGUI()` (`88df42e0e943404ba42043aebd37585c:06004D4B:M`); decompiler evidence shows vanilla starts from `MessagesTopLeftStandard`, applies lesson offset, and calls each live message's `Draw(xOffset, num)` before increasing by `26`. Runtime `zombieland/area_workflow_state actionMode:warning-ui` records the installed Zombieland prefix on this static GUI method. | Covered for dangerous-area warning ordering: from `EMPTY`, the verifier created one colonist-inside and one zombie-inside danger entry, added a real live RimWorld message, and screenshot `/Users/ap/Library/Application Support/RimWorld/Screenshots/zl_area_warning_ui.png` recorded the danger immediate window at `y=3` and the normal message immediate window at `y=43`, proving the warning is drawn before/above live messages. No warning-or-higher logs. |
| `Source/Patches.cs:5876` | `Message_Draw_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `Message.Draw(Int32 xOffset, Int32 yOffset)` (`88df42e0e943404ba42043aebd37585c:06004D3E:M`); decompiler evidence shows the method calculates its rect from the supplied `yOffset` before drawing the `ImmediateWindow`. Runtime `zombieland/area_workflow_state actionMode:warning-ui` records the installed Zombieland prefix on this method. | Covered for warning-driven message shifting: the verifier reflected the installed prefix and proved no shift while `ZombieAreaManager.warningShowing=false` (`16 -> 16`) and the expected 29-pixel shift while true (`16 -> 45`). The same live screenshot placed the normal message window at `y=43`, below the danger warning strip/window. No warning-or-higher logs. |
| `Source/Patches.cs:5887` | `Pawn_IdeoTracker_ExposeData_Patch` | `semantic/runtime` | Installed RimWorld 1.6 target is `Pawn_IdeoTracker.ExposeData()` (`88df42e0e943404ba42043aebd37585c:0600CD34:M`); the 1.4 and 1.6 signatures still match. The 1.6 body still scribes `ideo`, `previousIdeos`, certainty, join tick, and baby exposure before its `Scribe.mode == PostLoadInit` branch, but its fallback warning now gates on `Ideo == null && pawn.ShouldHaveIdeo` instead of the older non-baby check. Runtime `zombieland/get_status` records the installed Zombieland transpiler on this virtual method. | Covered for save-load suppression and ordinary pawn pass-through in `ZL_Ideo_Load_Suppression_00.rws`: before save and after reload, three ordinary colonists still had ideo trackers and `Astropolitan` ideos with certainty values, while the saved normal zombie had an ideo tracker but `Ideo=null` despite `ShouldHaveIdeo=true`. The post-load `rimbridge/list_logs minimumLevel=warning` result was empty, proving the zombie no-ideo fallback warning was suppressed without stripping ordinary colonist ideology state. `EMPTY` was reloaded afterward and warning-or-higher logs stayed empty. |

## Chainsaw, Equipment, Combat Jobs, And Defense Static Targets

This slice covers the 21 static rows in `patches-chainsaw-equipment-jobs`. It owns chainsaw tick/draft/gizmo/render behavior, smart melee bite blocking, ranged hit adjustment against zombies, active electrifier melee effects, zombie job whitelisting, faction goodwill suppression, story-danger/flee filtering, animal manhunter/prey response, and a disabled historical pathfinder hook. Relevant commit evidence includes `add9e67 Add chainsaw equip toggle bridge smoke`, `3533ac7 Add chainsaw slaughter bridge smoke`, `09cf58c Fix chainsaw repair workgiver`, `18aa8dc Cover chainsaw pressure drop`, `394e740 Optimize chainsaw ticking`, `4f4a288 Recheck chainsaw contracts`, `846e5cf Document chainsaw building impact`, `dd90b2f Cover active electrifier attack verbs`, `f3a44b2 Cover electrifier melee shock`, `62343fa Cover albino melee bite filtering`, `74f6efd Restore harmless zombie flee filtering`, `0c6dacb Add avoid grid door and danger smoke`, and `3981f82 Fix zombie movement with async path requests`.

| Location | Patch class | Status | RimWorld 1.6 evidence | Coverage disposition |
| --- | --- | --- | --- | --- |
| `Source/Patches.cs:607` | `Pawn_EquipmentTracker_EquipmentTrackerTick_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `Pawn_EquipmentTracker.EquipmentTrackerTick()` (`88df42e0e943404ba42043aebd37585c:060032D2:M`); decompiler evidence shows vanilla only ticks equippable verbs for equipment whose `tickerType` is not `Normal`, so equipped normal-ticker chainsaws need the Zombieland postfix to run `Chainsaw.Tick()`. Runtime `zombieland/chainsaw_equip_toggle` from `EMPTY.rws` equipped a fueled chainsaw, started it through its enabled command-action gizmo, and advanced 20 real game ticks. | Covered for equipped chainsaw ticking/fuel drain while held: `runningAfterToggle=true`, fuel changed `100 -> 99.93347`, and all 20 samples stayed running with `inactiveCounter` advancing to `40`; no warning-or-higher logs. |
| `Source/Patches.cs:620` | `Pawn_PathFollower_SetupMoveIntoNextCell_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `Verse.AI.Pawn_PathFollower.SetupMoveIntoNextCell()` (`88df42e0e943404ba42043aebd37585c:0600632E:M`); decompiler evidence shows it consumes the next path node into private `nextCell`, computes movement cost, and notifies doors. Runtime `zombieland/chainsaw_equip_toggle` from `EMPTY.rws` now starts a real pawn path while a chainsaw is equipped but not swinging. | Covered for the movement-angle branch: during a real `StartPath`/pather tick, `nextCell=(99,100)` while pawn position stayed `(100,100)`, `expectedAngle=270`, `chainsaw.angle=270`, `angleDelta=0`, `Moving=true`, `MovingNow=true`, and `swinging=false`. The same contract still covered fuel ticking and undraft stop behavior, with no warning-or-higher logs. |
| `Source/Patches.cs:636` | `Pawn_DraftController_setDrafted_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `Pawn_DraftController.Drafted` (`88df42e0e943404ba42043aebd37585c:17001E66:P`); decompiler evidence shows the setter clears priority work/job queues, manages reservations/carrying, and updates `draftedInt`. Runtime `zombieland/chainsaw_equip_toggle` from `EMPTY.rws` toggled the same drafted chainsaw pawn back to undrafted after the fuel-drain run. | Covered for the chainsaw-specific undraft branch: the chainsaw was running before undraft and `runningAfterUndraft=false` immediately after setting `Drafted=false`, with the chainsaw still equipped/unspawned and unbroken. Ordinary non-chainsaw drafted behavior remains vanilla-source evidenced. |
| `Source/Patches.cs:649` | `Pawn_MeleeVerbs_TryMeleeAttack_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `Pawn_MeleeVerbs.TryMeleeAttack(Thing target, Verb verbToUse = null, bool surpriseAttack = false)` (`88df42e0e943404ba42043aebd37585c:0600CD4F:M`); vanilla checks body busy/can-melee/usable melee verb, then calls `verb.TryStartCastOn(target, surpriseAttack)` and returns true. Runtime `zombieland/chainsaw_slaughter_zombie` from `EMPTY.rws` now includes direct suppression probes before the custom chainsaw tick. | Covered for all three Zombieland prefix exits: a chainsaw-equipped human returned `chainsawTryMeleeResult=false` against an adjacent normal zombie with injury staying `0 -> 0` and the zombie still live, while spawned `ZombieSpitter` and `ZombieSymbiant` each returned false from `TryMeleeAttack` against the chainsaw pawn. The same run then killed the normal zombie through the custom chainsaw slaughter path on tick 1 and produced no warning-or-higher logs. |
| `Source/Patches.cs:670` | `FloatMenuUtility_GetMeleeAttackAction_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `FloatMenuUtility.GetMeleeAttackAction(Pawn pawn, LocalTargetInfo target, out string failStr, bool ignoreControlled = false)` (`88df42e0e943404ba42043aebd37585c:06012817:M`); decompiler evidence shows it checks drafted/control/reach/violence/melee-verb gates, returns an `AttackMelee` ordered-job action on success, and returns null with a failure string otherwise. The private `FloatMenuOptionProvider_DraftedAttack.GetMeleeAttackAction` wrapper (`06012600:M`) delegates to this utility. Runtime `zombieland/chainsaw_slaughter_zombie` from `EMPTY.rws` now probes the utility before and after the chainsaw toggle. | Covered for running-chain suppression and vanilla pass-through: stopped chainsaw pawn returned `actionAvailable=true` with empty fail string, ordinary control pawn returned `actionAvailable=true` with empty fail string and `violentDisabled=false`, then after the chainsaw toggle the same chainsaw pawn returned `actionAvailable=false` with `runningAfterToggle=true`. The same contract still killed the adjacent normal zombie through the chainsaw slaughter path on tick 1 and produced no warning-or-higher logs. |
| `Source/Patches.cs:722` | `PawnRenderer_DrawEquipment_Patch` | `resolved/runtime` | `PawnRenderUtility.DrawEquipmentAndApparelExtras(Pawn, Vector3, Rot4, PawnRenderFlags)` exists (`88df42e0e943404ba42043aebd37585c:06002733:M`), and the 2026-06-01 decompiler refresh shows vanilla still draws aimed/carried equipment before worn apparel extras. Runtime `zombieland/chainsaw_slaughter_zombie` from `EMPTY.rws` left the equipped chainsaw pawn running and swinging after tick 1 (`angle=22.5`, `victimHeadDelta=1`, `fuel 100 -> 99.99234`, `HP 500 -> 499`). | Covered for the player-visible chainsaw draw aftermath without adding a new endpoint: generic screenshots `zl_chainsaw_draw_equipment_slaughter_visual__cell_rect.png` and close crop `zl_chainsaw_draw_equipment_slaughter_visual_close__cell_rect.png` show the running/swinging chainsaw pawn, slaughtered zombie/victim-head visual, and no duplicate vanilla equipment draw. The no-double-draw guarantee remains source-tied to the prefix returning false immediately after `PawnRenderUtility.DrawEquipmentAiming(chainsaw, vector, angle)`. Warning-or-higher logs were empty after the pass. |
| `Source/Patches.cs:757` | `Pawn_RotationTracker_UpdateRotation_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `Pawn_RotationTracker.UpdateRotation()` (`88df42e0e943404ba42043aebd37585c:060033C4:M`); decompiler evidence shows vanilla skips destroyed/fixed-rotation/handled-facing cases, faces busy focus targets or path `nextCell`, and otherwise sets drafted pawns to `Rot4.South`. Runtime `zombieland/chainsaw_slaughter_zombie` from `EMPTY.rws` now directly invokes the rotation tracker with the equipped chainsaw in swinging and non-swinging states. | Covered for the prefix gate: starting from `Rot4.North` (`0`), `chainsaw.swinging=true` preserved pawn rotation as North after `UpdateRotation`; after restoring `chainsaw.swinging=false`, the same drafted pawn changed to vanilla `Rot4.South` (`2`). The contract restored `swinging=false`, then still passed float-menu suppression and killed the adjacent zombie through the chainsaw slaughter path on tick 1 with no warning-or-higher logs. |
| `Source/Patches.cs:771` | `CompProperties_Refuelable_SpecialDisplayStats_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `CompProperties_Refuelable.SpecialDisplayStats(StatRequest req)` (`88df42e0e943404ba42043aebd37585c:06010898:M`); decompiler evidence shows vanilla yields base stats and adds a `Building` `ShotsBeforeRearm` entry for refuelable turrets. Runtime `zombieland/chainsaw_equip_toggle` from `EMPTY.rws` now directly enumerates the chainsaw refuelable props with a chainsaw stat request and a vanilla refuelable turret request. | Covered for both prefix replacement and pass-through: chainsaw request returned exactly one `Weapon_Melee` entry labeled `Fuel` with value `100` and priority `3171`; vanilla `Turret_MiniTurret` request returned the vanilla `Building` entry `Shots before rearm` with value `60` and no melee-category replacement. The same contract still passed movement angle, fuel ticking, and undraft stop behavior, with no warning-or-higher logs. |
| `Source/Patches.cs:794` | `Gizmo_RefuelableFuelStatus_GizmoOnGUI_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `Gizmo_SetFuelLevel.GizmoOnGUI(Vector2 topLeft, Single maxWidth, GizmoRenderParms parms)` (`88df42e0e943404ba42043aebd37585c:060108F7:M`); decompiler evidence shows vanilla immediately dereferences `refuelable.Props.showAllowAutoRefuelToggle` before normal slider rendering, so a null refuelable would NRE without the prefix. Runtime `zombieland/chainsaw_equip_toggle` from `EMPTY.rws` now inspects the installed Harmony prefix and the real chainsaw fuel gizmo. | Covered for the null guard and normal pass-through: installed prefix `ZombieLand.Patches+Gizmo_RefuelableFuelStatus_GizmoOnGUI_Patch.Prefix` returned false for null refuelable and true for the real chainsaw refuelable; the normal `Gizmo_SetFuelLevel` stayed visible with width `160`, fuel `100`, target fuel `100`, and `HasFuel=true`. The same contract still passed movement angle, refuelable stats, fuel ticking, and undraft stop behavior, with no warning-or-higher logs. |
| `Source/Patches.cs:806` | `Faction_TryAffectGoodwillWith_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `Faction.TryAffectGoodwillWith(Faction other, Int32 goodwillChange, Boolean canSendMessage, Boolean canSendHostilityLetter, HistoryEventDef reason, Nullable lookTarget)` (`88df42e0e943404ba42043aebd37585c:0600D21C:M`); decompiler evidence shows vanilla checks `CanChangeGoodwillFor`, mutates `RelationWith(other).baseGoodwill`, mirrors the relation onto the other faction, and may emit message/letter side effects. Runtime `zombieland/zombie_faction_world_contract` from `EMPTY.rws` now probes the installed prefix and the real method. | Covered for zombie-faction suppression and ordinary pass-through: installed prefix `ZombieLand.Patches+Faction_TryAffectGoodwillWith_Patch.Prefix` returned `prefixContinue=false` and `prefixResult=false`; zombie-to-player and player-to-zombie goodwill calls both returned false with hostile relations unchanged; an ordinary goodwill-capable outlander/player pair changed from `0` to `15` both ways through vanilla. No warning-or-higher logs. |
| `Source/Patches.cs:824` | `Projectile_ImpactSomething_Patch` | `semantic/runtime` | Installed RimWorld 1.6 target is `Projectile.ImpactSomething()` (`88df42e0e943404ba42043aebd37585c:060038DB:M`); decompiler evidence shows vanilla still has the intended-target laying miss branch and the pawn-interception posture modifier, both through `Pawn.GetPosture()` plus `Rand.Chance(...)`. Runtime `zombieland/area_workflow_state actionMode:ranged-projectiles` from `EMPTY.rws` now records the installed transpiler and exercises a real bolt-action bullet impact against a health-downed normal zombie. | Covered for downed-zombie projectile posture: the helper returned `PawnPosture.Standing` for a zombie, `RandChance(0.5, zombie)=true`, the installed target was `Verse.Projectile.ImpactSomething`, and under deterministic seed `1` where vanilla would miss the laying intended target, the real projectile impact damaged the downed zombie by `18.72` injury. No warning-or-higher logs. |
| `Source/Patches.cs:1090` | `DangerWatcher_CalculateDangerRating_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `DangerWatcher.CalculateDangerRating()` (`967ddb80559449f0a776dafa26a855d1:0600AE40:M`); decompiler evidence shows vanilla sums `map.attackTargetsCache.TargetsHostileToColony` through `CachedAffectsStoryDangerDelegate`, then maps combat power, free colonists, recent harm, and force-danger lords to `StoryDanger`. The current Zombieland postfix adds home-area zombie combat power through `StorytellerEventFilters.AffectsStoryDanger`. | Covered for the Zombieland danger predicate by the danger/flee runtime cases: a spawned active normal zombie in Home returned true, while an active normal zombie outside Home, a roped zombie, a public-downed zombie, and an unspawned zombie returned false. Bridge bookkeeping now records the installed `DangerWatcher_CalculateDangerRating_Patch` instead of the removed AffectsStoryDanger patch; rerun `actionMode:danger-flee` if this patch or predicate changes again. |
| `Source/Patches.cs:906` | `FleeUtility_ShouldFleeFrom_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `FleeUtility.ShouldFleeFrom(Thing t, Pawn pawn, Boolean checkDistance, Boolean checkLOS)` (`88df42e0e943404ba42043aebd37585c:06006A3D:M`); decompiler evidence shows vanilla checks distance, `alwaysFlee`, hostility, `ThreatDisabled`, `IAttackTargetSearcher`, and optional LOS before returning true. Runtime `zombieland/area_workflow_state actionMode:danger-flee` records the installed postfix on this overload. | Covered for harmless-zombie flee filtering and vanilla pass-through: a nearby normal zombie returned true and `SeesZombieAsThreat=true`; roped, confused, active-electric, and albino zombies remained hostile but returned false with `SeesZombieAsThreat=false`; an ordinary hostile pawn still returned true. No warning-or-higher logs. |
| `Source/Patches.cs:919` | `Verb_MeleeAttack_TryCastShot_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `Verb_MeleeAttack.TryCastShot()` (`88df42e0e943404ba42043aebd37585c:06013BBC:M`); decompiler evidence shows it immediately resolves melee hit/dodge/miss, applies melee damage, plays sounds/motes, staggers the target, and returns the combat result. Runtime `zombieland/area_workflow_state actionMode:smart-melee` records the installed prefix on this virtual method and invokes the patched `TryCastShot` directly for branch-result proof, because `Verb.TryStartCastOn` only reports whether the cast started. | Covered for all smart-melee prefix exits: a normal zombie `ZombieBite` against a melee-20 armed colonist with `safeMeleeLimit=1` returned false and caused no injury; a chainsaw-equipped colonist melee verb returned false and caused no injury; an armed colonist attacking a roped zombie returned false and killed the zombie; an ordinary armed colonist attacking a normal zombie had the prefix continue to vanilla and `Pawn_MeleeVerbs.TryMeleeAttack` returned true. No warning-or-higher logs. |
| `Source/Patches.cs:993` | `Verb_LaunchProjectile_TryCastShot_Patch` | `semantic/runtime` | Installed RimWorld 1.6 target is `Verb_LaunchProjectile.TryCastShot()` (`88df42e0e943404ba42043aebd37585c:06005829:M`); decompiler evidence shows vanilla performs forced-miss, wild-miss, and cover-miss checks before the intended-target launch. Runtime `zombieland/area_workflow_state actionMode:ranged-projectiles` from `EMPTY.rws` found the old transpiler branch was installed but did not affect a real `Verb_Shoot`/base-call path, so the patch now adds a first-priority prefix for the source-derived skip case and keeps other shots on vanilla. | Covered for nearby colonist zombie shots below intense difficulty: with `threatScale=1`, `COLONISTS_HIT_ZOMBIES_CHANCE=1`, and forced miss radius temporarily set to `9`, a real player bolt-action rifle shot at a visible zombie six cells away returned `castResult=true`, launched `Bullet_BoltActionRifle` with both used and intended target set to the zombie, and produced destination cell `(114,119)`, exactly the zombie cell, instead of a forced-miss cell. No warning-or-higher logs. |
| `Source/Patches.cs:1138` | `Pawn_JobTracker_StartJob_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `Pawn_JobTracker.StartJob(Job newJob, JobCondition lastJobEndCondition, ThinkNode jobGiver, Boolean resumeCurJobAfterwards, Boolean cancelBusyStances, ThinkTreeDef thinkTree, Nullable tag, Boolean fromQueue, Boolean canReturnCurJobToPool, Nullable keepCarryingThingOverride, Boolean continueSleeping, Boolean addToJobsThisTick, Boolean preToilReservationsCanFail)` (`88df42e0e943404ba42043aebd37585c:0600637C:M`); decompiler evidence shows vanilla increments `jobsGivenThisTick`, can enter the 10-jobs-in-one-tick recovery path, starts drivers, and always clears `startingNewJob` in `finally`. Runtime `zombieland/area_workflow_state actionMode:job-gate` records the installed prefix on this overload. | Covered for all Zombieland pawn classes gated by the prefix: normal zombie, spitter, and symbiant each started whitelisted `Wait`; each then had a reserved disallowed `HaulToCell` job rejected while current `Wait` remained active, `jobsGivenThisTick` was reset from `7` to `0`, `jobsGivenThisTickTextual` from `dirty` to empty, `startingNewJob` from true to false, and the reserved steel target was released. No warning-or-higher logs. |
| `Source/Patches.cs:1217` | `Pawn_MeleeVerbs_GetUpdatedAvailableVerbsList_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `Pawn_MeleeVerbs.GetUpdatedAvailableVerbsList(Boolean terrainTools)` (`88df42e0e943404ba42043aebd37585c:0600CD50:M`); decompiler evidence shows vanilla rebuilds melee verbs from pawn verbs, equipment, apparel, hediff verbs, mutant verbs, or terrain verbs, then returns weighted `VerbEntry` values. Runtime `zombieland/area_workflow_state actionMode:special-melee-verbs` records the installed postfix on this method. | Covered for bite-verb filtering: a normal zombie's real available melee verbs included `ZombieBite` and `TryGetMeleeVerb` selected it; an albino, an active electrifier, and a disabled electrifier all returned only `Scratch` melee damage defs and selected scratch verbs, proving the postfix removes `ZombieBite` for both special-zombie gates while leaving normal zombies unchanged. No warning-or-higher logs. |
| `Source/Patches.cs:1236` | `Pawn_MeleeVerbs_ChooseMeleeVerb_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `Verb_MeleeAttackDamage.DamageInfosToApply(LocalTargetInfo target)` (`88df42e0e943404ba42043aebd37585c:06013BCE:M`); decompiler evidence shows vanilla yields melee `DamageInfo` values from adjusted melee damage, armor penetration, melee damage def/tool/source, then extra melee damages and surprise extra damages. Runtime `zombieland/area_workflow_state actionMode:electrifier-melee-damage` records the installed postfix on this private iterator method. | Covered for active electrifier melee side effects and fallback: an active electrifier stalled a target's primary `Chainsaw` from `0` to `120` while yielding ordinary melee damage; an active electrifier converted a smokepop-belt hit into `ElectricalShock` with `ElectricalField`; an active electrifier deteriorated an inventory component-cost chainsaw by `2` HP while leaving the yielded melee damage non-electrical; and a disabled electrifier left an identical inventory component item unchanged with no `ElectricalShock`. No warning-or-higher logs. |
| `Source/Patches.cs:1336` | `PawnUtility_GetManhunterOnDamageChance_Patch` | `semantic/runtime` | Installed RimWorld 1.6 target is `PawnUtility.GetManhunterOnDamageChance(Pawn pawn, Thing instigator, Single distance)` (`88df42e0e943404ba42043aebd37585c:0600C66D:M`); decompiler evidence shows vanilla starts from the `ThingDef` overload, adds Scaria, applies distance, hunting stealth, pawn-instigator role factor, then clamps. Runtime `zombieland/area_workflow_state actionMode:animal-response` records the installed postfix on the pawn/thing overload. | Covered for zombie-specific animal revenge control after a semantic fix: human/non-zombie instigator stayed at the vanilla estimate `1` with the setting enabled and disabled; ordinary zombie instigator was reduced from vanilla `1` to `0.05` while enabled and zeroed while disabled; spitter/symbiant instigators stayed `0`. No warning-or-higher logs. |
| `Source/Patches.cs:1356` | `FoodUtility_GetPreyScoreFor_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `FoodUtility.GetPreyScoreFor(Pawn predator, Pawn prey)` (`88df42e0e943404ba42043aebd37585c:06006A56:M`); decompiler evidence shows vanilla computes a higher-is-better predator prey score from combat power ratio, health, body size, distance, humanlike penalty, and fence protection. Runtime `zombieland/area_workflow_state actionMode:animal-response` records the installed postfix on this method. | Covered for predator score shaping: with `animalsAttackZombies=false`, a far human scored `-138.36` while a nearby normal zombie scored `-10047.2`, so the human remained preferred; with `animalsAttackZombies=true`, the nearby normal zombie rose to `-117.2`, above the far human; spitter/symbiant scores remained `0` in both settings. No warning-or-higher logs. |
| `deleted from Source/Patches.cs` | `PathFinder_FindPath_Patch` | `deleted/obsolete` | Exact RimWorld 1.6 overload `PathFinder.FindPathNow(IntVec3 start, LocalTargetInfo target, TraverseParms traverseParms, PathFinderCostTuning? tuning, PathEndMode peMode, PathRequest.IPathGridCustomizer customizer)` exists (`88df42e0e943404ba42043aebd37585c:060025A6:M`), but this patch had `Prepare() => false` because 1.6 moved pathing into `PathFinder` jobs and `PathRequest` customization. | Deleted from current source. Avoid-grid/path-cost behavior belongs to the newer path customizer and avoidance rows; scenario coverage should verify those replacements, not this disabled hook. |

## Render, Specials, Apparel, And Stats Static Targets

This slice covers the 24 static rows in `patches-render-specials-apparel`. It includes label suppression, inspect tab hardening, double-tap downed-state presentation, downed crawler visuals, special zombie overlays, 1.6 pawn render node graphic routing, deflect sound suppression, SoS floating zombie drawing, generated zombie apparel texture detection, raging warmup scaling, and zombie stat overrides. Relevant commit evidence includes `787156f Fix zombie render tree initialization on load`, `aeeac24 Fix symbiant save load rendering`, `190ca31 Fix spitter duplicate body rendering`, `efdf9d7 Fix tar smoke rendering and verify zombie ball flight`, `f758d40 Render tar smoke as black gas`, `3daf2d5 Add electrifier bridge smoke`, `9638321 Add albino bridge smoke`, `1ac0117 Add electrifier active bridge smoke`, `1c0e9a4 Add albino scream bridge smoke`, `12cb6a2 Cover electrifier bullet absorption`, `62343fa Cover albino melee bite filtering`, `0146ec7 Cover albino special contracts`, `2770e6c Cover special zombie active threat counts`, `5a22ef0 Recheck special zombie bridge contracts`, and `1cd82e3 Rebuild Unity asset bundles from generated sources`.

| Location | Patch class | Status | RimWorld 1.6 evidence | Coverage disposition |
| --- | --- | --- | --- | --- |
| `Source/Patches.cs:2896` | `GenMapUI_DrawPawnLabel_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `GenMapUI.DrawPawnLabel(Pawn pawn, Vector2 pos, Single alpha, Single truncateToWidth, Dictionary truncatedLabelsCache, GameFont font, Boolean alwaysDrawBg, Boolean alignCenter)` (`88df42e0e943404ba42043aebd37585c:0600264B:M`); decompiler evidence shows this overload computes the label background rect and delegates to the newer `Rect` overload (`0600264C:M`). Runtime `zombieland/area_workflow_state actionMode:ui-state` records the installed prefix on the vector overload. | Covered for label suppression branch behavior: ordinary normal zombie `wasMapPawnBefore=false` returned `continueDraw=false`, former-map-pawn zombie `wasMapPawnBefore=true` returned `continueDraw=true`, and an ordinary human pawn returned `continueDraw=true`. No warning-or-higher logs. |
| `Source/Patches.cs:2910` | `MainTabWindow_Inspect_CurTabs_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `MainTabWindow_Inspect.CurTabs` (`88df42e0e943404ba42043aebd37585c:1700312F:P`); decompiler evidence shows vanilla returns null for screenshot mode, no selected inspectable, or unsupported multi-selection, while `Thing.GetInspectTabs()` returns `def.inspectorTabsResolved`. Runtime `zombieland/area_workflow_state actionMode:ui-state` records the installed postfix on the getter; runtime `zombieland/zombie_selection_respects_former_colonist` also records the same installed postfix while selecting a former-colonist zombie corpse. | Covered for null-hardening and ordinary pass-through: direct postfix converted null to an empty enumerable and preserved a non-null enumerable reference; selecting a spawned ordinary zombie returned a non-null empty tab set; selecting a spawned human returned 10 ordinary inspect tabs including health, needs, character, training, social, guest, prisoner, caravan, gear, and log. Also covered for selected former-colonist zombie corpses after clearing the resolved corpse-tab cache: selector accepted the corpse, `CurTabs` returned non-null with `tabCount=0`, and warning-or-higher logs were empty. |
| `Source/Patches.cs:2923` | `Pawn_Downed_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `Pawn.Downed` (`88df42e0e943404ba42043aebd37585c:170008B9:P`); decompiler evidence shows vanilla is a direct `health.Downed` getter. Runtime `zombieland/area_workflow_state actionMode:ui-state` records the installed prefix on the getter. | Covered for double-tap downed facade: a health-downed normal zombie had `health.Downed=true`, public `Downed=false` while `doubleTapRequired=true`, and public `Downed=true` while `doubleTapRequired=false`; a health-downed ordinary human returned public `Downed=true` in both settings. No warning-or-higher logs. |
| `Source/Patches.cs:3032` | `PawnDownedWiggler_WigglerTick_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `PawnDownedWiggler.ProcessPostTickVisuals(Int32 ticksPassed)` (`88df42e0e943404ba42043aebd37585c:060026CB:M`); decompiler evidence shows 1.6 added `ticksPassed` and vanilla now exits early for public `pawn.Downed=false`, but Harmony still runs the Zombieland postfix after the method. Runtime `zombieland/area_workflow_state actionMode:downed-crawler-visuals` records the installed postfix on the `Int32` overload. | Covered for downed crawler destination angle: a health-downed normal zombie had its private pather destination set to `(123,156)` and the real patched `ProcessPostTickVisuals(1)` updated `downedAngle` to the expected destination-vector plus jitter value `281.777618`. No warning-or-higher logs. |
| `Source/Patches.cs:3045` | `PawnRenderer_BodyAngle_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `PawnRenderer.BodyAngle(PawnRenderFlags flags)` (`88df42e0e943404ba42043aebd37585c:060026FF:M`); decompiler evidence shows vanilla still returns `wiggler.downedAngle` for downed or dead pawns after crawling/holder/bed branches. Runtime `zombieland/area_workflow_state actionMode:downed-crawler-visuals` records the installed prefix on the method. | Covered for downed-zombie-only body-angle override: real `renderer.BodyAngle(PawnRenderFlags.None)` returned the expected initial smoothed angle `641.7776`, then the expected second smoothed angle `644.7776` after a `48` degree wiggler shift; direct prefix probes skipped the original only for the health-downed zombie and passed through for a standing zombie and downed human. No warning-or-higher logs. |
| `Source/Patches.cs:3092` | `Root_Play_Update_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `Root_Play.Update()` (`88df42e0e943404ba42043aebd37585c:060015C6:M`); decompiler evidence shows vanilla update still calls play-state systems after `base.Update()` and guards long events/destroyed roots. Runtime `zombieland/area_workflow_state actionMode:root-play-hooks` records the installed postfix on the virtual update method. | Covered for the Zombieland play-root audio updater contract: the live probe spawned an electrifier and tanky zombie, inserted them into the current map `TickManager` humming/tank sets, repeatedly invoked the installed postfix until the `DateTime` cadence gate crossed, and observed both `ZombieElectricHum` and `ZombieTankMovement` sustainers created through the real `UpdateElectricalHumming()` and `UpdateTankMovement()` paths. Original private sustainer fields and sound prefs were restored afterward. No warning-or-higher logs. |
| `Source/Patches.cs:3108` | `Root_Play_SetupForQuickTestPlay_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is static `Root_Play.SetupForQuickTestPlay()` (`88df42e0e943404ba42043aebd37585c:060015C7:M`); decompiler evidence shows vanilla still builds a Crashlanded quick-test game/world. Runtime `zombieland/area_workflow_state actionMode:root-play-hooks` records the installed postfix on the static method. | Covered at the postfix boundary without destructively starting a new quick-test world: the probe dirtied `ZombieSettings.Values`, `ValuesOverTime`, and `SettingsDialog.scrollPosition`, invoked the installed postfix, and verified threat scale, double-tap, idle speed, creepy ambient sound, keyframe count/ticks, and scroll position all matched `ZombieSettingsDefaults`. The original live settings snapshot was restored afterward. No warning-or-higher logs. |
| `Source/Patches.cs:3125` | `PawnRenderer_RenderPawnAt_Patch` | `semantic/runtime-covered` | `PawnRenderer.RenderPawnAt(Vector3 drawLoc, Rot4? rotOverride, Boolean neverAimWeapon)` exists (`88df42e0e943404ba42043aebd37585c:060026F1:M`) and still ends after render/cache/shadow/stance/silhouette/debug cleanup before `results = default(...)`. `PawnRenderUtility.DrawEquipmentAndApparelExtras` exists (`88df42e0e943404ba42043aebd37585c:06002733:M`) and still draws aimed/carried equipment plus apparel extras before Zombieland's injected `RenderExtras` runs at the final return. | Covered for the reusable visual gauntlet slice. `zombieland/spawn_reference_lineup stageRenderStates:true` from `EMPTY` staged bomber, tanky, miner, electrifier absorb, healer beam, albino scream, toxic aura, rage, roped/confused icons, rope line, emerging render, spitter custom render, and symbiant render; screenshots `zl_special_visual_gauntlet_staged_prereload__cell_rect.png` and `zl_special_visual_gauntlet_staged_postreload__cell_rect.png` prove nonblank visuals before and after save-load. Warning-or-higher logs were empty after spawn, after reload, and after 30 post-reload ticks. |
| `Source/Patches.cs:3587` | `PawnRenderNode_Body_GraphicFor_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `PawnRenderNode_Body.GraphicFor(Pawn pawn)` (`88df42e0e943404ba42043aebd37585c:0600274E:M`); decompiler evidence shows vanilla resolves body graphics from dessicated, mutant, creepjoiner, and `story.bodyType.bodyNakedGraphicPath` branches. Runtime `zombieland/area_workflow_state actionMode:render-node-graphics` records the installed prefix on the virtual body render-node method. | Covered for 1.6 custom body graphic routing: a spawned normal zombie with a `ZombieRenderCompat` custom body returned that exact `Graphic_Multi` instance through the real patched `PawnRenderNode_Body.GraphicFor`, while an ordinary human with no custom graphic returned `continue=true` from the prefix and left the result unset for vanilla. No warning-or-higher logs. |
| `Source/Patches.cs:3600` | `PawnRenderNode_Head_GraphicFor_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `PawnRenderNode_Head.GraphicFor(Pawn pawn)` (`88df42e0e943404ba42043aebd37585c:060027BD:M`); decompiler evidence shows vanilla returns `null` when `pawn.health.hediffSet.HasHead` is false, skull graphics for dessicated pawns, otherwise `story.headType.GetGraphic`. Runtime `zombieland/area_workflow_state actionMode:render-node-graphics` records the installed prefix on the virtual head render-node method. | Covered for custom head graphic routing and the no-head guard: a spawned normal zombie with a `ZombieRenderCompat` custom head returned that exact `Graphic_Multi` instance through real `PawnRenderNode_Head.GraphicFor`; an ordinary human passed through to vanilla; and a headless zombie with a cached custom head returned `null` instead of drawing the cached head. This fixed the 1.6 render-node bypass of vanilla `HasHead`. No warning-or-higher logs. |
| `Source/Patches.cs:3619` | `Effecter_Trigger_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `Effecter.Trigger(TargetInfo A, TargetInfo B, Int32 overrideSpawnTick)` (`88df42e0e943404ba42043aebd37585c:0600535A:M`); decompiler evidence shows vanilla iterates `children` and calls `SubTrigger` on each child before returning the effecter. Runtime `zombieland/area_workflow_state actionMode:effecter-suppression` records the installed prefix on the trigger method. | Covered for zombie deflect-sound suppression and ordinary pass-through: a probe `SubEffecter` inserted into a real `Deflect_General` effecter was not triggered for a normal zombie target, while the same deflect effecter triggered once for an ordinary human and a non-deflect `Mine` effecter triggered once for a normal zombie. Direct prefix branch probes matched the same true/false split. No warning-or-higher logs. |
| `Source/Patches.cs:3534` | `Map_MapUpdate_Patch` | `resolved/runtime` | `Map.MapUpdate()` exists (`88df42e0e943404ba42043aebd37585c:06001CEC:M`); the patch is gated by `SoSTools.isInstalled`. Current SoS2 source/decompiler evidence exposes `OuterSpaceBiome` and `ShipOrbiting` semantics through the SoS support path. | `EV.RUNTIME.SOS2.SPACE_FLOATERS` proves floating space zombies draw only when Save Our Ship support is installed, floating zombies are enabled, the current map is the SoS outer-space biome, and the map is in play: `zombieland/get_status` reported `OuterSpaceBiome`, 100/10 floaters ready, screenshot evidence captured the SoS2 space background with Zombieland floaters above it, and no Zombieland warning-or-higher logs were emitted. |
| `Source/Patches.cs:3711` | `Graphic_Multi_Init_Patch` | `semantic/runtime` | Installed RimWorld 1.6 target is `Graphic_Multi.Init(GraphicRequest req)` (`88df42e0e943404ba42043aebd37585c:06003813:M`); decompiler evidence shows vanilla calls `Log.Error(...)` when all directional and fallback textures are missing, assigns `BaseContent.BadMat`, and returns. Runtime `zombieland/area_workflow_state actionMode:graphic-multi-texture` records the installed Zombieland transpiler on the init method. | Covered for suppressed missing-texture capture used by generated apparel probing: with `suppressError=true`, a real missing `Graphic_Multi.Init` set `textureError=true`, produced `BadMat`, and emitted no warning-or-higher logs; a valid human body graphic path left `textureError=false` and did not use `BadMat`; direct suppressed `CaptureTextureError` invocation also set the flag. The unsuppressed `Patches.Error(text)` branch is source-covered by the helper body and was not runtime-triggered to keep the log check clean. |
| `Source/Patches.cs:3640` | `Verb_TryStartCastOn_Patch` | `semantic/runtime` | Exact six-parameter overload `Verb.TryStartCastOn(LocalTargetInfo castTarg, LocalTargetInfo destTarg, Boolean surpriseAttack, Boolean canHitNonTargetPawns, Boolean preventFriendlyFire, Boolean nonInterruptingSelfCast)` exists (`88df42e0e943404ba42043aebd37585c:060057BB:M`) and still converts `(WarmupTime * statValue).SecondsToTicks()` before creating `Stance_Warmup`. Runtime `zombieland/area_workflow_state actionMode:warmup-scaling` records the installed Zombieland transpiler on this method. | Covered for the warmup scaling branch with real equipped `Verb_Shoot.TryStartCastOn` calls: a human control and a normal non-raging zombie with local zombie grid count `4` both produced `Stance_Warmup` `ticksLeft=120`, while a raging zombie with grid count `4` produced `30` ticks and a former-map-pawn zombie with grid count `5` produced `24` ticks. Direct `ModifyTicks` reflection matched the live stance ticks in all four cases. No warning-or-higher logs. |
| `Source/Patches.cs:4520` | `SkillUI_DrawSkill_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is `SkillUI.DrawSkill(SkillRecord skill, Rect holdingRect, SkillDrawMode mode, String tooltipPrefix)` (`967ddb80559449f0a776dafa26a855d1:06013023:M`). The last-priority transpiler changes only vanilla's single integer-label conversion call; if another mod or RimWorld version exposes zero or multiple compatible anchors, it warns once and returns the original instructions unchanged. Custom skill rows remain correct through the lower `SkillRecord.GetLevel`/`GetLevelForUI` patches even when they do not use this optional presentation enhancement. | `zombieland/symbiant_benefit_contract` (`op_0377abd162fd45a2a4fab419cc93ef8f`) recorded Zombieland transpiler ownership and exercised exact labels `5 + 8`, `5 + 6`, `5 + 4`, and `5 + 2` across difficulty brackets, partial cap `18 + 2`, full cap `20`, disabled `-`, and dormant `5`. A live Bio-tab screenshot also showed the enhanced labels in the real vanilla row layout. No operation-correlated warning-or-higher logs. |
| `Source/Patches.cs:4553` | `SkillUI_GetSkillDescription_Patch` | `resolved/runtime` | Installed RimWorld 1.6 target is private static `SkillUI.GetSkillDescription(SkillRecord sk)` (`967ddb80559449f0a776dafa26a855d1:06013024:M`). The postfix inserts one localized Symbiant line immediately after the localized vanilla `Skill level` line; if a UI mod replaces that text, it safely appends instead, and returns vanilla text unchanged when no same-map skill benefit applies. | The final benefit contract recorded Zombieland postfix ownership and exact tooltip lines for all four difficulty brackets, partial cap (`+2 applied`, `+8 before skill cap`), full cap (`+0 applied`, `+8 before skill cap`), and dormant absence. This is also inherited by compatible alternate UIs, including the inspected RimHUD path, when they call vanilla `GetSkillDescription`. No operation-correlated warning-or-higher logs. |
| `Source/Patches.cs:3682` | `StatExtension_GetStatValue_Patch` | `resolved/runtime` | `StatExtension.GetStatValue(Thing thing, StatDef stat, Boolean applyPostProcess, Int32 cacheStaleAfterTicks)` exists (`88df42e0e943404ba42043aebd37585c:0601386E:M`) and forwards to `stat.Worker.GetValue(...)`. Runtime `zombieland/area_workflow_state actionMode:zombie-stats` records the installed Zombieland prefix on this static extension method. The Symbiant host capacity benefits intentionally do not live in this extension patch: `Hediff_SymbiantSymbiosis.CurStage` contributes same-map `Moving` and `Manipulation` capacity modifiers consumed by the Health UI, stat workers, and gameplay. | Covered for the live stat-override matrix: an ordinary human passed through the prefix and kept positive vanilla `MoveSpeed`; zombie move speed matched settings/body/special math for idle Fat (`0.805`), tracking Thin (`2.76`), former-map-pawn Hulk (`5.52`), tanky (`0.0184` at tick rate `1`), and roped health-downed crawler (`0.4`); melee hit, dodge, pain-shock, tanky temperature min/max, and spitter `IncomingDamageFactor=1` all matched expected values in final runtime `op_95df047d9366451fa63c19836b27fcb9`. Spitter scaling is covered by the injury damage prefix, not the stat prefix. Optional `SmokeSensitivity`/`Suppressability` stats were absent in the active def set, so their ignored-stat branch remains source-covered. Symbiant Moving/Manipulation/stat/pathing and difficulty-scaled skill evidence is recorded in `TEST_COVERAGE.md` under the 2026-07-12 benefit passes. No warning-or-higher logs. |

## Hostility, Targeting, And Target Cache Static Targets

This slice covers the 11 static rows in `hostility-static`. It excludes `AttackTargetFinder_FriendlyFire_Patch`, which is already tracked in the dynamic section because it patches two methods through `TargetMethods()`. The static rows define who chooses zombies as targets, how active-threat checks treat Zombieland pawns, how drafted wait jobs auto-attack adjacent zombies, and how colony-hostile target caches avoid permanent danger/music pressure from zombies. Relevant commit evidence includes `d7f1a04 Cover zombie hostility rules`, `2770e6c Cover special zombie active threat counts`, `6beb859 Cover zombie target cache filtering`, and `74f6efd Restore harmless zombie flee filtering`.

| Location | Patch class | Status | RimWorld 1.6 evidence | Coverage disposition |
| --- | --- | --- | --- | --- |
| `Source/Patches_Hostility.cs:17` | `AttackTargetFinder_GetAvailableShootingTargetsByScore_Patch` | `resolved/runtime` | `AttackTargetFinder.GetAvailableShootingTargetsByScore(List rawTargets, IAttackTargetSearcher searcher, Verb verb)` exists and still filters raw targets before vanilla shootability/score calculation. Symbiants now use a narrow exception: ordinary hostile humanlike/mech pawns retain the target, while player/friendly/animal/turret/zombie/Anomaly-specific searchers retain their previous exclusions. | Final Steam-profile contract `op_6f0c8505306b465d94837e47925d32e0` returned the same Symbiant for two independent hostile rifle searchers and rejected player and animal controls; real assault and CE evidence is owned by `S-Symbiant-Symbiosis`. |
| `Source/Patches_Hostility.cs:257` | `AttackTargetFinder_BestAttackTarget_Patch` plus logical scan context | `resolved/runtime` | Steam 1.6 MVID `967ddb80559449f0a776dafa26a855d1` exposes `BestAttackTarget` as `0600655F`. The logical candidate reuses the caller's final validator and mirrors vanilla's hostility, Lord, threat/auto-targetable/active-threat, EMP-machine, nonburning, wick, noncombatant, fog, LOS/gas, effective-min-range, max-range, locus, duty, reachability, thick-roof, and `onlyRanged` gates against an exposed cell. When vanilla enters its weighted ranged branch, `GetRandomShootingTargetByScore` receives the logical candidate in the same list instead of a null-only postfix priority. Non-pawn searchers such as turrets still reject it. | Final normal-profile contract `op_ec618148359241e192eb6cee52f5f2a0` accepted cell distance `11` with root distance `12` and max `11.5`, rejected a false caller validator, rejected all cells under `NeedNotUnderThickRoof`, and retained player/animal negatives. Its warning query was empty. Real assault step/read `op_0ae218d8117a4929b76212b32e89c3a1`/`op_b84ffe059cb64930a35e9a30ae973cca` left both rifle raiders focused on and recording the Symbiant. |
| `Source/Patches_Hostility.cs:517` | `AttackTargetFinder_GetShootingTargetScore_Patch` | `resolved/runtime` | The ordinary-zombie priority prefix is unchanged. A Symbiant-only postfix replaces root distance, cover, and `ai_AvoidFriendlyFireRadius` contributions with the bound logical cell, including `TargetPriorityFactor`; `ShotReport.HitReportFor` supplies the same cell to cone/accuracy logic. The friendly-fire calculation preserves vanilla LOS and pawn/animal/building weights while retaining Zombieland's zombie exclusions. No flat priority is added. | Two independent rifle lines in final contract `op_ec618148359241e192eb6cee52f5f2a0` reached the same exposed cell from different sources; real vanilla and CE assault evidence proves the resulting score remained actionable. |
| `Source/ZombieSymbiantCombat.cs` | logical-cell ranged/melee patch set | `resolved/runtime` | Current 1.6 shootable-cell, shoot-line, shot-report, reachability, melee-job, projectile launch/impact, and melee-draw targets were verified against the Steam assembly. `Projectile.ImpactSomething` restores the owner only through `usedTarget` and runs the complete original method, so vanilla overhead thick-roof interception and thin-roof collapse occur before owner impact. Melee B/C reads reselect and rewrite both cells when shrinkage or expansion invalidates the stored shape. | Final contract `op_ec618148359241e192eb6cee52f5f2a0` proved one Pawn/root registration, two independent ranged bindings, two melee B/C pairs, and live rebinding from removed C `(125,120)` to C `(125,119)` with job B/C updated. Vanilla assault fire reduced the shared pool while the host remained healthy. |
| `Source/Patches.cs:Explosion_AffectCell_Patch` | logical-cell explosion adapter | `resolved/runtime` | `Explosion.AffectCell` still owns the per-cell loop and its `damagedThings` list; `DamageWorker.ExplosionDamageThing` uses the supplied cell for falloff and refuses duplicates. The postfix calls it only when the affected cell belongs to the active Symbiant and the owner has not already been damaged. | Vanilla and active-CE contracts each hit an unregistered remote logical cell, recorded one match/one damage call/no duplicate, and drained exactly 5 shared health. |
| `Source/CETools.cs` | `CETools_Patch5_SymbiantCanHit` through `CETools_Patch10_SymbiantRayCast` | `resolved/runtime` | CE 16.7.2 MVID `1d1ad084d03d41d297a9d8b61b2d964a` defines `ProjectileCE : ThingWithComps`, not `Verse.Projectile`. The adapter reflects CE's `intendedTarget` and `ExactPosition`, preserves CE blocker callbacks and roof order, and supplies a copied logical-cell list to final `ImpactSomething`, instant `RayCast`, and ordinary ballistic `CheckCellForCollision`; `CE_Utility.GetBoundsFor` is recentered only during those candidate tests. Explosives remain on the deduplicated explosion path. The late optional adapters fail open with per-method anchor warnings and never mutate `ThingGrid`. | Final active-CE setup `op_f031ded58cb54907a2db26e0b9bcaed5` bound both rifles to non-root `(105,100)` with root `(106,100)`. After 600 rendered ticks (`op_c8b50044e02545be9f00a761191cc5aa`), read `op_60003e33fbef4c7e97b3b7e57a1c6cf4` recorded 15 logical bounds tests, last cell `(105,100)`, 120 gunshot pool damage, both raiders focused on the Symbiant, and a healthy host. All three warning queries were empty; CE was then removed and the normal Steam loadout restored. |
| `Source/Patches_Hostility.cs:475` | `GenHostility_HostileTo_Thing_Thing_Patch` | `resolved/runtime` | Exact overload `GenHostility.HostileTo(Thing a, Thing b)` exists (`88df42e0e943404ba42043aebd37585c:0600D33F:M`). Runtime targeting evidence proved player `HostileTo(zombie)=true`, enemy `HostileTo(zombie)` false/true with `enemiesAttackZombies`, non-colony animal false/true with `animalsAttackZombies`, and infecting enemy false. | Covered in `S-Hostility-Area-Workflow actionMode:targeting`. |
| `Source/Patches_Hostility.cs:492` | `GenHostility_HostileTo_Thing_Faction_Patch` | `resolved/runtime` | Exact overload `GenHostility.HostileTo(Thing t, Faction fac)` exists (`88df42e0e943404ba42043aebd37585c:0600D341:M`). Runtime targeting evidence proved an enemy pawn is hostile to `ZombieDefOf.Zombies`, while spitter and symbiant targets are non-hostile to the non-player enemy faction. | Covered for pawn-to-zombie-faction and symbiant/spitter non-player faction behavior. |
| `Source/Patches_Hostility.cs:526` | `GenHostility_IsActiveThreat_Patch` | `resolved/runtime` | Exact overload `GenHostility.IsActiveThreatTo(IAttackTarget target, Faction faction, Boolean ignoreHives, Boolean canBeFogged)` exists (`88df42e0e943404ba42043aebd37585c:0600D34A:M`). Runtime targeting evidence proved normal zombies are not active threats to player or null factions, are not active threats to an enemy faction while `enemiesAttackZombies=false`, are active threats to an enemy faction under `AttackMode.Everything`, and are not active threats under `OnlyColonists`. | Covered for player/null/enemy/settings/attack-mode gates; fogged/hive booleans are vanilla pass-through context and remain lower-priority. |
| `Source/Patches_Hostility.cs` | `Pawn_ThreatDisabled_Symbiant_Patch` | `resolved/runtime` | Current RimWorld `Pawn.ThreatDisabled(IAttackTargetSearcher)` rejects pawns whose `mindState.Active` is false. The Symbiant intentionally uses a minimal inactive mind state, so hostility and `IsActiveThreatTo` alone could not make it selectable by hostile raiders. The postfix only clears the result for a spawned, living, non-destroyed `ZombieSymbiant`; existing `HostileTo` policy still prevents player automatic targeting. | Final `zombieland/symbiant_combat_isolation_contract` operation `op_6c0ad2f2027f4ceaa9fdca08cc876b1d` recorded installed ownership, kept player/animal targeting negative, returned the Symbiant from real hostile `AttackTargetFinder.BestAttackTarget`, and held the linked host unspawned in a same-map casket. Operation-correlated warning-or-higher logs were empty. |
| `Source/Patches_Hostility.cs:574` | `JobDriver_Wait_CheckForAutoAttack_Patch` | `resolved/runtime` | `JobDriver_Wait.CheckForAutoAttack()` exists (`88df42e0e943404ba42043aebd37585c:06005DF3:M`) and still calls `GenHostility.IsActiveThreatTo(pawn, base.pawn.Faction, ignoreHives: false)` before adjacent melee auto-attack. Runtime `S-Hostility-Area-Workflow actionMode:targeting` confirmed `net.pardeike.zombieland` owns the installed patch and a real drafted `Wait_Combat` job damaged adjacent active normal zombies and player-faction spitters, started real attacks against player-faction symbiants and ordinary hostile pawns, and left roped/confused adjacent normal zombies un-attacked and undamaged through 900 ticks. The roped fixture now maintains and samples `ropedBy` through the probe so this row tests the auto-attack filter rather than rope-decay side effects. | Covered in `S-Hostility-Area-Workflow`; non-player spitter/symbiant negatives are already represented by direct hostility checks and enemy target filtering. |
| `Source/Patches_Hostility.cs:620` | `AttackTargetsCache_TargetsHostileToColony_Patch` | `resolved/runtime` | `AttackTargetsCache.TargetsHostileToColony` exists (`88df42e0e943404ba42043aebd37585c:17000FE4:P`). Runtime targeting evidence proved the replacement colony-hostile cache contains a real hostile human and excludes normal, spitter, and symbiant zombies. | Covered with stale-entry cleanup in `PlayerHostilesWithoutZombies` so destroyed/despawned hostiles cannot remain visible. |
| `Source/Patches_Hostility.cs:632` | `AttackTargetsCache_RegisterTarget_Patch` | `resolved/runtime` | `AttackTargetsCache.RegisterTarget(IAttackTarget target)` exists (`88df42e0e943404ba42043aebd37585c:060065ED:M`) and still adds targets after map/faction hostility checks. Runtime targeting evidence showed a spawned hostile human entered the replacement colony-hostile cache while Zombieland targets did not. | Covered for non-zombie hostile add and zombie/spitter/symbiant exclusion. |
| `Source/Patches_Hostility.cs:650` | `AttackTargetsCache_DeregisterTarget_Patch` | `resolved/runtime` | `AttackTargetsCache.DeregisterTarget(IAttackTarget target)` exists (`88df42e0e943404ba42043aebd37585c:060065EE:M`). Runtime targeting evidence exposed stale destroyed hostiles, then passed after moving removal to a prefix and purging stale entries in the replacement cache getter; the destroyed hostile was absent after cache readback. | Covered for non-zombie hostile removal; zombie deregistration remains harmless because Zombieland targets are never added to the replacement cache. |

## Area Manager, Damage Flasher, And Leaner Static Targets

This slice covers the small specialized static groups outside `Source/Patches.cs`: 6 rows in `area-manager-static`, 3 rows in `damage-flasher-static`, and 2 rows in `zombie-leaner-static`. These rows are support behavior rather than core horde logic, but they are player-visible: dangerous-area management, warning UI semantics, green zombie-bite damage flashes, and zombie draw jitter. Relevant commit evidence includes `e4f9d64 Cover zombie area risk classifications`; damage flasher and leaner rows have no newer targeted commit names after the 1.6 baseline, so their scenario obligation is source-derived.

| Location | Patch class | Status | RimWorld 1.6 evidence | Coverage disposition |
| --- | --- | --- | --- | --- |
| `Source/ZombieAreaManager.cs:260` | `AreaManager_Patches.CanMakeNewAllowed` | `resolved/runtime` | `AreaManager.CanMakeNewAllowed()` exists (`88df42e0e943404ba42043aebd37585c:060018A5:M`). Runtime `S-Hostility-Area-Workflow` evidence returned `canMakeNewAllowed=true` and `tryMakeNewAllowed=true` while refreshing the five `ZL_Area_*` fixture areas. | Covered in `ZL_Area_Workflow_base.rws`; reloaded readback stayed green. |
| `Source/ZombieAreaManager.cs:268` | `AreaManager_Patches.SortAreas` | `resolved/runtime` | `AreaManager.SortAreas()` exists (`88df42e0e943404ba42043aebd37585c:060018A1:M`). Runtime `S-Hostility-Area-Workflow` evidence captured the custom area order before/after calling `SortAreas()` and returned `sortPreserved=true`. | Covered for Zombieland-managed custom order in `ZL_Area_Workflow_base.rws`. |
| `Source/ZombieAreaManager.cs:297` | `Dialog_ManageAreas_Patches.Constructor` | `resolved/runtime` | `Dialog_ManageAreas..ctor(Map map)` exists (`88df42e0e943404ba42043aebd37585c:06011E46:M`). Runtime `S-Hostility-Area-Workflow` evidence set selected area, selected index, and scroll position to non-default values before constructing the dialog, then returned `constructorReset=true`. | Covered for constructor reset before the scenario selects the target area. |
| `Source/ZombieAreaManager.cs:307` | `Dialog_ManageAreas_Patches.Prefix` | `resolved/runtime` | `Dialog_ManageAreas.DoWindowContents(Rect inRect)` exists (`88df42e0e943404ba42043aebd37585c:06011E47:M`). Runtime `S-Hostility-Area-Workflow` evidence opened the real `RimWorld.Dialog_ManageAreas`; screenshot `/Users/ap/Library/Application Support/RimWorld/Screenshots/zl_area_workflow_manage_areas__clip.png` showed custom Zombieland rows, selected `ZL_Area_ZombieInside`, color sliders, and `Show Zombie Risk` mode text `Zombie inside`. | Covered for area rows, risk-mode colors, selected content, and real patched dialog rendering. |
| `Source/ZombieDamageFlasher.cs:9` | `PawnRenderer_Constructor_With_Pawn_Patch` | `resolved/runtime` | `PawnRenderer..ctor(Pawn pawn)` exists (`88df42e0e943404ba42043aebd37585c:060026EB:M`) and still initializes `flasher = new DamageFlasher(pawn)`. Runtime `zombieland/area_workflow_state actionMode:visual-support` records the installed Zombieland postfix on the constructor. | Covered for renderer replacement: spawned zombie and human renderers both received `ZombieDamageFlasher`, proving the constructor postfix is live and ordinary pawn renderers remain compatible. No warning-or-higher logs. |
| `Source/ZombieDamageFlasher.cs:19` | `DamageFlasher_Notify_DamageApplied_Patch` | `resolved/runtime` | `DamageFlasher.Notify_DamageApplied(DamageInfo dinfo)` exists (`88df42e0e943404ba42043aebd37585c:060026B9:M`). Runtime `actionMode:visual-support` records the installed Zombieland prefix on this method. | Covered for damage-def capture: real `Notify_DamageApplied` calls recorded `ZombieBite` on the zombie flasher and `Cut` on the human flasher before damaged material selection. |
| `Source/ZombieDamageFlasher.cs:31` | `DamageFlasher_GetDamagedMat_Patch` | `resolved/runtime` | `DamageFlasher.GetDamagedMat(Material baseMat)` exists (`88df42e0e943404ba42043aebd37585c:060026B8:M`) and still returns a pooled damage-flash material using damage flash ticks. Runtime `actionMode:visual-support` records the installed Zombieland postfix on this method. | Covered for bite flash color and non-leakage: `ZombieBite` produced the expected green material color `(0, 0.8, 0, 1)`, while ordinary `Cut` damage on a separate base material stayed non-green. |
| `Source/ZombieLeaner.cs:9` | `Pawn_DrawTracker_DrawPos_Patch` | `resolved/runtime` | `Pawn_DrawTracker.DrawPos` exists (`88df42e0e943404ba42043aebd37585c:170006CE:P`). Runtime `actionMode:visual-support` records the installed Zombieland postfix on the property getter. | Covered for draw-position offset: a forced `ZombieLeaner.ZombieOffset` changed the real `DrawPos` by the expected `(0.21, 0, -0.13)` delta, and the ordinary human fixture did not have a `ZombieLeaner`. |
| `Source/ZombieLeaner.cs:21` | `Pawn_DrawTrackerDrawTrackerTick_Patch` | `resolved/runtime` | `Pawn_DrawTracker.ProcessPostTickVisuals(Int32 ticksPassed)` exists (`88df42e0e943404ba42043aebd37585c:06002676:M`). Runtime `actionMode:visual-support` records the installed Zombieland postfix on this method. | Covered for post-tick visual behavior: invoking the real `ProcessPostTickVisuals(1)` reset the emerging zombie offset to zero, and the wandering branch produced a nonzero jitter/extra offset update. No warning-or-higher logs. |

## Contamination Static Targets

This slice covers 68 static rows in `contamination-static`. Dynamic contamination rows are already audited in the dynamic sections, so this table only covers direct static Harmony targets. Treat this as source/decompiler disposition linked to the C0-C6 runtime/save-load suite rather than an open request to rebuild contamination coverage from scratch: active Core branches have runtime or save-load evidence, active Biotech now covers the pollution clear-pollution branch, active Odyssey now covers the sand clear-depth branch, while Biotech mech/gene/subcore and child-thought tails remain the DLC-final runtime queue. Relevant commit evidence since the 1.6 baseline includes `beeb01c Cover wild plant contamination`, `5f9f91e Cover contamination pollution and melee handoffs`, `a125cdb Cover smooth wall contamination handoffs`, `40c3613 Cover contamination carry and dispenser handoffs`, `92b46aa Cover contamination roof and drill yields`, `9091cb6 Fix roof collapse contamination for mineable leavings`, `fff7028 Cover contamination stack and mineable yield`, and `5f73f13 Cover contamination construction and corpse handoff`.

| Location | Patch class | Status | RimWorld 1.6 evidence | Coverage disposition |
| --- | --- | --- | --- | --- |
| `Source/ContaminationPatches.cs:15` | `Pawn_NeedsTracker_ShouldHaveNeed_Patch` | `resolved/runtime` | `Pawn_NeedsTracker.ShouldHaveNeed` exists (`88df42e0e943404ba42043aebd37585c:0600CB87:M`). Runtime C0 core rerun reflected the private need gate on a spawned colonist: with `Constants.CONTAMINATION=true`, `ShouldHaveNeed(CustomDefs.Contamination)=true` and the pawn had a `ContaminationNeed`; with the constant temporarily false, `ShouldHaveNeed=false` and `AddOrRemoveNeedsAsAppropriate()` removed the need. | Covered for enabled/disabled need gating in `zombieland/contamination_core_contract`; visible settings toggle persistence remains under `S-Settings-Persistence`. |
| `Source/ContaminationPatches.cs:26` | `Game_FinalizeInit_Patch` | `semantic/runtime` | `Game.FinalizeInit` exists (`88df42e0e943404ba42043aebd37585c:0600148C:M`) and still performs the game-start finalization sequence. Runtime C0 loaded `EMPTY` successfully with a live contamination grid, then removed the current map's grid entry and called the same `ContaminationManager.FixGrounds()` hook the postfix uses; it recreated a grid for map index `0` with `40000` cells, the live map reference, and the expected map width. | Covered for the finalize hook's ground-repair effect after load and for direct missing-grid repair. New-world singleton reset is covered separately by `WorldGenerator_GenerateWorld_Patch`. |
| `Source/ContaminationPatches.cs:37` | `WorldGenerator_GenerateWorld_Patch` | `resolved/runtime` | `WorldGenerator.GenerateWorld(Single, String, OverallRainfall, OverallTemperature, OverallPopulation, LandmarkDensity, List<FactionDef>, Single)` exists as `RimWorld.Planet.WorldGenerator.GenerateWorld` (`88df42e0e943404ba42043aebd37585c:06015455:M`) and still creates `Current.CreatingWorld`, runs setup steps and planet layers, finalizes the generated world, returns it, and clears `Current.CreatingWorld` in `finally`. Runtime C0 now records the installed Zombieland postfix owner on that target and invokes the postfix against a sentinel cached `ContaminationManager`. | Covered for the new-world singleton reset boundary: the probe installed a sentinel manager in the private `_instance` cache, the postfix cleared it, and the next `ContaminationManager.Instance` rebound to the real current-world component instead of the sentinel. The run returned `success=true`, warning-or-higher logs were empty, and `EMPTY` was reloaded afterward. |
| `deleted from Source/ContaminationPatches.cs` | `Thing_MakeThing_Patch` | `deleted/obsolete` | `ThingMaker.MakeThing` exists (`88df42e0e943404ba42043aebd37585c:060039BB:M`), but this patch had `Prepare() => false`. | Deleted from current source. No runtime scenario obligation; keep this as historical negative-space documentation only. |
| `Source/ContaminationPatches.cs:58` | `Thing_Destroy_Patch` | `resolved/runtime` | `Thing.Destroy(DestroyMode)` exists (`88df42e0e943404ba42043aebd37585c:06003937:M`) and still follows the spawned/despawn/leavings destroy path. Runtime C0 spawned contaminated `ComponentIndustrial` at `(106,100)`, confirmed its manager entry existed at contamination `0.37`, destroyed it through real `Thing.Destroy(DestroyMode.Vanish)`, and confirmed the contamination entry was removed. | Covered for direct thing destruction cleanup in `zombieland/contamination_core_contract`; leavings transfer stays covered by C3. |
| `Source/ContaminationPatches.cs:82` | `Thing_SpecialDisplayStats_Patch` | `resolved/runtime` | `Thing.SpecialDisplayStats()` exists (`88df42e0e943404ba42043aebd37585c:06003961:M`). Runtime C6 contract returned stat entry `Zombie Contamination` = `65.00%` for contaminated `Steel x25` and `0.00%` for clean `Steel x25`, matching the source design that the stat is always visible. | Covered for clean and contaminated thing stats with warning-or-higher logs empty before cleanup and after reloading `EMPTY`. |
| `Source/ContaminationPatches.cs:102` | `Widgets_ThingIcon_Patch` | `resolved/runtime` | Exact `Widgets.ThingIcon(Rect, Thing, Single, Nullable<Rot4>, Boolean, Single, Boolean)` overload exists (`88df42e0e943404ba42043aebd37585c:06004ED8:M`). Runtime screenshot `zl_c6_contamination_ui_fixture.png` showed the selected contaminated steel icon with contamination overlay. A 2026-06-14 player-save smoke opened the Akgul to Bellerose right-click FloatMenu path from `RightClickAndPhase2Breaking.rws` without new warning-or-higher logs after the existing save warnings. | Covered for contaminated selected thing icon. The alpha-derived texture slot is now clamped because FloatMenu fade/drag can call `Widgets.ThingIcon` with alpha outside the expected 0..1 range; this fixes player log A20CBD46 without changing ordinary contamination overlay selection. |
| `Source/ContaminationPatches.cs:120` | `MainTabWindow_Quests_DoRow_Patch` | `resolved/runtime` | `MainTabWindow_Quests.DoRow` exists (`88df42e0e943404ba42043aebd37585c:06012C69:M`) and still computes `Mathf.Max(quest.challengeRating, 1)` before the rating icons. Runtime C6 generated `Decontamination Offer` through `QuestNode_CreateDecontaminationPickupShuttle`; `zombieland/contamination_ui_quest_contract` returned `decontaminationMax.value=0` while an ordinary quest preserved `ordinaryMax.value=3`. The Quests tab layout showed `Decontamination Offer` with expiry `7d` and no rating text. | Covered for decontamination row suppression and ordinary quest preservation; accepted shuttle send/return flow remains in the quest outcome scenario. |
| `Source/ContaminationPatches.cs:136` | `PlaySettings_DoPlaySettingsGlobalControls_Patch` | `resolved/runtime` | `PlaySettings.DoPlaySettingsGlobalControls(WidgetRow, Boolean)` exists (`88df42e0e943404ba42043aebd37585c:06013DCF:M`) and still dispatches `DoMapControls(row)` only when `worldView=false`; `WidgetRow.ToggleableIcon(ref bool, Texture2D, string, SoundDef, string)` exists (`88df42e0e943404ba42043aebd37585c:06004EC8:M`) and still reserves a 24px icon slot, draws the on/off checkbox, and flips the bool on click. Runtime C6 contract set `ContaminationManager.showContaminationOverlay=true` and screenshot showed map contamination overlay drawing. | Covered for the direct toolbar click/no-displacement branch: visible-pixel UI evidence `zl_playsettings_contamination_toggle_off.png` and `zl_playsettings_contamination_toggle_restored.png` clicked the appended rightmost Zombieland play-settings icon from a playable map, flipped it green-check -> red-X -> green-check, and showed the vanilla wrapped control positions unchanged. Warning-or-higher logs stayed empty. |
| `Source/ContaminationPatches.cs:151` | `MouseoverReadout_MouseoverReadoutOnGUI_Patch` | `semantic/runtime` | `MouseoverReadout.MouseoverReadoutOnGUI` exists (`88df42e0e943404ba42043aebd37585c:06004D59:M`) and still calls `UI.MouseCell()`, `MouseoverUtility.GetGlowLabelByValue`, and `thing.LabelMouseover`. Generic hover screenshot and contract helper output both showed `Steel x25, 65.00 % contaminated` and `Lit (90%) Contaminated (65.00 %)`. The 2026-05-31 clean comparison rerun returned plain clean mouseovers `Steel x25` and `Lit (90%)`. | Covered for contaminated thing/glow labels and clean negative mouseover/glow labels; warning-or-higher logs were empty before cleanup and after reloading `EMPTY`. |
| `Source/ContaminationPatches.cs:203` | `InspectPaneUtility_PaneWidthFor_Patch` | `resolved/runtime` | `InspectPaneUtility.PaneWidthFor(IInspectPane)` exists (`88df42e0e943404ba42043aebd37585c:06012954:M`) and RimWorld 1.6 still returns `72f * Mathf.Max(6, visibleTabs)`. Runtime C6 width evidence selected a temporary clean `Steel x25` and the contaminated fixture `Steel x25`; both had `vanillaBaseWidth=432`, patched `PaneWidthFor=578`, and `addedWidth=146`. Generic UI layout then showed the live inspect tab width as `578` with the `65.00 % contamination` bar visible. | Covered for clean and contaminated thing selections; the temporary clean probe was destroyed afterward and warning-or-higher logs were empty. |
| `Source/ContaminationPatches.cs:216` | `InspectPaneFiller_DoPaneContentsFor_Patch` | `semantic/runtime` | `InspectPaneFiller.DoPaneContentsFor(ISelectable, Rect)` exists (`88df42e0e943404ba42043aebd37585c:06012945:M`) and still calls `DrawHealth(row, thing)`. Runtime screenshot `zl_c6_contamination_ui_fixture.png` showed `65.00 % contamination` bar in the inspect pane for selected contaminated steel. The clean comparison rerun selected clean `Steel x25` and contaminated `Steel x25`; both kept the expected patched inspect-pane width (`432 -> 578`) while the clean presentation stayed at `0.00%` and plain mouseover/glow labels. | Covered for clean and contaminated item inspect-pane integration; pawns/corpses remain final UI-polish variants. Warning-or-higher logs were empty before cleanup and after reloading `EMPTY`. |
| `Source/ContaminationPatches.cs:258` | `BeautyDrawer_DrawBeautyAroundMouse_Patch` | `resolved/runtime` | `BeautyDrawer.DrawBeautyAroundMouse()` exists (`88df42e0e943404ba42043aebd37585c:06004B2C:M`). RimWorld 1.6 `BeautyDrawerOnGUI()` still calls it on repaint when `ShouldShow()` is true, and vanilla `DrawBeautyAroundMouse()` still fills beauty-relevant cells and draws beauty labels. Runtime C6 prepared contaminated `Steel x25` at `(100,100)`, enabled the beauty overlay gate, fixed virtual hover on that cell, and captured a baseline screenshot plus a Shift-held screenshot. | Covered for Shift replacement: `zl_c6_beauty_shift_baseline.png` showed vanilla beauty labels around the hover cell, while `zl_c6_beauty_shift_held.png` showed only the Zombieland contamination label `65.0` over the contaminated stack. Warning-or-higher logs were empty before cleanup and after reloading `EMPTY`. |
| `Source/ContaminationPatchesBuilding.cs:42` | `Frame_CompleteConstruction_Patch` | `semantic/runtime` | `Frame.CompleteConstruction(Pawn)` exists (`88df42e0e943404ba42043aebd37585c:0600E8C0:M`) and still clears resources, makes the thing, and can set foundation/temp/final terrain. C1 proved reloaded-frame fallback from frame contamination; C2 proved fresh wall construction, floor construction, and bridge/foundation construction after staging the bridge over `WaterShallow`. The 2026-06-01 local `ZLTestTemporaryTerrain` fixture rerun supplied constructible `TerrainDef.temporary` `ZLTestTemporaryFloor`; the same terrain contract built `Frame_ZLTestTemporaryFloor`, set `tempAfter=ZLTestTemporaryFloor`, moved ground contamination `0 -> 0.57`, and matched the expected transfer with clean warning-or-higher logs. | Covered for C1/C2 building, terrain/foundation, temporary terrain, affect-floor, and reloaded-frame fallback construction paths. |
| `Source/ContaminationPatchesBuilding.cs:136` | `MinifyUtility_MakeMinified_Patch` | `resolved/runtime` | `MinifyUtility.MakeMinified(Thing)` exists (`88df42e0e943404ba42043aebd37585c:0600E7D2:M`). Runtime C2 proved stool contamination `0.6` moved from source building into minified form before install. | Covered in `ZL_Contamination_Persistence_15_build_replace.rws`. |
| `Source/ContaminationPatchesBuilding.cs:152` | `GenConstruct_PlaceBlueprintForInstall_Patch` | `resolved/runtime` | `GenConstruct.PlaceBlueprintForInstall(BuildableDef, IntVec3, Map, Rot4, Faction, Thing)` exists (`88df42e0e943404ba42043aebd37585c:0601353E:M`). Runtime C2 proved minified contamination moved into the install blueprint and final installed stool. | Covered in `ZL_Contamination_Persistence_15_build_replace.rws`. |
| `Source/ContaminationPatchesBuilding.cs:168` | `GenConstruct_PlaceBlueprintForReinstall_Patch` | `resolved/runtime` | `GenConstruct.PlaceBlueprintForReinstall(Building, IntVec3, Map, Rot4, Faction)` exists (`88df42e0e943404ba42043aebd37585c:0601353F:M`). Runtime C2 proved source contamination `0.45` moved into the reinstall blueprint and final reinstalled stool. | Covered in `ZL_Contamination_Persistence_15_build_replace.rws`. |
| `Source/ContaminationPatchesBuilding.cs:184` | `Blueprint_TryReplaceWithSolidThing_Patch` | `resolved/runtime` | `Blueprint.TryReplaceWithSolidThing(Pawn, ThingDef)` exists (`88df42e0e943404ba42043aebd37585c:0600E7FC:M`). Runtime C2 proved install and reinstall blueprints cleared to `0` after replacement while final buildings kept expected contamination. | Covered in `ZL_Contamination_Persistence_15_build_replace.rws`. |
| `Source/ContaminationPatchesBuilding.cs:200` | `SmoothableWallUtility_SmoothWall_Patch` | `semantic/runtime` | `SmoothableWallUtility.SmoothWall(IntVec3, Map, Single, Boolean)` exists (`88df42e0e943404ba42043aebd37585c:0601380B:M`) and still destroys, makes, and spawns the smoothed wall. Runtime C2 proved natural granite contamination `0.58` moved into smoothed granite and persisted after reload. | Covered in `ZL_Contamination_Persistence_15_build_replace.rws`. |
| `Source/ContaminationPatchesBuilding.cs:218` | `SmoothableWallUtility_Notify_BuildingDestroying_Patch` | `resolved/runtime` | `SmoothableWallUtility.Notify_BuildingDestroying(Building, DestroyMode)` exists (`88df42e0e943404ba42043aebd37585c:0601380A:M`). Runtime C2 proved smoothed-wall reversion preserved target contamination `0.63`; after reload, the reverted granite persisted at `(101,101)` with contamination `0.63`. | Covered in `ZL_Contamination_Persistence_15_build_replace.rws`. |
| `Source/ContaminationPatchesBuilding.cs:271` | `Building_SubcoreScanner_Tick_Patch` | `resolved/runtime` | `Building_SubcoreScanner.Tick()` exists (`88df42e0e943404ba42043aebd37585c:0600F0EC:M`) and, when occupied fabrication finishes, calls `EjectContents()` and places `ThingMaker.MakeThing(def.building.subcoreScannerOutputDef)` at `InteractionCell` through `GenPlace.TryPlaceThing`. | Covered by `zombieland/contamination_biotech_product_handoffs_contract`: a powered occupied `SubcoreSoftscanner` moved scanner contamination `0.58 -> 0.4872` and spawned `SubcoreRegular` at contamination `0.0928`, matching the active Biotech product handoff. Warning-or-higher logs contained only the known old `EMPTY` hidden-ritual migration warning. |
| `Source/ContaminationPatchesBuilding.cs:287` | `Building_GeneExtractor_Finish_Patch` | `resolved/runtime` | `Building_GeneExtractor.Finish()` exists (`88df42e0e943404ba42043aebd37585c:0600EE68:M`) and, for a contained pawn, creates a `Genepack` with `ThingMaker.MakeThing(ThingDefOf.Genepack)`, initializes genes, drops the pawn contents, and places the genepack through `GenPlace.TryPlaceThing`. Runtime showed the old `ThingMaker.MakeThing` handoff was too early for the real product path, so the patch now captures `ContainedPawn` in a prefix and transfers during the `GenPlace.TryPlaceThing` placement call before clearing the thread-static capture in a finalizer. | Covered by `zombieland/contamination_biotech_product_handoffs_contract`: a real GeneExtractor finish had `containedBeforeFinish=true`, moved pawn contamination `0.68 -> 0.6528`, and spawned a `Genepack` at contamination `0.0272`, matching `geneExtractorTransfer=0.16`. Warning-or-higher logs contained only the known old `EMPTY` hidden-ritual migration warning. |
| `Source/ContaminationPatchesBuilding.cs:304` | `Building_NutrientPasteDispenser_TryDispenseFood_Patch` | `resolved/runtime` | `Building_NutrientPasteDispenser.TryDispenseFood()` exists (`88df42e0e943404ba42043aebd37585c:0600F05D:M`) and still consumes hopper feed through `SplitOff` before making `MealNutrientPaste`. Runtime `zombieland/contamination_nutrient_paste_contract` spawned a powered dispenser plus hopper feed, produced an unspawned nutrient paste meal, transferred feed contamination `0.6 * 0.166000009` into meal contamination `0.09960001`, left the remaining feed stack at `0.6`, and confirmed the private active-feed list was cleared after the postfix. | Covered for real dispenser feed transfer and active-feed cleanup. |
| `Source/ContaminationPatchesFilth.cs:13` | `Pawn_FilthTracker_Notify_EnteredNewCell_Patch` | `resolved/runtime` | `Pawn_FilthTracker.Notify_EnteredNewCell()` exists (`88df42e0e943404ba42043aebd37585c:0600CCA9:M`) and still handles entry filth/drop pickup chances. Runtime C4 proved a pawn entering contaminated cell `(100,100)` gained contamination `0.009139201`, then persisted after reload with need/hediff state. | Covered in `ZL_Contamination_Persistence_25_cleaning_environment.rws` for cell-entry gain; negative/filth-drop variants remain broader filth scenarios. |
| `Source/ContaminationPatchesFilth.cs:55` | `CompSpawnerFilth_TrySpawnFilth_Patch` | `resolved/runtime` | `CompSpawnerFilth.TrySpawnFilth()` exists (`88df42e0e943404ba42043aebd37585c:06010AD8:M`) and directly calls `FilthMaker.TryMakeFilth` after selecting a reachable nearby cell. Runtime C3 recorded the installed Zombieland prefix/postfix on `RimWorld.CompSpawnerFilth::TrySpawnFilth()`. | Covered by `zombieland/contamination_filth_leavings_contract`: a synthetic real `CompSpawnerFilth` on contaminated steel parent at `(123,100)` spawned `Filth_Dirt` at `(124,100)`; parent contamination `0.62` produced filth contamination `0.025544`, matching `filthEqualize=0.0412`; warning-or-higher logs were empty. |
| `Source/ContaminationPatchesFilth.cs:65` | `GenLeaving_DoLeavingsFor_Patch` | `resolved/runtime` | Exact `GenLeaving.DoLeavingsFor(Thing, Map, DestroyMode, CellRect, Predicate<IntVec3>, List<Thing>)` overload exists (`88df42e0e943404ba42043aebd37585c:06013578:M`). Runtime C3 proved ship-chunk leavings received total contamination `0.320000023`; stable component leaving `Thing_ComponentIndustrial6908` reloaded with `0.160000011`. | Covered with compressed-chunk caveat: slag chunk transfer is runtime-proven, but slag chunks reload under regenerated ids/positions. |
| `Source/ContaminationPatchesFilth.cs:89` | `Thing_TakeDamage_Patch` | `resolved/runtime` | `Thing.TakeDamage(DamageInfo)` exists (`88df42e0e943404ba42043aebd37585c:0600395B:M`) and RimWorld 1.6 calls `GenLeaving.DropFilthDueToDamage` (`88df42e0e943404ba42043aebd37585c:0601357E:M`) for external violence when `DamageInfo.SpawnFilth` is true. Runtime C3 recorded the installed Zombieland prefix/postfix on `Verse.Thing::TakeDamage(DamageInfo)`. | Covered by `zombieland/contamination_filth_leavings_contract`: a contaminated real `ZombieThumper` with `filthLeaving=Filth_RubbleBuilding` took `240` Crush damage, spawned rubble filth at `(128,101)`, and filth contamination `0.026368` matched thumper contamination `0.64 * filthEqualize=0.0412`; warning-or-higher logs were empty. |
| `Source/ContaminationPatchesFilth.cs:98` | `HediffComp_DissolveGearOnDeath_Notify_PawnKilled_Patch` | `resolved/runtime` | `HediffComp_DissolveGearOnDeath.Notify_PawnKilled()` exists (`88df42e0e943404ba42043aebd37585c:06013FC7:M`) and spawns configured filth through `FilthMaker.TryMakeFilth` when the pawn is spawned and `Props.filth` is set. Runtime C3 recorded the installed Zombieland prefix/postfix on `RimWorld.HediffComp_DissolveGearOnDeath::Notify_PawnKilled()`. | Covered by `zombieland/contamination_filth_leavings_contract`: a synthetic real dissolve-gear comp on a spawned colonist at `(147,100)` set pawn contamination `0.49` and requested `Filth_Slime`; the spawned filth reached contamination `0.020188`, matching `filthEqualize=0.0412`; warning-or-higher logs were empty. |
| `Source/ContaminationPatchesFilth.cs:107` | `Projectile_Liquid_DoImpact_Patch` | `resolved/runtime` | `Projectile_Liquid.DoImpact(Thing hitThing, IntVec3 cell)` exists as the private helper (`88df42e0e943404ba42043aebd37585c:0600F7FF:M`) called by `Projectile_Liquid.Impact`; it spawns configured projectile filth through `FilthMaker.TryMakeFilth` before terrain/damage handling. Runtime C3 recorded the installed Zombieland prefix/postfix on `RimWorld.Projectile_Liquid::DoImpact(Thing, IntVec3)`. | Covered by `zombieland/contamination_filth_leavings_contract`: a real `Proj_Foam` liquid projectile invoked `DoImpact` with contaminated steel as `hitThing`; source contamination `0.57` produced `Filth_FireFoam` at `(155,100)` with contamination `0.0234840009`, matching `filthEqualize=0.0412`; warning-or-higher logs were empty. |
| `Source/ContaminationPatchesFilth.cs:117` | `TunnelHiveSpawner_Tick_Patch` | `resolved/runtime` | RimWorld 1.4 `TunnelHiveSpawner.Tick()` emitted tunnel rubble filth directly, but RimWorld 1.6 moved that rubble tick into `GroundSpawner.Tick()` (`88df42e0e943404ba42043aebd37585c:0600F379:M`); `TunnelHiveSpawner.Spawn(Map, IntVec3)` (`88df42e0e943404ba42043aebd37585c:0600F470:M`) only creates the hive/insects. The patch now targets `GroundSpawner.Tick()` and filters to spawned `TunnelHiveSpawner` instances. Runtime C3 recorded the installed Zombieland prefix/postfix on `RimWorld.GroundSpawner::Tick()`. | Fixed and covered by `zombieland/contamination_filth_leavings_contract`: a spawned `TunnelHiveSpawner` at `(159,100)` forced one real rubble tick from contaminated ground `0.63`, producing `Filth_Dirt` at `(161,101)` with contamination `0.0259560011`, matching `filthEqualize=0.0412`; warning-or-higher logs were empty. |
| `Source/ContaminationPatchesFilth.cs:134` | `DamageWorker_Flame_Apply_Patch` | `resolved/runtime` | `DamageWorker_Flame.Apply(DamageInfo, Thing)` exists (`88df42e0e943404ba42043aebd37585c:06002950:M`), calls `base.Apply`, and then spawns `Filth_Ash` over the destroyed non-pawn victim's occupied rect. Runtime C3 recorded the installed Zombieland prefix/postfix on `Verse.DamageWorker_Flame::Apply(DamageInfo, Thing)`. | Covered and fixed by snapshotting source contamination/map index before vanilla destruction can clear the victim state. The first live run reproduced clean ash (`0`) after the thumper was destroyed and nested leavings cleared `filthSource`; the fixed rerun spawned four `Filth_Ash` at contamination `0.030076`, matching thumper snapshot `0.73 * filthEqualize=0.0412`; warning-or-higher logs were empty. |
| `Source/ContaminationPatchesFilth.cs:143` | `Verse_Explosion_TrySpawnExplosionThing_Patch` | `resolved/runtime` | `Explosion.TrySpawnExplosionThing(ThingDef, IntVec3, Int32)` exists as a private helper (`88df42e0e943404ba42043aebd37585c:060036F9:M`) and still routes filth defs through `FilthMaker.TryMakeFilth`. Runtime C3 recorded the installed Zombieland prefix/postfix on that private helper. | Covered by `zombieland/contamination_filth_leavings_contract`: a real `Explosion` with damaged steel sources at `0.22` and `0.74` invoked the private helper for `Filth_Dirt`; spawned filth at `(168,100)` reached `0.0304880012`, matching the highest damaged source `0.74 * filthEqualize=0.0412`, and did not match the lower source value `0.009064`; warning-or-higher logs were empty. |
| `Source/ContaminationPatchesFilth.cs:153` | `RoofCollapserImmediate_DropRoofInCellPhaseTwo_Patch` | `resolved/runtime` | `RoofCollapserImmediate.DropRoofInCellPhaseTwo(IntVec3, Map)` exists (`88df42e0e943404ba42043aebd37585c:06002203:M`) and calls `FilthMaker.TryMakeFilth` when the collapsed roof def has `filthLeaving`. Runtime C3 recorded the installed Zombieland prefix/postfix on the private phase-two helper. | Covered by `zombieland/contamination_roof_collapse_contract`: a temporary `RoofRockThick` at `(106,100)` with ground contamination `0.61` spawned `Filth_RubbleRock`; filth contamination `0.025132` matched `filthEqualize=0.0412`, and ground dropped to `0.584868`; warning-or-higher logs were empty. |
| `Source/ContaminationPatchesFilth.cs:178` | `PregnancyUtility_SpawnBirthFilth_Patch` | `resolved/runtime` | RimWorld 1.6 target is the private helper `PregnancyUtility.SpawnBirthFilth(Pawn mother, IntVec3 center, ThingDef filth, Int32 radius)` (`88df42e0e943404ba42043aebd37585c:0600C6C3:M`), which loops `BirthFilthCount.RandomInRange` and calls `FilthMaker.TryMakeFilth` near the supplied center. Runtime C3 recorded the installed Zombieland prefix/postfix on that private helper. | Covered by `zombieland/contamination_filth_leavings_contract`: a mother pawn at `(135,100)` with contamination `0.52` invoked the private helper for `Filth_AmnioticFluid` radius `2`; five birth filths spawned and all had contamination `0.021424`, matching `filthEqualize=0.0412`; warning-or-higher logs were empty. |
| `Source/ContaminationPatchesFilth.cs:187` | `Corpse_ButcherProducts_Patch` | `resolved/runtime` | `Corpse.ButcherProducts(Pawn, Single)` exists (`88df42e0e943404ba42043aebd37585c:060036B5:M`) and RimWorld 1.6 delegates product creation to the `InnerPawn.ButcherProducts` iterator before spawning butcher blood filth. Runtime C3 first exposed that the old postfix only left source context for blood filth: alpaca meat/leather stayed `0` while butcher blood reached `0.0271920022`. | Fixed by applying corpse contamination to each yielded product while keeping the source context alive for blood filth. `zombieland/contamination_filth_leavings_contract` now proves an alpaca corpse at `0.66` produces `Meat_Alpaca x126` and `Leather_Camel x37` both at `0.0271920022`, matching `filthEqualize=0.0412`; butcher blood also matched and warning-or-higher logs were empty. |
| `Source/ContaminationPatchesFilth.cs:203` | `Pawn_HealthTracker_DropBloodFilth_Patch` | `resolved/runtime` | `Pawn_HealthTracker.DropBloodFilth()` exists (`88df42e0e943404ba42043aebd37585c:0600333D:M`). Runtime C3 proved contaminated pawn blood filth was produced at `0.02884` from pawn contamination `0.7`. | Runtime covered for contaminated blood creation; clean-pawn negative remains part of broader filth scenario. |
| `Source/ContaminationPatchesIncidents.cs:39` | `ThingSetMaker_Generate_Patch` | `resolved/runtime` | Exact `ThingSetMaker.Generate(ThingSetMakerParams)` overload exists (`88df42e0e943404ba42043aebd37585c:06009ACE:M`) and still returns the postprocessed generated list. Runtime C3 recorded the installed Zombieland postfix on that overload. | Covered by `zombieland/contamination_generation_handoffs_contract`: a generated `ComponentIndustrial x3` and `Steel x25` received contamination `0.225987986` and `0.151718691` with `randomThingCreateChance=1`; warning-or-higher logs were empty. |
| `Source/ContaminationPatchesIncidents.cs:56` | `TradeDeal_AddAllTradeables_Patch` | `resolved/runtime` | `TradeDeal.AddAllTradeables()` exists (`88df42e0e943404ba42043aebd37585c:060113C2:M`) and still adds trader goods into `Tradeable.thingsTrader`, plus trader silver when absent. Runtime C3 recorded the installed Zombieland postfix on the private helper. | Covered by `zombieland/contamination_generation_handoffs_contract`: a synthetic real `ITrader` supplied `ComponentIndustrial x3` and `Steel x25`, `TradeSession.SetupWith` built a real `TradeDeal` with 3 tradeables, both original goods were visible in trader tradeables and received nonzero contamination; warning-or-higher logs were empty. |
| `Source/ContaminationPatchesIncidents.cs:81` | `MechClusterUtility_SpawnCluster_Patch` | `resolved/runtime` | RimWorld 1.6 target is now `MechClusterUtility.SpawnCluster(IntVec3, Map, MechClusterSketch, Boolean, Boolean, String)` (`88df42e0e943404ba42043aebd37585c:0600BB72:M`); it returns the `spawnedThings` list after spawning sketch buildings/pawns and setting mechanoid faction state. Runtime C3 recorded the installed Zombieland postfix on that 1.6 signature. | Covered by `zombieland/contamination_generation_handoffs_contract`: a real mech-cluster sketch spawned `Thing_Barricade6903` at `(172,100)` and the returned spawned thing received uniform contamination `4.659071e-6` with `mechClusterChance=1`; warning-or-higher logs were empty. |
| `Source/ContaminationPatchesMineable.cs:14` | `MapGenerator_GenerateContentsIntoMap_Patch` | `resolved/runtime` | `Verse.MapGenerator.GenerateContentsIntoMap(IEnumerable<GenStepWithParams>, Map, Int32, Boolean)` exists (`88df42e0e943404ba42043aebd37585c:06001F32:M`) and still runs ordered GenSteps under `Rand`/`RockNoises` cleanup. Runtime started a built-in debug colony, sampled 81 generated cells, and saved `ZL_MapGeneration_Contamination_01.rws`. | Covered for generated-map contamination seeding and persistence: cell `(150,100)` contained `MineableSteel` with contamination, then after reload read back as contamination `0.0016`; warning-or-higher logs were empty. |
| `Source/ContaminationPatchesMineable.cs:85` | `Mineable_Destroy_Patch` | `resolved/runtime` | `Mineable.Destroy(DestroyMode)` exists (`88df42e0e943404ba42043aebd37585c:0600E945:M`). Runtime C3 captured mineable contamination before destruction during the `DestroyMined` path. | Covered for mined steel yield in `ZL_Contamination_Persistence_20_world_products.rws`. |
| `Source/ContaminationPatchesMineable.cs:94` | `Mineable_DestroyMined_Patch` | `resolved/runtime` | `Mineable.DestroyMined(Pawn)` exists (`88df42e0e943404ba42043aebd37585c:0600E944:M`). Runtime C3 mined `MineableSteel` and reloaded yielded `Thing_Steel6915` at contamination `0.65`. | Covered in `ZL_Contamination_Persistence_20_world_products.rws`. |
| `Source/ContaminationPatchesMineable.cs:104` | `Mineable_TrySpawnYield_Patch` | `semantic/runtime` | Exact `Mineable.TrySpawnYield(Map, Boolean, Pawn)` overload exists (`88df42e0e943404ba42043aebd37585c:0600E947:M`). Runtime C3 proved spawned steel yield inherited mined-cell contamination and persisted after reload. | Covered for resource-yield path; chunk-specific persistence remains documented under GenLeaving caveat. |
| `Source/ContaminationPatchesMineable.cs:126` | `CompDeepDrill_TryProducePortion_Patch` | `semantic/runtime` | `CompDeepDrill.TryProducePortion(Single, Pawn)` exists (`88df42e0e943404ba42043aebd37585c:0600FECB:M`). Runtime C3 produced deep-drill `Steel x45` and reloaded `Thing_Steel6917` with contamination `0.420000017`. | Covered in `ZL_Contamination_Persistence_20_world_products.rws`. |
| `Source/ContaminationPatchesMisc.cs:19` | `Pawn_Kill_Patch` | `resolved/runtime` | `Pawn.Kill(DamageInfo?, Hediff)` exists (`88df42e0e943404ba42043aebd37585c:0600306B:M`) and still preserves held map/position state during death. Runtime `zombieland/contamination_zombie_death_contract` killed a normal zombie and an ordinary colonist from clean cells: the zombie death cell changed `0 -> 0.0432` from `zombieDeathAdd`, while the human death cell stayed `0`. | Covered for zombie death-cell contamination and ordinary pawn negative. |
| `Source/ContaminationPatchesMisc.cs:31` | `Fire_DoComplexCalcs_Patch` | `resolved/runtime` | `Fire.DoComplexCalcs()` exists (`88df42e0e943404ba42043aebd37585c:0600F501:M`) and still scans flammables at the fire position. Runtime C4 proved one real complex-calc pass reduced component and ground contamination from `0.4` to `0.292`, then reloaded component `Thing_ComponentIndustrial6901` and cell `(104,100)` at `0.292`. | Covered in `ZL_Contamination_Persistence_25_cleaning_environment.rws`; long-running fire spread remains out of scope for this row. |
| `Source/ContaminationPatchesMisc.cs:69` | `GenReciepe_MakeRecipeProducts_Patch` | `resolved/runtime` | `GenRecipe.MakeRecipeProducts(RecipeDef, Pawn, List<Thing>, Thing, Precept_ThingStyle, ThingStyleDef, Int32)` exists (`88df42e0e943404ba42043aebd37585c:06005555:M`) and still yields normal `ThingMaker.MakeThing` products before postprocessing. Runtime `zombieland/contamination_recipe_product_contract` used a butcher-table bill giver and a custom component recipe with contamination factors isolated to recipe transfer: ingredient steel moved `0.8 -> 0.4`, and the unspawned `ComponentIndustrial` product received `0.4`. | Covered for ingredient-to-product recipe transfer; bench/worker equalization is source-gated here and remains additive because the contract isolates the product-transfer branch. |
| `Source/ContaminationPatchesMisc.cs:94` | `ThingOwner_TryTransferToContainer_Patch` | `resolved/runtime` | Exact `ThingOwner.TryTransferToContainer(Thing, ThingOwner, Int32, out Thing, Boolean)` overload exists (`88df42e0e943404ba42043aebd37585c:06003A17:M`). Runtime C1 proved partial `4` and full remaining `6` transfers between real frame resource containers; target frame direct contamination was `0.75`, including held resources was `1.5` before save, and the post-reload frame completion fallback preserved handoff despite held-resource contamination reloading clean. | Covered for stack/container C1; reuse `complete_frame_by_id` in C2 for broader construction/replacement chains. |
| `Source/ContaminationPatchesMisc.cs:145` | `Thing_TryAbsorbStack_Patch` | `resolved/runtime` | `Thing.TryAbsorbStack(Thing, Boolean)` exists (`88df42e0e943404ba42043aebd37585c:0600395F:M`). Runtime C1 proved a real stack merge: `Steel x6` at contamination `0.2` absorbed `Steel x4` at contamination `0.8`, source was destroyed, target count became `10`, and weighted target contamination became `0.44`. | Covered in `zombieland/contamination_stack_absorb_contract` and persisted through C1 state reads. |
| `Source/ContaminationPatchesMisc.cs:188` | `Thing_SplitOff_Patch` | `resolved/runtime` | `Thing.SplitOff(Int32)` exists (`88df42e0e943404ba42043aebd37585c:06003960:M`). Runtime C0/C1 split coverage proved a contaminated stack split preserved contamination on both source and split results; C1 persisted the resulting stack state after reload. | Covered for ordinary stack split propagation; comp-specific split is tracked separately below. |
| `Source/ContaminationPatchesMisc.cs:234` | `ThingWithComps_SplitOff_Patch` | `resolved/runtime` | `ThingWithComps.SplitOff(Int32)` exists (`88df42e0e943404ba42043aebd37585c:06003A89:M`). Runtime C0 split `ComponentIndustrial x10` at contamination `0.6` into source `x6` and split `x4`, both retaining contamination `0.6`. | Covered in `zombieland/contamination_core_contract` for comp stack split propagation. |
| `Source/ContaminationPatchesMisc.cs:246` | `Thing_Ingested_Patch` | `semantic/runtime` | `Thing.Ingested(Pawn, Single)` exists (`88df42e0e943404ba42043aebd37585c:0600397F:M`) and still calls `IngestedCalculateAmounts`, which the transpiler replaces to capture partial-stack consumption. Runtime `zombieland/contamination_ingestion_contract` ingested one meal from a contaminated five-meal stack: eater contamination changed `0 -> 0.09600001`, need and hediff severity matched, and the remaining meal stack changed `0.5 -> 0.403999984`. | Covered for partial-stack food ingestion transfer and eater need/hediff synchronization. Clean ingestion remains represented by the core clean-state negative and can be sampled in broader player smoke. |
| `Source/ContaminationPatchesMisc.cs:276` | `MinifiedThing_SplitOff_Patch` | `resolved/runtime` | `MinifiedThing.SplitOff(Int32)` exists (`88df42e0e943404ba42043aebd37585c:0600E7BD:M`) and still calls `base.SplitOff`, then creates a fresh inner thing for the split result. Runtime C0 recorded the installed Zombieland prefix/postfix on the real target. | Covered and fixed by `zombieland/contamination_core_contract`: first live run exposed double transfer from the generic `Thing.SplitOff` postfix plus the minified-specific proportional transfer (`0.7` on split instead of expected `0.2`). The fixed run clears the generic copy before proportional transfer; a spawned minified Stool stack `x5` at contamination `0.5` split off `x2`, leaving source `x3` at `0.3` and split `x2` at `0.2`; warning-or-higher logs were empty. |
| `Source/ContaminationPatchesMisc.cs:338` | `ExecutionUtility_ExecutionInt_Patch` | `resolved/runtime` | Exact private `ExecutionUtility.ExecutionInt(Pawn, Pawn, Boolean, Int32, Boolean)` overload exists (`88df42e0e943404ba42043aebd37585c:060134E6:M`) and still drops blood, applies execution-cut damage, then kills the victim if needed. Runtime C3 recorded the installed Zombieland prefix/postfix on the private helper. | Covered by `zombieland/contamination_filth_leavings_contract`: a spawned executioner invoked `ExecutionInt` on a victim at contamination `0.68`; the victim died, a corpse was produced, and execution blood at `(172,100)` reached `0.028016001`, matching `0.68 * filthEqualize=0.0412`; warning-or-higher logs were empty. |
| `Source/ContaminationPatchesMisc.cs:347` | `TendUtility_DoTend_Patch` | `resolved/runtime` | `TendUtility.DoTend(Pawn, Pawn, Medicine)` exists (`88df42e0e943404ba42043aebd37585c:06006F92:M`). `zombieland/contamination_tending_contract` passed with medicine stack consumption and expected doctor/patient contamination `0.4`/`0.18`. | Covered for real tending transfer; broader medical workflow can remain in player-scenario smoke. |
| `Source/ContaminationPatchesMisc.cs:373` | `PawnUtility_GainComfortFromCellIfPossible_Patch` | `resolved/runtime` | Exact `PawnUtility.GainComfortFromCellIfPossible(Pawn, Int32, Boolean)` overload exists (`88df42e0e943404ba42043aebd37585c:0600C636:M`). `zombieland/contamination_rest_comfort_contract` passed after null-safe cell-target handling: pawn contamination reached `0.00352`, ground fell to `0.79648`, matching `restEqualize`. | Covered for contaminated cell rest comfort; bed/edifice transfer can be sampled in player-scenario smoke. |
| `Source/ContaminationPatchesMisc.cs:392` | `Pawn_CarryTracker_CarryHandsTickInterval_Patch` | `resolved/runtime` | `Pawn_CarryTracker.CarryHandsTickInterval(Int32)` exists (`88df42e0e943404ba42043aebd37585c:060032BD:M`) and remains a stable carried-thing tick hook. Runtime `zombieland/contamination_carry_tracker_contract` put contaminated `Steel x20` into a drafted colonist's hands, advanced to the source cadence tick, and invoked the real carry tick path: pawn contamination changed `0 -> 0.060200002`, carried item contamination changed `0.7 -> 0.6398`, and the item remained held. | Covered for carried-item equalization on the real carry tick cadence. Repetition-after-drop remains a broader haul/player-flow negative. |
| `Source/ContaminationPatchesMisc.cs:412` | `PawnUtility_Mated_Patch` | `resolved/runtime` | `PawnUtility.Mated(Pawn, Pawn)` exists (`88df42e0e943404ba42043aebd37585c:0600C63A:M`). Runtime C5 recorded the installed Zombieland postfix on the real static method. | Covered by `zombieland/contamination_social_transfer_contract`: spawned mating pawns at contamination `0.8` and `0.2` equalized to `0.5`/`0.5`; warning-or-higher logs were empty. |
| `Source/ContaminationPatchesMisc.cs:423` | `JobDriver_Lovin_MakeNewToils_Patch` | `semantic/runtime` | `JobDriver_Lovin.MakeNewToils()` exists (`88df42e0e943404ba42043aebd37585c:06007587:M`) and still has a lay-down toil with an init action. Runtime C5 recorded the installed Zombieland postfix on the real `MakeNewToils()` target. | Covered by `zombieland/contamination_social_transfer_contract`: a real `Lovin` job on a double-bed fixture reached the lay-down path under the patch's `ticksLeft <= 25000` gate at tick 154 and matched one 0.1 equalization transfer (`0.8`/`0.2` -> about `0.7399`/`0.26`); warning-or-higher logs were empty. |
| `Source/ContaminationPatchesMisc.cs:453` | `Corpse_InnerPawn_Setter_Patch` | `resolved/runtime` | `Corpse.InnerPawn` setter exists (`88df42e0e943404ba42043aebd37585c:17000A00:P`) and still replaces the corpse's inner container content. Runtime `zombieland/contamination_corpse_inner_pawn_contract` killed a pawn with contamination `0.47`, produced a fresh human corpse whose inner pawn matched the source pawn, and confirmed the corpse inherited contamination `0.47`. | Covered for corpse/inner-pawn synchronization during real death/corpse creation. |
| `Source/ContaminationPatchesMisc.cs:510` | `Recipe_RemoveImplant_ApplyOnPawn_Patches` | `semantic/runtime` | `Recipe_RemoveImplant.ApplyOnPawn(Pawn, BodyPartRecord, Pawn, List<Thing>, Bill)` exists in both RimWorld 1.4 (`0f44124915ec469b8a0ef6daf05b1c19:06005EFB:M`) and 1.6 (`88df42e0e943404ba42043aebd37585c:0600C36B:M`) with the same spawn-before-remove shape. Runtime C3 recorded the installed Zombieland transpiler and exposed/fixed an instance-argument bug: the replacement must load `ldarg.1` for the patient pawn, not `ldarg.0` for the recipe worker. | Covered by `zombieland/contamination_misc_spawn_handoffs_contract`: synthetic remove-implant surgery removed an `ArchotechArm` hediff from a contaminated patient and spawned one `ArchotechArm` at the doctor's cell; patient contamination moved `0.67 -> 0.6329267`, and spawned contamination `0.0370733365` matched measured holder-aware source loss `0.0370733142`; warning-or-higher logs were empty. |
| `Source/ContaminationPatchesMisc.cs:526` | `CompLifespan_Expire_Patch` | `semantic/runtime` | `CompLifespan.Expire()` exists (`88df42e0e943404ba42043aebd37585c:06003B8B:M`) and still calls `GenSpawn.Spawn(Props.plantDefToSpawn, parent.Position, parent.Map)` before destroying the parent. Runtime C3 recorded the installed Zombieland transpiler on the real target. | Covered by `zombieland/contamination_misc_spawn_handoffs_contract`: a synthetic real `CompLifespan` parent at contamination `0.75` expired into `Plant_Grass` at contamination `0.124500006`, matching `generalTransfer=0.166000009`; the clean control expired into a clean plant; warning-or-higher logs were empty. |
| `Source/ContaminationPatchesMisc.cs:542` | `RoofCollapserImmediate_DropRoofInCellPhaseOne_Patch` | `semantic/runtime` | `RoofCollapserImmediate.DropRoofInCellPhaseOne(IntVec3, Map)` exists (`88df42e0e943404ba42043aebd37585c:06002202:M`). Runtime C3 decompiled the method shape, ran phase one through reflection, and reloaded regenerated collapsed rocks at `(99,100)` with ground-backed contamination `0.67`. | Covered for phase-one collapsed-rock product persistence. The companion phase-two roof filth row is now covered separately by `RoofCollapserImmediate_DropRoofInCellPhaseTwo_Patch`. |
| `Source/ContaminationPatchesPlants.cs:43` | `WildPlantSpawner_SpawnPlant_Patch` | `resolved/runtime` | `WildPlantSpawner.SpawnPlant(IntVec3, Map, Single, Boolean)` exists (`88df42e0e943404ba42043aebd37585c:0600BD01:M`). Runtime C3 spawned grass through the real wild-plant spawner and reloaded `Thing_Plant_Grass6929` with contamination `0.201600015`. | Covered in `ZL_Contamination_Persistence_20_world_products.rws`. |
| `Source/ContaminationPatchesPlants.cs:99` | `IncidentWorker_AmbrosiaSprout_TryExecuteWorker_Patch` | `semantic/runtime` | `IncidentWorker_AmbrosiaSprout.TryExecuteWorker(IncidentParms)` exists (`88df42e0e943404ba42043aebd37585c:0600A95B:M`). Runtime C3 ran the real ambrosia incident, spawned 13 ambrosia, and reloaded sampled ambrosia `Thing_Plant_Ambrosia6931`/`6932`/`6933` at `0.68`. | Covered in `ZL_Contamination_Persistence_20_world_products.rws`. |
| `Source/ContaminationPatchesPlants.cs:116` | `Plant_TrySpawnStump_Patch` | `semantic/runtime` | `Plant.TrySpawnStump()` exists (`88df42e0e943404ba42043aebd37585c:0600F78C:M`). Runtime C3 spawned an oak stump through the real plant method and reloaded `Thing_ChoppedStump6928` with contamination `0.55`. | Covered in `ZL_Contamination_Persistence_20_world_products.rws`. |
| `Source/ContaminationPatchesPollutionSnow.cs:11` | `JobDriver_ClearPollution_ClearPollutionAt_Patch` | `resolved/runtime` | `JobDriver_ClearPollution.MakeNewToils()` exists (`88df42e0e943404ba42043aebd37585c:06007270:M`) and still clears after `5600f` work, calls `ClearPollutionAt`, and spawns `ThingDefOf.Wastepack`; `ClearPollutionAt` still unpollutes nearby cells through `Unpollute` before radial fallback. | Covered by `zombieland/contamination_clear_pollution_contract` in the final DLC stretch. A real active-Biotech clear-pollution job reached the `tickIntervalAction` completion path, cleared the polluted cell after `5604` ticks, and the wrapped `Unpollute` subject received worker contamination `0.0765600055`, matching ground `0.66 * pollutionAdd=0.116000004`. |
| `Source/ContaminationPatchesPollutionSnow.cs:41` | `GridUtility_Unpollute_Patch` | `resolved/runtime` | `GridsUtility.Unpollute(IntVec3, Map)` exists (`88df42e0e943404ba42043aebd37585c:06001B52:M`) and still calls `map.pollutionGrid.SetPolluted(c, false)`, dirties snow meshes, and conditionally dirties Odyssey sand. | Covered by `zombieland/contamination_clear_pollution_contract` in the final DLC stretch. The real clear-pollution job reached `Unpollute`, changed `pollutedBefore=true` to `pollutedAfter=false`, and transferred polluted ground contamination `0.66` into the remembered worker at `0.0765600055`. |

## Fleshmass Collision Static Targets

This slice was audited against the installed Steam RimWorld `1.6.4871 rev597`
assembly, MVID `967ddb80559449f0a776dafa26a855d1`.

| Location | Patch class | Status | RimWorld 1.6 evidence | Coverage disposition |
| --- | --- | --- | --- | --- |
| `Source/Patches_Fleshmass.cs:11` | `CompFleshmass_Notify_Killed_Patch` | `resolved/runtime` | `CompFleshmass.Notify_Killed(Map, DamageInfo?)` exists at `967ddb80559449f0a776dafa26a855d1:06010099:M`. Vanilla still owns player/null-instigator accounting; `CompGrowsFleshmassTendrils.Notify_FleshmassDestroyedByPlayer(Thing)` remains the response increment boundary at `967ddb80559449f0a776dafa26a855d1:06010221:M`. The postfix adds only actual Zombieland-faction kills, uses the destroyed cell's stored spawned grower source, and removes only a newly created attributed `ThreatBig` letter. | Direct contract operations `op_d4dfd7ecacfe4136b8efdc6ce5176f36` and `op_b3c165266582463596f697356e37e9cf` passed zombie root/cascade, player, null-instigator, compatible non-heart grower, touching-field, nonlethal, and lost-source accounting. Async operation `op_a42ed6862ad642dda5d70266355eaa84` proved real ordinary and suicide responses spawned assaulting fleshbeasts while leaking zero response letters. |
| `Source/Patches_Fleshmass.cs:34` | `Explosion_AffectCell_Fleshmass_Patch` | `resolved/runtime` | Private `Verse.Explosion.AffectCell(IntVec3)` exists at `967ddb80559449f0a776dafa26a855d1:060036FD:M`. A prefix/finalizer pair scopes only `SuicideBombDamage` cell effects, unwinds attribution even on exception, and does not persist attribution across calls or saves. | The direct contract proved the installed owner on the exact private method. The live response stage observed a real armed suicide zombie, collateral wall destruction, 13 destroyed flesh cells, response progress `1 -> 174`, one assault-lord fleshbeast, and zero leaked `ThreatBig` letters. |

The deliberate-target boundary is source-owned rather than a new Harmony
target. `Source/FleshmassCollision.cs` supplies the single flesh-family and
category predicate, while `Source/ZombieStateHandler.cs:1414` and
`Source/ZombieStateHandler.cs:1464` route sourced-active `DoorsOnly` and
generic building candidates through it. The 36-case direct contract and the
five-stage live scenario cover the ordinary, tank, suicide, former-colonist,
special, child, heart-immunity, `OnlyColonists`, and unrelated-building
branches.
