# Zombieland 1.6 Scenario Definitions

These are scenario-level tests derived from `TEST_COVERAGE.md`. They intentionally sit above the existing focused bridge contracts. Their job is to prove that already-covered primitives work together as player-facing RimWorld behavior.

Work source-first: scenario runtime should follow a static/decompiler pass over the relevant Harmony patch group. A scenario is not a substitute for confirming that RimWorld 1.6 still exposes the same target, signature, closure/delegate role, and semantics that the patch expects.

Do not add a custom bridge endpoint for each scenario. Prefer generic operations:

- load a named save
- spawn or find pawns/things/terrain by def name
- set settings and colonist toggles
- issue jobs or debug actions
- step real game ticks
- read map/pawn/thing/log/UI state
- save, reload, and re-read state
- capture visual evidence only for visual/UI/effect assertions

Only add Zombieland-specific bridge code when a required assertion cannot be expressed through generic RimWorld/RimBridge operations or a stable shared helper.

Keep custom contracts at scenario scale. A contract is worth keeping when it is reusable evidence for a named scenario row or cluster; a one-off hunch probe should be removed or folded into a generic helper after use. Escalate from narrow contracts into saved scenario fixtures that prove the behavior survives alongside adjacent systems and through save-load. Before adding another bridge contract, run `scripts/coverage-inventory.sh --bridge-tools` and check whether an existing generic primitive, scenario fixture, or retained evidence helper already covers the setup/read/wait shape.

## Scenario Template

Each scenario should record:

| Field | Requirement |
| --- | --- |
| ID | Stable scenario id, for example `S-Settings-Persistence`. |
| Goal | One player-facing behavior cluster. |
| Prior evidence | Commit hashes for narrow behaviors this scenario should not duplicate. |
| Source checks | Zombieland files plus RimWorld 1.6 decompiler targets that define the expected behavior. |
| Fixture | Save name and whether immutable, generated, or disposable. |
| Setup | Generic bridge/debug actions that create the state. |
| Runtime | Tick/frame/load/save/actions to execute. |
| Assertions | Compact machine-readable assertions where possible. |
| Artifacts | Log summary, save name, screenshot/cell-rect, timing sample, or source audit note. |
| Completion | Exact condition for marking this scenario covered. |

Every completed session should leave a boundary note in the handoff/final answer: build status, tests run, save names touched, files changed, temporary diagnostics removed or explicitly marked, docs updated, and the next blocker or next slice.

## S-Source-Patch-Audit

Goal: create and maintain the source-derived map from Zombieland code surfaces to required scenario coverage, with every Harmony patch group checked against RimWorld 1.6 before runtime scenarios rely on it.

Prior evidence:
- `90f62d4 Establish RimWorld 1.6 port baseline`
- Many later commits prove individual patch groups by behavior, but there is no whole-mod patch inventory.

Source checks:
- Run `scripts/coverage-inventory.sh` for the summary, `scripts/coverage-inventory.sh --patch-groups` for the TSV patch-group inventory, `scripts/coverage-inventory.sh --dynamic-patches` for dynamic target lookup classes, and `scripts/coverage-inventory.sh --static-summary` for scenario-owned static patch slices.
- Zombieland: every file listed under `Harmony Patch Files`, every file listed under `Gameplay Classes`, and every hook listed under `Serialization Tick UI Hooks`.
- RimWorld 1.6 decompiler: resolve target members for all `[HarmonyPatch]` groups, especially dynamic `TargetMethod()`/`TargetMethods()` patches and transpilers in `Source/Patches.cs`, `Source/Patches_Hostility.cs`, `Source/ContaminationPatches*.cs`, `Source/CETools.cs`, `Source/SoSTools.cs`, `Source/RimConnectSupport.cs`, `Source/ZombieAreaManager.cs`, `Source/ZombieDamageFlasher.cs`, `Source/ZombieLeaner.cs`, `Source/Assets.cs`, and `Source/Service.cs`.
- RimWorld 1.4 decompiler: use only when the intended legacy behavior is unclear from current code or commit evidence.

Fixture:
- No map save required for the source audit itself.
- The output should be a compact generated inventory artifact or a checked matrix update, not a chronological log.

Setup:
- Generate the current source/commit inventory.
- Group patch targets by player-facing subsystem rather than by file size.
- For each group, attach existing commit evidence if the long history already proves the behavior.

Runtime:
- Use decompiler lookups for target existence and signature shape.
- Do not run live RimWorld unless a target is ambiguous or a source check reveals a likely semantic drift.

Assertions:
- Every Harmony patch group has one of: covered by existing commit evidence, assigned to a named scenario in this file, intentionally optional integration, or marked as a current gap.
- `static` rows have a RimWorld 1.6 target member signature check; `dynamic` rows have their target lookup expression checked; `base` rows are only accepted when paired with concrete method-level patch rows.
- Dynamic patch target lookup has an explicit RimWorld 1.6 member id, a semantic-runtime patch-application check for compiler-generated closure searches, or an unavailable-mod classification.
- Obsolete RimWorld 1.4-only targets are not automatically failures, but they need a written disposition: covered by a new RimWorld 1.6 patch, covered by vanilla 1.6 behavior, assigned to a visual/runtime scenario, or queued for code removal.
- No large patch file is treated as covered merely because another patch in the same file passed.

Artifacts:
- Compact inventory output from `scripts/coverage-inventory.sh`.
- Dynamic target classification output from `scripts/coverage-inventory.sh --dynamic-patches`.
- Static target slice output from `scripts/coverage-inventory.sh --static-summary`.
- Target-level dispositions in `TEST_PATCH_AUDIT.md` when source/decompiler evidence is too detailed for the coverage matrix.
- Updated `TEST_COVERAGE.md` entries only when the audit changes cluster status.
- Optional separate generated artifact if the target-member table becomes too large for the hand-maintained matrix.

Current runtime evidence:
- Active-language acceptance is mandatory whenever `1.6/Languages` changes. Test every supported language in a separate fresh RimWorld process: set the language before launch, verify RimWorld's live `activeLanguage` matches the intended legacy folder, require an empty complete-Player.log error summary at the main menu, start the built-in Crashlanded dev map to visual-ready, and require the complete log to remain clean after map load. Never use an in-map language switch as evidence; it reloads play data and can invalidate the running game. The 2026-07-11 matrix passed for English, Chinese Simplified, Chinese Traditional, French, German, Polish, Russian, Spanish, Spanish Latin, and Turkish. Russian initially exposed the malformed official Odyssey alternative `[tradeAdj_fem] [tradeNoun_fem]`; Zombieland now repairs that exact string to `r_name->[tradeAdj_fem] [tradeNoun_fem]` only while Russian is active and only while the upstream defect remains. Shipped Polish and Turkish translations also contained description values with trailing spaces, so the early `Def.ConfigErrors` prefix trims descriptions only for those two active languages before RimWorld validates them. After rebuilding, all ten fresh-process runs loaded visual-ready dev maps with empty deduplicated error summaries; none reproduced the original static-atlas exception or null-texture warning. The zombie-silence settings follow-up reran the same live-active-language-gated matrix after changing `ZombieFreeEvents_Help`; all ten languages again reached visual-ready maps with empty complete-log error summaries.
- Added 2026-05-30 with `zombieland/startup_support_state` from `EMPTY.rws` after decompiling `UIRoot_Entry.Init()`, `ParseHelper.FromString(String, Type)`, `MainMenuDrawer.Init()`, `TimeControls.DoTimeControlsGUI(Rect)`, `MemoryUtility.ClearAllMapsAndWorld()`, `Root.Update()`, and `Root.Shutdown()`. The live probe returned `success=true` and recorded installed Zombieland patch owners on the entry-root asset loader, legacy `AreaRiskMode` parse prefix, main-menu startup-error postfix, time-control service postfix, clear-maps service prefix, root update prefix, and root shutdown prefix. It proved `Assets.initialized=true`, `Metaballs` resolved as shader `Custom/Metaballs`, `Dust(Clone)` instantiated with a particle system and renderer, legacy parsed values migrated as `IfInside -> ColonistInside` and `IfOutside -> ColonistOutside`, current `ZombieInside` parsing stayed unchanged, the normal startup-error state was clean (`errorCount=0`, no `Dialog_ErrorMessage`), a temporary `TimeControlService` subscriber received exactly one current-speed notification, and a temporary `ClearMapsService` subscriber received exactly one cleanup callback by invoking only the Zombieland prefix to avoid clearing the live game. The expanded root-lifecycle probe reset `ZombielandMod.frameWatch`, invoked only the Zombieland `Root_Update_Patch` prefix, and proved the stopwatch became running; it also set `Tools.avoider.running=true`, invoked only the Zombieland `Root_Shutdown_Patch` prefix, and proved the flag became false without calling vanilla `Root.Shutdown`. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Added 2026-05-31 by extending the same reusable startup contract for `A.DEFS.COLDLOAD`. Static/decompiler checks covered RimWorld 1.6 `DefDatabase<T>.AddAllInMods()`, `GraphicData.Init()/ConfigErrors()`, `ContentFinder<T>.Get/GetAllInFolder()`, `SoundDef.ResolveReferences()`, and `SubSoundDef.ResolveReferences()`, so the live verifier reads resolved DefDatabase state instead of reparsing XML. From a fresh deployed build and `EMPTY.rws`, `zombieland/startup_support_state` returned `success=true`: it enumerated 154 active Zombieland-owned defs across 23 DefDatabase types, observed zero config errors, zero unresolved class fields, zero database enumeration errors, initialized 40 Zombieland `ThingDef` graphics plus two `PawnKindDef` body graphics, and confirmed all 38 Zombieland `SoundDef` entries had resolved grains. The seven sustain sound defs were `ChainsawIdle`, `ChainsawWork`, `ThumperLifting`, `ThumperOperating`, `ZombieElectricHum`, `ZombieTankMovement`, and `ZombiesClosingIn`. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Added 2026-05-31 source/decompiler sound-trigger ledger. RimWorld 1.6 decompiler confirmed `Verse.Sound.SoundStarter.PlayOneShotOnCamera`, `PlayOneShot`, and `TrySpawnSustainer` still enforce the same one-shot/sustainer/on-camera validity checks that turn missing or mismatched sound defs into log evidence. The per-sound source map is recorded in `coverage/ZL_SOUND_TRIGGER_INDEX.tsv`; it assigns each custom sound to an owning scenario cluster and prevents overclaiming J audio coverage from def resolution alone.
- Added 2026-05-31 reusable runtime sound recorder. The new `zombieland/sound_events_state` tool uses RimWorld's built-in `DebugSoundEventsLog` instead of bespoke per-sound probes, so existing scenarios can produce direct audio evidence. From `EMPTY.rws`, the recorder captured `OneShot` events for `Scream` via `scream_with_albino`, `Hacking` via `hack_flickable_with_albino`, `ToxicSplash` via `kill_toxic_splasher`, `TarSmokePop` via `damage_dark_slimer`, `ElectricShock` by rendering a staged electrifier from `spawn_reference_lineup stageRenderStates:true`, `BallSpit` via `spit_zombie_ball`, `BallImpact` via `impact_zombie_ball`, `SpitterMove` by driving a spawned spitter from `Idle` into `Moving` with `wait_for_semantic_change`, two `ZombieEating` events via `zombie_eats_corpse_contract`, `ZombieRage` via `rimconnect_rage_kill_contract`, `ZombieDigOut` via `spawn_zombie appearDirectly=false` plus five stepped ticks, `ZombieHit` via both `smash_with_tanky` reaching the building/AttackStatic branch and a generic adjacent colonist/zombie AttackMelee branch driven by `wait_for_semantic_change`, `Smash` via `area_workflow_state actionMode:smart-melee`, `WallPushing` via `wall_push_over_wall_contract`, and `ZombiesRising` via `incident_threat_state setupFixture=true actionMode:scheduledWave`. From `ZL_Infection_Medical_00.rws`, the recorder wrapped `infection_workflow_state actionMode:execute` and captured `OneShot: LetterArrive_TurnedZombie - (Camera)` when the countdown corpse converted and received the turned-zombie letter. A source-guided follow-up fixed the previous ambient miss by starting the recorder before loading a fresh `EMPTY` map, so `TickManager` had no preexisting ambient sustainer; the recorder captured `[20:45:59] SustainerSpawn: ZombiesClosingIn - (Camera)`. Another source-guided pass enabled the recorder before loading `ZL_Defense_Room_base.rws`, so the thumper fixture hit `ZombieThumper.SpawnSetup`; the recorder captured `[21:22:18] SustainerSpawn: ThumperOperating - (World from Thing_Thumper6918)`. A 2026-06-01 ambient-volume pass extended generic `zombieland/get_status` with `ZombiesClosingIn` target/current/sustainer volume state and used `wait_for_semantic_change` from `ZL_CE_EMPTY_00` with one normal zombie to prove the evening ramp, night target, and morning fade-down path. `rimbridge/list_logs minimumLevel=warning` returned no entries after the passes.
- Added 2026-06-03 with `zombieland/awareness_cues_contract`, a reusable fog-of-war/awareness evidence contract for the new cue switches. From `EMPTY.rws` under active load order Harmony/Core/Anomaly/Dubs Performance Analyzer/Zombieland/RimBridgeServer, the contract proved all new cue defaults are true, `showZombieEventLetters` allows one `ThreatSmall` zombie-attack letter and suppresses it when disabled, `playZombieEventSiren` captures `OneShot: ZombiesRising - (Camera)` and suppresses it when disabled, `playSpecialZombieAmbientSounds` creates then stops the electric/tank sustainers when disabled, `playZombieActionSounds` captures `TankyTink` and `Bzzt` and suppresses both when disabled, `playWallAndSabotageSounds` captures exactly one real `WallPushing` sound through the `Stumble` job and suppresses it when disabled, `showZombieThoughtBubbles` spawns one `ZombieThought` mote and suppresses it when disabled, and `deadBecomesZombieMessage` now produces or suppresses the player-colonist conversion letter exactly according to the setting. The first run exposed a real `Tools.ConvertToZombie` callback abort when copied pawn age triggered a RimWorld life-stage notification; `Tools.ConvertToZombie` now treats age-copy failure as non-fatal so the rest of conversion setup and the letter still run. Final operation `op_afff80382cda4452abbe197f45d29aef` returned `success=true`, post-run `zombieland/get_status` reported zero spawned zombies and zero non-zero zombie-grid cells, and `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Added 2026-05-31 source/decompiler UI-surface ledger. Existing scenario evidence already covers several UI surfaces that should not be re-tested casually under J: main-menu settings entry, settings/modals, area management, danger warning/message offset, top-right zombie stats, threat forecast tooltip, contamination UI, defense gizmos, and zap/rope float-menu options. RimWorld 1.6 decompiler checks confirmed the relevant UI targets still exist and keep the roles Zombieland patches depend on, including `SelectionDrawer.DrawSelectionOverlays`, `MapInterface.MapInterfaceUpdate`, `MapInterface.MapInterfaceOnGUI_AfterMainTabs`, `GlobalControlsUtility.DoDate`, `MainTabWindow_Menu.RequestedTabSize`, `MainTabWindow_Menu.DoWindowContents`, `MainMenuDrawer.DoMainMenuControls`, `PlaySettings.DoPlaySettingsGlobalControls`, `MouseoverReadout.MouseoverReadoutOnGUI`, `InspectPaneUtility.PaneWidthFor`, `InspectPaneFiller.DoPaneContentsFor`, `Widgets.ThingIcon(Rect, Thing, ...)`, `Dialog_ManageAreas.DoWindowContents`, `AreaManager.CanMakeNewAllowed`, `AreaManager.SortAreas`, `Messages.MessagesDoGUI`, and `Message.Draw`. The de-duplication map is recorded in `coverage/ZL_UI_SURFACE_INDEX.tsv`.

Completion:
- Covered when all patch groups and gameplay extension classes have explicit coverage disposition and the audit can be regenerated without hand-scanning large source files.

## S-Settings-Persistence

Goal: prove that default settings, in-game settings, time keyframes, per-colonist toggles, and modal settings surfaces still work and persist in RimWorld 1.6.

Prior evidence:
- `13f3b89 Restore Zombieland menu texture`
- `dc443f1 Fix map-target debug actions`
- Behavior contracts that toggle settings internally are useful reference, but they do not cover UI or persistence.

Source checks:
- Zombieland: `Source/Main.cs`, `Source/ZombieSettings.cs`, `Source/SettingsDialog.cs`, `Source/Dialog_Settings.cs`, `Source/Dialog_AdvancedSettings.cs`, `Source/Dialog_ApparelBlacklist.cs`, `Source/Dialog_BiomeList.cs`, `Source/Dialog_ThumperSettings.cs`, `Source/ColonistSettings.cs`.
- RimWorld 1.6 decompiler: `RimWorld.Planet.WorldComponent.ExposeData()` exists (`88df42e0e943404ba42043aebd37585c:06015389:M`) and still routes through `BackCompatibility.PostExposeData(this)`; `Verse.ModSettings.ExposeData()` exists (`88df42e0e943404ba42043aebd37585c:0600252B:M`); `RimWorld.Dialog_ModSettings(Mod mod)` exists (`88df42e0e943404ba42043aebd37585c:06011E7F:M`); `Dialog_ModSettings.PreClose()` exists (`88df42e0e943404ba42043aebd37585c:06011E80:M`) and still calls `mod.WriteSettings()`.
- Confirm that `Dialog_Settings` still extends a valid `Page` flow and that its save/close behavior still writes world settings through the same component path.

Fixture:
- `EMPTY.rws` as immutable input.
- Generated save: `ZL_Settings_Persistence_00.rws`.

Setup:
- Load `EMPTY`.
- Ensure at least two colonists exist or spawn deterministic colonists.
- Open mod settings from main menu or use a bridge settings setter plus one UI confirmation path.
- Set representative defaults before world load: spawn mode, attack mode, smash mode, infection chance, eating toggles, avoidance, contamination base factor, visual toggles.

Runtime:
- Start/load map, open in-game Zombieland settings, change the same representative settings.
- Add two settings keyframes with different values and step to lower, middle, and upper tick windows.
- Toggle one colonist on/off for auto-avoid, auto-double-tap, and auto-extract.
- Open and close apparel blacklist, biome blacklist, advanced constants, and thumper settings.
- Save, reload, and re-read settings and colonist config.

Assertions:
- Defaults persist after `WriteSettings` and restart.
- `ZombieSettings.Values` and `ValuesOverTime` persist through save-load.
- `CalculateInterpolation` returns lower, interpolated, and upper values at expected ticks.
- Per-colonist toggles persist and are respected by workgiver or avoidance checks.
- Modal edits update the correct backing settings or constants.
- Log summary contains no settings serialization, missing translation, or UI exceptions.

Artifacts:
- Save files `ZL_Settings_Persistence_00.rws` and `ZL_Settings_Persistence_01_modals.rws`.
- Deduped log summary.
- One settings-window screenshot only if UI layout or labels look suspect.

Completion:
- Covered when all assertions pass through a save-load cycle and the source checks are recorded with the RimWorld 1.6 target member ids.

Current runtime evidence:
- Added 2026-05-30 with `zombieland/settings_state`, a reusable settings tool with `read`, `prepare`, and `verify` modes. From `EMPTY`, `actionMode:prepare openSettingsDialog:true` created three `ZombieSettings.ValuesOverTime` keyframes at 0, 2, and 5 days, set representative fields across spawn mode, attack mode, smash mode, eating, infection, avoidance, visuals, max zombie count, special threat, apparel blacklist, biome blacklist, and contamination base factor, spawned `ZL_Settings_Colonist`, and set its persisted `ColonistConfig` to `autoAvoidZombies=false`, `autoDoubleTap=false`, `autoExtractZombieSerum=true`. The tool also opened the real `Dialog_ModSettings` path and returned `settingsCategory="Zombieland settings"`.
- The fixture was saved as `ZL_Settings_Persistence_00.rws`, reloaded, then `zombieland/settings_state actionMode:verify openSettingsDialog:true` returned `success=true`: the `ZombieSettings` world component existed, all three keyframes persisted with ticks `0`, `120000`, and `300000`, deterministic interpolation samples matched the source `CalculateInterpolation` contract at day 0, day 1, day 2, day 3, and day 6, and the spawned colonist's three toggles persisted through `ColonistSettings`. The live `Values` after reload had advanced slightly from day 0 because save/load elapsed real game ticks, so the verifier intentionally asserts fixed interpolation samples rather than treating current tick-dependent values as constants. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- A `zombieland/get_status` read after cold-loading `ZL_Settings_Persistence_00.rws` proved the `Game.FinalizeInit` postfix applied global def settings from the live save state: `ThingDefOf.MealSurvivalPack` was labeled `Twinkie`, had the Twinkie description, and had `twinkieCachedGraphicApplied=true`; `CustomDefs.Zombie.race.baseHealthScale=0.5419833` matched the non-default live `ZombieSettings.Values.healthFactor=0.541991651`. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Added 2026-07-11 for the French play-data reload regression: two quick-test maps were started in the same process. The Twinkie cached graphic was rebuilt from the current texture at `GlobalTextureAtlasManager.BakeStaticAtlases`, and both `zombieland/get_status` reads reported French language loaders clean, the cached graphic applied, and both relevant textures present. `Player.log` counted two game initializations with zero meal-print atlas errors, null GUI textures, off-thread Twinkie loads, or XML/language errors; the final warning-or-higher log read was empty.
- Modal coverage was added through the same tool with `actionMode:modal` from `ZL_Settings_Persistence_00.rws`. The live run opened and validated real `Dialog_ApparelBlacklist`, `Dialog_BiomeList`, `Dialog_AdvancedSettings`, `Dialog_ThumperSettings`, and the parent `Dialog_ModSettings` path. It proved the apparel dialog sanitizes invalid apparel and preserves a real candidate (`Apparel_PlateArmor`), the biome dialog retains a real biome candidate (`AridShrubland`) and exposes the current biome list, the advanced dialog `PreClose()` writes a changed `Constants.DEBUG_MAX_ZOMBIE_COUNT` to `ZombielandAdvancedSettings.json` and the verifier restores the previous value afterward, and the thumper settings dialog can open against a real spawned `ZombieThumper` with live intensity/interval backing fields. The modal-backed keyframe changes were saved as `ZL_Settings_Persistence_01_modals.rws`, reloaded, and `actionMode:verify` stayed green with day-0 interpolation showing `blacklistedApparel=["Apparel_PlateArmor"]` and `biomesWithoutZombies=["AridShrubland"]`. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Downstream behavior coverage was added with `actionMode:behavior` from `ZL_Settings_Persistence_01_modals.rws`. The verifier reused the persisted `ZL_Settings_Colonist` config (`autoAvoidZombies=false`, `autoDoubleTap=false`, `autoExtractZombieSerum=true`) and proved the corresponding setting gates on real game objects: non-forced and forced serum extraction both accepted a spawned `ZombieCorpse` and produced `ExtractZombieSerum`; non-forced double-tap rejected an infected human corpse while forced double-tap accepted it and produced `DoubleTap`; with `betterZombieAvoidance=true`, active avoid danger (`avoidCost=972`, `shouldAvoid=true`), and a non-forced `Goto` job, the auto-avoid patch did not convert the pawn to `Flee` while the persisted colonist toggle was false. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- The behavior verifier was extended on 2026-05-30 for the four `WorkGiver_Scanner` fanout avoidance patches. After decompiling RimWorld 1.6 `HasJobOnCell`, `JobOnCell`, `HasJobOnThing`, and `JobOnThing`, the live run recorded installed Zombieland transpilers on 12 `HasJobOnCell`, 12 `JobOnCell`, 73 `HasJobOnThing`, and 97 `JobOnThing` concrete workgiver overrides. With a tracking zombie making target cell `(100,100)` dangerous (`avoidCost=972`) and a spawned `Steel` marker in that cell, direct calls into each private patch `ShouldAvoid` helper returned true for non-forced auto-avoid-enabled work and false for forced work or `autoAvoidZombies=false`. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Generated-apparel exclusion coverage was added with `zombieland/settings_state actionMode:apparel-generation`, keeping the check inside the reusable settings fixture tool instead of adding a standalone bridge contract. After decompiling RimWorld 1.6 and 1.4 `PawnApparelGenerator.PossibleApparelSet.PairOverlapsAnything(ThingStuffPair)`, the entry-scene probe recorded the installed Zombieland prefix, verified ordinary `Apparel_PlateArmor`/`Gold` returned non-overlap in an empty generated set, and verified `Apparel_BombVest` was recognized as a Zombieland def and forced to overlap. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Per-colonist gizmo coverage was added with `actionMode:gizmos` from `ZL_Settings_Persistence_01_modals.rws`. The verifier used the saved settings map as the fixture anchor, spawned and destroyed a temporary capable colonist, seeded vanilla prioritized work, and enumerated the real `PriorityWork.GetGizmos()` output. It proved the vanilla clear-priority command still appears (`vanillaPriorityCommandCount=1`), the three Zombieland commands are present and enabled when their global gates and pawn capabilities allow them, the disabled-state descriptions begin as "Ignore zombies", "Only extract zombie serum when ordered", and "Only double tap when ordered", and invoking the three `Command_Action` delegates flips `ColonistConfig` from all false to all true with the enabled descriptions updating to automatic avoid/extract/double-tap text. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Added 2026-05-31 with a generic in-game menu UI pass from `EMPTY.rws`. Static/decompiler evidence confirmed RimWorld 1.6 `MainMenuDrawer.DoMainMenuControls` still has exactly two `OptionListingUtility.DrawOptionListing` calls, `MainTabWindow_Menu.RequestedTabSize` still returns vanilla `450x390`, and `DoWindowContents(Rect)` still delegates to `MainMenuDrawer.DoMainMenuControls`. Runtime `rimworld/open_main_tab mainTabId=Menu` produced a `450x442` menu, matching Zombieland's `addedHeight=52`; `rimworld/get_ui_layout surfaceId=main-tab:Menu` showed the injected `Zombieland` option and `Brrainz` link; clicking the `Zombieland` target opened real `Dialog_ModSettings` for `brrainz.zombieland` with `settingsCategory="Zombieland settings"`. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Added 2026-05-31 with `zombieland/settings_state actionMode:new-game` from `EMPTY.rws`. Static/decompiler evidence confirmed RimWorld 1.6 `Page_SelectScenario.BeginScenarioConfiguration` still creates the new game, assigns the scenario, calls `PreConfigure()`, and links the first config page; `PageUtility.StitchedPages` still assigns `prev`/`next`; and `Game.InitNewGame` still generates/finalizes the starting map before new-game components. Runtime patch-boundary evidence recorded installed Zombieland patch owners for all three rows, proved the scenario-configuration prefix resets sentinel game settings and scroll to the saved day-0 defaults, proved a real patched `PageUtility.StitchedPages` call inserts `Dialog_Settings` into the stitched setup page chain, and proved the `Game.InitNewGame` postfix changes a temporary player colonist hostility response from `Ignore` to `Attack`. `rimbridge/list_logs minimumLevel=warning` returned no entries before and after reloading `EMPTY`.
- Added 2026-07-12 to the same settings scenario: `actionMode:new-game` operation `op_5f0011f54cbe4dd7bb7568f9bdfa5702` passed the eight-shape malformed-default matrix through the installed scenario prefix. The disposable `ZL_Settings_CorruptTimeline_00` sequence then persisted valid/null/null-values/valid world keyframes and reloaded them through the real Scribe `PostLoadInit` path; verification `op_5ca8fbfdde92466198626151a6c2481a` retained ticks `0/120000`, removed both invalid slots, and interpolated day 1 to `threatScale=1.5` and `maximumNumberOfZombies=300`. One expected Zombieland repair warning was present, no error entries were present, the complete log summary was empty, and `EMPTY` was reloaded paused afterward.
- Added 2026-05-31 with `zombieland/settings_state actionMode:defaults-write-test` at the entry scene. The tool wrote a deterministic defaults fixture through `ZombielandMod.WriteSettings()` (`maximumNumberOfZombies=137`, `threatScale=0.37`, `zombieBiteInfectionChance=0.12345`, `attackMode=Everything`, one day-0 keyframe, and marker apparel/biome lists), the XML contained those values, RimWorld was restarted through GABS, and a fresh `actionMode:read` proved `ZombieSettingsDefaults.group`, `groupOverTime`, and default day 0/1/2/3/6 interpolation samples reloaded from disk. The backed-up user XML was restored byte-identically, RimWorld was restarted again, and the original defaults (`maximumNumberOfZombies=2000`, `zombieBiteInfectionChance=0.5027528`) were back. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Added 2026-05-31 by extending `zombieland/settings_state actionMode:modal` for non-destructive save-then-uninstall UI coverage. Static/decompiler evidence confirmed RimWorld 1.6 `Dialog_SaveFileList` still derives from `Dialog_FileList`, `Dialog_FileList.DoFileInteraction(string)` remains the abstract hook, and the vanilla save dialog still exposes typed-save input. Runtime from `ZL_Settings_Persistence_01_modals.rws` opened the real settings dialog, verified `Dialog_SaveThenUninstall` overrides `DoFileInteraction(string)`, has `ShouldDoTypeInField=true`, uses the translated overwrite button and `bottomAreaHeight=85`, opens as a typed save dialog, and is reached through a destructive confirmation message with a live confirm action. The test did not execute `ZombieRemover.RemoveZombieland`; destructive copy-save behavior remains owned by `S-Uninstall-Hygiene`. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Added 2026-05-31 with a generic mouse-driven advanced-settings smoke from `ZL_Settings_Persistence_01_modals.rws`. Runtime UI layout/click primitives opened `Dialog_ModSettings`, scrolled to `Advanced Settings`, opened `ZombieLand.Dialog_AdvancedSettings`, toggled the visible `SHOW_PHEROMONE_GRID` checkbox false to true, verified `isChecked=true` in the next layout capture, toggled it back to false, verified restoration, and closed both dialogs. This intentionally covered one real UI row edit through the player-visible path without expanding the bridge with a one-off contract. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Added 2026-06-01 with a static/decompiler pass over the advanced settings constants support path. Source inspection confirmed `Constants.Current/Load/Save/Apply` owns the `ConstantAttribute` default dictionary, JSON merge at `GenFilePaths.ConfigFolderPath/ZombielandAdvancedSettings.json`, version upgrade rewrite, Newtonsoft scalar and array coercion, and stale-key ignore behavior; `Dialog_AdvancedSettings.PreClose()` writes the current static-field state after the UI mutates bool/int/float/int-array/float-array controls. RimWorld 1.6 decompiler evidence confirmed `GenFilePaths.ConfigFolderPath`, `Window.PreClose()`, and `WindowStack.Add/TryRemove/IsOpen` still exist, with only unrelated 1.4-to-1.6 additions or optional-parameter drift. The live config file exists and shows `DEBUG_MAX_ZOMBIE_COUNT=-1` restored after earlier modal tests. This closes the source backstop for `SRC.Constants.AdvancedSettings`; future work should add a runtime pass only when constants migration or the advanced settings dialog changes.
- Added 2026-05-31 with a generic mouse-driven biome-list smoke from `ZL_Settings_Persistence_01_modals.rws`. Runtime UI layout/click primitives opened `Dialog_ModSettings`, clicked the visible `Biomes` button, opened `ZombieLand.Dialog_BiomeList`, toggled `Arid shrubland` checked to unchecked to checked again, verified the states in follow-up layout captures, closed both dialogs, and a final `zombieland/settings_state actionMode:read` confirmed `AridShrubland` was restored in live values and the persisted keyframe sample. `rimbridge/list_logs minimumLevel=warning` returned no entries, and no RimWorld save was written.
- Added 2026-05-31 with a generic mouse-driven apparel-list modal button smoke from `EMPTY.rws`. Runtime UI layout/click primitives opened `Dialog_ModSettings`, scrolled to `Loot from zombies`, opened `ZombieLand.Dialog_ApparelBlacklist`, clicked `Select All`, and readback proved 25 blacklisted apparel defNames in live values, keyframe 0, and day 0/1/2/3/6 interpolation samples. The same modal path clicked `Deselect All`, final readback proved the list was empty again, `EMPTY.rws` was reloaded visual-ready/paused, and `rimbridge/list_logs minimumLevel=warning` returned no entries. No RimWorld save was written.
- Added 2026-05-31 with a mouse-driven main-settings slider smoke from `EMPTY.rws`. Static/decompiler evidence confirmed RimWorld 1.6 `Widgets.HorizontalSlider` uses mouse-down plus drag state to calculate the new value. Runtime opened the real `Dialog_ModSettings`, reset the settings scroll to the visible `Threat scale` row, used real native pointer drag input because the bridge has click targets but no drag gesture, and `zombieland/settings_state actionMode:read` proved `threatScale=5` in live values, keyframe 0, and day 0/1/2/3/6 interpolation samples. `EMPTY.rws` was reloaded visual-ready/paused without saving, and final readback proved `threatScale=1` was restored.
- Added 2026-05-31 with a source/decompiler disposition for remaining settings text/numeric fields. Source inspection found the main settings numeric text fields route through `DialogExtensions.Dialog_Integer` into RimWorld 1.6 `Widgets.TextFieldNumeric`, while advanced-settings text fields parse with `int.TryParse`/`float.TryParse` and already have modal backing-state evidence. Decompiler evidence confirmed `Widgets.TextFieldNumeric`, `IsPartiallyOrFullyTypedNumber`, and `ResolveParseNow` still implement vanilla partial/full numeric entry and clamped parse behavior. Since the underlying numeric values already have save-load/defaults/semantic readback coverage, native keyboard text-entry is not a distinct required runtime path unless this wrapper changes or a reusable bridge text-entry primitive is added.
- Added 2026-05-31 with a generic mouse-driven main-settings radio smoke from the live in-game `Dialog_ModSettings`. Runtime UI layout/click primitives clicked the visible `When do zombies appear?` row from `All the time` to `When it is dark`; `zombieland/settings_state actionMode:read` proved `spawnWhenType=WhenDark` in `values`, keyframe 0, and day 0/1/2/3/6 interpolation samples. The same UI path clicked `All the time` again, and a final read proved `spawnWhenType=AllTheTime` was restored. The settings dialog was closed, `rimbridge/list_logs minimumLevel=warning` returned no entries, and no RimWorld save was written.
- Added 2026-05-31 with a generic mouse-driven new-colony wizard smoke. Static/source evidence checked `Dialog_Settings`, `SettingsDialog`, `Page_SelectScenario_BeginScenarioConfiguration_Patch`, `PageUtility_StitchedPages_Patch`, and `Game_InitNewGame_Patch`. Runtime from the entry scene used the real main-menu `New colony` button and generic UI primitives for the setup pages; the observed player path was `Page_SelectScenario` Next to `Page_SelectStoryteller`, then storyteller Next to `ZombieLand.Dialog_Settings`, correcting the earlier shorthand that placed Zombieland settings between scenario and storyteller. On the wizard settings page, the visible `When do zombies appear?` row was clicked from `All the time` to `When it is dark`; `zombieland/settings_state actionMode:read` proved `settingsCategory="Zombieland defaults"`, `hasGameSettings=false`, and `spawnWhenType=WhenDark` in active defaults, default keyframe 0, and default day 0/1/2/3/6 interpolation samples. The same wizard UI path restored `All the time`, final readback proved `spawnWhenType=AllTheTime`, `EMPTY.rws` was reloaded visual-ready/paused without creating a colony save, and `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Added 2026-06-01 with a generic mouse-driven keyframe Duplicate/Delete smoke from `ZL_CE_EMPTY_00` with Combat Extended active. Source inspection of `DialogTimeHeader.Draw()` and RimWorld 1.6 decompiler evidence for `Widgets.ButtonText`/`ButtonTextWorker` confirmed the controls are ordinary button targets. Runtime opened real `Dialog_ModSettings`, captured the visible `Duplicate` button, clicked it, and `zombieland/settings_state actionMode:read` proved the keyframe list changed from one entry to two (`0` and `1D` / `60000` ticks). The follow-up layout exposed `Delete`; clicking it returned the live keyframes to one entry. `rimbridge/list_logs minimumLevel=warning` returned no entries, and no RimWorld save was written.
- Added 2026-05-31 with a generic downstream settings overlay smoke from `EMPTY.rws`. Static/source evidence checked `SettingsDialog`, `ZombieSettings.showHealthBar`, `Zombie.DrawGUIOverlay()`, and RimWorld 1.6 `Pawn.DrawGUIOverlay()`/`Thing.DrawGUIOverlay()` overlay dispatch. Runtime spawned one normal zombie at `(100,100)`, damaged it through the generic pawn-targeted debug action `Actions\Apply damage...\Arrow`, targeted it with the virtual hover primitive, and used the visible `Zombies health bar` checkbox in `Dialog_ModSettings`. With the checkbox on, `/Users/ap/Library/Application Support/RimWorld/Screenshots/zl_settings_show_health_bar_on_damaged_full.png` showed the blue bar under the hovered zombie; with the same checkbox off, `/Users/ap/Library/Application Support/RimWorld/Screenshots/zl_settings_show_health_bar_off_damaged_full.png` removed the bar. Image comparison found 3000 changed pixels in bbox `(1805,1149)-(1924,1173)`. The checkbox was restored, the hover target cleared, `EMPTY.rws` was reloaded visual-ready/paused, and no RimWorld save was written.
- Added 2026-05-31 with a manual mouse-hover screenshot after the user moved the real cursor to the colonist settings square. Screenshot `/Users/ap/Library/Application Support/RimWorld/Screenshots/zl_manual_mouse_square_ready.png` shows the highlighted square and the player-facing readout `Daze: ZL_Settings_Colonist`; the capture did not advance ticks or perform an input action, and warning-or-higher logs were empty.
- Added 2026-06-03 with `zombieland/awareness_cues_contract` from `EMPTY.rws`. This covers the new Awareness Cues setting section by proving default-on behavior plus enabled/disabled runtime gates for zombie incident letters, the incident siren, special-zombie electric/tank ambient sustainers, action sounds, wall-push/sabotage sounds through a real `Stumble` job, zombie thought bubbles, and the existing `deadBecomesZombieMessage` conversion letter setting. It also found and fixed a `Tools.ConvertToZombie` age-copy callback abort so player-colonist conversion letters are no longer skipped by a life-stage notification exception. Final status after the contract had zero spawned zombies, zero non-zero zombie-grid cells, and no warning-or-higher logs.
- Added 2026-06-03 localization coverage for the same Awareness Cues section after manual UI showed raw new keys. The active RimWorld 1.6 path was the versioned `1.6/Languages/.../Keyed/*.xml` copy, not only root `Languages/...`; both root and `1.6` files now contain the seven label keys and seven `_Help` keys for English, ChineseSimplified, French, German, Russian, and Turkish, while `1.4/Languages` was not changed. XML parse, source key-coverage, build/deploy, deployed-folder key-coverage, restart, runtime `zombieland/awareness_cues_translation_state`, and warning-log checks passed. Runtime operation `op_4032ac9dfb2d48c88d81e073e87fca59` reported `CanTranslate=true` for all seven labels and all seven help keys in loaded English.
- The `S-Settings-Persistence` baseline is covered. It proves default settings restart persistence, in-game world settings, keyframe interpolation plus mouse-driven keyframe Duplicate/Delete controls, colonist-toggle serialization, new-game setup patch boundaries, mouse-driven new-colony wizard settings, generated-apparel exclusion of Zombieland apparel defs, `Game.FinalizeInit` def application for Twinkie replacement and non-default zombie health scale, real mod settings dialog construction from both bridge and in-game menu entry, modal construction/backing-state behavior for apparel/biome/advanced/thumper settings, mouse-driven main settings radio and slider edits, source/decompiler-backed text/numeric field disposition, mouse-driven advanced-settings checkbox, biome-list checkbox, apparel-list Select All/Deselect All button edit, the visible `Show zombie statistics` checkbox driving the live `Alert_DaysUntilSpawning` surface, the visible `Zombies health bar` checkbox driving the hovered zombie overlay, Awareness Cues downstream gates for letters/sounds/sustainers/thought bubbles, manual colonist-settings hover visibility, non-destructive save-then-uninstall UI entry, save-load for the modal-backed keyframe values, downstream setting-gate behavior for serum extraction, double-tap, and auto-avoid false, broad `WorkGiver_Scanner` fanout avoidance predicates, and direct `PriorityWork.GetGizmos()` command presence/actions for the three per-colonist toggles. A 2026-06-01 source/decompiler reread confirmed remaining visible controls share the same already-evidenced radio/checkbox/slider/numeric/button helper paths, so future settings work should be named downstream UI-edit/effect pairs or changed settings UI code rather than another broad baseline rerun.

## S-Anomaly-Music-Replacement

Goal: prove that each Anomaly music sequence reaches the ordinary RimWorld playback command and that the Zombieland music-share setting substitutes the exact numbered replacement without changing the Anomaly transition that selected it.

Source checks:
- RimWorld 1.6 decompiler target `MusicManagerPlay.PlaySong(SongDef, bool, bool)` is `967ddb80559449f0a776dafa26a855d1:0600E63F:M`; Anomaly sequence selection converges on this method through `PlayNextSongInSequence()` and `MusicSequenceWorker.SelectSong()`.
- Anomaly `Songs_Sequences.xml` defines `HorrorRelax` at priority 100, `HorrorTension` at 150, and `HorrorCombat` at 200. The fixture satisfies their real predicates with a `Noctolith`, `UnnaturalDarkness`, and two hostile awake `Metalhorror` pawns whose combined combat power produces natural `DangerWatcher.High`.

Fixture and automation:
- Disposable save `ZL_Anomaly_Music_100.rws` under the Steam `rimbridge-test` data root.
- Run `scripts/anomaly-music-replacement-scenario.lua` with either the exact five-package focused loadout or the exact nine-package Core-plus-all-official-DLC loadout. Both shapes include Harmony, RimBridgeServer, Anomaly, and Zombieland and reject unrelated optional mods. It starts a fresh paused debug colony, runs paired deterministic 0% controls and 100% replacements, cleans up, leaves the Zombieland share at 100%, and saves the fixture only after every postcondition passes.
- A scenario-scoped Harmony observer prefixes `PlaySong` before Zombieland and postfixes after RimWorld. It records the source request, final `SongDef` argument, `CurrentSong`, and Unity `AudioSource.clip`, then unpatches itself in cleanup.

Runtime evidence, 2026-08-01, all-official-DLC root operation `op_c431cbd690dc4a5e95fad4620ab9cda6`:

| Transition | Real predicate fixture | 0% Anomaly request | 100% song actually played |
| --- | --- | --- | --- |
| `HorrorRelax` | spawned `Noctolith`, no danger | `Shamblers_Blues` | `03 Zombieland – Shambler` |
| `HorrorTension` | active `UnnaturalDarkness`, no danger | `Have_They_Come_For_Us` | `05 Zombieland – Have They` |
| `HorrorCombat` | two hostile awake `Metalhorror` pawns, `DangerWatcher.High` | `They_See_You` | `07 Zombieland – They See` |

Each pass made exactly one observed `PlaySong` call. All three 100% passes preserved the 0% source request, rewrote its final argument to the expected 1:1 replacement, and matched `CurrentSong` plus the active Unity clip. The same run validated all ten numbered mappings at disabled, 0%, 50% boundary, and 100%. Every disabled 50% mapping also preserved the next shared `Verse.Rand` value, proving the disabled feature consumes no replacement roll. The loadout contract accepted both exact supported shapes and rejected an injected unexpected package.

The result did not become successful until its typed cleanup and final-settings records were both successful: the observer, condition, building, and entities were removed, danger returned to `None`, and music ended enabled at 100%. Its pure finalization-gate contract separately rejected simulated cleanup, final-settings, scenario, and unsupported-loadout failures. Root-correlated warning query `op_f883960316754f0384cd800e86567be3` was empty. Reload operation `op_e7d11f20386c45bfb96a5c53e15673fe` found the save compatible with all nine recorded packages, and settings read `op_9001f6c9692c4cd597616583182beada` confirmed `playZombielandMusic=true` and `zombielandMusicShare=100` after reload.

Completion:
- Covered for all three Anomaly transition tiers, exact 1:1 substitution at 100%, the complete ten-song percentage contract, disabled RNG neutrality, exact minimal and all-official-DLC loadouts, actual clip playback, post-cleanup/final-settings success gating, and save-load persistence.

## S-Core-Horde-Loop

Goal: prove the main zombie loop over real time: spawn, wander, sense, track, rage, attack/eat/smash, react to fire, die, leave corpse/filth, and survive save-load.

Prior evidence:
- `3981f82 Fix zombie movement with async path requests`
- `f43506a Add path avoidance recalculation smoke`
- `36915af Restore avoid-grid path costs`
- `2bce190 Add zombie door close smoke`
- `635be71 Restore zombie blood filth contract`
- `8d4e084 Cover zombie fire attachment state`
- `7ccd5e4 Verify zombie fire rain vulnerability`
- `1233bb1 Extend burn-longer fire hooks`
- `c6f2b87 Optimize zombie tick sampling`
- `4e90c65 Trim zombie ticking hot path`
- Live `EMPTY.rws` fire/tar sub-contract pass: rain vulnerability stayed vanilla for human fire and disabled-setting zombie fires but was intermittently suppressed for normal/spitter/symbiant zombies with `zombiesBurnLonger=true`; sampled `Fire.DoFireDamage` injury was lower for normal/spitter/symbiant zombies; `zombieland/zombie_fire_attachment_state_contract` confirmed installed `CompAttachBase.AddAttachment` and `RemoveAttachment` postfixes and proved `isOnFire` stays true until the last attached fire is removed; burning `TarSlime` spread fire to adjacent tar and emitted `TarSmoke`; `FireWatcher.UpdateObservations` counted map plus human fire while excluding attached normal/spitter/symbiant zombie fires. The 2026-06-06 source-aware fire survival pass proved null/natural and wild-animal fire give ordinary zombies the 4x survival boost, player/raid/player-owned-animal/plain-zombie sources do not, and boosted-zombie propagation does. Its live operation `op_885476d352a34171b4612871107caa8e` returned `success=true`, with real injury lowered `1 -> 0.5` and synthetic fire-damage helper output `16 -> 4`; the existing fire damage and attachment contracts also reran green. The only warning-or-higher logs were pre-contract CameraPlus residue in `EMPTY.rws`; after log sequence `12`, the warning-or-higher log check was clean.
- Live `EMPTY.rws` tar-movement/blood sub-contract pass: `zombieland/tar_slime_move_cost_contract` confirmed the installed `Pawn_PathFollower.CostToMoveIntoCell(Pawn, IntVec3)` postfix and proved tar-cell costs match the difficulty-1 formulas for human (`91.2000046`), normal zombie (`122.8`), and spitter (`122.8`), with clear-cell costs differing from tar-cell costs. `zombieland/zombie_blood_filth_contract` confirmed installed `Pawn_HealthTracker.DropBloodFilth()` patches and proved `zombiesDropBlood=true` lets human/normal zombie/spitter drop one `Filth_Blood` while tanky drops none, and `zombiesDropBlood=false` still lets human blood drop while blocking normal zombie/spitter blood. Both fixtures now clean up spawned pawns/tar/filth before returning. The warning-or-higher log check was clean.
- Live `EMPTY.rws` records/clamor/mothball sub-contract pass: human records mutated normally while normal zombie records stayed hidden and unchanged, `WorldPawns.ShouldMothball` returned true for the human control but false for a normal zombie, and human clamor reached a listener while normal/spitter/symbiant zombie clamor sources produced no listener effects. The warning-or-higher log check was clean.

Source checks:
- Zombieland: `Source/Zombie.cs`, `Source/ZombieGenerator.cs`, `Source/ZombieStateHandler.cs`, `Source/JobDriver_Stumble.cs`, `Source/JobGiver.cs`, `Source/PheromoneGrid.cs`, `Source/TickManager.cs`, `Source/ZombieWanderer.cs`, `Source/ZombieCorpse.cs`.
- RimWorld 1.6 decompiler: `Pawn_PathFollower`, `JobDriver`, `Thing.TakeDamage`, `Fire`, `Building_Door`, and any Harmony targets touched by the horde path.

Fixture:
- Generated save: `ZL_Core_HordeLoop_base.rws`.
- Map layout: one room, one corridor, one door, one weak wall segment, one corpse, one downed pawn, one drafted colonist, one fire source.

Setup:
- Set zombie count limits low enough for deterministic assertions.
- Spawn mixed normal zombies at known distances.
- Create smell/hearing stimuli: moving pawn, weapon fire or equivalent sound event, corpse/downed pawn.

Runtime:
- Step real game ticks in phases: idle, stimulus, tracking, door/wall contact, eating, fire, death/corpse.
- Save during an active path request or active job if feasible, reload, continue stepping.

Assertions:
- Zombies do not drop destination because an async path request has no current path yet.
- Pheromone or tracking state changes after stimulus and fades later.
- Door, wall, corpse/downed-pawn, and fire behavior match settings.
- Corpse/desiccation/blood filth behavior appears according to settings.
- Low-consciousness zombies remain awake-capable while ordinary low-consciousness pawns follow vanilla sleep/down behavior.
- Non-zombie pawn downing and killing clear kill-circle pheromone cells, while spitter/symbiant downing attempts are suppressed.
- Reloaded zombies keep valid draw/jobs/tick registration.
- Log summary contains no pathing, render-tree, job-driver, or stale-map exceptions.

Artifacts:
- Save file before and after active loop.
- Save file: `ZL_Core_HordeLoop_grid_reset.rws`.
- Compact bridge state dump.
- One screenshot/cell-rect if visual clustering or corpse state is ambiguous.
- Optional performance sample for tick time, but the dense perf scenario remains separate.

Current runtime evidence:
- Current adaptive-ticking evidence, 2026-07-11: the bounded FIFO scheduler and the `zombieland/zombie_ticking_budget_contract` preserve a remote zombie's displayed `MoveSpeed` at `0.322 -> 0.322` while its real `Pawn_PathFollower.CostToPayThisTick` payment changes `1 -> 4.75` at remote rate `0.210526317`. `zombieland/zombie_ticking_feedback_contract` validates native-demand normalization/capping, completion feedback, global-only throttling, simulation-only emergency, hysteresis recovery, and the reflected live `TickManagerUpdate` boundary. `zombieland/zombie_ticking_scheduler_contract` final operation `op_bf8c38ac6c00478cb17c812875cce330` gives each of 25 continuously remote zombies exactly two services in 240 emergency preparations (maximum silence 120), never defers priority work, discards five stale churn entries, keeps the queue at 25, and consumes no RimWorld RNG.
- Added 2026-05-30 with `zombieland/ambient_temperature_contract` from `EMPTY.rws`. The live run spawned a human, human corpse, normal zombie, zombie corpse, spitter, and symbiant on an outdoor map with local cell temperature `4.5` degrees and outdoor temperature about `4.67`. Ordinary human and human corpse `AmbientTemperature` matched the cell temperature (`4.5`), while `Zombie`, `ZombieCorpse`, `ZombieSpitter`, and `ZombieSymbiant` all returned the forced normal `21` degrees from `Thing_AmbientTemperature_Patch`. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Added 2026-05-31 with existing faction contracts from `EMPTY.rws`. `zombieland/zombie_faction_world_contract` proved the new-world faction-generation prefix is installed and mutates synthetic generation lists correctly: empty lists receive `Zombies`, prefilled lists do not duplicate it, and null lists are accepted. The same contract proved there is exactly one loaded `Zombies` faction, it is hidden and not temporary, all 11 non-zombie factions including the player are mutually hostile with it, and the goodwill patch blocks zombie-faction goodwill changes while ordinary faction goodwill still changes. `zombieland/zombie_faction_pawn_generation_contract` then generated a normal zombie through the Zombies faction and confirmed symbiant/spitter generation stays unspawned. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Added 2026-05-30 with `zombieland/zombie_damage_log_association_suppression` from `EMPTY.rws`. The live run applied damage to an ordinary human, normal zombie, spitter, and symbiant. The human control produced one result hediff and one new combat-log text association, while all three Zombieland pawn classes produced result hediffs with no combat-log text association. The first attempt exposed a fixture cleanup bug that left damaged spawned pawns behind; the bridge tool now destroys those pawns before returning, and the rerun completed with no warning-or-higher log entries.
- Added 2026-05-30 with `zombieland/zombie_ticking_budget_contract` from `EMPTY.rws`. The live run spawned 20 normal zombies, forced `ZombieTicker.PercentTicking=0.25`, and proved the Zombieland map tick manager selected a five-zombie live subset and counted exactly five production ticks. Its former 4x `MoveSpeed` compensation was intentionally replaced on 2026-07-11 by bounded path-payment compensation; current displayed `MoveSpeed` is sampling-rate invariant. The same contract anesthetized a sample zombie and human control to `Consciousness=0.01`: the zombie still returned `PawnCapacitiesHandler.CanBeAwake=true`, while the human control returned false. Its `MakeDowned` probe set `Constants.KILL_CIRCLE_RADIUS_MULTIPLIER=2`, seeded 197 pheromone timestamps around a human, invoked `Pawn_HealthTracker.MakeDowned`, and proved all seeded timestamps were cleared while the human became health-downed. The same reflected invocation left both `ZombieSpitter` and `ZombieSymbiant` not health-downed. The expanded `Pawn.Kill` probe confirmed the installed `PawnComponentsUtility.RemoveComponentsOnKilled` postfix, killed a colonist through the real 1.6 kill path, observed `pawn.Map == null` and `pawn.MapHeld != null` after kill, verified carry/needs/jobs/stances removal, and proved all 197 seeded pheromone timestamps were cleared through the kill-time map fallback. The expanded pass also proved `TickManager.NothingHappeningInGame` returned true with no pending zombie spawns and false with `ZombieGenerator.ZombiesSpawning=1`, a human `Bullet_BoltActionRifle` launch changed 341 pheromone cells while a `ZombieSpitter` launcher changed none, and direct zombie `WillCollideWithPawnAt`/`PawnCollisionPosOffsetFor` probes returned false/zero. The 2026-05-31 `Pawn_PathFollower.StartPath` pass found the old posture rewrite anchored on the wrong 1.6 zero constant, fixed it to the `Pawn_JobTracker.posture` store, and proved a health-downed pathing zombie remains in `LayingMask`. The 2026-05-31 grid-repopulation extension used the real `JobDriver_Stumble.ExecuteMove` path from a post-load-like state with `lastGotoPosition` invalid and proved the destination zombie-count grid changed `0 -> 1`, the source cell stayed `0`, `lastGotoPosition` became the destination, and the pather targeted that same destination. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Added 2026-05-31 with direct `Thing.Position` movement branch evidence from `EMPTY.rws`. Static/decompiler comparison confirmed the RimWorld 1.6 position setter still has the same same-cell guard, spawned deregistration/reregistration, gas/grid/region notifications, and reachability-cache clear as RimWorld 1.4. Existing movement contracts covered tanky pheromone trails, dark-slimer `TarSlime`, and StickyGoo toxic buildup. The follow-up `zombieland/position_side_effects_contract` then covered the remaining base-mod prefix branches in one row-level contract: installed patch owner on `Verse.Thing::set_Position(Verse.IntVec3 value)`, spitter trail `changedCount=280`, ordinary zombie clogging timestamp `320038000 -> 320008000`, colonist avoid-danger contact timestamp `5038`, and `OnlyColonists` attraction gating with colonist trail `changedCount=49` versus hostile non-colonist and manhunter non-colonist `changedCount=0`. A later active Vehicle Framework pass reran the same contract from `ZL_CE_EMPTY_00` with the local `ZLTestVehicle` fixture active; it spawned concrete `ZLTestScoutVehicle`, moved it through `Thing.Position`, and proved `VehicleTools.BumpTimestamps` with `vehicleHook.success=true`, `moveSpeed=10`, `changedCount=709`, and `timestampAfter=320039000`. `rimbridge/list_logs minimumLevel=warning` returned no entries before and after reloading `EMPTY` and after the Vehicle Framework passes.
- Added 2026-05-30 with `zombieland/zombie_ticking_feedback_contract` from `EMPTY.rws`. The live run spawned 20 normal zombies, reflected `Verse_TickManager_TickManagerUpdate_Patch.Prefix`, and proved the adaptive accounting reset `ZombieTicker.zombiesTicked`, sized `maxTicking=40` and `currentTicking=10` at `PercentTicking=0.25`. It then ran one real RimWorld `TickManager.DoSingleTick()` through the Harmony postfix and proved the per-tick production path selected five live zombies and counted five zombie ticks. Finally, the reflected postfix fed back `100/400` as slot `0.25`, advanced the feedback index to `1`, and produced averaged `PercentTicking=0.90625`. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Added 2026-05-31 by refreshing the focused `BRIDGE.Eating` contracts from `EMPTY.rws` after source inspection of `JobDriver_Stumble.TickAction -> ZombieStateHandler.Eat -> CanIngest -> EatDelay -> EatBodyPart`. `zombieland/zombie_eats_corpse_contract` proved disabled corpse eating keeps `eatTarget=null` with no missing parts, enabled corpse eating removes body parts from human and animal corpses after the source-derived fat-zombie delay (`300` ticks, hit at tick `301`), and non-flesh mech corpses are rejected. `zombieland/zombie_eats_downed_pawn_contract` proved the same disabled/enabled split for live downed pawns, removed body parts from downed human and animal targets at tick `301`, and rejected a downed non-flesh mech pawn. `EMPTY.rws` was reloaded visual-ready/paused afterward, and warning-or-higher logs remained empty.
- Added 2026-05-31 by expanding `zombieland/zombie_clamor_suppression` after a source/decompiler pass over `GenClamor.DoClamor`, `Pawn.HearClamor`, and `Pawn_MindState.Notify_ClamorImpact`. The pass found that RimWorld 1.6 human impact clamors can mutate hearer `canSleepTick`, while the Zombieland prefix only checked the clamor source. `Pawn_HearClamor_Patch` now suppresses the call when either source or hearer is a Zombieland pawn. Runtime from `EMPTY.rws` still proved the original fanout behavior, with a human source reaching the human listener once and normal/spitter/symbiant zombie sources producing zero listener effects. The new direct-hearer assertion used a human impact clamor: the human listener advanced `canSleepTick 4038 -> 6038`, while the normal zombie hearer stayed at `4038`. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Added 2026-05-30 with focused avoidance/door contracts from `EMPTY.rws`, extended 2026-05-31 after static/decompiler passes over the async path-request lifecycle and `Pawn_PathFollower.StartPath`. `zombieland/avoid_grid_costs_route_new_path` proved the synchronous 1.6 `PathFinder.FindPathNow` route used Zombieland avoid costs: the baseline path crossed `11` avoid cells with avoid cost `7940`, while the avoid-enabled path crossed `0` avoid cells with avoid cost `0`. The same contract now creates real `Verse.PathFinder.CreateRequest(...)` requests while the avoid grid is active: no-customizer requests receive `ZombieAvoidGridPathCustomizer`, the native offset grid is created before and disposed after both real `PathRequest.Resolve(null)` and real `PathRequest.Dispose()`, and a caller-supplied probe customizer remains preserved and undisposed by the Zombieland patch. Its `StartPath` probe confirms the installed patch owner, proves the destroyed-target helper suppresses only destroyed-zombie targets, calls real `StartPath` with a destroyed zombie target without warning-or-higher logs, and proves a health-downed zombie with public `Pawn.Downed=false` starts a real path with posture `LayingMask`. `zombieland/avoid_grid_interrupts_existing_path` started a real non-forced `Goto`, let a path form with `18` nodes, then spawned a tracking zombie beside the fourth lookahead cell; that cell had avoid cost `972` and `Pawn_PathFollower.NeedNewPath()` flipped from false to true. `zombieland/avoid_grid_blocks_door_and_danger` proved an avoid-danger door cell returned `Deadly` and blocked normal `PawnCanOpen`, while drafted and player-forced cases still opened. `zombieland/zombie_manual_door_close_ignored` proved a normal zombie cannot schedule manual close (`ticksUntilClose` stayed `12345`) but a colonist can (`110`). `zombieland/albino_does_not_hold_door_open` proved normal zombies still hold doors open (`10 -> 109`) while albinos do not (`10 -> 9`). `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Added 2026-05-30 with generic `zombieland/spawn_zombie`, `rimworld/save_game`, `rimworld/load_game_ready`, and `zombieland/get_status` from `EMPTY.rws`. The fixture spawned three ordinary zombies and deliberately primed stale zombie-count grid values `2`, `3`, and `4` at their cells, giving `zombieGrid.nonZeroCells=3`, `totalZombieCount=9`, `maxZombieCount=4`, and all three live ordinary zombies with positive grid counts before save. The fixture was saved as `ZL_Core_HordeLoop_grid_reset.rws`; after reload, the same three zombies were loaded and cached, but `zombieGrid.nonZeroCells=0`, `totalZombieCount=0`, `maxZombieCount=0`, and all three live ordinary zombies had no stale grid count. Combined with the 2026-05-31 movement-grid subprobe in `zombieland/zombie_ticking_budget_contract`, this covers both the load/finalize reset boundary and first movement-driven grid repopulation. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Added 2026-05-31 with a generic mixed horde-loop fixture from `EMPTY.rws`; no scenario-specific endpoint was added. The static/source pass checked `JobDriver_Stumble.TickAction`, `ValidDestination`, `Track`, `PossibleMoves`, `Wander`, `ExecuteMove`, `Zombie.CustomTick`, `TickManager.ZombieTicking`, and the RimWorld 1.6 `Pawn_PathFollower.StartPath`/`PatherTick` lifecycle. The first failed attempt showed that paused `rimworld/step_game_ticks` is the wrong primitive for this loop because it mirrors Dev TickOnce and bypasses the normal `TickManagerUpdate` prefix that prepares `ZombieTicker.managers`; `rimworld/set_time_speed Normal` is required for this over-time evidence. The clean fixture spawned one disabled-work colonist bait at `(100,100)` and six zombies through generic tools: two normal, one tanky operator, one miner, one albino, and one dark slimer. After 12 seconds at normal speed, the bait colonist was `FleeAndCower` at `(125,67)`, all six zombies were alive with active jobs, five were pathing via `Stumble`, one normal had entered `Tracking`, the albino had entered `Sabotage`, and five destination cells had live zombie-count grid values. The fixture was saved as `ZL_Core_HordeLoop_over_time_00.rws`, reloaded, and advanced another 10 seconds at normal speed. Post-reload, the bait colonist was still fleeing at `(125,37)`, all six zombies were alive and cached, three zombies were `Tracking`, all six were either moving/pathing or sabotaging, and five destination cells had repopulated grid counts. Warning-or-higher logs were empty before save, after reload, and after post-reload runtime. Visual artifacts: `zl_core_hordeloop_over_time_prereload__cell_rect.png` and `zl_core_hordeloop_over_time_postreload__cell_rect.png`.
- Added 2026-05-31 with a generic pheromone/senses fixture from `EMPTY.rws`; no scenario-specific endpoint was added. The static/decompiler pass checked `PheromoneGrid`, `Tools.PheromoneFadeoff`, `Tools.RadiusForPawn`, the `Thing.Position` pheromone prefix, `JobDriver_Stumble.TickAction`, `ZombieStateHandler.Track`, `ExecuteMove`, RimWorld 1.6 `Thing.Position`, `Pawn_PathFollower.PatherTick`, `JobDriver.DriverTick`, and `Pawn.HearClamor`. Two reusable helpers were added: `zombieland/pheromone_state` to read/clear/set bounded timestamp areas, and `zombieland/place_wall_line` to create simple wall fixtures for pathing/scent scenarios. The live fixture placed a 25-cell player wood-wall line at x=101, spawned two normal zombies on the west side at `(99,100)` and `(99,112)`, set a fresh radius-4 pheromone source behind the wall at `(102,100)`, and set a stale radius-4 source behind the wall at `(102,112)` with age `10820` game ticks against `fadeoffGameTicks=10800`. The fixture was saved as `ZL_Core_Pheromone_Senses_00.rws`.
- After roughly six seconds at normal speed, the fresh source still had `freshCells=49`, including non-walkable wall cells and walkable cells on both sides of the wall, proving the scent field itself is not line-of-sight gated. The zombie in that lane moved from `(99,100)` to `(100,99)`, was actively pathing to `(100,98)`, and entered `state=Tracking`. The stale source had `freshCells=0` with timestamp ages above the fadeoff threshold, and its zombie remained `state=Wandering` while taking an ordinary wander move. Warning-or-higher logs were empty after the runtime window. The post-runtime state was saved as `ZL_Core_Pheromone_Senses_after_runtime_00.rws`, then `EMPTY.rws` was reloaded and verified with zero live/cached zombies and clean warning-or-higher logs.
- Added 2026-05-31 with a generic room/corridor gauntlet fixture from `EMPTY.rws`; no scenario-specific endpoint was added. The static/decompiler pass rechecked RimWorld 1.6 `Building_Door.PawnCanOpen(Pawn)`, `Building_Door.Tick()`, `FireUtility.TryStartFireIn(...)`, `Pawn.Kill(...)`, `Pawn_HealthTracker.MakeDowned(...)`, and `JobDriver_AttackStatic.MakeNewToils()` against the current Harmony rows before runtime. Three generic helpers were added or used for reusable setup evidence: `zombieland/place_thing`, `zombieland/spawn_pawn_fixture`, and `zombieland/read_cell_things`. The final fixture saved as `ZL_Core_RoomCorridor_Gauntlet_00.rws` uses a long player wood-wall barrier at x=104 with a single wooden door at `(104,100)`, a weak adjacent wall at `(103,102)`, a human corpse at `(106,100)`, a health-downed human at `(106,102)`, a burning human at `(106,98)` in the saved setup, a fresh radius-4 pheromone source centered at `(106,100)`, and two normal zombies west of the barrier.
- After roughly six seconds at normal speed, one zombie reached `(103,100)` and entered a real `AttackStatic` job with report `attacking wooden door (83%)`; the door changed from 90/104 HP in the saved setup to 86/104 HP. The second zombie remained alive in real `Stumble`/`Tracking` adjacent to the barrier and the weak wall, while the corpse and downed pawn stayed present on the far side. The post-runtime state was saved as `ZL_Core_RoomCorridor_Gauntlet_after_runtime_00.rws`. Reloading that save preserved the active `AttackStatic` door job and `Stumble` tracking job with the door still at 86/104 HP; after another short normal-speed run, both zombies resumed without warning-or-higher logs. `EMPTY.rws` was reloaded afterward and warning-or-higher logs remained empty.
- Added 2026-05-31 with existing `zombieland/smash_with_tanky` plus the generic `zombieland/wait_for_semantic_change` primitive from `EMPTY.rws`; no new wall-smash endpoint was added. The tanky probe spawned tanky zombie `Thing_Zombie6898` at `(100,100)`, selected adjacent wooden wall `Thing_Wall6899` at `(100,101)`, recalculated the route parent through the wall, handed off from `Stumble` to real `AttackStatic` with report `attacking wooden wall.`, and saw the installed job run. The bounded semantic wait then observed initial wall HP `195` and target disappearance/destruction by the first 10-tick sample. `EMPTY.rws` was reloaded afterward.

Completion:
- Covered for real-time mixed-horde movement, tracking, special-zombie job coexistence, save-load persistence, post-reload grid repopulation, through-wall pheromone tracking, stale pheromone rejection, direct zombie-hearer suppression for non-zombie impact clamor, focused eating contracts, direct tanky wall destruction through `AttackStatic`, and a combined room/corridor gauntlet that co-locates door pressure, wall barrier, corpse, downed pawn, fire attachment, pheromone attraction, active zombie jobs, save, reload, and post-reload continuation without adding a scenario-specific endpoint. No known C baseline gap remains in the core active-mod configuration; future work should be named co-location, endurance, optional-mod, DLC, or regression variants.

## S-Infection-Medical

Goal: prove zombie bites, infection states, medical recipes, surgery targets, corpse conversion timers, and infection alerts behave as a coherent RimWorld medical workflow.

Prior evidence:
- `623822f`, `fd1c12f`, `d637d99`, `2fd1969`, `2cf6be8`, `89a3c45`, `9c46bc0`
- `bac414e`, `7127f37`, `0235865`, `b7d560e`

Source checks:
- Zombieland: `Source/Hediff_Injury_ZombieBite.cs`, `Source/HediffComp_Zombie_Infecter.cs`, `Source/HediffComp_Zombie_TendDuration.cs`, `Source/Recipe_CureZombieInfection.cs`, `Source/JobDriver_DoubleTap.cs`, `Source/JobDriver_ExtractZombieSerum.cs`, `Source/WorkGivers.cs`, `Source/ZombieCorpse.cs`, `Source/Alerts.cs`.
- RimWorld 1.6 decompiler: `Verse.HediffUtility.CanHealNaturally(Hediff_Injury)` (`40ca93f8c2cf4459bd8a72c25d1bfb81:06002CCE:M`), `RimWorld.Recipe_RemoveBodyPart.GetPartsToApplyOn(Pawn, RecipeDef)` (`40ca93f8c2cf4459bd8a72c25d1bfb81:0600C371:M`), and `Verse.Hediff.ShouldRemove` (`40ca93f8c2cf4459bd8a72c25d1bfb81:1700074D:P`).

Fixture:
- Immutable input: `EMPTY.rws`.
- Generated saves: `ZL_Infection_Medical_00.rws` for the pre-action player workflow and `ZL_Infection_Medical_after_workflow_00.rws` after cure, double-tap, extraction, and conversion.

Setup:
- Spawn one doctor, one bitten patient, and one animal or non-human negative case.
- Apply deterministic bite stages: hidden, harmless, infectable, infecting, and death-conversion-ready.
- Stage serum, extract, infected corpses, and one removable bitten body part.

Runtime:
- Run compact medical patch contracts for natural healing, bite persistence, and remove-body-part targets.
- Verify extract/serum filter visibility while zombie pawn/corpse defs stay hidden from ordinary filters.
- Execute cure, double-tap, serum extraction, and corpse conversion countdown in one map session.
- Save during the countdown, reload, and continue until either conversion or prevention is proven.
- Read infection alerts before and after cure/conversion.

Assertions:
- Hidden, infectable, and infecting zombie bites do not heal naturally and do not disappear when severity reaches zero.
- Harmless bites, ordinary cuts, and non-human/non-infectable cases keep vanilla healing/removal behavior.
- `RemoveBodyPart` offers every distinct bitten part exactly once and does not add null or duplicate surgery targets.
- `ZombieExtract` and zombie serum remain available in filters/tree listings while zombie pawn and corpse defs stay hidden.
- Cure/double-tap/extract/corpse conversion behaviors match the focused contracts in the same loaded workflow.
- Infection alerts point to the expected pawns and clear after cure, harmless treatment, or conversion.
- Log summary contains no health-card, recipe, hediff, corpse-rot, or conversion exceptions.

Current runtime evidence:
- Added 2026-05-30 with `zombieland/infection_medical_state` from `EMPTY.rws`. The live run spawned a temporary colonist and grizzly bear, applied hidden, harmless, infectable, and infecting `ZombieBite` hediffs plus a normal `Cut`, and verified natural healing behavior: hidden/infectable/infecting bites returned `CanHealNaturally=false`, harmless bites and ordinary cuts returned true, and an animal `ZombieBite` with infection state `None` also returned true through vanilla behavior. With severity set to zero, hidden/infectable/infecting bites returned `ShouldRemove=false`, while the harmless bite and ordinary cut returned true. `Recipe_RemoveBodyPart.GetPartsToApplyOn` returned all four distinct bitten parts once each, with no missing or duplicate bitten parts. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Added 2026-05-30 by extending the same `zombieland/infection_medical_state` contract to cover `Pawn_Tick_Patch`. The live run invoked protected `Verse.Pawn.Tick()` by reflection. A final-stage bitten human updated its custom pawn infection state from `None` to `Infecting` after one real tick. For the contamination gate, a clean control pawn and a contaminated but still spawned/alive/not-downed pawn each carried a running fueled chainsaw for 40 reflected `Pawn.Tick()` calls; the clean pawn at effectiveness `1` consumed `0.06652832` fuel, while the contaminated pawn at effectiveness `0.335` consumed `0.0266113281`, proving post-base pawn tick work is throttled by contamination effectiveness. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Added 2026-05-30 with `zombieland/infection_medical_state actionMode:pawn-kill` and refreshed 2026-07-11 for faction-aware spitter loot. The contract covers normal-zombie job/apparel cleanup, bitten-human death infection, and animal controls. In standard non-tribal `PlayerColony`, `op_cd6e231366d940dd87b0c20ee6a7542f` proved the aggressive spitter dropped one forbidden `Zombie100Serum` and reported `makeshiftSerumAvailable=false`; tribal games select the medically equivalent makeshift def instead.
- Added 2026-05-31 with `zombieland/infection_medical_state actionMode:health-card-living` and `actionMode:health-card-dead` from `EMPTY.rws` after decompiling RimWorld 1.6 `HealthCardUtility.DrawOverviewTab`. The live fixture records the installed Zombieland postfix, selects the prepared pawn/corpse, and opens the real `ITab_Pawn_Health`; `rimworld/get_ui_layout` then provides generic UI evidence rather than a custom UI parser. The living run selected infecting colonist `Thing_Human6898`; the Health tab overview showed vanilla capacity rows plus `This body is infected with the zombie virus!` at the bottom of the left overview column and the right hediff list included `Zombie bite (36.0h to become a zombie)`. After reloading `EMPTY`, the dead run selected corpse `Thing_Corpse_Human6901`; the Health tab overview showed the same infected-body label below the dead-pawn pain row, and the right hediff list included `Zombie infection` plus the countdown `Zombie bite (36.0h to become a zombie)`. Warning-or-higher logs were empty before and after the final `EMPTY` reload.
- Added 2026-05-30 with `zombieland/zombie_corpse_alert_forbid_contract` from `EMPTY.rws`. The live run proved a human corpse still returns true from the unburied-colonist alert helper and becomes forbidden when outside the home area. Normal and former zombie corpses both returned false for the alert helper and stayed unforbidden after the same outside-home forbid call. The same run verified serum extraction remains available for both zombie corpse variants and double-tap remains unavailable for both. Its `ThingMaker.MakeThing` probe created `Corpse_Zombie` as `ZombieCorpse` and `Corpse_ZombieSpitter` as `ZombieSpitterCorpse`; both defs had smelt/mine/steal/butcher/smelt-product/caravan-loading/item-overlay/tooltip behavior disabled, `neverMultiSelect=true`, empty inspector tabs, standable passability, stack limit 1, and `selectable=true`. The interaction extension created a corpse stockpile and proved `WorkGiver_HaulCorpses.JobOnThing` returned `HaulToCell` for the human corpse while `zone.Accepts(zombieCorpse)=false`, unforced zombie corpse haul was null, and forced zombie corpse haul was also null through the storage/filter layer. The reservation extension proved a steel control returned `CanReserve=true` and `Reserve=true`, a non-former zombie returned `CanReserve=false` and `Reserve=false`, and a former-map-pawn zombie returned `CanReserve=true` but `Reserve=false` through the reserve prefix. It also added `Steel` inventory payloads to a human, normal zombie, spitter, and symbiant, then proved `AnythingToStrip=true` only for the human control and false for all three Zombieland pawn kinds. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Added 2026-05-30 with `zombieland/zombie_extract_filter_visibility` from `EMPTY.rws`. The live run proved `ThingFilter.SetAllow` allows `ZombieExtract` and `ZombieSerumSimple` but blocks `Corpse_Zombie` and `Zombie`; the reflected private `Listing_TreeThingFilter.Visible(ThingDef)` probe likewise returned visible for extract/serum and hidden for zombie corpse/pawn defs. `ZombieSerumFilterWorker` did not exclude extract, and `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Rechecked 2026-07-11 through `op_c158a531d7ab473f87e44a8d6c1c6d0a`: extract is visible only under `Raw resources`, and the tribal recipe explicitly accepts it. Normal `PlayerColony` does not show or create makeshift serum and cannot obtain it through trading, generation, scattering, debug spawning, or aggressive-spitter loot; any attempted creation resolves to ordinary 100% serum. Tribal runs show both makeshift and ordinary 100% serum. Both are medical stand-ins at purity 100/surgery factor 1, while acquisition alone differs by faction.
- Added 2026-05-30 with `zombieland/convert_infected_corpse_to_zombie` from `ZL_Settings_Persistence_00.rws`. The rot-stage run used the saved `ZL_Settings_Colonist`, applied a final-stage bite, killed the pawn, moved its corpse from `Fresh` to `Rotting`, proved the conversion queue contained the corpse (`queueCountBeforeRun=1`), and converted it into one normal zombie preserving the pawn label. The rare-tick run reloaded the same save, applied the same bite/death setup, forced the corpse's `Hediff_ZombieInfection.ticksWhenBecomingZombie` from `25039` to `5038` at `GenTicks.TicksGame=5039`, called `Corpse.TickRare()` while the corpse was still `Fresh`, proved the queue changed from empty to containing the corpse, and converted it into one normal zombie. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Added 2026-06-01 after the two-fork review with a fresh build/restart and `EMPTY.rws` under Harmony, Core plus official DLCs, Zombieland, and RimBridgeServer. `zombieland/convert_infected_corpse_to_zombie recoverableLootProbe:true` (`op_75e3b8a661ba4807a80c0343b446f851`) spawned a player colonist fixture, applied a final bite, killed it into a corpse, staged a `Gun_BoltActionRifle` and `Silver x7` on the corpse inner pawn, triggered rot-stage conversion, verified one normal zombie was created, verified the staged items were in zombie inventory with no equipped weapon, killed the zombie, and verified both items dropped. `zombieland/zombie_extract_filter_visibility` (`op_401e43e89a7d4f558d6184ecf0dd271a`) then proved the new `ZombieExtract` raw-resource category remains visible/allowed alongside `ZombieSerumSimple` while zombie pawn/corpse defs remain blocked. The post-test log journal contained only bridge/load info entries. A first pre-death staging attempt deliberately failed the probe because vanilla pawn death dropped carried gear before corpse conversion, so the retained probe stages on the corpse inner pawn to test conversion-time recoverable inventory.
- Added 2026-05-30 with `zombieland/zombie_health_needs_upkeep_suppression` from `EMPTY.rws`. The infected-colonist branch added final-stage zombie bites to alert-tracked colonists and proved need clamping (`Food` assignments `0.13` and `0.93` both read back as `0.5` while the untracked control kept `0.13`), pain suppression (`PainTotal=0` vs control `0.118750006`), full living capacities under anesthetic (`Consciousness=1`, `Moving=1` vs control `Consciousness=0.01`), dead-pawn capacity fallback (`Consciousness=0`), and mental-state suppression (`TryStartMentalState(Berserk, forced:true)` returned true for the control and false for the tracked infected pawn). The same run equipped the control mental-state pawn with `Thing_Chainsaw6921` before the forced mental state and proved the chainsaw spawned at the pawn cell afterward, covering the non-infected chainsaw drop postfix. The same tool confirmed installed prefixes for `Pawn_NeedsTracker.AllNeeds`, `AddOrRemoveNeedsAsAppropriate`, and `NeedsTrackerTickInterval`, then proved zombie needs are hidden from `AllNeeds`, cleared by reconciliation with direct fields rebound, and not ticked by `NeedsTrackerTickInterval(150)`. It also proved immunity suppression with human controls; `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Added 2026-05-31 with reusable `zombieland/infection_workflow_state` modes `setup`, `read`, and `execute`. The static/decompiler pass checked RimWorld 1.6 `AlertReport`, `Corpse.TickRare()`, `Corpse.TickRareInt()`, `Hediff.ExposeData()`, `HediffComp.CompExposeData()`, and `Pawn_HealthTracker.ExposeData()`, then matched those save/alert/tick surfaces against current Zombieland `Alerts`, `Recipe_CureZombieInfection`, `JobDriver_DoubleTap`, `JobDriver_ExtractZombieSerum`, `WorkGivers`, and corpse-conversion patches.
- The setup pass loaded `EMPTY`, staged one capable doctor plus hidden, imminent, and infecting live colonists; one infected corpse with a future conversion tick; one infected corpse for double-tap; and one zombie corpse for serum extraction. It saved `ZL_Infection_Medical_00.rws`. Reloading that save and running `read` proved the hidden, imminent, and infecting alerts each pointed to exactly one expected pawn, the need-clamping infected-colonist cache contained the infecting pawn, the countdown corpse retained one `Hediff_ZombieInfection` with `ticksUntilConversion=2499`, the double-tap corpse still had a brain, and the zombie corpse remained tracked for extraction.
- The post-reload `execute` pass applied the real cure recipe with `ZombieSerumSimple`, making the curable bite harmless, clearing `mayBecomeZombieWhenDead`, dropping curable surgery parts to zero, and clearing the imminent alert. It then ran the real forced `DoubleTap` job until the corpse brain was missing, forced the timer path afterward, and proved no conversion queue entry was added. It ran the real forced `ExtractZombieSerum` job until the zombie corpse was destroyed and one `ZombieExtract` existed. Finally, it proved the saved countdown corpse path by forcing its persisted timer elapsed, calling `Corpse.TickRare()`, observing `queueCount 0 -> 1`, and converting the queued corpse into one normal zombie preserving the pawn label. The post-action state was saved as `ZL_Infection_Medical_after_workflow_00.rws`; final `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Static support-surface pass added 2026-06-01: the hediff, hediff-comp, cure recipe, double-tap/extract/rope job-driver, serum filter, alert, and workgiver source was reread against current RimWorld 1.6 decompiler evidence. The direct `Hediff`, `HediffComp`, `RecipeWorker`, `JobDriver`, and `SpecialThingFilterWorker` overrides used by these classes still exist with compatible signatures. The only changed files in this support set since the port baseline are the already-covered port adaptations: `JobDriver_DoubleTap` now uses `PathEndMode.ClosestTouch` instead of `OnCell`, and `WorkGivers` now use `PathFinder.FindPathNow` plus null-faction chainsaw repair acceptance. No new runtime pass was justified because the existing focused and combined infection workflow already owns behavior and save-load evidence.

Completion:
- Covered for the named infection/medical workflow: focused medical contracts, alert state, save-load during a corpse conversion countdown, double-tap prevention, serum extraction, cure behavior, and post-action conversion now have live evidence in one loaded map session. Remaining work is additive only if we promote actual mouse-driven bill/operation UI, bed-quality treatment branches, or amputation/removal workflow to their own matrix rows.

## S-Social-Selection

Goal: prove Zombieland pawns and corpses stay out of ordinary RimWorld social/thought systems while former-colonist zombies remain selectable and identifiable where the mod intentionally allows it.

Prior evidence:
- `6d2afda`, `2ca0e7b`

Source checks:
- Zombieland: social, corpse, thought, interaction, label-color, selector, and map-click patches in `Source/Patches.cs`; bridge fixtures in `Source/BridgeTools/ZombielandBridgeTools.Social.cs`.
- RimWorld 1.6 decompiler: `RelationsUtility.HasAnySocialMemoryWith`, `Pawn_RelationsTracker.OpinionOf`, `RelationsUtility.PawnsKnowEachOther`, `ThoughtHandler.GetSocialThoughts`, `Corpse.GiveObservedThought`, `Corpse.GiveObservedHistoryEvent`, `ThoughtUtility.CanGetThought`, `Pawn_InteractionsTracker.TryInteractWith`, `PawnNameColorUtility.PawnNameColorOf`, `Selector.SelectInternal`, `ThingSelectionUtility.SelectableByMapClick`, and `Pawn_IdeoTracker.ExposeData`.

Fixture:
- Immutable input: `EMPTY.rws`.

Runtime:
- Spawn a colonist actor, an ordinary zombie, and a former-colonist zombie converted from a player pawn.
- Exercise social memories, acquaintance, opinions, social thoughts, thought eligibility, direct interactions, interaction ticking, observed zombie corpses, map-click selection, selector selection, and label color.

Assertions:
- Zombies do not become social-memory partners, known pawns, opinion targets, social-thought targets, interaction partners, or ordinary thought recipients.
- Zombie-inflicted damage does not create ordinary `HarmedMe` social memories, while human-on-human damage still does.
- Zombie interaction tick processing is skipped while an ordinary pawn control still runs the deterministic vanilla `Off` social-mode branch.
- Observed zombie corpses do not emit ordinary corpse thoughts or history events.
- Zombie no-ideo fallback warnings are suppressed during save-load while ordinary colonists keep their ideology tracker state.
- Ordinary live zombies and ordinary zombie corpses are not map-click selectable.
- Former-colonist live zombies and corpses are map-click selectable, former-colonist corpses can be selected through the real selector, selected former-colonist zombie corpses expose a non-null empty inspect-tab list, and former-colonist zombies use the Zombieland label color.
- Log summary contains no social, thought, selector, or corpse exceptions.

Current runtime evidence:
- Added 2026-05-30 with `zombieland/zombie_social_thought_suppression` from `EMPTY.rws`. The live run proved `hasAnySocialMemoryWithZombie=false`, both `PawnsKnowEachOther` directions false, both opinions `0`, `socialThoughtCountAboutZombie=0`, both `TryInteractWith(..., Chitchat)` directions false, `GiveObservedThought(actor)=null`, and `GiveObservedHistoryEvent(actor)=null` for a killed normal zombie's `ZombieCorpse`. The same run proved the ordinary actor can receive `DebugBad` while the normal zombie cannot. It confirmed the installed `SituationalThoughtHandler_AppendSocialThoughts_Patch` target and directly probed the prefix branch, with `humanOtherResult=true` for an ordinary human target and `zombieOtherResult=false` for a normal zombie target. The interaction-tick subprobe temporarily forced both actor and zombie into no-faction `Off` social mode, set both private `wantsRandomInteract` flags true, ticked both interaction trackers for 60 ticks, and proved the actor flag cleared while the zombie flag remained true. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Added 2026-05-30 with `zombieland/zombie_damage_memory_suppression` from `EMPTY.rws`. The live run confirmed the installed Zombieland transpiler on `Pawn_HealthTracker.PreApplyDamage`, then damaged a human with a human attacker and a second human with a normal zombie attacker. The human control path added one `HarmedMe` memory (`humanDamageMemoryDelta=1`), while the zombie-instigated path dealt real damage (`zombieDamageTotal=2.594751`) and added zero memories (`zombieDamageMemoryDelta=0`). The tool now destroys all spawned fixtures before returning, and `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Added 2026-05-30 with `zombieland/zombie_death_thought_suppression` from `EMPTY.rws`. The live run confirmed installed Zombieland patch owners on `PawnDiedOrDownedThoughtsUtility.TryGiveThoughts`, `IndividualThoughtToAdd..ctor`, and `Thought_Tale.OpinionOffset`. It proved the adult death-thought boundary with an ordinary human control and an adult normal zombie: the human death path added one `KnowColonistDied` memory, while invoking the same death-thought utility for the killed adult zombie added zero memories. The child/killed-child branch was recorded as a dependency skip because the active local configuration lacks `BodyTypeDefOf.Child` and a killed-child `Thought_Tale` def; `zombieland/child_zombie_generation_contract` reports the same skip, and a 2026-05-31 local install inspection confirmed no official DLC child surface is installed locally. A 2026-06-01 decompiler refresh confirmed the vanilla `ThoughtDefOf.KilledChild` memory addition is also gated by active Biotech in both RimWorld 1.4 and 1.6, so fixture XML alone is insufficient. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Updated 2026-06-14 from player save `RightClickAndPhase2Breaking.rws`. The log ref D913C0A was traced to Core-only `TaleDefOf.KilledChild` being null in `TaleUtility.Notify_PawnDied` for child-aged humanlike zombies such as Bene/Maviz. The contract now confirms `TaleRecorder_RecordTale_Patch` is installed and `nullTaleDefHandled=true`; the provided save loaded visual-ready, the contract passed, and post-contract warning-or-higher logs contained only pre-existing dangling `Tale_*` save references.
- Added 2026-05-30 with `zombieland/zombie_selection_respects_former_colonist` from `EMPTY.rws`. The live run proved ordinary live zombies and ordinary zombie corpses are not map-click selectable, former-colonist live zombies and corpses are map-click selectable, the real selector rejects ordinary zombie corpses and selects former-colonist zombie corpses, and only the former-colonist zombie returned the Zombieland label color `{r:0.7,g:1,b:0.7,a:1}`. The expanded run decompiled `MainTabWindow_Inspect.CurTabs` and `Thing.GetInspectTabs`, caught the stale `Corpse_Zombie.def.inspectorTabsResolved` cache still exposing five pawn tabs, fixed the zombie-corpse def rewrite to clear that cache, then proved selected former-colonist zombie corpses return a non-null empty inspect-tab list (`tabCount=0`, `directTabCount=0`, `defInspectorTabResolvedCount=0`). `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Added 2026-05-31 with generic save/load evidence from `EMPTY.rws` after decompiling RimWorld 1.6 and 1.4 `Pawn_IdeoTracker.ExposeData()`. `zombieland/incident_threat_state setupFixture:true` created three ordinary colonists, `zombieland/spawn_zombie` added one normal zombie, and `zombieland/get_status` recorded the installed `Pawn_IdeoTracker_ExposeData_Patch` transpiler. Before saving, the three colonists had ideology trackers and `Astropolitan` ideos, while the normal zombie had an ideo tracker but `Ideo=null` despite `ShouldHaveIdeo=true`. The fixture was saved as `ZL_Ideo_Load_Suppression_00.rws`, reloaded with `rimworld/load_game_ready`, and `zombieland/get_status` showed the same ordinary colonist ideology state and the same zombie null-ideo state. `rimbridge/list_logs minimumLevel=warning` returned no entries after reload, proving the zombie no-ideo fallback warning was suppressed without stripping ordinary pawn ideology initialization. `EMPTY` was reloaded afterward and the follow-up warning-or-higher log check was empty.

Completion:
- Covered for the intended social, thought, selection, label-color, observed-corpse, selected former-colonist corpse inspect-tab, and zombie ideology-load warning boundaries by the focused contracts.

## S-Special-Gauntlet

Goal: prove all special zombie types work together on one controlled map and that their visuals/effects remain correct across damage, action, death, cleanup, and save-load.

Prior evidence:
- `2325c82`, `860832a`, `81e6c2b`, `d6d2870`, `3daf2d5`, `b0d97f5`, `5ba1b80`, `9638321`
- `04c6412`, `58350bc`, `c181649`, `aeeac24`
- `b95123c`, `4d723cb`, `8c75244`, `1c0e9a4`, `1ac0117`, `1bedcf1`
- `dd90b2f`, `12cb6a2`, `f3a44b2`, `62343fa`, `68ebd8c`
- `cd0e93b`, `0146ec7`, `5a22ef0`

Source checks:
- Zombieland: `Source/Zombie.cs`, `Source/ZombieSpitter.cs`, `Source/ZombieBall.cs`, `Source/ZombieSymbiant.cs`, `Source/ZombieSymbiantRenderer.cs`, `Source/JobDriver_Spitter.cs`, `Source/JobDriver_Symbiant.cs`, `Source/JobDriver_Sabotage.cs`, `Source/TarSmoke.cs`, `Source/TarSlime.cs`, `Source/BombVest.cs`, `Source/OverlayDrawer.cs`.
- RimWorld 1.6 decompiler: projectile launch/impact path, pawn draw/render-node path, melee verb path, explosion/damage application path.

Fixture:
- Generated save: `ZL_Special_Gauntlet_base.rws`.
- Layout: separate lanes for suicide bomber, toxic splasher, tanky, miner, electrifier, albino, dark slimer, healer, spitter, symbiant.

Setup:
- Spawn one special per lane plus one colonist target, one powered building, one wall/mineable, one door, one wounded zombie, and one ranged attacker.

Runtime:
- Step each lane enough for the special action to trigger.
- Damage or kill each special and observe death effects.
- Save after active effects exist, reload, step again.

Assertions:
- Each special reaches the intended action without blocking the others.
- Death/impact effects create expected things/hediffs/flecks/gas/damage.
- Tar smoke and sticky goo affect targeting/move costs.
- Electrifier behaves differently against ranged and melee attacks.
- Tanky armor/smash and miner mining still occur in mixed context.
- Albino scream/sabotage does not create unwanted bite/infection.
- Visuals are nonblank and recognizable for active specials and effects after reload.

Artifacts:
- Save before action, save after action, save after reload.
- One wide screenshot or cell-rect montage.
- Deduped log summary.

Current runtime evidence:
- Added 2026-05-30 with `zombieland/zombie_skin_color_contract` from `EMPTY.rws`. The live run set an ordinary human `Pawn_StoryTracker.SkinColorBase` to `{r:0.25,g:0.2,b:0.15,a:1}` and verified that the human kept that vanilla story color while a naturally generated `Zombie` returned `Color.white` through `Pawn_StoryTracker_SkinColorBase_Patch`. Spawned `ZombieSpitter` and `ZombieSymbiant` instances did not naturally have story trackers in this fixture, so the probe attached temporary `Pawn_StoryTracker` instances only to exercise the patch type guard; both then returned `Color.white` and reported `storyInjectedForProbe=true`. `rimbridge/list_logs minimumLevel=warning` returned no entries. This is a narrow property/patch-boundary proof, not visual rendering coverage.
- Added 2026-05-30 with `zombieland/zombie_gene_rejection_contract` from `EMPTY.rws`. The active fixture is Core-only (`biotechActive=false`), so the contract used temporary in-memory `GeneDef`/`Gene` instances to exercise the installed `Pawn_GeneTracker.AddGene` overloads without requiring Biotech defs. The human control had a natural gene tracker, accepted both the public `AddGene(GeneDef, bool)` and private `AddGene(Gene, bool)` paths, and gene count increased `2 -> 3 -> 4`. The normal `Zombie` had a natural gene tracker and rejected both overloads with null results and count `0 -> 0 -> 0`. Spawned `ZombieSpitter` and `ZombieSymbiant` had no natural gene tracker in this fixture, so the probe attached temporary `Pawn_GeneTracker` instances; both rejected both overloads with null results and count `0 -> 0 -> 0`. `rimbridge/list_logs minimumLevel=warning` returned no entries. This covers the patch boundary, not a full Biotech DLC gameplay workflow.
- Added 2026-05-30 with `zombieland/area_workflow_state actionMode:special-damage` after decompiling private RimWorld 1.6 `DamageWorker_AddInjury.ApplyDamageToPart`; final evidence was refreshed after the spitter health fix in `op_494c283d92374003a9f52527edb58248`. The live run recorded the installed Zombieland prefix and exercised the special-damage branch matrix with real `TakeDamage` calls. With `spitterThreat=1`, spitter non-ranged `Cut` input `2` dealt `5` direct core injury while ranged `Bullet` input `10` dealt `1`. A former-map-pawn zombie took `1.84822178` bullet injury from input `12`, versus `3.43253374` on an ordinary zombie. Albino bullet attempts produced `8` blocked and `1` hit, while explosive `Bomb` dealt `1`. Damaging a dark slimer spawned `37` `TarSmoke` things and changed its cell gas to `TarSmoke`. An active electrifier absorbed ranged bullet input `4` for `0` damage and added `4` absorb markers, while active electrifier `Cut` damage passed through for `1.41176474` on dry active-electrifier cells. The build was clean and `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Added 2026-05-30 during the mixed-targeting rerun: the first `EMPTY` reload crashed before the verifier ran because `ZombieLand.ZombieSymbiant.Finalize()` disposed a Unity `ComputeBuffer` on the GC finalizer thread (`Graphics device is null`). The code now releases `ZombieSymbiant` render resources from game-thread `DeSpawn`/`Destroy` and removes Unity cleanup from finalizers. After rebuilding, restarting through GABS, and rerunning `zombieland/area_workflow_state setupFixture:true actionMode:targeting`, the fixture spawned and cleaned multiple symbiants successfully with `success=true`; `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Added 2026-05-31 with the reusable visual branch of `zombieland/spawn_reference_lineup`: from `EMPTY`, `stageRenderStates:true` spawned all eight common special zombies plus wounded healer target, electrifier absorb marker, bomber light, albino scream, healer beam, raging normal, roped normal with rope anchor, confused normal, emerging normal, spitter, and symbiant. Static/decompiler checks covered RimWorld 1.6 `PawnRenderer.RenderPawnAt(Vector3, Rot4?, bool)` (`88df42e0e943404ba42043aebd37585c:060026F1:M`), `PawnRenderUtility.DrawEquipmentAndApparelExtras` (`06002733:M`), `DynamicDrawManager.DrawSilhouettes(NativeArray<ThingCullDetails>)` (`06001982:M`), `SilhouetteUtility.ShouldDrawSilhouette(Thing)` (`060028AC:M`), `PawnApparelGenerator.PossibleApparelSet.PairOverlapsAnything(ThingStuffPair)` (`0600C436:M`), and `GameDataSaveLoader.LoadGame(string)` (`0600358A:M`). The first pass exposed three real save-load/render issues: custom-drawn spitter/symbiant pawns triggered vanilla silhouette null-reference logs while `drawHighlight=true`; a saved spitter job could tick with `JobDriver_Spitter.spitter == null`; and releasing `ZombieSymbiant` `ComputeBuffer` resources from the `MemoryUtility.ClearAllMapsAndWorld` prefix crashed because that prefix runs inside RimWorld's asynchronous load thread. Fixes were to set `<drawHighlight>false</drawHighlight>` on `ZombieSpitter` and `ZombieSymbiant`, lazily resolve the spitter pawn in `JobDriver_Spitter`, and release symbiant render resources from main-thread `GameDataSaveLoader.LoadGame(string)`/`Root.Shutdown` lifecycle patches.
- Final live evidence used save `ZL_Special_Visual_Gauntlet_00.rws`. The pre-reload screenshot is `/Users/ap/Library/Application Support/RimWorld/Screenshots/zl_special_visual_gauntlet_staged_prereload__cell_rect.png`; the post-reload screenshot is `/Users/ap/Library/Application Support/RimWorld/Screenshots/zl_special_visual_gauntlet_staged_postreload__cell_rect.png`. Log gates were empty before spawn, after spawn, after save-load, and after a 30-tick post-reload smoke. The post-reload smoke advanced `5039 -> 5069` ticks; `zombieland/list_zombies` returned 15 Zombieland pawns, including the saved symbiant and a spitter still on `currentJob=Spitter` with `state=Moving` and a live pather destination. Player-log inspection after the final pass found no `ComputeBuffer`, `NullReference`, `Graphics device is null`, native crash, warning, or error lines in the recent log tail. This covers the active visual/save-load slice; staged transient render effects such as healer/electrifier absorb are pre-reload visual evidence because those helper lists are not scribed.
- Added 2026-05-31 with `zombieland/special_gauntlet_state`, a reusable scenario-scale contract with `setup`, `effects`, and `read` modes. Static/decompiler checks covered the current special action/death path before runtime: `Verse.Pawn.Kill(DamageInfo?, Hediff)` (`88df42e0e943404ba42043aebd37585c:0600306B:M`), `Verse.GenExplosion.DoExplosion` (`060054D1:M`), `Verse.Projectile.Launch` (`060038D2:M`), `Verse.Projectile.Impact` (`060038DC:M`), `Verse.DamageWorker_AddInjury.ApplyDamageToPart` (`0600291B:M`), `Verse.ArmorUtility.GetPostArmorDamage` (`0600296A:M`), and `RimWorld.StunHandler.Notify_DamageApplied` (`0600F6D7:M`), plus current Zombieland source for `Zombie.Kill`, `DropStickyGoo`, `CustomTick`, `ZombieStateHandler.Smash/Mine/CanSmash`, `JobDriver_Spitter.Shoot`, `ZombieBall.Launch/Impact`, `JobDriver_Sabotage`, `TickManager.ExecuteExplosions`, and `BombVest.Explode`.
- The setup pass loaded `EMPTY`, cleared existing Zombieland pawns, spawned all eight common specials, spitter, symbiant, a wounded normal zombie, and two bait colonists around `(96,96)`, marked the local home area, and saved `ZL_Special_Gauntlet_base.rws`; warning-or-higher logs were empty. The effects pass proved the lanes in one map: suicide bomber death queued one `TickManager` explosion and `ExecuteExplosions()` drained it; toxic splasher death increased sticky goo by `2`; dark slimer bullet damage produced `13` `TarSmoke` and gas `TarSmoke`; healer cleared the wounded zombie hediff by tick `10` and queued one heal entry; active electrifier bullet damage dealt `0`, added absorb markers, and EMP disabled it; tanky `AttackStatic` destroyed a spawned wall (`195 -> 0` HP); miner damaged a mineable (`500 -> 491` HP); albino stayed on `Sabotage` and set `scream=45`; spitter spawned a `ZombieBall`; symbiant remained spawned on the `Symbiant` job.
- The first action save, `ZL_Special_Gauntlet_after_effects_00.rws`, exposed a real save-load defect: suicide-bomb combat-log entries serialized the scaled runtime `DamageDef` as `UnnamedDef`, and loading the save produced `Could not load reference to Verse.DamageDef named UnnamedDef`. Decompiler evidence for `Verse.Scribe_Defs.Look` (`06003634:M`) and `Verse.BattleLogEntry_ExplosionImpact.ExposeData` (`06001405:M`) showed the save path writes `value.defName` and reloads through def lookup. `Source/BombVest.cs` now restores `defName = CustomDefs.SuicideBomb.defName` after field-copying the base def into `SuicideBombDamage`.
- Final action evidence after the fix used `ZL_Special_Gauntlet_after_effects_01.rws` and `/Users/ap/Library/Application Support/RimWorld/Screenshots/zl_special_gauntlet_after_effects_fixed__cell_rect.png`. Searching the save XML found only `<damageDef>SuicideBomb</damageDef>` for the explosion-impact entries and no `UnnamedDef`. Loading `ZL_Special_Gauntlet_after_effects_01.rws` produced no warning-or-higher logs. A post-reload `read advanceTicks=30` pass advanced to tick `5316` with `advanceError=null`, reported `corpseCount=2`, `humanCount=2`, `zombieCount=9`, `stickyGoo=2`, `tarSmoke=13`, `zombieBalls=1`, and saved `ZL_Special_Gauntlet_postreload_01.rws`; the post-reload screenshot is `/Users/ap/Library/Application Support/RimWorld/Screenshots/zl_special_gauntlet_postreload_fixed__cell_rect.png`. Warning-or-higher logs remained empty.

Completion:
- Covered when all special lanes pass in one map session and at least the visual lanes are checked after reload.
- Current status: covered for the controlled special action/death gauntlet plus visual save-load slice. Broader natural combat-room cross-interactions with animals, raiders, turrets, powered buildings, and DLC-specific Biotech behavior remain additive D-cluster work rather than blockers for this scenario row.

## S-Contamination-Persistence

Goal: prove contamination is durable and player-visible across cells, pawns, things, jobs, effects, replacement chains, and save-load.

Prior evidence:
- `11a0c90`, `2bee432`, `22b45b3`, `fa02d3f`, `7e27948`
- `2c02244`, `324cfb2`, `ae37dda`, `b394e63`, `23b889f`, `68fb03e`
- `cf0479e`, `662d902`, `065b1a5`, `5f73f13`, `fff7028`, `f73fe81`
- `92b46aa`, `40c3613`, `33b4ad2`, `a125cdb`, `f8f709d`, `a908279`, `5f9f91e`
- `45520ed`, `5b550e4`, `beeb01c`, `51b739b`

Source checks:
- Zombieland: `Source/ContaminationManager.cs`, `Source/ContaminationSerializer.cs`, `Source/ContaminationEffect.cs`, `Source/ContaminationNeed.cs`, `Source/Hediff_Contamination.cs`, `Source/MentalState_Contamination.cs`, `Source/JobDriver_Contamination*.cs`, `Source/ContaminationPatches*.cs`, `Source/QuestDecontaminateColonists.cs`.
- RimWorld 1.6 decompiler: `Scribe`, `ThingOwner.TryTransferToContainer`, `Thing.TryAbsorbStack`, construction frame/building replacement path, plant work toils, mineable/deep drill yield path, pollution/snow clear toils.

Fixture:
- Immutable input: `EMPTY.rws`.
- Generated saves: `ZL_Contamination_Persistence_00_setup.rws`, `ZL_Contamination_Persistence_10_handoffs.rws`, `ZL_Contamination_Persistence_20_player.rws`, and `ZL_Contamination_Persistence_30_quest.rws`.

Setup:
- Create contaminated pawns, cells, terrain, stackable items, corpse, plant, mineable, frame, building, and active contamination effect.
- Create one pawn in each contamination mental/job state or stage those jobs sequentially.

Runtime:
- Save immediately after setup, reload, verify state.
- Execute replacement chains: stack absorb/split, blueprint/frame/building, minify/install, plant harvest/sow, mine/drill, snow/pollution clear, corpse handoff.
- Trigger exposure and treatment progression.
- Start decontamination quest if available and save while active.

Runtime batch plan:

| Batch | Save target | Existing bridge contracts to treat as history/narrow evidence | Generic runtime actions to sequence | Extra assertions still needed |
| --- | --- | --- | --- | --- |
| `C0-state-serializer` | `ZL_Contamination_Persistence_00_setup.rws`; `ZL_MapGeneration_Contamination_01.rws` | `zombieland/contamination_core_contract`, `zombieland/contamination_effect_manager_contract`, `zombieland/read_contamination_state` | Load `EMPTY`, create fixed cells/things/pawns, set contamination values, save, reload, and read the same cells/things/pawns through generic state queries; for map-generation seeding, start one debug colony and sample generated-map contamination before/after save-load. | Covered 2026-05-29: core/effect contracts passed before and after reload; exact persisted stack contamination read back as `0.6`. Extended 2026-05-30: the same core contract now also covers need gating, ground-grid repair, destroy cleanup, comp-stack split propagation, and real `ThingComp_MakeThing_Patch` gatherable-resource fanout behavior. Extended 2026-06-01: a built-in debug colony generated a 250x250 map, `zombieland/read_contamination_state` sampled 81 cells and found generated contamination on `MineableSteel` at `(150,100)`, then reload of `ZL_MapGeneration_Contamination_01.rws` read the cell back at `0.0016` with clean warning logs. |
| `C1-stack-container` | `ZL_Contamination_Persistence_10_handoffs.rws` | `zombieland/contamination_stack_absorb_contract`, `zombieland/contamination_carry_tracker_contract`, `zombieland/contamination_ingestion_contract`, `zombieland/contamination_thing_owner_transfer_contract`, `zombieland/complete_frame_by_id`, `zombieland/write_contamination_state`, `zombieland/read_contamination_state` | Split and merge stacks, move items through a pawn/container/frame, ingest contaminated food, save, reload, and re-read contamination. | Covered 2026-05-29: `TryAbsorbStack`, `SplitOff`, carry ticks, ingestion, contaminated stack save-load, `ThingOwner.TryTransferToContainer`, and reloaded frame completion fallback passed. |
| `C2-build-replace` | `ZL_Contamination_Persistence_15_build_replace.rws` | `zombieland/contamination_building_install_contract`, `zombieland/contamination_frame_construction_contract`, `zombieland/contamination_terrain_construction_contract`, `zombieland/contamination_smooth_wall_contract`, `zombieland/contamination_smooth_wall_reversion_contract`, `zombieland/read_contamination_state` | Build from contaminated resources, complete frame, minify, install, reinstall, smooth wall, destroy/revert wall, save, reload. | Covered 2026-05-29: building, terrain/foundation, blueprint, minified, installed, smoothed, and reverted wall chains passed and persisted after reload. |
| `C3-world-products` | `ZL_Contamination_Persistence_20_world_products.rws` | `zombieland/contamination_filth_leavings_contract`, `zombieland/contamination_generation_handoffs_contract`, `zombieland/contamination_mineable_yield_contract`, `zombieland/contamination_deep_drill_contract`, `zombieland/contamination_plant_stump_contract`, `zombieland/contamination_plant_harvest_contract`, `zombieland/contamination_plant_sow_contract`, `zombieland/contamination_wild_plant_spawn_contract`, `zombieland/contamination_ambrosia_sprout_contract`, `zombieland/contamination_roof_collapse_contract`, `zombieland/read_contamination_state` | Generate products through real jobs/incidents where cheap, otherwise call the same vanilla method path, save, reload, and inspect spawned products. | Covered 2026-05-29 with caveat and extended 2026-05-30: stable products persisted with exact contamination, pawn carried filth, vomit filth, birth filth, dissolve-gear death filth, liquid projectile filth, tunnel-hive rubble filth, explosion filth, reward/loot generation, trade goods, mech-cluster spawn products, comp-spawned filth, damage-caused filth, flame ash, roof-collapse phase-two filth, and corpse butcher products now inherit contamination, and compressed slag chunks are runtime-proven but reload under regenerated ids/positions, so persisted leavings coverage uses the stable component leaving. Dependency-gated product rows now have decompiler target-shape evidence but still require active Biotech/content setup for subcore scanner, gene extractor, and mech disassembly runtime claims. |
| `C4-cleaning-environment` | `ZL_Contamination_Persistence_25_cleaning_environment.rws` | `zombieland/contamination_cell_fire_contract`, `zombieland/contamination_clear_snow_contract`, `zombieland/contamination_clear_pollution_contract`, `zombieland/read_contamination_state` | Contaminate cells, enter cells, burn cells/things, clear snow/sand/pollution and clean filth with real jobs, save, reload. | Covered 2026-05-29 for active core config and extended 2026-05-30: cell entry, fire reduction, snow clearing, and clean-filth transfer are covered; pollution skipped because active config lacks official Biotech pollution support. A 2026-05-31 local inspection confirmed the official `Data` folder contains only `Core` and no official DLC packages are exposed by `rimworld/list_mods includeInactive=true`; a 2026-06-01 local `ZLTestPollution` fixture then supplied `Wastepack` and `ClearPollution`, but the clear-pollution contract still cleanly skipped because `PollutionGrid.SetPolluted` is gated behind `ModsConfig.BiotechActive`. |
| `C5-player-effects` | `ZL_Contamination_Persistence_30_player_effects.rws` | `zombieland/contamination_rest_comfort_contract`, `zombieland/contamination_melee_equalize_contract`, `zombieland/contamination_tending_contract`, `zombieland/contamination_hallucination_contract`, `zombieland/contamination_mimic_contract`, `zombieland/contamination_sleepwalk_contract`, `zombieland/contamination_breakdown_contract`, `zombieland/contamination_hoard_pather_failure_contract`, `zombieland/contamination_hoard_driver_flow_contract`, `zombieland/read_contamination_effect_state` | Run exposure, rest, melee, tending, mental effects, and hoarding in one colony map, save during active jobs, reload, and continue ticks. | Covered 2026-05-29 for active player effects: focused rest/melee/tending passed, and the combined save-load workflow kept hallucination, mimic, sleepwalk, breakdown, and pather-failure hoard active together after reload and 270 post-reload ticks with no warning-or-higher logs. |
| `C6-ui-quest` | `ZL_Contamination_Persistence_30_quest.rws` | `zombieland/contamination_ui_quest_contract`, `zombieland/contamination_accepted_quest_contract` | Use generic UI/state reads for inspect pane, thing icons, mouseover, overlay toggle, quest list, decontamination quest start/save/reload, accepted pickup loading, send signal, return success, and legacy outcome branches. | Covered 2026-05-30 for contamination visibility, decontamination quest generation through the Core-safe 1.6 pickup transporter node, challenge-rating suppression, quest-tab row integration, save/reload persistence, accepted send/return success, pickup destruction failure, `SentUnsatisfied` failure, and sent-pawn death legacy success. |

GABS/RimBridge execution shape:

- Start from `games_status`; if stopped, use `games_start` and then `rimworld/load_game_ready` for `EMPTY`.
- Discover live names with `games_tool_names` and inspect only `rimworld/load_game_ready`, `rimworld/save_game`, `rimworld/list_saves`, the required generic state/read tools, and the existing contamination contracts that are being used as narrow checks.
- Prefer one batch runner sequence per row above. Do not add a new Zombieland bridge endpoint unless a row cannot be asserted through generic spawn/set/job/tick/read/save calls plus the already-existing narrow contracts.
- After each batch, save the named target, run the log summarizer, and store only compact state differences in the artifact note.

Current runtime evidence:

- `C0-state-serializer` is covered by live evidence from 2026-05-29. GABS started RimWorld, loaded `EMPTY`, ran `zombieland/contamination_core_contract` and `zombieland/contamination_effect_manager_contract` successfully, saved `ZL_Contamination_Persistence_00_setup.rws`, reloaded that save, and ran both contracts successfully again.
- The 2026-05-30 C0 core rerun extended the same generic contract from `EMPTY`. It reflected private `Pawn_NeedsTracker.ShouldHaveNeed` and proved `CustomDefs.Contamination` is available with `Constants.CONTAMINATION=true` but hidden and removed by `AddOrRemoveNeedsAsAppropriate()` when the constant is temporarily false. It removed the current map's contamination ground entry, invoked `ContaminationManager.FixGrounds()`, and proved map index `0` was repaired with a live map reference and `40000` cells. It spawned contaminated `ComponentIndustrial` at `(106,100)`, confirmed the manager entry existed at `0.37`, destroyed it through real `Thing.Destroy`, and confirmed the entry was gone. The existing comp split path still preserved `ComponentIndustrial x10` contamination `0.6` onto both source `x6` and split `x4`. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- A 2026-05-31 C0 new-world reset rerun extended `zombieland/contamination_core_contract` after decompiling `RimWorld.Planet.WorldGenerator.GenerateWorld(...)`. The contract recorded the installed Zombieland postfix owner on `WorldGenerator.GenerateWorld`, installed a sentinel `ContaminationManager` into the private `_instance` singleton cache, invoked the same postfix, confirmed `_instance` was cleared, and confirmed the next `ContaminationManager.Instance` rebound to the real current-world component rather than the sentinel. The full core contract returned `success=true`, `rimbridge/list_logs minimumLevel=warning` returned no entries, and `EMPTY` was reloaded afterward.
- A follow-up C0 core run on 2026-05-30 added `ThingComp_MakeThing_Patch` fanout evidence to the same generic contract after decompiling representative 1.6 component methods. Runtime recorded 10 installed Zombieland transpiler targets; `CompMechCarrier.PostSpawnSetup` was confirmed as the candidate that no longer calls `ThingMaker.MakeThing`. The live branch used a contaminated player-faction alpaca and real `CompHasGatherableBodyResource.Gathered`: parent contamination changed `0.72 -> 0.60048`, and the produced `WoolAlpaca x45` received contamination `0.119520009` with seed `2`, matching `generalTransfer`. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- A follow-up C0 core run on 2026-05-30 added `MinifiedThing_SplitOff_Patch` evidence to the same generic contract after decompiling RimWorld 1.6 `MinifiedThing.SplitOff`. The first live probe exposed a double-transfer bug: generic `Thing.SplitOff` copied source contamination `0.5` to the split minified stack, then the minified-specific postfix transferred another `0.2`, so split contamination became `0.7`. The fixed postfix clears the generic copy before proportional transfer. The rerun split a spawned minified Stool stack `x5` at contamination `0.5`; the source stack became `x3` at `0.3`, the split stack became `x2` at `0.2`, and the installed target was `RimWorld.MinifiedThing::SplitOff(Int32)`. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Generic state reads after reload confirmed first-run state persisted at the expected cells: `Thing_Human6898` at `(100,100)`, `Thing_Human6904` at `(101,100)`, and `ComponentIndustrial x6` at `(103,100)`.
- The reusable `zombieland/read_contamination_state` tool read exact post-reload contamination values: saved component `Thing_ComponentIndustrial6902` at `(103,100)` had contamination `0.6`, matching the pre-save contract value; saved pawns `Thing_Human6898` and `Thing_Human6904` had stored contamination `0`, matching their cleared contract end state.
- Generic selection semantics confirmed `Thing_Human6904` still had `ContaminationJobForceRest`, `ContaminationStateForceRest`, and inspect text `Mental state: Resting` after reload. `rimbridge/list_logs` returned no warning-or-higher entries for the C0 run.
- `C1-stack-container` is covered by live evidence from 2026-05-29 and rerun evidence from 2026-05-30. `zombieland/contamination_stack_absorb_contract` passed with weighted merge `0.2 x6 + 0.8 x4 => 0.44 x10`. `zombieland/contamination_carry_tracker_contract` passed on the current build by putting contaminated `Steel x20` into a drafted colonist's hands, advancing to the carry cadence tick, and changing pawn/item contamination to `0.060200002`/`0.6398` while the item stayed held. The same carry rerun proved the count/reserve overload by splitting contaminated `Steel x20` into carried `Steel x5` and spawned leftover `Steel x15`, both at contamination `0.7`, with installed patch targets on both overloads. `zombieland/contamination_ingestion_contract` passed by ingesting one meal from a contaminated five-meal stack, changing eater contamination to `0.09600001` with matching need/hediff severity and leaving the meal stack at `0.403999984`. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- A 2026-05-30 C1 handoff rerun added recipe and dispenser production edges from `EMPTY`. `zombieland/contamination_recipe_product_contract` used a butcher-table bill giver and an isolated recipe-transfer setup: contaminated `Steel x10` moved from `0.8` to `0.4`, and the unspawned `ComponentIndustrial` recipe product received `0.4`. `zombieland/contamination_nutrient_paste_contract` built a powered nutrient paste dispenser plus hopper, consumed one contaminated survival meal from a stack of two, produced an unspawned `MealNutrientPaste` at contamination `0.09960001` from transfer factor `0.166000009`, left the remaining feed stack at `0.6`, and confirmed the private active-feed list was cleared after dispensing. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- The `ZL_Contamination_Persistence_10_handoffs.rws` save was created after the C1 contracts plus a generic spawned `Steel x13` stack written to contamination `0.33`. After reload, `zombieland/read_contamination_state` confirmed `Thing_Human6904` at `(100,100)` stored `0.09600001`, `Thing_MealSurvivalPack6907` at `(103,100)` stored `0.403999984`, and `Thing_Steel6909` at `(106,100)` stored `0.33`. `rimbridge/list_logs` returned no warning-or-higher entries.
- A fresh C1 transfer run loaded `EMPTY`, ran `zombieland/contamination_thing_owner_transfer_contract` with `cleanup:false`, and saved `ZL_Contamination_Persistence_10_handoffs.rws`. The exact `ThingOwner.TryTransferToContainer(Thing, ThingOwner, Int32, out Thing, Boolean)` path passed for partial `4` and full remaining `6` transfers between frame resource containers: source count reached `0`, target count reached `10`, target frame contamination was `0.75`, and target frame including held wood was `1.5` before save.
- After reload, `zombieland/read_contamination_state` confirmed the serializer edge: target frame `Thing_Frame_Wall6899` at `(103,100)` kept direct contamination `0.75`, while its held `WoodLog x10` reloaded with contamination `0`. This drove the source fallback in `Frame_CompleteConstruction_Patch.ClearAndDestroyContents`, which uses frame contamination when held resource contamination is absent after reload.
- `zombieland/complete_frame_by_id` then completed reloaded frame `Thing_Frame_Wall6899` through real `Frame.CompleteConstruction`. It selected contamination source `0.75`, built `Thing_Wall6905`, and matched configured construction transfer values: final wall contamination `0.326435983` versus expected `0.326436`, worker contamination `0.00356400013` versus expected `0.00356400013`. `rimbridge/list_logs` returned no warning-or-higher entries.
- `C2-build-replace` is covered by live evidence from 2026-05-29. GABS loaded `EMPTY`, then ran the construction/replacement contracts with `cleanup:false` where fixture persistence was needed. `zombieland/contamination_frame_construction_contract` built `Thing_Wall6903` from contaminated wood with final contamination `0.348198384` versus expected `0.3481984`.
- `zombieland/contamination_building_install_contract` proved minify/install/reinstall and blueprint replacement: source stool contamination `0.6` moved to minified form, install blueprint, and final `Thing_Stool6907`; reinstall source contamination `0.45` moved to reinstall blueprint and final `Thing_Stool6908`.
- `zombieland/contamination_terrain_construction_contract` proved floor and bridge/foundation construction after fixture correction from RimWorld 1.6 decompiled `Frame.CompleteConstruction`: `WoodPlankFloor` cell `(104,100)` reached ground contamination `0.62`; `Bridge` over `WaterShallow` cell `(108,100)` reached ground contamination `0.74` with `foundationAfter`/`topAfter` `Bridge`. A 2026-05-30 extension decompiled `JobDriver_AffectFloor.MakeNewToils` and added real floor-affecting work coverage: `RemoveFloor` removed a contaminated `WoodPlankFloor` at `(112,100)` on tick 45, and worker contamination moved `0 -> 0.0172800012`, matching `0.8 * floorAdd=0.0216`; `rimbridge/list_logs minimumLevel=warning` returned no entries. A 2026-06-01 rerun loaded `ZL_CE_EMPTY_00` with local fixture mod `local.zltesttemporaryterrain` active before Zombieland and reused the same generic terrain construction contract: `ZLTestTemporaryFloor` built through `Frame_ZLTestTemporaryFloor`, `tempAfter=ZLTestTemporaryFloor`, ground contamination moved `0 -> 0.57`, expected transfer matched, floor/bridge/affect-floor subprobes still passed, and warning-or-higher logs were empty. The fixture remains installed locally but inactive by default; `ModsConfig.xml` was restored after the run.
- `zombieland/contamination_smooth_wall_contract` with `cleanup:false` proved natural-to-smoothed granite contamination `0.58` and nested reversion contamination `0.63`; after save/reload, `zombieland/read_contamination_state` confirmed `Thing_SmoothedGranite6924` at `(100,100)` contamination `0.58`, `Thing_SmoothedGranite6925` at `(100,101)` contamination `0.18`, reverted `Thing_Granite6932` at `(101,101)` contamination `0.63`, `Thing_Wall6903` contamination `0.348198384`, `Thing_Stool6907` contamination `0.6`, `Thing_Stool6908` contamination `0.45`, floor cell `0.62`, and bridge cell `0.74`. `rimbridge/list_logs` returned no warning-or-higher entries.
- `C3-world-products` is covered by live evidence from 2026-05-29. Existing contracts were extended with `cleanup:false` so generated products can be saved and read after reload. All real-path contracts passed before save from `EMPTY`: ambrosia incident spawned 13 ambrosia at contamination `0.68`; wild plant spawned grass at `0.201600015`; roof collapse spawned collapsed rocks at `0.67`; harvest produced raw rice at `0.256`; sow produced a rice plant at `0.207860008` and worker equalized to `0.68214`; filth/leavings produced blood at `0.02884` plus slag/component leavings at `0.160000011`; mineable yield produced steel at `0.65`; deep drill produced `Steel x45` at `0.420000017`; stump creation produced a chopped stump at `0.55`.
- A 2026-05-30 C3/corpse-edge rerun from `EMPTY` added two death/corpse transfer checks. `zombieland/contamination_zombie_death_contract` killed a normal zombie and an ordinary colonist from clean cells: the zombie death cell changed `0 -> 0.0432`, while the human death cell stayed `0`. `zombieland/contamination_corpse_inner_pawn_contract` killed a pawn with contamination `0.47`, produced a fresh human corpse whose inner pawn matched the source pawn, and confirmed the corpse inherited contamination `0.47`. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- A 2026-05-30 C3 butcher-products rerun from `EMPTY` extended `zombieland/contamination_filth_leavings_contract` after decompiling `Corpse.ButcherProducts`, `Pawn.ButcherProducts`, and `Thing.ButcherProducts`. The first live run found a real bug: the installed patch was present and butcher blood filth inherited contamination, but yielded alpaca meat/leather stayed at `0`. `Corpse_ButcherProducts_Patch` now applies corpse contamination to every yielded product while preserving the filth-source context for blood filth. The fixed live run butchered an alpaca corpse at contamination `0.66`; `Meat_Alpaca x126`, `Leather_Camel x37`, and butcher blood filth all reached expected contamination `0.0271920022` with `filthEqualize=0.0412`. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- A 2026-05-30 C3 vomit-filth rerun from `EMPTY` extended the same contract after decompiling `JobDriver_Vomit.MakeNewToils` and the generated tick delegate. The live run started real `JobDefOf.Vomit`; the installed Zombieland prefix/postfix target was `RimWorld.JobDriver_Vomit::<MakeNewToils>b__4_1(System.Int32 delta)`. The pawn had contamination `0.58`, produced `Filth_Vomit` at `(116,100)` after 94 ticks, and the filth contamination `0.023896` matched `filthEqualize=0.0412`. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- A 2026-05-30 C3 comp-spawned-filth rerun from `EMPTY` extended the same contract after decompiling `CompSpawnerFilth.TrySpawnFilth`. The live run used a synthetic real `CompSpawnerFilth` on a contaminated steel parent so the direct reachable-cell `FilthMaker.TryMakeFilth` path is covered without a DLC or def dependency; the installed Zombieland prefix/postfix target was `RimWorld.CompSpawnerFilth::TrySpawnFilth()`. Steel at `(123,100)` with contamination `0.62` spawned `Filth_Dirt` at `(124,100)`, and the filth contamination `0.025544` matched `filthEqualize=0.0412`. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- A 2026-05-30 C3 damage-filth rerun from `EMPTY` extended the same contract after decompiling `Thing.TakeDamage` and `GenLeaving.DropFilthDueToDamage`. The live run damaged a contaminated real `ZombieThumper` because its def satisfies the vanilla damage-filth prerequisites; the installed Zombieland prefix/postfix target was `Verse.Thing::TakeDamage(Verse.DamageInfo dinfo)`. The thumper at `(129,100)` had contamination `0.64`, took `240` Crush damage, spawned `Filth_RubbleBuilding` at `(128,101)`, and the filth contamination `0.026368` matched `filthEqualize=0.0412`. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- A 2026-05-30 C3 birth-filth rerun from `EMPTY` extended the same contract after decompiling `PregnancyUtility.SpawnBirthFilth`. RimWorld 1.6 exposes the target as a private helper `SpawnBirthFilth(Pawn mother, IntVec3 center, ThingDef filth, Int32 radius)`; the installed Zombieland prefix/postfix target matched that helper. A mother pawn at `(135,100)` with contamination `0.52` invoked the helper for `Filth_AmnioticFluid` radius `2`; five filths spawned at contamination `0.021424`, matching `filthEqualize=0.0412`. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- A 2026-05-30 C3 roof-collapse phase-two rerun from `EMPTY` extended `zombieland/contamination_roof_collapse_contract` after decompiling `RoofCollapserImmediate.DropRoofInCellPhaseTwo`. The live run set a temporary `RoofRockThick` at `(106,100)` over ground contamination `0.61`; the installed Zombieland prefix/postfix target was `Verse.RoofCollapserImmediate::DropRoofInCellPhaseTwo(Verse.IntVec3 c, Verse.Map map)`. The private helper spawned `Filth_RubbleRock` at contamination `0.025132`, matching `filthEqualize=0.0412`, and ground contamination dropped to `0.584868`. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- A 2026-05-30 C3 flame-ash rerun from `EMPTY` extended `zombieland/contamination_filth_leavings_contract` after decompiling `DamageWorker_Flame.Apply` and `DamageWorker.Apply`. The first run found a real bug: the installed `DamageWorker_Flame_Apply_Patch` was present, but four ash filths stayed clean because vanilla destroyed the contaminated thumper before ash creation and nested leavings cleanup cleared the live source reference. `Filth_MakeThing_Patch` now keeps an optional source contamination/map-index snapshot for destruction-delayed filth sources. The fixed rerun destroyed a contaminated thumper at `(141,100)` with source snapshot `0.73`; four `Filth_Ash` instances spawned at contamination `0.030076`, matching `filthEqualize=0.0412`. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- A 2026-05-30 C3 dissolve-gear death-filth rerun from `EMPTY` extended `zombieland/contamination_filth_leavings_contract` after decompiling `HediffComp_DissolveGearOnDeath.Notify_PawnKilled`. The live run used a synthetic real dissolve-gear comp on a spawned colonist; the installed Zombieland prefix/postfix target was `RimWorld.HediffComp_DissolveGearOnDeath::Notify_PawnKilled()`. The pawn at `(147,100)` had contamination `0.49`; invoking the comp produced `Filth_Slime` at contamination `0.020188`, matching `filthEqualize=0.0412`. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- A 2026-05-30 C3 liquid-projectile-filth rerun from `EMPTY` extended `zombieland/contamination_filth_leavings_contract` after decompiling `Projectile_Liquid.Impact` and its private `DoImpact(Thing hitThing, IntVec3 cell)` helper. The live run invoked a real `Proj_Foam` liquid projectile helper with contaminated steel as `hitThing`; the installed Zombieland prefix/postfix target was `RimWorld.Projectile_Liquid::DoImpact(Verse.Thing hitThing, Verse.IntVec3 cell)`. Source contamination `0.57` produced `Filth_FireFoam` at `(155,100)` with contamination `0.0234840009`, matching `filthEqualize=0.0412`. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- A 2026-05-31 static dependency-boundary pass decompiled the remaining C3 source-only product rows without adding new bridge tools. RimWorld 1.6 `Building_SubcoreScanner.Tick()` still finishes occupied fabrication by `EjectContents()` then placing `ThingMaker.MakeThing(def.building.subcoreScannerOutputDef)` through `GenPlace.TryPlaceThing`; `Building_GeneExtractor.Finish()` still creates a `Genepack` with `ThingMaker.MakeThing(ThingDefOf.Genepack)` and places it through `GenPlace.TryPlaceThing`; and `JobDriver_DisassembleMech.MakeNewToils()` still gates on `ModLister.CheckBiotech("Disassemble mech")`, creates disassembly products from `MechanitorUtility.IngredientsFromDisassembly(Mech.def)`, and places them through `GenPlace.TryPlaceThing`. The active live load order is Core-only (`Harmony`, `Core`, `Zombieland`, `RimBridgeServer`) for these branches, so they remain dependency-unavailable for runtime behavior rather than being counted as covered by the Core fixture.
- A 2026-06-01 C3 misc-spawn handoff rerun fixed and covered `GenSpawn_Spawn_Replacement_Patch` for tunnel jelly. The live verifier first exposed that the old expression-based Harmony target installed on `GroundSpawner.Spawn`, leaving `TunnelJellySpawner.Spawn` products clean. The patch now targets the protected `TunnelJellySpawner.Spawn(Map, IntVec3)` override directly. Rerunning `zombieland/contamination_misc_spawn_handoffs_contract` from `ZL_CE_EMPTY_00` reported the installed target as `TunnelJellySpawner.Spawn`, spawned two `InsectJelly` stacks totaling 18 at contamination `0.2552` from radius-ground contamination `0.58` and `jellyAdd=0.44`, and warning-or-higher logs were empty.
- A 2026-05-30 C3 tunnel-hive-rubble rerun from `EMPTY` extended `zombieland/contamination_filth_leavings_contract` after comparing RimWorld 1.4 and 1.6 decompiled tunnel code. RimWorld 1.4 created tunnel filth in `TunnelHiveSpawner.Tick()`, but RimWorld 1.6 moved the rubble tick to `GroundSpawner.Tick()` and made `TunnelHiveSpawner.Spawn()` only create hives/insects. `TunnelHiveSpawner_Tick_Patch` now targets `GroundSpawner.Tick()` and filters to spawned `TunnelHiveSpawner` instances. The live run forced one real rubble tick from contaminated tunnel ground `0.63`; the installed Zombieland prefix/postfix target was `RimWorld.GroundSpawner::Tick()`, and `Filth_Dirt` spawned at `(161,101)` with contamination `0.0259560011`, matching `filthEqualize=0.0412`. `rimbridge/list_logs minimumLevel=warning` returned no entries.

- A 2026-05-30 C3 explosion-filth rerun from `EMPTY` extended `zombieland/contamination_filth_leavings_contract` after decompiling `Verse.Explosion.StartExplosion`, `AffectCell`, `TrySpawnSingleThing`, and the private `TrySpawnExplosionThing` helper. RimWorld 1.6 still routes explosion-spawned filth through `FilthMaker.TryMakeFilth`; the live verifier spawned a real `Explosion`, injected damaged steel sources at contamination `0.22` and `0.74`, and invoked the private helper for `Filth_Dirt`. The installed Zombieland prefix/postfix target was `Verse.Explosion::TrySpawnExplosionThing(Verse.ThingDef thingDef, Verse.IntVec3 c, Int32 count)`, and `Filth_Dirt` spawned at `(168,100)` with contamination `0.0304880012`, matching the highest damaged source `0.74 * filthEqualize=0.0412` and not the lower source value `0.009064`. `rimbridge/list_logs minimumLevel=warning` returned no entries.

- A 2026-05-30 C3 generation-handoffs run from `EMPTY` added `zombieland/contamination_generation_handoffs_contract` for reward/loot, trade, mech-cluster, and skyfaller products after decompiling `ThingSetMaker.Generate`, `TradeDeal.AddAllTradeables`, `MechClusterUtility.SpawnCluster`, and `Skyfaller.SpawnThings`. The installed targets were `ThingSetMaker.Generate(ThingSetMakerParams)`, private `TradeDeal.AddAllTradeables()`, the 1.6 `MechClusterUtility.SpawnCluster(IntVec3, Map, MechClusterSketch, Boolean, Boolean, String)` signature, and `Skyfaller.<SpawnThings>b__59_0(Thing, Int32)`. With random contamination chances forced to `1`, a generated `ComponentIndustrial x3` and `Steel x25` received contamination `0.225987986` and `0.151718691`; synthetic trader goods appeared in real `TradeDeal` trader tradeables and received nonzero contamination; a real mech-cluster sketch spawned `Thing_Barricade6903` at `(172,100)` with uniform contamination `4.659071e-6`; and direct `SpawnThings()` probes placed `ShipChunk` at `(172,108)` and `MineableSteel` at `(177,108)` with `meteoriteAdd=0.6`. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- A 2026-05-30 C3 execution-blood run from `EMPTY` extended `zombieland/contamination_filth_leavings_contract` after decompiling private `ExecutionUtility.ExecutionInt`. RimWorld 1.6 still drops blood, applies `ExecutionCut`, then kills the victim if damage did not already do so. The installed target was `RimWorld.ExecutionUtility::ExecutionInt(Pawn executioner, Pawn victim, Boolean huntingExecution, Int32 bloodPerWeight, Boolean spawnBlood)`. A spawned executioner invoked the private helper on a victim at contamination `0.68`; the victim died, `Thing_Corpse_Human6967` was produced, and `Filth_Blood` at `(172,100)` reached `0.028016001`, matching `0.68 * filthEqualize=0.0412`. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- A 2026-05-30 C3 carried-filth run from `EMPTY` extended `zombieland/contamination_filth_leavings_contract` after decompiling `Pawn_FilthTracker.GainFilth`. RimWorld 1.6 still creates new carried filth with `ThingMaker.MakeThing(filthDef)` inside the two-argument overload. The installed `Filth_MakeThing_Patch` transpiler targets included both `Pawn_FilthTracker.GainFilth(ThingDef, IEnumerable<string>)` and `FilthMaker.TryMakeFilth(..., out Filth, ...)`. A spawned pawn at contamination `0.58` gained one carried `Filth_Dirt` at contamination `0.023896`, matching `0.58 * filthEqualize=0.0412`; warning-or-higher logs were empty.
- A 2026-05-30 C5 social-transfer run from `EMPTY` added `zombieland/contamination_social_transfer_contract` after decompiling `PawnUtility.Mated` and `JobDriver_Lovin.MakeNewToils`. The installed targets were `RimWorld.PawnUtility::Mated(Pawn male, Pawn female)` and `RimWorld.JobDriver_Lovin::MakeNewToils()`. Direct mating transfer equalized spawned pawns from `0.8`/`0.2` to `0.5`/`0.5`. A real `Lovin` job on a double-bed fixture reached the patched lay-down branch under the patch's `ticksLeft <= 25000` gate on tick 154; the pair matched one 0.1 transfer with lover/partner contamination about `0.7399`/`0.26`. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- A 2026-05-30 C3 misc-spawn-handoffs run from `EMPTY` added `zombieland/contamination_misc_spawn_handoffs_contract` after decompiling `CompLifespan.Expire`. RimWorld 1.6 still spawns `Props.plantDefToSpawn` through `GenSpawn.Spawn` before destroying the parent. The installed target was `Verse.CompLifespan::Expire()`. A synthetic real lifespan comp on a spawned `ComponentIndustrial` parent at `(100,106)` with contamination `0.75` expired into `Plant_Grass` at contamination `0.124500006`, matching `0.75 * generalTransfer=0.166000009`; a clean control at `(104,106)` expired into a clean plant. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- The same 2026-05-30 C3 misc-spawn-handoffs contract now covers `MedicalRecipesUtility_GenSpawn_Spawn_Patches` for both installed target branches. Runtime patch evidence recorded both installed targets, `MedicalRecipesUtility.SpawnNaturalPartIfClean` and `SpawnThingsFromHediffs`. A spawned colonist at `(108,106)` with contamination `0.69` received an `ArchotechArm` hediff on an arm, and the real `SpawnThingsFromHediffs` call spawned one `ArchotechArm` at `(109,106)` with contamination `0.03818`. That matched the measured holder-aware pawn source loss `0.0381799936` after the pawn moved to `0.65182`, not the nominal transfer request `0.11454`. The natural-part branch temporarily set a clean arm's `spawnThingOnRemoved` to `ComponentIndustrial`, called the real `SpawnNaturalPartIfClean` on a pawn at `(112,106)` with contamination `0.64`, restored the def immediately, and spawned `ComponentIndustrial` at `(113,106)` with contamination `0.035413336`, matching measured pawn source loss `0.0354133248`; `rimbridge/list_logs minimumLevel=warning` returned no entries.
- The same 2026-05-30 C3 misc-spawn-handoffs contract now covers and fixes `Recipe_RemoveImplant_ApplyOnPawn_Patches`. Decompiler checks showed RimWorld 1.4 and 1.6 have the same `Recipe_RemoveImplant.ApplyOnPawn` spawn-before-remove shape. The first live branch found that the Zombieland replacement was loading `ldarg.0` from an instance method, which passed the recipe worker instead of the patient pawn and left the spawned implant clean. After changing the transpiler to load `ldarg.1`, the live rerun removed an `ArchotechArm` hediff from a contaminated patient at `(112,106)`, spawned one `ArchotechArm` at the doctor's cell `(113,106)`, moved patient contamination `0.67 -> 0.6329267`, and gave the spawned item contamination `0.0370733365`, matching measured holder-aware source loss `0.0370733142`. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- A 2026-05-30 C3 building-products run from `EMPTY` added `zombieland/contamination_building_products_contract` for building-returned products. Decompiler evidence showed RimWorld 1.6 `Building_FermentingBarrel.TakeOutBeer()` still makes `ThingDefOf.Beer`, copies `wortCount` to the stack count, calls `Reset()`, and returns the unspawned beer; `Building_GeneAssembler.Finish()` still makes `ThingDefOf.Xenogerm`, initializes it from `genepacksToRecombine`, places it near the interaction cell, and resets. Runtime patch evidence recorded both `Misc_Building_Patch` targets, `Building_GeneAssembler.Finish()` and `Building_FermentingBarrel.TakeOutBeer()`. The exercised barrel branch used a synthetic fermented barrel at `(100,110)` with contamination `0.66`; `TakeOutBeer()` returned unspawned `Beer x17` with contamination `0.109560013`, matching `fermentingBarrelTransfer=0.166000009`, and reset the barrel to `0.55044`. The GeneAssembler branch recorded a clean dependency skip because the active configuration lacks `GeneAssembler`, `Genepack`, and `Xenogerm` defs; a 2026-05-31 local install inspection confirmed no official Biotech data/package is installed locally. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- A 2026-05-30 C3 repair-transfer run from `EMPTY` added `zombieland/contamination_repair_contract` after decompiling the RimWorld 1.6 `JobDriver_Repair.MakeNewToils` tick delegate. The generated delegate still increments `TargetThingA.HitPoints` and calls `Map.listerBuildingsRepairable.Notify_BuildingRepaired((Building)TargetThingA)`. A spawned colonist at `(101,114)` ran a real `Repair` job on a damaged player `Wall` at `(100,114)`; the installed target was `RimWorld.<>c__DisplayClass8_0::<MakeNewToils>b__1()`. At tick 24, HP moved `194 -> 195`, wall contamination moved `0.75 -> 0.72504`, and worker contamination moved `0.15 -> 0.17496`, matching `repairTransfer=0.0416`. The same contract now records the installed `JobDriver_RepairMech_MakeNewToils_Patch` target, `RimWorld.JobDriver_RepairMech::<MakeNewToils>b__10_2(System.Int32 delta)`, and cleanly skips the live mech branch because the active configuration has `BiotechInstalled=false` and `JobDefOf.RepairMech=null`; a 2026-05-31 local install inspection confirmed no official Biotech data/package is installed locally. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- A 2026-06-01 plant-harvest regression from `EMPTY` followed the dynamic closure-search audit that found RimWorld 1.6's new `ThingComp.GetAdditionalHarvestYield()` product branch inside `JobDriver_PlantWork.MakeNewToils()`. After refactoring the patch to use a scoped harvest-plant context instead of a single `ThingMaker.MakeThing` transpiler, `zombieland/contamination_plant_harvest_contract` harvested real rice: source plant contamination `0.64`, `plantTransfer=0.4`, resulting `RawRice x6` contamination `0.256`, and warning-or-higher logs empty. Core data did not expose a non-empty additional-yield plant comp, so the new 1.6 branch remains a static/decompiler-backed obligation tied to the same context path until such a def/mod is active.
- The C3 save `ZL_Contamination_Persistence_20_world_products.rws` was created and reloaded after tightening fixture selectors and fixing compressed-map contamination edges. Generic readback succeeded with no missing stable targets: ship chunk `Thing_ShipChunk6902` at `0.48`, component leaving `Thing_ComponentIndustrial6908` at `0.160000011`, mineable steel `Thing_Steel6915` at `0.65`, deep-drill steel `Thing_Steel6917` at `0.420000017`, raw rice `Thing_RawRice6922` at `0.256`, sown rice `Thing_Plant_Rice6926` at `0.207860008`, stump `Thing_ChoppedStump6928` at `0.55`, wild grass `Thing_Plant_Grass6929` at `0.201600015`, and ambrosia `Thing_Plant_Ambrosia6931`/`6932`/`6933` at `0.68`. Cell readback also proved collapsed rocks persisted through the regenerated compressed-map id at `(99,100)` with contamination `0.67`.
- C3 caveat: RimWorld serializes slag chunks as compressed map things and reloads them under regenerated ids/positions around the ship chunk. The runtime leavings contract proves slag contamination before save, but the durable leavings assertion is the stable component product. `rimbridge/list_logs` returned no warning-or-higher entries for the final C3 run.
- `C4-cleaning-environment` is covered for the active core configuration by live evidence from 2026-05-29 and extended runtime evidence from 2026-05-30. RimWorld 1.6 decompiler checks confirmed the current shapes of `Pawn_FilthTracker.Notify_EnteredNewCell`, `Fire.DoComplexCalcs`, `JobDriver_ClearSnowAndSand.MakeNewToils`, `JobDriver_CleanFilth.MakeNewToils`, `JobDriver_ClearPollution.MakeNewToils`, `JobDriver_ClearPollution.ClearPollutionAt`, and `GridsUtility.Unpollute`. GABS loaded `EMPTY`; `zombieland/contamination_cell_fire_contract` passed with entry pawn contamination `0.009139201`, entry cell `0.8`, component/fire-cell contamination reduced from `0.4` to `0.292`; `zombieland/contamination_clear_snow_contract cleanup:false` passed with snow cleared from depth `1` to `0` and worker contamination `0.0443520024`; the same reusable contract now also covers real `Clean` work on contaminated `Filth_Dirt`, with `TicksSinceThickened=601`, installed target `RimWorld.<>c__DisplayClass7_0::<MakeNewToils>b__1(System.Int32 delta)`, filth contamination `0.64`, `filthTransfer=0.0416`, destroyed filth after 26 ticks, and worker contamination `0.026624`; `zombieland/contamination_clear_pollution_contract` now cleanly reports the hard dependency gate. A local `ZLTestPollution` fixture supplied `Wastepack` and `ClearPollution`, but the loaded `ZL_CE_EMPTY_00` runtime still returned `success=true`, `skipped=true`, `wastepackDefAvailable=true`, `clearPollutionJobDefAvailable=true`, `biotechActive=false`, because RimWorld `PollutionGrid.SetPolluted` refuses to mutate cells unless Biotech is active. Warning-or-higher logs were empty after the rebuilt contract skipped before calling `Pollute`.
- The C4 save `ZL_Contamination_Persistence_25_cleaning_environment.rws` was created and reloaded. Generic readback confirmed entry cell `(100,100)` at `0.8`, fire cell `(104,100)` at `0.292`, snow-cleared cell `(101,100)` at `0.72`, entry pawn `Thing_Human6898` at `0.009139201` with contamination need/hediff, snow worker `Thing_Human6903` at `0.0443520024` with contamination need/hediff, and component `Thing_ComponentIndustrial6901` at `0.292`. `rimbridge/list_logs` returned no warning-or-higher entries for the C4 run.
- `C5-player-effects` is covered by live evidence from 2026-05-29 for active player-effect persistence. `zombieland/contamination_rest_comfort_contract` passed after fixing null cell-target handling in `ContaminationManager.UsesCellBackedContamination`: pawn contamination reached `0.00352` and ground fell to `0.79648`, matching `restEqualize`. `zombieland/contamination_melee_equalize_contract` passed with real melee damage and expected attacker/target contamination `0.132800013`/`0.667199969`. `zombieland/contamination_tending_contract` passed with medicine stack consumption and expected doctor/patient contamination `0.4`/`0.18`.
- Active C5 mental effects: `zombieland/contamination_sleepwalk_contract` passed, waking the bed occupant into `Flee`; `zombieland/contamination_breakdown_contract` passed and kept `ContaminationJobBreakdown`; `zombieland/contamination_hoard_driver_flow_contract` passed pickup/drop flow and kept `ContaminationJobHoard` through the 30-tick window. Custom job-driver `ExposeData()` coverage was added for hallucination, mimic, sleepwalk, and hoard fields before the final save-load check.
- The C5 save `ZL_Contamination_Persistence_30_player_effects.rws` was created and reloaded after the scribe fix. `zombieland/read_contamination_effect_state` confirmed three active contamination jobs after reload and after 30 ticks: sleepwalker `Thing_Human6899` kept `ContaminationJobSleepwalk` with `waitUntil=5252`; breakdown pawn `Thing_Human6905` kept `ContaminationJobBreakdown` and continued pathing toward `(123,164)`; hoarder `Thing_Human6958` kept `ContaminationJobHoard`, `state=moveToThing`, selected `Thing_Silver6957`, and continued pathing. `rimbridge/list_logs` returned no warning-or-higher entries for the final non-hallucination C5 run.
- C5 focused failures were resolved on 2026-05-29 after the broader save-load run. `zombieland/contamination_hallucination_contract` passed after avoiding recursive path callbacks and making ghost motes use offscreen-safe static motes; it kept `ContaminationJobHallucination`, ran the 30-tick source-derived loop, and moved the ghost. `zombieland/contamination_mimic_contract` passed after the fixture gained a second farther victim; it kept `ContaminationJobMimic`, switched tracked victim, applied `ZombieScare`, and started `Flee` on the scared victim. `zombieland/contamination_hoard_pather_failure_contract` passed after the hoard driver no longer starts paths inside pather callbacks and no longer calls `StopDead()` on failure; the follow-up read advanced 360 real ticks with the pawn still in `ContaminationJobHoard`, first moving to silver and then carrying it back to storage.
- Final combined C5 run: after adding a persisted assigned-bed reference to `JobDriver_ContaminationHoard` and filtering unsaved dummy rejected things from `rejectedThings` serialization, GABS loaded `EMPTY`, ran hoard pather-failure, hallucination, mimic, sleepwalk, and breakdown in one map, saved `ZL_Contamination_Persistence_30_player_effects.rws`, reloaded it, and read state with `zombieland/read_contamination_effect_state`. Immediately after reload all five active jobs were present: `ContaminationJobHoard`, `ContaminationJobHallucination`, `ContaminationJobMimic`, `ContaminationJobSleepwalk`, and `ContaminationJobBreakdown`. After 30 ticks they all remained active. After another 240 ticks, hoard had `roomCellCount=25`, `state=moveToStorage`, and carried `Thing_Silver6947`; hallucination kept `ghostPresent=true`; mimic remained in `ContaminationJobMimic`; sleepwalk stayed in its `waitUntil=5323` window; breakdown continued pathing. `rimbridge/list_logs` returned no warning-or-higher entries for the final save-load run.
- `C6-ui-quest` is covered for generation, UI persistence, and accepted success by live evidence from 2026-05-29. RimWorld 1.6 decompiler checks confirmed the patched UI targets still have the expected shapes: `Thing.SpecialDisplayStats()`, `Widgets.ThingIcon(Rect, Thing, Single, Nullable<Rot4>, Boolean, Single, Boolean)`, `MainTabWindow_Quests.DoRow` with `Mathf.Max(quest.challengeRating, 1)`, `MouseoverReadout.MouseoverReadoutOnGUI` with `UI.MouseCell()`, glow label, and `LabelMouseover`, `InspectPaneUtility.PaneWidthFor(IInspectPane)`, `InspectPaneFiller.DoPaneContentsFor(ISelectable, Rect)` with `DrawHealth(row, thing)`, and `BeautyDrawer.DrawBeautyAroundMouse()`.
- Generic bridge operations first proved the visual path without custom code: from `EMPTY`, `rimworld/spawn_thing` created `Steel x25`, `zombieland/write_contamination_state` set both steel and cell `(99,100)` to `0.65`, `rimworld/set_hover_target` hovered the cell, and screenshot `zl_c6_hover_contaminated_steel.png` showed `Steel x25, 65.00 % contaminated` plus `Lit (90%) Contaminated (65.00 %)`.
- The reusable `zombieland/contamination_ui_quest_contract` then prepared a screenshot-ready fixture from `EMPTY`: selected contaminated `Thing_Steel6898` at `(100,100)`, set overlay enabled, and returned `contaminationStatPresent=true` with stat entry `Zombie Contamination` = `65.00%`; patch-helper semantics returned `Steel x25, 65.00 % contaminated` and `Lit (90%) Contaminated (65.00 %)`. Screenshot `zl_c6_contamination_ui_fixture.png` showed the inspect-pane `65.00 % contamination` bar, selected steel icon overlay, and green contamination overlay marker on the map. `rimbridge/list_logs` returned no warning-or-higher entries for the bounded C6 visual run.
- Added 2026-06-14 player-save right-click smoke from `RightClickAndPhase2Breaking.rws` for log ref A20CBD46. The failing path was `Widgets_ThingIcon_Patch` indexing `Constants.thingIconTextures` from an unclamped FloatMenu alpha. After clamping the alpha-derived slot, the provided save loaded visual-ready, Akgul was selected, a right-click context menu was opened on Bellerose, and warning-or-higher logs after the existing load warnings were empty. The bridge did not expose a mouse-drag-off-active-area primitive, so this validates the same right-click FloatMenu/ThingIcon path rather than the exact drag-away gesture.
- A 2026-05-31 C6 inspect-width rerun extended the same reusable contract after decompiling `InspectPaneUtility.PaneWidthFor(IInspectPane)`. From `EMPTY`, the contract selected a temporary clean `Steel x25` and the contaminated fixture `Steel x25`; both measured `vanillaBaseWidth=432`, patched `PaneWidthFor=578`, and `addedWidth=146`. Generic `rimworld/get_ui_layout` confirmed the live `MainTabWindow_Inspect` surface width was `578` and still displayed the `65.00 % contamination` bar. The temporary clean probe at `(101,100)` was destroyed afterward, and `rimbridge/list_logs minimumLevel=warning` returned no entries.
- A 2026-05-31 C6 clean/contaminated comparison rerun used the same reusable contract rather than adding a new endpoint. Source/decompiler checks confirmed `Thing.SpecialDisplayStats()` remains a general stat-yield path, `MouseoverReadout.MouseoverReadoutOnGUI()` still reads `UI.MouseCell()`, glow labels, and `LabelMouseover`, and `InspectPaneFiller.DoPaneContentsFor()` still calls `DrawHealth(row, thing)`. From `EMPTY`, the contract compared clean `Steel x25` at `(101,100)` against contaminated `Steel x25` at `(100,100)`: clean stat was `0.00%`, clean thing mouseover was `Steel x25`, clean glow mouseover was `Lit (90%)`, contaminated stat was `65.00%`, contaminated thing mouseover was `Steel x25, 65.00 % contaminated`, and contaminated glow mouseover was `Lit (90%) Contaminated (65.00 %)`. Both clean and contaminated selections kept the expected patched inspect-pane width (`432 -> 578`). The temporary fixture was cleared by reloading `EMPTY`, and `rimbridge/list_logs minimumLevel=warning` returned no entries before cleanup and after reload.
- The final clean-negative C6 UI polish pass used only generic tools from `EMPTY.rws`. `zombieland/spawn_pawn_fixture` created a clean player pawn `Thing_Human6898` and a clean human corpse `Thing_Corpse_Human6913`. `rimworld/select_pawn` plus `rimworld/get_selection_semantics` showed the clean pawn selected in the normal inspect pane with ordinary pawn tabs and no contamination-specific inspect string, while screenshot `/Users/ap/Library/Application Support/RimWorld/Screenshots/zl_c6_clean_pawn_negative_ui.png` showed the expected `0.00 % contamination` bar and no green icon/overlay artifact. Clean hover screenshots `/Users/ap/Library/Application Support/RimWorld/Screenshots/zl_c6_clean_pawn_hover_negative_ui.png` and `/Users/ap/Library/Application Support/RimWorld/Screenshots/zl_c6_clean_corpse_hover_negative_ui.png` showed plain pawn/corpse mouseover labels and clean glow text (`Lit (90%)`) without contamination suffixes or overlay labels. `rimworld/clear_hover_target` and `rimworld/load_game_ready EMPTY` restored the baseline, and warning-or-higher logs remained empty.
- A 2026-05-31 C6 beauty-overlay rerun kept the same reusable fixture and added the missing Shift-key visual check after decompiling `BeautyDrawer.BeautyDrawerOnGUI()`, `ShouldShow()`, and `DrawBeautyAroundMouse()`. From `EMPTY`, `zombieland/contamination_ui_quest_contract` selected contaminated `Steel x25` at `(100,100)`, set `showBeauty=true`, and generic bridge operations fixed hover on that cell. Screenshot `zl_c6_beauty_shift_baseline.png` showed vanilla beauty labels around the hover cell, while `zl_c6_beauty_shift_held.png` was captured with Shift held and showed only the Zombieland contamination label `65.0` over the contaminated stack. `rimbridge/list_logs minimumLevel=warning` returned no entries before cleanup and after reloading `EMPTY`. BrrainzTools currently lacks a hold-modifier-during-capture primitive, so this visual probe used System Events only to hold and release Shift around the bridge screenshot.
- A 2026-05-31 play-settings toolbar rerun resolved the remaining direct toggle-click patch polish after decompiling `PlaySettings.DoPlaySettingsGlobalControls(WidgetRow, bool)`, `PlaySettings.DoMapControls(WidgetRow)`, and `WidgetRow.ToggleableIcon(ref bool, Texture2D, string, SoundDef, string)`. From the loaded settings fixture, visible-pixel evidence clicked the appended rightmost Zombieland play-settings icon. Screenshot `zl_playsettings_contamination_toggle_off.png` shows the icon flipped green-check to red-X while the vanilla wrapped controls kept the same positions, and `zl_playsettings_contamination_toggle_restored.png` shows the icon restored. `rimbridge/list_logs minimumLevel=warning` returned no entries. BrrainzTools could capture and crop the Unity window, but its `--press-at` path requires AX pressable elements and Unity does not expose these icon pixels that way, so the click used a temporary CoreGraphics/System Events coordinate fallback.
- The stale 1.4 `Util_TransportShip_Pickup` dependency was replaced by `ZombieLand.QuestNode_CreateDecontaminationPickupTransporter`, which creates the Core-safe `ZombieLand_DecontaminationTransportPod` thing with `CompTransporter` and a dedicated `QuestPart_DecontaminationPickupTransporter`. This avoids DLC shuttle code in the active Core-only configuration while keeping the legacy target signals: pickup `Destroyed`, `SentUnsatisfied`, and loaded-colonist `SentSatisfied`. From `EMPTY`, `zombieland/contamination_ui_quest_contract` returned `success=true`, `pickupPathAvailable=true`, `oldPickupUtilityDefPresent=false`, generated `Decontamination Offer`, and found the expected parts: `QuestPart_DecontaminationPickupTransporter`, `QuestPart_DecontaminateColonists`, and `QuestPart_TransporterPawns_TendWithMedicine`.
- The same C6 run proved challenge-rating UI semantics: `decontaminationMax.value=0` while an ordinary quest preserved `ordinaryMax.value=3`. After saving `ZL_Contamination_Persistence_30_quest.rws` and reloading it, the read-only contract stayed green with `decontaminationQuestPresent=true`, contaminated `Steel x25` still at `(100,100)`, and no new quest creation. The Quests tab layout showed row text `Decontamination Offer` with expiry `7d` and no rating text; screenshot `zl_c6_decontamination_quest_tab.png.png` captured the tab. `rimbridge/list_logs` returned no warning-or-higher entries for the final generation/UI C6 run.

- Accepted C6 success is covered by live evidence from `EMPTY` after replacing the missing 1.4 pickup shuttle utility with `QuestNode_CreateDecontaminationPickupTransporter` and `QuestPart_DecontaminationPickupTransporter`. The first accepted-flow attempt proved why vanilla `ThingDefOf.Shuttle` is not viable in the active Core-only configuration: a custom `TransportShipDef` fixed the `ShipJob_Arrive.TryStart()` null dereference, but `CompShuttle.PostSpawnSetup()` still logged the expansion-system error for `Shuttle`. The Core-safe pickup pod path uses `ZombieLand_DecontaminationTransportPod` with `CompTransporter` and no shuttle/DLC comp. `zombieland/contamination_accepted_quest_contract` accepted the quest, advanced `3500` ticks to pod arrival, loaded one colonist into the real transporter container, observed `partSentSatisfied=true`, forced the treatment return timer, and ended with `questState=EndedSuccess`; `rimbridge/list_logs` returned no warning-or-higher entries.
- Accepted C6 legacy outcome branches are covered by live evidence from fresh `EMPTY` loads on 2026-05-30. `outcomeMode=destroyPickup` accepted the quest, advanced `3500` ticks to pod arrival, destroyed the spawned `ZombieLand_DecontaminationTransportPod`, and ended with `questState=EndedFailed` while the treatment part stayed `NeverEnabled`. `outcomeMode=sendUnsatisfied` sent the pickup target signal and ended with `questState=EndedFailed` while the pod remained spawned. `outcomeMode=killSentPawn` loaded one colonist, observed `partSentSatisfied=true`, killed the sent pawn while away, and ended with `questState=EndedSuccess`, matching the RimWorld 1.4 `ColonistsDied` quest outcome. `QuestPart_DecontaminateColonists.Notify_PawnKilled` now ignores unrelated pawn deaths and only emits the branch signal for the sent subject. `rimbridge/list_logs minimumLevel=warning` returned no entries after the branch run.

Assertions:
- Serialized cell and thing contamination values match before/after reload within defined tolerance.
- Handoff chains preserve or clear contamination according to code rules.
- Need/hediff/effect/mental jobs progress and clear as expected.
- Quest part can start and remains valid across save-load.
- Log summary contains no Scribe node, component, job driver, or quest exceptions.

Artifacts:
- Before/after reload contamination state dump per runtime batch.
- Save files named by the batch table above.
- Deduped log summary.

Completion:
- Covered when every runtime batch above passes through a save-load cycle and any remaining untested row in `TEST_PATCH_AUDIT.md` has an explicit reason such as unavailable DLC/mod setup or obsolete disabled code.

## S-Defense-Room

Goal: prove the main player defensive tools work together: shocker, thumper, chainsaw, wall push, doors, power, repairs, and colonist combat/work.

Prior evidence:
- `fb618b6 Add zombie shocker bridge smoke`
- `ca51d4f Add thumper impact bridge smoke`
- `add9e67 Add chainsaw equip toggle bridge smoke`
- `3533ac7 Add chainsaw slaughter bridge smoke`
- `09cf58c Fix chainsaw repair workgiver`
- `18aa8dc Cover chainsaw pressure drop`
- `394e740 Optimize chainsaw ticking`
- `4f4a288 Recheck chainsaw contracts`
- `0cefc00 Restore thumper shockwave effect`
- `bddf65f Align thumper wave origin with damage`
- `895b05b`, `3b04020`, `2288209`, `7ff08fd` for wall push.

Source checks:
- Zombieland: `Source/Chainsaw.cs`, `Source/ZombieThumper.cs`, `Source/ZombieShocker.cs`, `Source/JobDriver_ZapZombies.cs`, `Source/JobDriver_FixBrokenChainsaw.cs`, `Source/CompActivatable.cs`, `Source/CompBreakable.cs`, `Source/PlaceWorker_ZombieShocker.cs`, `Source/WorkGivers.cs`.
- RimWorld 1.6 decompiler: power net connection/update, building placement, gizmo generation, job driver toil lifecycle, building damage.

Fixture:
- Generated save: `ZL_Defense_Room_base.rws`.

Setup:
- Small powered base room with battery/conduit, shocker, thumper, door, wall segment, repair resources, chainsaw-equipped colonist, and zombie group outside.

Runtime:
- Toggle shocker and thumper gizmos.
- Step attack wave through door/wall pressure.
- Use chainsaw in combat and against a building.
- Damage/break chainsaw or building and run repair/fix workgiver.
- Save with devices active, reload, continue.

Assertions:
- Placement/power/fuel/battery gates reject and accept correctly.
- Shocker affects room/zombies and consumes or depends on power as designed.
- Thumper visual wave origin matches damage origin.
- Chainsaw state, pressure, damage, and repair persist.
- Wall push warnings and damage occur only under gate conditions.
- Log summary contains no gizmo, comp, power-net, or job-driver exceptions.

Artifacts:
- State dump for devices before/after reload.
- Screenshot only for thumper wave/shocker effect if visual check is needed.

Completion:
- Covered when one room scenario exercises all devices and persists their state.

Current runtime evidence:
- Partial coverage added 2026-05-30 with reusable `zombieland/defense_room_state`, a scenario-level setup/read tool rather than another single-behavior smoke. RimWorld 1.6 decompiler checks confirmed the relevant current members before implementation: `PowerNetManager.UpdatePowerNetsAndConnections_First()`, `CompPowerTrader.PowerOn`, `CompPowerTrader.PostExposeData()`, and `ThingWithComps.GetGizmos()`.
- From `EMPTY`, `zombieland/defense_room_state setupFixture:true activateChainsaw:true advanceTicks:120` created a durable powered defense fixture and returned `success=true`: shocker `Thing_ZombieShocker6914` was on a wall, had a valid 9-cell room, `PowerOn=true`, one battery in its power net, and build-copy/reconnect gizmos; thumper `Thing_Thumper6918` was fueled `150 / 250`, switchable active, `intensity=0.25`, `intervalTicks=208`, `radius=12`, and exposed forbid, on/off, target-fuel, auto-refuel, and settings gizmos; equipped chainsaw `Thing_Chainsaw6923` was held by drafted colonist `Thing_Human6920`, activated through its command-action gizmo, running, and consuming fuel; spawned chainsaw `Thing_Chainsaw6925` was broken and tracked by `BrokenManager`; battery `Thing_Battery6917` held about `600 / 600 Wd`; placement checks accepted the valid shocker wall cell and rejected inside-room/no-room cells.
- Shocker non-wiping placement coverage was added on 2026-05-30 after decompiling `GenSpawn.SpawningWipes`, where vanilla wipes edifice-over-edifice placements. The same `zombieland/defense_room_state setupFixture:true` read now records the installed Zombieland prefix on `GenSpawn.SpawningWipes`. From `EMPTY.rws`, the shocker fixture cell `(100,100)` contained both `Wall` (`Thing_Wall6905`) and `ZombieShocker` (`Thing_ZombieShocker6914`), the shocker reported `OnWall=true`, direct patched `SpawningWipes(ZombieShocker, Wall, true)` and `SpawningWipes(ZombieShocker, Door, true)` both returned false, while the control `SpawningWipes(Wall, Wall, true)` returned true. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- The fixture was saved as `ZL_Defense_Room_base.rws`, reloaded, advanced 120 ticks, and read again with `zombieland/defense_room_state setupFixture:false`. Post-reload state stayed green: `poweredShocker=true`, `activeThumper=true`, `equippedRunningChainsaw=true`, `brokenChainsaws=1`, `brokenManagerCount=1`, `batteryCount=1`, one colonist, and one zombie. The equipped chainsaw remained unspawned but referenced to its pawn, running, drafted, and fuel-bearing; the broken ground chainsaw remained spawned, broken, allowed, fueled, and tracked. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Post-reload action checks added 2026-05-30 using the same generic tool from fresh `ZL_Defense_Room_base.rws` reloads. `actionMode:zapShocker` ran the real `ZapZombies` job, hit at tick 309, set `paralyzedUntilAfter=321719`, and emitted 8 zap motes. `actionMode:repairChainsaw` ran the real `FixBrokenChainsaw` job, completed at tick 1000, repaired the saved broken chainsaw, deregistered it from `BrokenManager`, and consumed one component from the stack (`3 -> 2`). `actionMode:chainsawBuilding` used the saved drafted colonist with a running chainsaw against an adjacent spawned wall and zombie; the wall took 80 HP damage, the chainsaw dropped/spawned, HP dropped `500 -> 480`, fuel decreased, the chainsaw became broken, and `BrokenManager` tracked it. `actionMode:thumperImpact` forced an active thumper impact from the saved fixture; fuel dropped `150 -> 149`, impact landed at tick 145, `lastImpactTicks` advanced `5159 -> 5305`, and distance falloff hit walls for 2 HP at range 3, 1 HP at range 12, and 0 HP at range 22. `rimbridge/list_logs minimumLevel=warning` returned no entries after these runs.
- Thumper infestation coverage added 2026-05-30 using the same `zombieland/defense_room_state` scenario tool with `actionMode:infestationThumper` from `ZL_Defense_Room_base.rws`. The probe checked the installed RimWorld 1.6 `InfestationCellFinder.CalculateLocationCandidates` and `GetScoreAt` shapes, temporarily created a thick-roofed vanilla-positive candidate around saved thumper `Thing_Thumper6918`, and restored the roof state before returning. Candidate cell `(110,99)` scored `13.9857321` with the thumper inactive, generated `148` inside-radius candidates, then scored `0` with the active thumper and generated `0` inside-radius candidates. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Door/wall pressure coverage added 2026-05-30 after a RimWorld 1.4/1.6 decompiler comparison found that `JobDriver_AttackStatic.MakeNewToils()` moved the tick delegate from `<MakeNewToils>b__1` in 1.4 to `<MakeNewToils>b__2(int delta)` in installed 1.6. `Source/Patches.cs` now selects the generated tick delegate by its one-`int` parameter signature instead of hardcoding the stale closure name. From a fresh `ZL_Defense_Room_base.rws` reload, `actionMode:wallDoorPressure` passed: wall push started from `(102,108)`, crossed wall `(103,108)`, landed at `(104,108)` on tick 102, left the wall intact, cleared the constructed roof at the destination, and produced exactly one `DangerousSituation` letter for a home-area wall. The door half started a real zombie `AttackStatic` job against closed door `Thing_Door6935`, confirmed Harmony had patched installed 1.6 target `Verse.AI.<>c__DisplayClass5_0::<MakeNewToils>b__2(System.Int32 delta)` with owner `net.pardeike.zombieland`, opened the door, and observed the zombie leave `AttackStatic` on the next tick. A follow-up sound-recorder pass from `EMPTY.rws` wrapped `zombieland/wall_push_over_wall_contract` and captured `[20:54:04] OneShot: WallPushing - (World from (100, 0, 100), Map_0)` while logs stayed clean. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Turret fuel-consumption coverage was added on 2026-05-30 after decompiling `CompRefuelable.ConsumeFuel`, which subtracts the input amount and clamps fuel to zero. `zombieland/defense_room_state setupFixture:true actionMode:turretFuel` from `EMPTY.rws` returned `success=true`, recorded the installed Zombieland prefix on `CompRefuelable.ConsumeFuel`, set `reducedTurretConsumption=0.5`, and called the real patched method on transient fixtures. A `Turret_MiniTurret` consumed `2` fuel from input `4`, while a non-turret `ZombieThumper` control consumed the full `4`; a low-fuel turret consumed from `1` to `0` without going negative. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Float-menu zap/rope coverage was added on 2026-05-31 after decompiling installed RimWorld 1.6 `FloatMenuMakerMap.GetOptions(List<Pawn>, Vector3, out FloatMenuContext)`. The existing `zombieland/defense_room_state actionMode:floatMenu` path now calls the real patched method from `ZL_Defense_Room_base.rws` instead of introducing a separate one-off endpoint. It recorded the installed Zombieland postfix on `FloatMenuMakerMap.GetOptions`; a reachable/reservable colonist clicking powered valid-room shocker `Thing_ZombieShocker6914` received one enabled `Start zombie shocker` option; clicking a temporary confused/paralyzed zombie received one enabled `Capture this zombie` option; clicking a temporary ordinary non-confused zombie produced no rope option; and an empty selected-pawn list produced no Zombieland custom options. The temporary zombies were destroyed, the fixture was reloaded, and `rimbridge/list_logs minimumLevel=warning` returned no entries before and after reload.
- Shocker/thumper audio evidence was added on 2026-05-31 with the generic `zombieland/sound_events_state` recorder, not a new special-purpose endpoint. Static/decompiler checks confirmed installed RimWorld 1.6 still calls `Effecter.children[i].SubTrigger(A, B, overrideSpawnTick)`, leaves `SubEffecter.SubEffectTick` virtual, and only adds an optional `force=false` parameter to `SubEffecter.SubTrigger` compared with RimWorld 1.4. From `ZL_Defense_Room_base.rws`, `defense_room_state actionMode:zapShocker` returned `success=true`, paralyzed zombie `Thing_Zombie6928`, emitted 16 zap motes, and the recorder captured direct sound events for `ShockingRoom`, `ShockingZombie`, `ZombieTracking`, `ThumperImpact`, `ThumperClang`, `ThumperRelease`, `ThumperConnecting`, `ThumperLiftingStart`, `ThumperLifting` sustain spawn/end, and `ChainsawIdle` sustain end. A later source-guided pass enabled the recorder before loading the same save, exercising `ZombieThumper.SpawnSetup`, and captured `SustainerSpawn: ThumperOperating` from thumper `Thing_Thumper6918`. `rimbridge/list_logs minimumLevel=warning` returned no entries and the recorder was restored to `writeSoundEventsRecord=false`.
- Player-facing visual/audio evidence was completed on 2026-05-31 without adding another endpoint. The static/decompiler refresh confirmed installed RimWorld 1.6 `Effecter.EffectTick(TargetInfo, TargetInfo)` still dispatches child `SubEffectTick`, and `SubEffecter.SubEffectTick` plus `SubTrigger(TargetInfo, TargetInfo, int, bool force=false)` remain virtual, so Zombieland's shocker/thumper effect paths still attach at the intended layer. From `ZL_Defense_Room_base.rws`, a fresh `defense_room_state actionMode:zapShocker` pass again returned `success=true`, paralyzed zombie `Thing_Zombie6928`, and emitted 16 zap motes. The generic recorder captured `ShockingZombie`, `ShockingRoom`, `ThumperImpact`, `ThumperClang`, `ThumperRelease`, `ZombieTracking`, `ThumperConnecting`, `ThumperLiftingStart`, `ThumperLifting` sustain spawn/end, `ThumperOperating` sustain spawn during load, and `ChainsawIdle` sustain end. Screenshot `/Users/ap/Library/Application Support/RimWorld/Screenshots/zl_defense_shocker_zap_visual_audio__cell_rect.png` shows the powered shocker room, blue zap arcs on the paralyzed zombie, colonist, active thumper, battery and room layout. From `EMPTY.rws`, existing `zombieland/thumper_visual_wave_damage_contract` returned `success=true`, proved active smoke dust with `Smoke_thin`, `Particles/Standard Surface`, expanding radius `2 -> 23`, synchronized start speed, enabled color-over-lifetime, expected 90-degree transform, and distance falloff damage, then screenshot `/Users/ap/Library/Application Support/RimWorld/Screenshots/zl_defense_thumper_visual_wave_contract__cell_rect.png` captured the thumper and expanding smoke wave across target walls. `rimbridge/list_logs minimumLevel=warning` returned no entries after the visual/audio pass, and `EMPTY.rws` was reloaded afterward.
- Generic selected-gizmo coverage added 2026-05-30 from `ZL_Defense_Room_base.rws` without adding a Zombieland endpoint. `rimworld/list_colonists` found drafted chainsaw pawn `Thing_Human6920`, `rimworld/select_pawn` selected it, and `rimworld/list_selected_gizmos` returned six selection-scoped gizmos including the equipped running chainsaw's enabled `Command_Action` with hotkey `Misc6` and description `Turn chainsaw off to conserve fuel`. This proves the `Pawn_EquipmentTracker.<GetGizmos>g__YieldGizmos|30_0` patch exposes chainsaw controls through RimWorld's normal selected-pawn gizmo path, not only through direct `Chainsaw.GetGizmos()` bridge reads. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Focused chainsaw equipment coverage added 2026-05-30 with existing `zombieland/chainsaw_equip_toggle` from `EMPTY.rws` after decompiling installed RimWorld 1.6 `Pawn_EquipmentTracker.EquipmentTrackerTick()`, `Pawn_DraftController.Drafted`, `Pawn_PathFollower.SetupMoveIntoNextCell()`, `CompProperties_Refuelable.SpecialDisplayStats()`, and `Gizmo_SetFuelLevel.GizmoOnGUI()`. Vanilla equipment ticking only ticks equippable verbs for non-normal ticker equipment, so the Zombieland postfix owns equipped chainsaw ticking. The movement extension equipped chainsaw `Thing_Chainsaw6901`, started a real path while the chainsaw was equipped but not swinging, and proved the path-setup postfix rotated the chainsaw toward RimWorld's selected next cell: position `(100,100)`, `nextCell=(99,100)`, `expectedAngle=270`, `chainsaw.angle=270`, `angleDelta=0`, `Moving=true`, and `MovingNow=true`. The stats extension directly enumerated the chainsaw refuelable special display stats and returned exactly one `Weapon_Melee` entry labeled `Fuel` with value `100`; the vanilla pass-through control used `Turret_MiniTurret` and returned the expected `Building` entry `Shots before rearm` with value `60` instead of a melee replacement. The fuel-gizmo guard found the installed `Gizmo_SetFuelLevel.GizmoOnGUI` prefix, proved it returns false for a null refuelable and true for the real chainsaw refuelable, and confirmed the normal fuel gizmo stayed visible with width `160`, fuel `100`, target fuel `100`, and `HasFuel=true`. The same live run started the chainsaw through its enabled command-action gizmo, advanced 20 real ticks, and proved fuel changed `100 -> 99.93347` while all samples stayed running and `inactiveCounter` advanced to `40`. It then set `Drafted=false` and proved `runningAfterUndraft=false` while the chainsaw remained equipped/unspawned and unbroken. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Vanilla melee suppression coverage added 2026-05-30 by extending the existing `zombieland/chainsaw_slaughter_zombie` contract from `EMPTY.rws`. After decompiling installed RimWorld 1.6 `Pawn_MeleeVerbs.TryMeleeAttack`, the live run first directly called `TryMeleeAttack` before the chainsaw slaughter tick: the chainsaw-equipped human returned `false` against the adjacent normal zombie, the zombie injury stayed `0 -> 0`, and the zombie remained live. Spawned `ZombieSpitter` and `ZombieSymbiant` also each returned `false` from `TryMeleeAttack` against the chainsaw pawn. The same contract then activated chainsaw `Thing_Chainsaw6901`; tick 1 killed and destroyed the normal zombie, added one victim head, reduced chainsaw HP `500 -> 499`, and consumed fuel `100 -> 99.99234`. The first pass exposed a fixture cleanup issue where temporary special zombies left on-map produced repeated `DynamicDrawManager.DrawSilhouettes` null-reference logs; the contract now destroys the temporary spitter/symbiant after collecting evidence, and the clean rerun produced no warning-or-higher logs.
- Float-menu melee-action coverage added 2026-05-30 in the same `zombieland/chainsaw_slaughter_zombie` contract after decompiling installed RimWorld 1.6 `FloatMenuUtility.GetMeleeAttackAction` and the drafted-attack provider wrapper. Before the chainsaw toggle, the equipped but stopped chainsaw pawn returned a vanilla melee action against the adjacent normal zombie with empty fail string. A temporary ordinary control colonist at `(92,100)` also returned a vanilla melee action with empty fail string and `violentDisabled=false`. After the same contract toggled the chainsaw on, the running chainsaw pawn returned no melee action while `runningAfterToggle=true`. The normal zombie was still killed through the chainsaw slaughter path on tick 1, and `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Rotation-tracker swing coverage added 2026-05-30 in the same `zombieland/chainsaw_slaughter_zombie` contract after decompiling installed RimWorld 1.6 `Pawn_RotationTracker.UpdateRotation`. The probe set the drafted chainsaw pawn to `Rot4.North` (`0`), set `chainsaw.swinging=true`, called `UpdateRotation`, and observed the pawn stay North. It then restored `chainsaw.swinging=false`, reset the pawn to North, called `UpdateRotation` again, and observed vanilla drafted fallback set the pawn to `Rot4.South` (`2`). The contract restored `swinging=false` before the float-menu and slaughter checks, still killed the adjacent zombie through the chainsaw path on tick 1, and `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Chainsaw draw-equipment visual coverage added 2026-06-01 without adding a new endpoint. After decompiling installed RimWorld 1.6 `PawnRenderUtility.DrawEquipmentAndApparelExtras(Pawn, Vector3, Rot4, PawnRenderFlags)`, the existing `zombieland/chainsaw_slaughter_zombie` contract from `EMPTY.rws` staged the focused visual state: actor `Thing_Human6898` at `(100,100)` had equipped chainsaw `Thing_Chainsaw6901`, `running=true`, `swinging=true`, `angle=22.5`, killed the adjacent normal zombie on tick 1, added one victim head, reduced chainsaw HP `500 -> 499`, and consumed fuel `100 -> 99.99234`. Generic `rimworld/screenshot_cell_rect` then captured `/Users/ap/Library/Application Support/RimWorld/Screenshots/zl_chainsaw_draw_equipment_slaughter_visual__cell_rect.png` and close crop `/Users/ap/Library/Application Support/RimWorld/Screenshots/zl_chainsaw_draw_equipment_slaughter_visual_close__cell_rect.png`, showing the running/swinging chainsaw pawn and slaughter/victim-head visual with no duplicate vanilla equipment draw. `rimbridge/list_logs minimumLevel=warning` returned no entries after the pass.
- Remaining work: no known S-Defense-Room gap remains in the core active-mod configuration. Add new work only for named variants such as optional-mod equipment interactions, new art/audio changes, or a deliberately broader mixed-combat defense-room endurance scenario.

## S-Incident-Threat

Goal: prove player-visible incident scheduling, spawn modes, letters, threat forecast, and zombie limit behavior.

Prior evidence:
- `2cc1beb Cover event special zombie spawning`
- `6dd03e1 Cover incident scheduling calculation`
- `aab7620 Cover incident alert wave letters`
- `2ef5967 Cover random zombie type weights`
- `f44b336 Cover child zombie generation gate`
- `c2dd3b5 Cover zombie faction world state`
- `b7d560e Cover incident infection and ticking budget`

Source checks:
- Zombieland: `Source/ZombieIncidents.cs`, `Source/TickManager.cs`, `Source/ZombieWeather.cs`, `Source/ZombieFreeEventManager.cs`, `Source/GameCondition_ZombieFreeEvent.cs`, `Source/Alerts.cs`, incident/letter/faction/game-condition defs.
- RimWorld 1.6 decompiler: `IncidentWorker.TryExecute`, letter stack API, map edge spawn helpers, room/fog APIs, weather/GUI tooltip path.

Fixture:
- Generated save: `ZL_Incident_Threat_base.rws`.

Setup:
- Configure deterministic incident interval and small zombie cap.
- Prepare cells for edge spawn, soft ground spawn, dark spawn, fogged-room spawn, and blocker replacement.

Runtime:
- Step enough real ticks for a scheduled incident.
- Force each spawn mode if waiting would be wasteful, but still run one natural scheduled incident.
- Toggle dynamic threat and zero-threat death settings.
- Run `actionMode:zombieFreeModeMatrix` to cover all four `useDynamicThreatLevel`/`zombieFreeEvents` combinations without advancing the fixture clock.
- Use `actionMode:pathingRegions` for the focused region/room rebuild pathing refresh proof.
- Save before forecast changes, reload, verify forecast and scheduling state.

Assertions:
- Letter text/type and target are correct.
- Spawn location respects selected mode and blocked areas.
- Special mix and child gates respect settings.
- Zombie cap is not exceeded.
- Threat forecast and tooltip match current settings.
- Initial grace always reports active suppression and 0% effective threat; its graph interval is silence-styled exactly when recurring silence is enabled.
- A later injected schedule window suppresses threat only when recurring silence is enabled, while constant threat remains 100% outside grace/silence.
- The mode-matrix contract restores the settings timeline, raw schedule, and any pre-existing silence game-condition duration; dynamic-off runs expose no threat forecast after a real GUI frame.
- Zero-threat behavior kills or preserves zombies according to settings.
- Zombie smart-wander pathing regions repopulate after a real region/room rebuild.

Artifacts:
- Incident state dump.
- Letter list.
- Screenshot of forecast tooltip if UI path is used.
- Deduped log summary.

Completion:
- Covered when one natural incident plus forced mode checks pass and persist through reload.

Current runtime evidence:
- Partial coverage added 2026-05-30 with reusable `zombieland/incident_threat_state`, a scenario-level setup/read/action tool. RimWorld 1.6 decompiler checks confirmed the incident hook targets before relying on runtime behavior: `IncidentWorker.TryExecute`, `IncidentWorker_Raid.TryExecuteWorker`, and `PawnGroupKindWorker.GeneratePawns`.
- From `EMPTY`, `setupFixture:true actionMode:read` created three capable armed colonists and prepared a deterministic all-over-map spawn field at `(100,124)`. The spawn field is intentional fixture setup: the baseline `EMPTY` map is reachable but all `SterileTile`, and all-over-map validation rejects removable top-layer/floor terrain while edge spawning does not. After the fixture edit, diagnostics reported 2,401 all-over valid spawn cells and 4,365 valid event spots. The fixture was saved as `ZL_Incident_Threat_base.rws`.
- From a fresh `ZL_Incident_Threat_base.rws` reload, `actionMode:all` returned `success=true`. The alert-wave matrix spawned four normal zombies from edges and produced one `ThreatSmall` `Zombie attack` letter, then spawned four normal zombies in all-over-map mode and produced one `ThreatSmall` `Direct attack of zombies` letter. Explicit event spawning matched all requested types: suicide bomber, toxic splasher, tanky operator, miner, electrifier, albino, dark slimer, healer, and normal.
- The same run covered the incident hook/faction slice: generated human incident pawns received a zombie bite, animals did not, and the `IncidentWorker.TryExecute` postfix reduced incident-generated bites to harmless; zombie-faction normal pawn generation returned a spawned Zombieland `Zombie`, while symbiant and spitter pawn kinds stayed on the vanilla generation path. The threat forecast contract toggled dynamic threat on/off and confirmed `ZombieWeather` forecast labels and disabled-threat fallback. The scheduler-driven wave forced the real scheduler calculation through `ZombiesRising.ZombiesForNewIncident`, executed `SpawnEventProcess`, spawned 14 zombies without exceeding the computed cap, and produced one `ThreatSmall` `Zombie attack` letter. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Spawn-mode and zero-threat coverage was added to the same rollup later on 2026-05-30. After rebuilding with `scripts/build-quiet.sh` at 0 warnings and 0 errors, a fresh `ZL_Incident_Threat_base.rws` reload passed `zombieland/incident_threat_state actionMode:all`. Ambient population checks forced `TickManager.IncreaseZombiePopulation` with `populationSpawnCounter=-1`: `AllTheTime/AllOverTheMap` spawned one zombie on soil with no removable top layer, `AllTheTime/FromTheEdges` spawned one zombie in an edge-reachable region, `WhenDark/AllOverTheMap` spawned one zombie on a roofed `PsychGlow.Dark` soil cell, and `InEventsOnly` spawned none. Fog checks in the same scenario covered fogged-door player entry spawning two room zombies while hostile entry did not, fog-blocker deconstruction spawning two room zombies and unfogging the room, and `DestroyMode.WillReplace` blocker destruction spawning no zombies and preserving fog. The scenario wrapper cleans temporary fog fixtures before final readback. Zero-threat checks ran the actual `Zombie.CustomTick` branch at threat level `0`: with `zombiesDieOnZeroThreat=true`, the sampled zombie took `405` injury severity and died at tick sample `548`; with the setting disabled, the comparison zombie took no injury and survived. The final rollup log check again returned no warning-or-higher entries.
- Raid-worker patch coverage was added to the same tool after RimWorld 1.6 decompiler inspection of `IncidentWorker_Raid.TryExecuteWorker(IncidentParms)`: the vanilla worker still generates raid info, sends the standard raid letter, makes lords, teaches weapon/shield concepts, and returns `true`. The bridge scenario temporarily probes `ZombiesRising.TryExecute` and then invokes the real installed `IncidentWorker_Raid.TryExecuteWorker` method with a zombie-faction `IncidentParms`. `EdgeWalkIn` observed `spawnHowType=FromTheEdges` inside `ZombiesRising.TryExecute`, returned `false` from the raid worker, and restored the sentinel `AllOverTheMap` mode afterward. `CenterDrop` observed `spawnHowType=AllOverTheMap`, returned `false`, and restored the sentinel `FromTheEdges` mode afterward. The probe prevents coroutine side effects, so the check ended with `newZombieCount=0` and `newLetterCount=0` while proving the call route and restore behavior.
- Raid-cadence regression coverage was added on 2026-07-02 as `zombieland/incident_threat_state actionMode:raidCadence`. The first deploy exposed a startup initializer bug in `ZombieStateHandler`: `EffecterDefOf.Mine.Spawn()` ran from a static field before the effecter def was safe in the isolated 1.6 loadout, throwing a fatal `NullReferenceException`; the mine effecter is now created lazily at the mining use site. After the paired mod/BridgeTools deploy and a fresh RimWorld quick-test colony, `actionMode:raidCadence` returned `success=true` in operation `op_bdaea68ac9c44c6aac5ddb08e5bac463`. The contract sampled vanilla `RaidEnemy.CanFireNow`, `StorytellerUtility.DefaultThreatPointsNow`, wealth, danger rating, hostile target cache contents, and deterministic `Storyteller.MakeIncidentsForInterval()` proposals at baseline, with controlled normal/spitter/symbiant zombies alive, during a forced zombie-free silence window, and after those zombies were killed into corpses/drops. Threat points stayed `35`, total wealth stayed `15994.8105`, Zombieland hostile target count stayed `0`, danger stayed `None`, and deterministic proposals matched (`ShipChunkDrop:1`, no raid/threat-big proposals) across all phases. The operation-specific warning-or-higher log check returned no entries; the only global warning-or-higher entries were RimBridge startup timing lines.
- Forecast UI coverage was added to the same tool after extracting the actual `GlobalControlsUtility.DoDate` patch's forecast formatter and geometry helpers. `actionMode:forecastUi` spawns one temporary zombie to force the zombie-count readout, enables dynamic threat, computes the same right-aligned readout rectangles used by the patch, and verifies the vanilla date rect, `1 Zombies` rect, `0% threat level` rect, and 720x320 tooltip rect are all on-screen and non-overlapping. It also opens a preview window that renders `ZombieWeather.GenerateTooltipDrawer`; `rimworld/take_screenshot` captured that window at `/Users/ap/Library/Application Support/RimWorld/Screenshots/zl_forecast_tooltip_preview__clip.png`, and visual inspection confirmed the tooltip renders the `Threat Forecast`, `Next 14 days`, and `Next 4 quadrums` graph sections.
- Pathing-region coverage was added on 2026-05-31 after decompiling `RegionAndRoomUpdater.CreateOrUpdateRooms()`, `RebuildAllRegionsAndRooms()`, and `TryRebuildDirtyRegionsAndRooms()`. From `EMPTY`, `zombieland/incident_threat_state actionMode:pathingRegions` returned `success=true` and recorded the installed Zombieland postfix on `Verse.RegionAndRoomUpdater.CreateOrUpdateRooms()`. The verifier reused the fog-room fixture builder to create a proper 25-cell room, observed an initial `zombiePathing` graph with 291 indexed valid regions, cleared `backpointingRegions` and `backpointingRegionsIndices` to zero, then called the real `RebuildAllRegionsAndRooms()` path. After the rebuild, the postfix repopulated the graph with 291 indexed valid regions: one root region, 290 child/backpointing regions, and valid `GetWanderDestination` samples through the door/normal-region parent chain. `rimbridge/list_logs minimumLevel=warning` returned no entries before cleanup, `EMPTY` was reloaded to discard the temporary room, and the follow-up warning-or-higher log check was also empty.
- Zombie-free silence coverage was added in 2026-06 through `zombieland/incident_threat_state` action modes `zombieFreeEvent`, `zombieFreeAmbientSound`, `zombieFreeOverlap`, `zombieFreeReview`, `zombieFreeSchedule`, `zombieFreeForecast`, and `zombieFreeHover`. These contracts cover the global `GameCondition_ZombieFreeEvent`, spitter leave-map start handling, ambient `ZombiesClosingIn` suppression during silence, schedule generation and difficulty/keyframe continuity, forecast rendering spans, overlap grouping for letters/game-condition duration, forced debug-window start-at-now behavior, temporary contract schedule restore, forecast enable/disable clipping across keyframes, and the forecast hover trigger on the `X Zombies` readout.
- The intended initial-grace/silence matrix is encoded in `actionMode:zombieFreeModeMatrix`: all four dynamic-threat/silence combinations keep initial grace at 0% with gameplay suppression active; the silence toggle controls whether that initial 0% interval is green silence styling and whether later scheduled windows activate; dynamic-off threat is 100% outside both; and all temporary settings/schedule/condition state restores. Live paired-deploy operation `op_ca322489b9e44ad7aa820442f0ab8338` passed every case without advancing the fixture clock, restoring the exact settings timeline, raw window schedule, next-cluster tick, and active condition duration. With dynamic threat off, `rimworld/get_ui_layout` captured the enabled `Zombie-free travel events` checkbox directly below `Days until zombies come` and a separate unchecked `Use dynamic threat level` control farther down; after closing settings, the real map UI frame contained no threat-level or threat-forecast label. Visual evidence is `/Users/ap/Library/Application Support/RimWorld/Screenshots/zl_silence_setting_below_initial_days__clip.png`; the complete Player.log error summary and post-startup warning-or-higher journal were empty.
- Zombie-faction world coverage was extended on 2026-05-30 from `EMPTY.rws` through `zombieland/zombie_faction_world_contract`: exactly one hidden zombie faction existed, zombie/player hostility stayed mutual, the installed `Faction.TryAffectGoodwillWith` prefix suppressed zombie-facing goodwill changes in both directions without relation mutation, and an ordinary outlander/player relation still changed goodwill from `0` to `15` both ways through vanilla.
- Current H coverage: scheduler-driven wave execution, cap accounting, edge/all-over alert letters, special-type event requests, incident infection reduction, zombie-faction pawn generation, zombie-faction goodwill suppression, ordinary goodwill pass-through, ambient spawn modes, fog-room spawn hooks, blocker replacement suppression, zombie pathing-region rebuild refresh, zero-threat branches, raid worker patch boundary, zombie-free silence events, and forecast tooltip UI/visual rendering. `zombieland/incident_threat_state actionMode:all` includes the non-window forecast UI geometry check and returned `success=true`; the latest faction-world/pathing/zombie-free contracts and follow-up `rimbridge/list_logs minimumLevel=warning` returned no entries.

## S-Hostility-Area-Workflow

Goal: prove the player-facing dangerous-area workflow and the surrounding avoidance/hostility behavior that is too broad for the older narrow contracts.

Prior evidence:
- `f651efa Add colonist avoidance interrupt smoke`
- `0416696 Add workgiver avoidance smoke`
- `0c6dacb Add avoid grid door and danger smoke`
- `d7f1a04 Cover zombie hostility rules`
- `6beb859 Cover zombie target cache filtering`
- `e4f9d64 Cover zombie area risk classifications`
- `a0302d6 Cover colonist outside area risk`

Source checks:
- Zombieland: `Source/Patches_Hostility.cs`, `Source/ZombieAreaManager.cs`, `Source/ZombieAvoider.cs`, `Source/WorkGivers.cs`, and the avoidance/workgiver patches in `Source/Patches.cs`.
- RimWorld 1.6 decompiler rows are tracked in `TEST_PATCH_AUDIT.md` under `hostility-static`, `area-manager-static`, and `patches-avoidance-doors-workgivers`.

Fixture:
- Generated save: `ZL_Area_Workflow_base.rws`.
- Area labels: `ZL_Area_Ignore`, `ZL_Area_ColonistInside`, `ZL_Area_ColonistOutside`, `ZL_Area_ZombieInside`, and `ZL_Area_ZombieOutside`.

Runtime:
- Use `zombieland/area_workflow_state` as the scenario-level setup/read tool.
- Open the real `RimWorld.Dialog_ManageAreas` when visual proof is needed, then capture it with `rimworld/take_screenshot`.
- Use `actionMode:behavior` for the combined area-assignment, dangerous-area warning, avoid-grid danger, drafted-exemption, player-forced exemption, and undrafted work-interruption proof.
- Use `actionMode:warning-ui` for the focused live `MessagesDoGUI`/`Message.Draw` warning ordering and message-offset proof.
- Use `actionMode:targeting` for the mixed player/enemy/animal target-choice proof. This action builds and destroys a transient combat fixture from the reusable area save; do not save the spawned target pawns as a persistent colony state.
- Use `actionMode:bug-reports` for the focused raider/zombie distance-cap and spitter-health regression proof before running broader targeting/stat matrices.
- Use `actionMode:ranged-projectiles` for the focused forced-miss/projectile-impact proof without rerunning the full mixed-targeting fixture.

Assertions:
- Additional allowed areas can be created and custom area order is not overwritten by `SortAreas()`.
- The manage-area constructor resets selected area, selected index, and scroll state before the scenario selects the target area.
- The real patched manage-area dialog renders Zombieland area rows, color-coded risk labels, selected-area content, and the `Show Zombie Risk` mode button.
- Area risk modes and colors persist through save-load.

Artifacts:
- Save file: `ZL_Area_Workflow_base.rws`.
- Save file: `ZL_Area_Workflow_behavior.rws`.
- Screenshot: `/Users/ap/Library/Application Support/RimWorld/Screenshots/zl_area_workflow_manage_areas__clip.png`.
- Screenshot: `/Users/ap/Library/Application Support/RimWorld/Screenshots/zl_area_workflow_danger_overlay__clip.png`.
- Screenshot: `/Users/ap/Library/Application Support/RimWorld/Screenshots/zl_area_warning_ui.png`.
- Deduped log summary.

Completion:
- Covered when the area-management workflow and the mixed-faction combat extensions both pass from reusable saves and produce no warning-or-higher logs.

Current runtime evidence:
- Added 2026-05-30 with `zombieland/area_workflow_state`. From `EMPTY`, `setupFixture:true openManageDialog:true` returned `success=true`: `AreaManager.CanMakeNewAllowed()` and `TryMakeNewAllowed` were true, `SortAreas()` preserved the custom order, the `Dialog_ManageAreas` constructor reset selected/scroll state, all five `ZL_Area_*` fixture areas had four active cells, and each non-ignore area reported the expected `AreaRiskMode` and label color.
- The tool opened the real `RimWorld.Dialog_ManageAreas`; `rimworld/get_screen_targets` reported it as the focused top dialog, and `rimworld/take_screenshot` captured `/Users/ap/Library/Application Support/RimWorld/Screenshots/zl_area_workflow_manage_areas__clip.png`. Visual inspection confirmed the Zombieland area list, selected `ZL_Area_ZombieInside`, the orange selected row, color sliders, and `Show Zombie Risk` button reading `Zombie inside`.
- The fixture was saved as `ZL_Area_Workflow_base.rws`, reloaded, then `zombieland/area_workflow_state setupFixture:false openManageDialog:false` returned `success=true` with all five areas, modes, colors, and active-cell counts intact. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Behavior coverage was added after RimWorld 1.6 decompiler checks confirmed the live shapes of `JobDriver.DriverTick`, `DangerUtility.GetDangerFor`, `JobGiver_ConfigurableHostilityResponse.TryGetAttackNearbyEnemyJob`, and `Pawn.Destroy`. From `ZL_Area_Workflow_base.rws`, `zombieland/area_workflow_state setupFixture:true actionMode:behavior` returned `success=true`: the colonist was assigned to `ZL_Area_ColonistInside`, the normal tracking zombie was in `ZL_Area_ZombieInside`, `ZombieAreaManager.pawnsInDanger` contained both entries, the avoid-grid cost at the colonist was `972`, normal danger was `Deadly`, player-forced danger stayed non-Deadly, drafted `Wait_Combat` remained non-flee after 5 ticks, undrafted `Wait_Combat` became a player-forced `Flee` on tick 1 with destination `(96,94)` outside avoid danger, and a non-forced undrafted `AttackMelee` job in avoid danger also became player-forced `Flee` on tick 1 with a safe destination. The 2026-06-01 rerun extended the same behavior action for assigned-area flee policy: with an assigned safe destination available, `ZL_Area_PreferInAreaFlee` fled to `(98,94)` inside `ZL_Area_ColonistInside`; with only the current danger cell allowed, `ZL_Area_EmergencyFallbackFlee` fled to `(98,94)` outside the assigned area as the emergency fallback; after Matthew reproduced a vanilla `FleeAndCower` path outside `Area 1` in `ZTest`, the action also proved `JobGiver_ConfigurableHostilityResponse.TryGetFleeJob`/`FleeUtility.FleeJob` retargeting for Zombieland threats: vanilla `CellFinderLoose.GetFleeDest` would have chosen `(75,93)` outside the assigned area, but the patched `FleeAndCower` job used `(63,85)` inside `ZL_Area_ColonistInside`, and an already-running `FleeAndCower` job with the old outside target was retargeted to the same inside-area cell after one tick. The same behavior action records the installed `JobGiver_ConfigurableHostilityResponse.TryGetAttackNearbyEnemyJob` prefix/transpiler and proves both branches: while the colonist was in player-forced `Flee` and a hostile nearby zombie existed, the prefix returned false/null; for a far active electrifier at squared distance `144`, vanilla reach returned false while the transpiled helper returned true, and a far normal-zombie control stayed false/false. The behavior action also includes the `Pawn_Destroy_Patch` cleanup subprobe: a spawned colonist and normal zombie were inserted into `ZombieAreaManager.pawnsInDanger`, the colonist had a real `ColonistSettings` config, and real `Pawn.Destroy(DestroyMode.Vanish)` removed both danger entries plus the colonist config.
- The behavior run produced the live danger overlay screenshot `/Users/ap/Library/Application Support/RimWorld/Screenshots/zl_area_workflow_danger_overlay__clip.png`, saved the extended fixture as `ZL_Area_Workflow_behavior.rws`, reloaded that save, and reran `actionMode:behavior` successfully. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Focused warning/message UI coverage was added on 2026-05-31 after decompiling `Messages.MessagesDoGUI()` and `Message.Draw(Int32, Int32)`. From `EMPTY`, `zombieland/area_workflow_state setupFixture:true actionMode:warning-ui` returned `success=true`, recorded installed `net.pardeike.zombieland` owners on both patch targets, created one colonist-inside and one zombie-inside danger entry, and added one real live message `Zombieland area warning UI probe` targeting both pawns. The direct installed-prefix probe proved no message offset while `ZombieAreaManager.warningShowing=false` (`16 -> 16`) and the expected 29-pixel offset while true (`16 -> 45`). `rimworld/take_screenshot fileName=zl_area_warning_ui` captured `/Users/ap/Library/Application Support/RimWorld/Screenshots/zl_area_warning_ui.png`; screen-target metadata recorded the warning immediate window at `y=3` and the live message immediate window at `y=43`, proving the alert draws above normal messages. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Mixed-targeting coverage was added to the same tool after decompiler checks confirmed the RimWorld 1.6 shapes of `Verse.AI.AttackTargetFinder.BestAttackTarget`, private `GetAvailableShootingTargetsByScore`, private `GetShootingTargetScore`, and both private friendly-fire score offset methods. From `ZL_Area_Workflow_behavior.rws`, `zombieland/area_workflow_state setupFixture:true actionMode:targeting` returned `success=true` and cleaned up all spawned attackers/targets before returning. The transient fixture created an armed player colonist, armed hostile colonist, player warg, normal zombie, roped zombie, confused zombie, active electrifier, albino, suicide bomber, spitter, and symbiant.
- The targeting run proved: player rifle filtering kept normal/albino/suicide/spitter/symbiant candidates while excluding roped, confused, and active electric zombies; enemy filtering returned no candidates with `enemiesAttackZombies=false`; with it enabled, enemy filtering allowed ordinary normal/roped/suicide targets and excluded confused, albino, spitter, symbiant, and active electric targets for an unsuitable ranged weapon; public `BestAttackTarget` returned null for roped/confused player-specific validators, null for enemy normal while enemies were disabled, normal while enabled, null for enemy spitter/symbiant/electric validators, null for animals while `animalsAttackZombies=false`, and normal while enabled. The shooting-score patch produced `normalScore=100.487808` and `suicideScore=112.218658`. Friendly-fire patch evidence confirmed both target methods have `net.pardeike.zombieland` owners and the helper removes zombies while preserving a non-zombie pawn. A focused rerun from `EMPTY` invoked both real private friendly-fire score helpers with the same armed player searcher: zombie blast/cone offsets were positive (`10.8`/`1.08907568`), while a nearby colonist produced the expected penalties (`-7.2`/`-0.7260504`).
- The 2026-06-06 bug-report rerun added a focused raider-distance regression check after the enemy `BestAttackTarget` fix. `zombieland/area_workflow_state setupFixture:true actionMode:bug-reports` (`op_aa6bbf26340641fda2cfde2656336839`) spawned a hostile bolt-action raider plus normal zombies at squared distances `36`, `81`, `100`, and `625` under `enemiesAttackZombies=true`. Available-target and public `BestAttackTarget` paths both kept the `36` and `81` targets, rejected `100` and `625`, and still reported the rejected zombies as hostile active threats. This specifically covers the reported raider ping-pong case where long-range weapons were allowed to select zombies around 25 tiles away while base-wall jobs competed.
- The first live targeting pass exposed a real bug in `AttackTargetFinder_BestAttackTarget_Patch`: the friendly fallback returned a cached zombie before the caller validator was re-applied. The patch now updates `thing` after fallback selection and lets the existing final validator guard null invalid results. After that fix and the violence-capable shooter generation guard, `scripts/build-quiet.sh` passed with 0 warnings/0 errors, the final live targeting run passed, and `rimbridge/list_logs minimumLevel=warning` returned no warning-or-higher entries.
- Direct hostility and target-cache coverage were folded into the same targeting action after decompiling `AttackTargetsCache.RegisterTarget`, `DeregisterTarget`, `TargetsHostileToColony`, `Notify_ThingSpawned`, `Notify_ThingDespawned`, and `TargetsHostileToFaction`. The final live run proved player `HostileTo(zombie)=true`, enemy and non-colony animal `HostileTo(zombie)` obey `enemiesAttackZombies`/`animalsAttackZombies`, infecting enemy pawns are not hostile to zombies, spitter/symbiant are non-hostile to a non-player faction, zombie active-threat checks return false for player/null/enemy-disabled settings, true for enemy faction with `AttackMode.Everything`, and false for `OnlyColonists`. The replacement `TargetsHostileToColony` cache contained the real hostile human before destruction, excluded normal/spitter/symbiant zombies, and removed the hostile after destruction.
- The first target-cache pass exposed a real stale-entry bug: `AttackTargetsCache_DeregisterTarget_Patch` removed from the replacement cache too late for the despawn path, and the replacement cache did not defend against destroyed entries at read time. The patch now removes on `DeregisterTarget` prefix and `PlayerHostilesWithoutZombies` purges null, destroyed, despawned, wrong-map, and no-longer-hostile entries before returning the cache. After that fix, `scripts/build-quiet.sh` passed with 0 warnings/0 errors, `actionMode:targeting` returned `success=true`, and `rimbridge/list_logs minimumLevel=warning` returned no warning-or-higher entries.
- Drafted wait auto-attack coverage was folded into `actionMode:targeting` after decompiling `JobDriver_Wait.CheckForAutoAttack`. The live run confirmed the Harmony owner `net.pardeike.zombieland` is installed on `CheckForAutoAttack`; active adjacent normal zombies and player-faction spitters were damaged by a real `Wait_Combat` job, player-faction symbiants and ordinary hostile pawns started real melee attacks, and roped/confused adjacent normal zombies were neither attacked nor damaged through 900 ticks. The first fixture pass used `GenTicks.TicksGame` for `paralyzedUntil` and falsely let the confused zombie become attackable once real ticks advanced; the fixture now uses `GenTicks.TicksAbs`, matching `ZombieStateHandler` and `Zombie.IsRopedOrConfused`. The harness now generates relation-free/no-random-gear test pawns, equips the wait auto-attack actor with a materialized melee weapon, maintains the intended roped state during the roped negative probe, treats symbiant/ordinary-hostile positives as attack-start checks because adjacent melee damage can be masked by stun/retaliation while still proving those targets were not filtered out, and only counts attack start for the roped negative case when the actor enters a melee job or the target is damaged, not from transient stance state alone. After that correction, `scripts/build-quiet.sh` passed with 0 warnings/0 errors, `actionMode:targeting` returned `success=true`, and `rimbridge/list_logs minimumLevel=warning` returned no warning-or-higher entries.
- Non-pawn turret coverage was added to the same targeting action after decompiling `AttackTargetFinder.BestAttackTarget`, `AttackTargetsCache.GetPotentialTargetsFor`, `Building_TurretGun.CurrentEffectiveVerb`, and `Building_TurretGun.AttackVerb`. The live run used a real materialized `Turret_MiniTurret`/`Building_TurretGun`, confirmed `net.pardeike.zombieland` owns the installed `BestAttackTarget` patch, and proved the turret selected normal, spitter, and symbiant targets while rejecting roped, confused, and active-electric zombies when its bullet verb could not harm electric zombies. The general search assertion accepts any returned target that is allowed by the same state rules, because a real cache can also contain earlier valid zombies from the larger mixed-targeting fixture. Final `scripts/build-quiet.sh` passed with 0 warnings/0 errors, `actionMode:targeting` returned `success=true`, and `rimbridge/list_logs minimumLevel=warning` returned no warning-or-higher entries.
- Friendly/mech/thing available-target branch coverage was folded into the same targeting action after decompiling `AttackTargetFinder.GetAvailableShootingTargetsByScore`. The branch probes use the existing prefix/postfix invocation helper against real spawned friendly human, friendly mech, player mech, player turret, and hostile turret searchers plus the mixed zombie target set. The first live pass exposed a real bug: `removeAllZombies` removed ordinary `Zombie` instances but let `ZombieSpitter` and `ZombieSymbiant` survive for a player mech under `AttackMode.OnlyHumans`. The patch now removes all three Zombieland pawn target types when `removeAllZombies` is active and makes `removeSpitter` remove both spitter and symbiant. The final live run proved all branch cases passed, including empty player-mech `OnlyHumans`, allowed player-mech `Everything`, player thing filtering, enemy thing disabled/enabled filtering, and friendly human/mech filtering. `scripts/build-quiet.sh` passed with 0 warnings/0 errors and `rimbridge/list_logs minimumLevel=warning` returned no warning-or-higher entries.
- Tar-smoke melee targeting was added to `actionMode:targeting` after checking the `Verb.CanHitTargetFrom` patch branch. The live run spawned an adjacent player melee actor and hostile human target, verified the melee verb could hit before smoke, spawned real `TarSmoke` on the target cell, verified `gasAtTargetAfter=TarSmoke`, and proved the same melee verb still returned `CanHitTargetFrom=true`. This complements the earlier focused ranged tar-smoke tools (`zombieland/tar_smoke_blocks_ranged_targeting` and `zombieland/tar_smoke_blocks_human_ranged_targeting`) by covering the source branch that deliberately returns early for melee attacks. The same final run had `actionSucceeded=true`, `scripts/build-quiet.sh` passed with 0 warnings/0 errors, and `rimbridge/list_logs minimumLevel=warning` returned no warning-or-higher entries.
- Tar-smoke standard aim chance was added to the same targeting action after decompiling `ShotReport.HitReportFor`, `CoverUtility.CalculateCoverGiverSet`, and `ShotReport.AimOnTargetChance_StandardTarget`. A real rifle shooter/hostile target pair produced `AimOnTargetChance_StandardTarget=0.612535536` before smoke; real `TarSmoke` on the target cell made the standard aim chance `0` and made ranged `CanHitTargetFrom=false`. The cover-list branch is covered directly with a synthetic `ShotReport.covers` entry containing a `TarSmoke` `CoverInfo`, which also returned `0`; this is intentionally recorded separately because RimWorld 1.6 cover discovery uses `GetCover`, while `TarSmoke` is a gas. The final `actionMode:targeting` run returned `success=true`, `scripts/build-quiet.sh` passed with 0 warnings/0 errors, and `rimbridge/list_logs minimumLevel=warning` returned no warning-or-higher entries.
- Anomaly creature/zombie matrix coverage was added on 2026-06-02 with the reusable `zombieland/anomaly_zombie_matrix` endpoint after a static/decompiler pass over Anomaly pawn kinds, `GenHostility`, `Pawn.activity`, `CompActivity`, and `CompCanBeDormant`. The bridge now reports activity/dormancy state, vanilla threat state, attack-mode attraction, and reverse hostility under `enemiesAttackZombies`/`animalsAttackZombies`. Classification: nominal rows are awake non-humanlike entities ignored by `OnlyColonists`/`OnlyHumans` and targetable under `Everything`, humanlike shamblers/ghouls targetable under `OnlyHumans` and `Everything`, non-humanlike shambler gorehulk targetable only under `Everything`, entity reverse hostility following `enemiesAttackZombies`, and player ghoul hostility coming from player/zombie faction hostility while `Tools.IsHostileToZombies=false`. Player ghoul not counting for `OnlyColonists` is nominal because RimWorld `Pawn.IsColonist` excludes `IsSubhuman`, and installed Anomaly `Ghoul` inherits `consideredSubhuman=true`. Definite wrong behavior was fixed: passive/deactivated `CompActivity` pawns and sleeping `CompCanBeDormant` pawns no longer attract or become attackable by zombies, including under `AttackMode.Everything`. An installed-def audit now compares the matrix against every non-abstract Anomaly `PawnKindDef`: entity/fleshbeast/mutant creatures are covered, `Nociosphere` is covered by the policy-edge verifier, `Researcher` is classified out as player scenario content, and the Horaxian human combat pawn kinds were added as hostile human DLC controls. Runtime evidence proved passive Fleshmass nucleus, dormant Metalhorror, and dormant Fingerspike are non-targets while awake Metalhorror/Fingerspike remain targetable under `Everything`; it also proved `Horaxian_Underthrall`, `Horaxian_Gunner`, and `Horaxian_Highthrall` are ignored by `OnlyColonists`, targetable by `OnlyHumans`/`Everything`, and follow `enemiesAttackZombies`. The full 24-row built-in matrix then passed with `ticks:60`; active Fleshmass nucleus despawned through the expected heart-transition path, cleanup removed staged pawns/zombies, and warning-or-higher logs were empty. The follow-up `zombieland/anomaly_policy_edges` verifier proved passive Nociosphere is non-targetable, active Nociosphere is targetable only under `Everything`, reverse Nociosphere hostility follows `enemiesAttackZombies`, and static/5-tick/30-tick reruns stayed log-clean while the 30-tick window showed active Nociosphere killing both staged zombies. The verifier now refreshes `ZombieSettings.Values` after tick advancement so interpolated settings replacement does not make post-tick probes stale. `FleshmassHeart` is a non-pawn building that zombies do not attack in any attack mode. `zombieland/anomaly_engagement_matrix` now covers actual pheromone/tracking behavior for all 24 matrix rows across all three zombie attack modes. Full `ticks:90` runs returned 72/72 nominal with `enemiesAttackZombies=true` and 72/72 nominal with it disabled; eligible rows seeded pheromones and put the zombie into `Tracking`, while ineligible rows did not seed and did not track. A detailed reverse-attack-disabled comparison for Gorehulk, Metalhorror, Fingerspike, entity Ghoul, and Horaxian_Gunner returned 15/15 nominal, and an isolated Gorehulk rerun showed the entity kept `GotoWander` instead of attacking the zombie while reverse hostility stayed false. A combined `ticks:180` full-matrix stress pass produced noisy Gorehulk/Fingerspike/passive-Fleshmass rows, but isolated reruns from `EMPTY` were nominal; this is a fixture-window limitation rather than an unresolved behavior finding. Warning-or-higher logs were empty, zero zombies remained, and the probe cleanup left zero non-zero zombie-grid cells. No unresolved Anomaly creature/zombie policy or engagement behavior remains from this slice. A broad `zombieland/anomaly_story_smoke` pass then covered the five named fun/perf/exploit scenarios from `EMPTY` with `ticks:240` and `zombieCount:32`, returning `classificationCounts={nominal:5}` and log-clean cleanup. Horaxian moat behavior is nominal under `OnlyHumans` with `enemiesAttackZombies=false`: 28 zombies spawned, 15 tracked, the Horaxian_Gunner was damaged and fought instead of becoming a free exploit. Player ghoul choke behavior is nominal policy-wise: `OnlyColonists` produced no ghoul attackability and 0 tracking because ghouls are subhuman, while `OnlyHumans` produced attackability and 24 tracking; final fun/exploit tuning remains a gameplay-footage question, not a current code defect. Invisible/stealth Sightstealer and Revenant under `Everything` were both attackable and each drew 16 tracking zombies, so the current default is to allow broad zombie-smell detection only in the intentionally unhinged mode. Fleshmass field behavior is nominal: zombies ignored the `FleshmassHeart` building, left its hit points unchanged, and tracked the nearby Gorehulk, so they disrupt local fleshbeasts without trivializing the heart. Active Nociosphere field behavior is technically nominal but design-sensitive: 32/32 staged zombies died within 240 ticks, the Nociosphere survived, the pass completed in 67ms with 343 zombie ticks, and no warnings appeared, so the current small-scale result is clean meat-blender/performance evidence rather than interesting-chaos proof. Screenshots or longer footage are only needed for subjective story tuning after this code coverage slice.
- Added 2026-06-04 with `zombieland/anomaly_targeting_overrides` from `EMPTY` after simplifying the actual enum values to `Automatic`, `Never`, and `Allow`. The Anomaly exception settings add four compact tri-state overrides under the existing attack rule: ghouls, shamblers/mutant humans, other Anomaly entities, and Nociosphere. Operation `op_ff5172fd6eb04c8b960f97464883d127` passed 15/15 rows, proving Automatic preserves base behavior, explicit Never suppresses otherwise-valid targets under `Everything`, explicit Allow makes ghouls, shamblers, Metalhorrors, and Nociosphere targetable under stricter base modes, and dormant/deactivated hard gates still win. The visible settings crop `/Users/ap/Library/Application Support/RimWorld/Screenshots/zl_anomaly_settings_ghouls_ui_2.png` proves the Anomaly exception block sits after `WallPushing`, includes the new Ghoul row, uses white Anomaly group labels, and displays indented compact radio rows with `Automatic (Allow/Mixed/Never)`, `Never`, and `Allow`, without raw translation keys or a duplicate reverse-row label.
- Added 2026-06-04 with `zombieland/anomaly_zombie_hostility_overrides` from `EMPTY` after deploying the matching `1.6` DLL with `scripts/build-quiet.sh --restart-rimworld`. The reverse-direction Anomaly setting remains one compact tri-state row for non-player Anomaly pawns choosing zombies as targets. Operation `op_d5d37409359342c5bd1eec7a390b0d6a` passed 7/7 rows: automatic-disabled kept entities non-hostile to zombies when base enemy/animal zombie hostility was off, automatic-enabled followed base hostility when enabled, forced `Allow` overrode global enemy/animal hostility off for other entities, shamblers, and active Nociosphere, forced `Never` overrode global hostility on, and dormant Metalhorror stayed hard-gated despite the Allow override. The follow-up warning-log check returned no entries in `op_3fc7ac46e9e04960b435f6567ccac442`.
- Downed-zombie combat continuation was added to `actionMode:targeting` after checking the two `Pawn.Downed` replacement patch shapes. The bridge records runtime Harmony targets by patch class: `Toils_Combat_FollowAndMeleeAttack_KillIncappedTarget_Patch` owned `Verse.AI.<>c__DisplayClass6_0::<FollowAndMeleeAttack>b__0(System.Int32 delta)`, while `JobDriver_AttackStatic_TickAction_Patch` owned seven installed methods: the two `JobDriver_AttackStatic` generated delegates, `Toils_Jump.<JumpIfTargetDowned>b__0`, `TargetingParameters.CanTarget`, `VerbUtility.AllowAdjacentShot`, `Stance_Warmup.StanceTick`, and `Verb_Shoot.WarmupComplete`. The decompiled installed `Pawn_MindState.MeleeThreatStillThreat` no longer reads `Pawn.Downed`, so it correctly did not appear in the runtime patch list. The live fixture forced `doubleTapRequired=false`, spawned health/public-downed normal zombies, then proved a drafted melee colonist kept `AttackMelee` with `job.killIncappedTarget=false` and damaged the zombie by tick 10, and a drafted rifle colonist kept `AttackStatic` with `job.killIncappedTarget=false` and entered firing cooldown by tick 129. `scripts/build-quiet.sh` passed with 0 warnings/0 errors, `actionMode:targeting` returned `success=true`, and `rimbridge/list_logs minimumLevel=warning` returned no warning-or-higher entries.
- Focused ranged-projectile coverage was added on 2026-05-30 after decompiling `Projectile.ImpactSomething` and `Verb_LaunchProjectile.TryCastShot`. The first live probe found the old launch transpiler helper returned true for a nearby visible zombie, but the real `Verb_Shoot`/base-call path still launched to a forced-miss cell. `Verb_LaunchProjectile_TryCastShot_Patch` now handles the source-derived skip case with a prefix that launches directly at the intended target and leaves non-matching shots on vanilla. After deploying to the actual active mod path, `zombieland/area_workflow_state setupFixture:true actionMode:ranged-projectiles` from `EMPTY.rws` returned `success=true`: at `threatScale=1` and `COLONISTS_HIT_ZOMBIES_CHANCE=1`, a player bolt-action rifle shot at a visible zombie six cells away with forced miss radius `9` launched `Bullet_BoltActionRifle` with used/intended target both set to the zombie and destination cell exactly `(114,119)`, the zombie cell. The same projectile was then forced through `ImpactSomething` under seed `1`, where vanilla would miss the laying target, and damaged the health-downed zombie by `37.44` injury. The same run recorded installed patch owners for `DamageWorker_AddInjury.IsHeadshot` and `HediffSet.AddDirect`, proved bullet-to-head returned true and changed a normal zombie to `ShouldDie`, proved bullet-to-torso returned false and left `Tracking`, proved bad `Cut` on `Brain` changed state to `ShouldDie`, and proved bad `Cut` on `Torso` left `Tracking`. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Focused danger/flee coverage was added on 2026-05-30 after decompiling the former `DangerWatcher.AffectsStoryDanger` surface and `FleeUtility.ShouldFleeFrom`. `zombieland/area_workflow_state setupFixture:true actionMode:danger-flee` from `EMPTY.rws` returned `success=true` for the danger predicate and flee cases; current bridge bookkeeping reports the installed `DangerWatcher_CalculateDangerRating_Patch` instead of the removed AffectsStoryDanger patch. For story danger, a spawned active normal zombie in Home returned true, while an active normal zombie outside Home, a roped zombie, a public-downed zombie, and an unspawned zombie returned false; the probe sets `doubleTapRequired=false` so the downed case reaches the danger patch's public `Pawn.Downed` branch instead of the self-healing display override. For flee filtering, a nearby normal zombie returned true and `SeesZombieAsThreat=true`, roped/confused/active-electric/albino zombies remained hostile but returned false with `SeesZombieAsThreat=false`, and an ordinary hostile pawn still returned true. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Focused smart-melee coverage was added on 2026-05-30 after decompiling `Verb_MeleeAttack.TryCastShot`, `Verb.TryStartCastOn`, and the scratch/bite `ChooseHitPart` overrides. `zombieland/area_workflow_state setupFixture:true actionMode:smart-melee` from `EMPTY.rws` returned `success=true` and recorded the installed Zombieland prefixes on `Verb_MeleeAttack.TryCastShot`, `DamageWorker_Scratch.ChooseHitPart`, and `DamageWorker_Bite.ChooseHitPart`. The verifier calls the patched `TryCastShot` method directly for branch-result cases because `Verb.TryStartCastOn` only reports whether the cast started. A normal zombie `ZombieBite` against a melee-20 armed colonist with `safeMeleeLimit=1` returned false and caused no injury; a chainsaw-equipped colonist melee verb returned false and caused no injury; an armed colonist attacking a roped zombie returned false and killed the zombie; and an ordinary armed colonist attacking a normal zombie had the prefix continue to vanilla with `Pawn_MeleeVerbs.TryMeleeAttack=true`. The same run proved the downed hit-part prefixes are live and narrow: standing zombie scratch/bite controls left `DamageInfo.Height=Undefined`, while health-downed zombie scratch returned `left leg` and bite returned `right leg`, both with `height=Bottom` and `depth=Outside`. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Focused job-gate coverage was added on 2026-05-30 after decompiling `Pawn_JobTracker.StartJob` and `Pawn.ClearReservationsForJob`. `zombieland/area_workflow_state setupFixture:true actionMode:job-gate` from `EMPTY.rws` returned `success=true` and recorded the installed Zombieland prefix on the full RimWorld 1.6 `StartJob` overload. A normal zombie, spitter, and symbiant each successfully started whitelisted `Wait`; each then had a reserved disallowed `HaulToCell` job rejected while current `Wait` stayed active, `jobsGivenThisTick` reset from `7` to `0`, `jobsGivenThisTickTextual` reset from `dirty` to empty, `startingNewJob` reset from true to false, and the reserved steel target was released. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Focused special melee verb coverage was added on 2026-05-30 after decompiling `Pawn_MeleeVerbs.GetUpdatedAvailableVerbsList`. `zombieland/area_workflow_state setupFixture:true actionMode:special-melee-verbs` from `EMPTY.rws` returned `success=true` and recorded the installed Zombieland postfix on the real RimWorld 1.6 method. A normal zombie's available melee damage defs included `ZombieBite`, and `TryGetMeleeVerb` selected a `ZombieBite` verb. An albino, an active electrifier, and a disabled electrifier each returned only `Scratch` damage defs and selected scratch verbs, proving the postfix removes `ZombieBite` for both special gates while preserving normal zombies. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Focused special-damage coverage was added on 2026-05-30 after decompiling `DamageWorker_AddInjury.ApplyDamageToPart` and `ArmorUtility.GetPostArmorDamage`, then refreshed after the spitter health fix. `zombieland/area_workflow_state actionMode:special-damage` returned `success=true` and recorded installed Zombieland prefixes on both real methods. Final operation `op_494c283d92374003a9f52527edb58248` covered spitter melee/ranged scaling (`Cut` input `2 -> 5` direct core injury, `Bullet` input `10 -> 1`), former-map-pawn reduction (`1.84822178` versus ordinary `3.43253374`), albino bullet blocking/pass-through plus explosive pass-through (`8` blocked, `1` hit, bomb `1`), dark-slimer smoke-on-hit (`TarSmoke` delta `37`), and active-electrifier ranged absorption (`0` bullet damage, `4` absorb markers) while cut damage still passed through (`1.41176474`). The same runtime action covers tanky armor through direct real `ArmorUtility` calls: an unarmored human torso stayed ordinary (`10 -> 10`, no deflect/diminish), while shield, helmet, and suit branches absorbed damage with `deflected=true`/`diminished=true` and reduced armor from `1` to `0.9600951`, `0.807393`, and `0.9205075`; `diminished=true` is the branch that calls `Tools.PlayTink`. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Added 2026-06-07 with `zombieland/area_workflow_state setupFixture:true actionMode:spitter-readiness` from `EMPTY.rws`. Operation `op_14106eb9e3d94cb58cef8319df2666a8` proved the colony-weapon readiness factor leaves no-colonist and armed-colony spitters at `1`, lowers weak-colony spitters to `0.43`, and gives partial support `0.6372727` at difficulty `1`. The same fixture proved weak-colony scaled Bullet/Cut amounts are `2.32558131x` armed baseline and that the real injury prefix still applies through `TakeDamage`; follow-up `bug-reports` (`op_03d8a60e8d674635a7dec19de39966f5`) and `special-damage` (`op_a4793903b6db4488bdb3ce1090fcb3d6`) stayed green, with warning-or-higher logs empty.
- Focused electrifier melee damage coverage was added on 2026-05-30 after decompiling `Verb_MeleeAttackDamage.DamageInfosToApply`. `zombieland/area_workflow_state setupFixture:true actionMode:electrifier-melee-damage` from `EMPTY.rws` returned `success=true` and recorded the installed Zombieland postfix on the private iterator method. The live probe proved an active electrifier stalls a target's primary `Chainsaw` from `0` to `120`, converts a smokepop-belt hit into `ElectricalShock` with `ElectricalField`, deteriorates an inventory component-cost chainsaw by `2` HP while preserving non-electrical yielded melee damage, and leaves the same inventory component item unchanged when the electrifier is disabled. The first response-shape pass also exposed and fixed a bridge serialization hazard by keeping raw `DamageInfo` objects out of the returned JSON. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Focused electrifier EMP coverage was added on 2026-05-30 after decompiling `StunHandler.Notify_DamageApplied`. `zombieland/emp_electrifier` from `EMPTY.rws` returned `success=true` and confirmed the installed Zombieland prefix on `StunHandler.Notify_DamageApplied`. The verifier spawned an active electrifier, applied real EMP damage amount `5`, proved `activeBefore=true`, `activeAfter=false`, and moved `electricDisabledUntil` from `0` to `5338` at `tickBefore=5038`. The spawned fixture was cleaned up and `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Focused active-electrifier attack-verb, time-slowdown, and zombie attack-start coverage was added on 2026-05-30 after decompiling `Pawn.TryGetAttackVerb`, private `Verb.CausesTimeSlowdown`, and `Pawn.TryStartAttack`. `zombieland/active_electrifier_attack_verb_contract` from `EMPTY.rws` returned `success=true` and confirmed the installed Zombieland prefix/postfix on all three methods. The live probe proved the spitter early-exit returns no verb, a bolt-action rifle still returns a ranged verb against a normal zombie, the same actor falls back to safe melee against an active electrifier, and `Weapon_GrenadeFrag` keeps its non-melee location-targeting pass-through. It also proved time slowdown is suppressed only for distant zombie targets: far normal zombie `false` at squared distance `49`, close normal zombie `true` at `10`, and far hostile human `true` at `49` with threshold `16`. The attack-start subprobe temporarily set the normal zombie kind to disable `Violent`, confirmed the attacker reported `WorkTagIsDisabled(Violent)=true`, then still started a `ZombieBite` attack, entered `Stance_Cooldown`, and injured the adjacent colonist by `3.69211769`. The fixture cleaned spawned pawns/restored the kind def and `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Focused animal-response coverage was added on 2026-05-30 after decompiling `PawnUtility.GetManhunterOnDamageChance`, `PawnUtility.GetManhunterChanceFactorForInstigator`, `FoodUtility.GetPreyScoreFor`, and `FoodUtility.BestPawnToHuntForPredator`. The first source pass found and fixed a real semantic bug: disabling `zombiesCauseManhuntingResponse` zeroed all animal revenge, including non-zombie instigators, despite the setting text saying only zombies should anger animals. `zombieland/area_workflow_state setupFixture:true actionMode:animal-response` from `EMPTY.rws` returned `success=true`: human instigator stayed at vanilla `1` with the setting enabled and disabled; normal zombie instigator reduced from vanilla `1` to `0.05` while enabled and `0` while disabled; spitter/symbiant instigators stayed `0`; with `animalsAttackZombies=false`, a far human prey score `-138.36` beat nearby normal zombie `-10047.2`; with it enabled, the nearby zombie rose to `-117.2`; and spitter/symbiant prey scores stayed `0`. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Focused UI-state coverage was added on 2026-05-30 after decompiling `GenMapUI.DrawPawnLabel`, `MainTabWindow_Inspect.CurTabs`, and `Pawn.Downed`. `zombieland/area_workflow_state setupFixture:true actionMode:ui-state` from `EMPTY.rws` returned `success=true` and recorded the installed Zombieland patch owners on all three methods. The live probe proved ordinary zombie labels are suppressed (`continueDraw=false`), former-map-pawn zombie labels and ordinary human labels pass through, zombie inspect selection returns a non-null empty tab list, ordinary human inspect selection returns 10 normal inspect tabs, and a health-downed zombie reports public `Downed=false` only while `doubleTapRequired=true` while `pawn.health.Downed` remains true. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Focused downed-crawler visual coverage was added on 2026-05-30 after decompiling `PawnDownedWiggler.ProcessPostTickVisuals(Int32)` and `PawnRenderer.BodyAngle(PawnRenderFlags)`. `zombieland/area_workflow_state setupFixture:true actionMode:downed-crawler-visuals` from `EMPTY.rws` returned `success=true` and recorded installed Zombieland patch owners on both methods. The live probe set a health-downed normal zombie's pather destination to `(123,156)`, invoked the real patched post-tick visual method, matched the expected downed crawler angle `281.777618`, then invoked real `BodyAngle` twice to verify initial and smoothed downed-zombie angles. Direct prefix probes confirmed the patch skips vanilla only for a health-downed zombie and passes through for a standing zombie and a downed human. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Focused `Root_Play` hook coverage was added on 2026-05-30 after decompiling `Root_Play.Update()` and `Root_Play.SetupForQuickTestPlay()`. `zombieland/area_workflow_state setupFixture:true actionMode:root-play-hooks` from `EMPTY.rws` returned `success=true` and recorded installed Zombieland patch owners on both methods. The live probe spawned an electrifier and tanky zombie, inserted them into the current map `TickManager` tracking sets, and invoked the installed update postfix until the sound-update cadence gate crossed, proving the real electrical-hum and tank-movement sustainer paths run from the hook. The quick-test postfix was probed non-destructively by dirtying `ZombieSettings.Values`, `ValuesOverTime`, and `SettingsDialog.scrollPosition`, invoking the postfix, verifying they matched `ZombieSettingsDefaults`, and restoring the original settings snapshot. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Focused 1.6 render-node graphic coverage was added on 2026-05-30 after decompiling `PawnRenderNode_Body.GraphicFor(Pawn)` and `PawnRenderNode_Head.GraphicFor(Pawn)`. `zombieland/area_workflow_state setupFixture:true actionMode:render-node-graphics` from `EMPTY.rws` returned `success=true` and recorded installed Zombieland patch owners on both virtual render-node methods. The live probe set custom `ZombieRenderCompat` body/head graphics on spawned normal zombies and verified the real patched body/head render nodes returned those exact graphics, while an ordinary human with no custom graphics passed through to vanilla. It also reproduced the important 1.6 edge case by removing a zombie head after a custom head graphic was cached, then verified the fixed head prefix returns `null` instead of drawing the cached head. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Focused effecter suppression coverage was added on 2026-05-30 after decompiling `Effecter.Trigger(TargetInfo, TargetInfo, Int32)`. `zombieland/area_workflow_state setupFixture:true actionMode:effecter-suppression` from `EMPTY.rws` returned `success=true` and recorded the installed Zombieland prefix on the real trigger method. The live probe inserted a counting `SubEffecter` into real `Effecter` instances and invoked the patched trigger path: `Deflect_General` on a normal zombie skipped the child (`triggerCount=0`), `Deflect_General` on an ordinary human triggered once, and non-deflect `Mine` on a normal zombie triggered once. Direct prefix probes matched the same branch split. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Focused `Graphic_Multi` missing-texture capture coverage was added on 2026-05-30 after decompiling `Graphic_Multi.Init(GraphicRequest)`. `zombieland/area_workflow_state setupFixture:true actionMode:graphic-multi-texture` from `EMPTY.rws` returned `success=true` and recorded the installed Zombieland transpiler on the real init method. Under `suppressError=true`, a missing texture path `ZombielandTextureProbe/Missing_5038_6898` set `Graphic_Multi_Init_Patch.textureError=true` and used `BaseContent.BadMat`; a valid spawned-human body path `Things/Pawn/Humanlike/Bodies/Naked_Male` left `textureError=false` and did not use `BadMat`; and a direct suppressed `CaptureTextureError` call set the flag. The unsuppressed `Patches.Error(text)` branch is source-covered but intentionally not runtime-triggered to keep the log check clean. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Focused warmup-scaling coverage was added on 2026-05-30 after decompiling `Verb.TryStartCastOn(LocalTargetInfo, LocalTargetInfo, bool, bool, bool, bool)`. `zombieland/area_workflow_state setupFixture:true actionMode:warmup-scaling` from `EMPTY.rws` returned `success=true` and recorded the installed Zombieland transpiler on the real six-parameter method. The live probe equipped a human and three normal zombies with real bolt-action rifle verbs, forced `warmupTime=2`, and invoked patched `TryStartCastOn` against a line-of-sight target cell. The human control and normal non-raging zombie both produced `Stance_Warmup.ticksLeft=120`; the non-raging zombie deliberately had local zombie grid count `4`, proving grid count alone does not scale. A raging zombie at grid count `4` produced `30` ticks, and a former-map-pawn zombie at grid count `5` produced `24` ticks. Direct `ModifyTicks` reflection matched each live stance result. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Focused zombie-stat coverage was added on 2026-05-30 after decompiling `StatExtension.GetStatValue(Thing, StatDef, bool, int)` and was extended after decompiling `VerbProperties.GetDamageFactorFor(Tool, Pawn, HediffComp_VerbGiver)`. `zombieland/area_workflow_state actionMode:zombie-stats` returned `success=true` and recorded the installed Zombieland stat prefix and damage-factor postfix on the real methods; final rerun `op_95df047d9366451fa63c19836b27fcb9` refreshed the spitter health expectation. The probe temporarily set `moveSpeedIdle=0.25`, `moveSpeedTracking=0.75`, `spitterThreat=2.25`, `damageFactor=1.25`, `ZombieTicker.PercentTicking=1`, and `TimeSpeed.Normal`, then restored the prior state. Real `GetStatValue` calls proved ordinary human `MoveSpeed` passes through, idle Fat zombie speed is `0.805`, tracking Thin is `2.76`, former-map-pawn Hulk is `5.52`, tanky is `0.0184`, roped health-downed crawler is `0.4`, melee hit/dodge/pain-shock branches match former/downed/tanky/tracking/albino/body-type expectations, tanky comfortable temperature is `-999..999`, and spitter `IncomingDamageFactor` is `1`. Spitter damage scaling is now intentionally covered by `DamageWorker_AddInjury_ApplyDamageToPart_Patch` instead of a stat override. Real `GetDamageFactorFor` calls proved human pass-through `1`, Fat zombie `5`, Thin `0.625`, former Hulk `18.75`, and fully armored tanky shield+helmet+suit `60`. `SmokeSensitivity` and `Suppressability` were absent in the active def set, so their ignored-stat branch is source-covered only. `rimbridge/list_logs minimumLevel=warning` returned no entries.
- Focused visual-support coverage was added on 2026-05-30 after decompiling `Pawn_DrawTracker..ctor(Pawn)`, `PawnRenderer..ctor(Pawn)`, `DamageFlasher.Notify_DamageApplied`, `DamageFlasher.GetDamagedMat`, `Pawn_DrawTracker.DrawPos`, and `Pawn_DrawTracker.ProcessPostTickVisuals(Int32)`. `zombieland/area_workflow_state setupFixture:true actionMode:visual-support` from `EMPTY.rws` returned `success=true` and recorded installed Zombieland patch owners on all six methods. The probe spawned a normal zombie and human, confirmed the human draw tracker held `CustomPawnState`, round-tripped extension-backed infection state `None -> Infecting -> None`, and confirmed the zombie still reported `Infected` while using `ZombieLeaner`. It also confirmed both renderer flashers were `ZombieDamageFlasher`, proved a real `ZombieBite` notification was captured and turned the damaged material green `(0, 0.8, 0, 1)`, proved a real `Cut` notification stayed non-green on a separate material, and proved the real patched `DrawPos` added a forced `ZombieLeaner` offset of `(0.21, 0, -0.13)` while the human kept a non-zombie leaner. Finally it invoked real `ProcessPostTickVisuals(1)`: the `Emerging` branch reset the offset to zero, and the `Wandering` branch produced a nonzero visual offset. `rimbridge/list_logs minimumLevel=warning` returned no entries.

### Dynamic friendly and enemy zombie response

- Added 2026-07-18 from the reusable all-DLC base `ZL_Group_Response_Base.rws`. `zombieland/group_response_contract` (`op_60fca6d97b4348078dfa8e4a146f0314`) passed all eight deterministic threshold cases, including the `1` shooter/`1` zombie decision, asymmetric `1.25`/`0.85` hysteresis, zero-shooter hard gate, and the rule that zero pressure cannot enter a new `Full` response. `zombieland/group_response_map_contract` (`op_e73453c2b7c74d2fb98e277e4e7f24f1`) passed 17 live-map cases covering recent harm, cleared `meleeThreat`, expiry, tanky pressure, the 120-tick Lord cache, Lord-less/fixed policies, friendly/enemy `IsActiveThreatTo` precedence, no ranged weapon, and friendly primary-pool targeting beyond nine cells while enemy response was Minimal. The settings helper (`op_822bf9b732be4dc2bad14fe88a82f27c`) passed seven default, legacy-bool migration, case-insensitive parse, round-trip, and invalid-value cases.
- Follow-up review hardening on 2026-07-18 reran `zombieland/group_response_contract` as `op_bc11b6bc83f04d1aa32d097f3703c3a4`: the original eight confidence cases still passed, and two cache-age cases proved a 599-tick-old `Full` result still seeds the `0.85` stay threshold while an entry exactly 600 ticks old resets to `Minimal` and therefore uses the `1.25` enter threshold. Its operation-correlated warning-or-higher query returned no entries. The design now also records the deliberate targeting asymmetry: friendly Full humans and mechs admit eligible zombies into the primary shooting pool out to weapon range, while enemy humans keep their existing nine-cell filter.
- The final compatibility rerun `op_2918ddc9e7db4d86b64320bd425b0cf2` expanded the live map contract to 22/22. Under `OnlyColonists`, an enemy human's ranged verb remained excluded while its melee verb selected the adjacent tracking zombie; under default `OnlyHumans`, a real hostile Scyther also selected the adjacent tracking zombie. A neutral-faction turret under friendly `Adaptive` plus `Everything` retained the zombie in the real shooting-pool patch, while `Minimal` removed it. The three rows failed before the narrow production fixes and passed afterward; operation-correlated warning-or-higher logs were empty. The deterministic cache contract was also rerun as `op_3e66d996482b402aa968a2276db1ff38` and remained 8/8 plus 2/2 with empty correlated logs.
- `zombieland/group_response_survival_matrix` reloads the base before every row, triggers a real `VisitorGroup` or `RaidEnemy`, preserves its Lord, normalizes only the requested capable rifle count, stages ordinary/tanky zombies, refreshes the normal pheromone attraction surface, runs ordinary jobs at `Superfast`, and records bounded policy/combat events plus raw and aggregate survival data. Final replicated strong runs (`op_d14019e516bd473b9bd544e2ac5bfe17`) put `5` capable shooters against `4` ordinary zombies: all three visitor and all three raid trials entered ranged response, all 30 group members survived, and the six rows averaged `3.33` zombie kills. Final weak runs (`op_f30e9b6780d64f57ae2dbbd038d24cc7`) put `3` shooters against `8`: none of six visitor/raid rows entered ranged response, and member survival was `88.9%`. Every trial reloaded independently and reported no warning-or-higher logs.
- Tanky weighting and the named sustained-contact artifact were covered by `op_ecb43c0ae7a24c06a5194e053c18b338`. Four shooters against one ordinary plus one tanky zombie (pressure `4`) entered `Full`; the recorded enemy row changed at tick `269`, returned to `Minimal` at tick `1106` with a zombie still present—`657` ticks after the latest landed harm—and re-entered `Full` after another hit at tick `2172`. One ordinary plus two tanky zombies (pressure `7`) remained `Minimal` for both relationships. This is the deliberate bounded fire/stop/hit/fire stutter, not the earlier transient-`meleeThreat` flutter; the policy now uses positive `lastHarmTick` and allows a downed harmed member to remain the anchor.
- Live persistence was prepared, saved as `ZL_Group_Response_Settings_Persistence.rws` (`op_68410809c59a4a76897fe9e5143a4269`), reloaded, and verified in `op_c1d168a081044f268f56f710fa631062`: current values, defaults, three keyframes, interpolation, and the colonist fixture all survived, including independent friendly/enemy response enums. Real `Dialog_ModSettings` layout capture `op_23439ee63c3a4b8b977104e92c817db5` exposed separate Friendly and Enemy response sections with Minimal, Adaptive, and Full choices in the measured scroll layout. The initial performance contract on `ZL_Ticking_Player_2000.rws` (`op_b6bd8a6d41224b48975f88c11feb45db`) measured 200 forced recomputations against 2,000 real cached zombies at mean `87.5665 µs`, max `982.9 µs`, and amortized mean `0.7297 µs` per game tick; 10,000 cached calls averaged `0.04266 µs`. Its operation-correlated warning-or-higher query was empty.
- Performance follow-up used the current Steam 1.6 assembly MVID `967ddb80559449f0a776dafa26a855d1` to distinguish two similarly named APIs: `LordManager.LordOf(Pawn)` is a nested scan, but production calls `Pawn.GetLord()`, whose `LordUtility.GetLord(Pawn)` (`060068D7`) directly returns `p.lord`; `Lord.AddPawnInternal` and `Lord.RemovePawn` maintain it. A second per-pawn response memo was therefore rejected as duplicate, potentially stale state, and TTL staggering was rejected without a measured synchronized-spike scenario. The real issue was the unprovoked friendly postfix fallback: baseline `op_2ff1c952194f403db87b3f7f21c922e6` made 1,995 response calls and took `3,117.3 µs` on 2,000 zombies. After one canonical `Tools.IsHostileToZombies` guard plus the cached `pawn.health.capacities.GetLevel(Moving)` accessor, final `op_6d69936f0d5e4037bd454dba3289132d` made one response call and took `367.1 µs` including reflective harness overhead, while 200 forced recomputations averaged `75.07 µs` and 100,000 cache hits averaged `0.041255 µs`. Final map `op_5509ec44a15e440e9c72833ab1bbc763` remained 22/22 and deterministic `op_97a7d6f2a37045918fd45a1bcb24c76d` remained 8/8 plus 2/2; all three correlated warning-or-higher queries were empty.
- Review-driven non-pawn fallback coverage then extended the live contract from the shooting-pool prefix to real `AttackTargetFinder.BestAttackTarget`. With only the new assertions deployed, red operation `op_9129843d03554a4faecc1d2ef8483fea` reproduced the defect: a neutral turret under friendly `Minimal` had its zombie removed from the primary pool but the postfix returned it anyway. The production fallback now keeps the pawn-specific canonical hostility gate and applies `GroupZombieResponse.AllowsFullResponse` to non-pawns before scanning. Green operation `op_61b0bf68fad74a139cb1e07fd66871ac` passed 24/24: the turret selected the zombie under `Adaptive` plus `Everything` and real `BestAttackTarget` returned null under `Minimal`. The final 2,000-zombie performance rerun `op_325f5b77f23c40cca987cda8176f73e2` retained one mode check, averaged `80.7785 µs` per forced recomputation and `0.042674 µs` per cached call, and both green operations had empty correlated warning-or-higher logs.

## S-External-Mod-Audit

Goal: prove optional integration code is either compatible with installed 1.6 mods or explicitly marked unavailable with source/decompiler evidence.

Prior evidence:
- `560065a Cover RimConnect zombie actions`
- `1143ea7 Cover RimConnect super drop`

Source checks:
- Zombieland: `Source/CETools.cs`, `Source/RimConnectSupport.cs`, `Source/CameraPlusSupport.cs`, `Source/DubsTools.cs`, `Source/SoSTools.cs`, `Source/VehicleTools.cs`, `Source/AlienTools.cs`, `Source/Customization.cs`.
- RimWorld 1.6 decompiler plus optional mod assemblies where installed.

Fixture:
- No single save. Use one small generated save per installed integration if runtime testing is possible.

Runtime matrix:
- RimConnect: action registration, settings UI injection, spawn/kill/rage/super drop, notification.
- Combat Extended: armor reroute, projectile distance/noise, armor utility, ammo user interactions.
- Humanoid Alien Races: framework member resolution, HAR-patched Human pawn semantics, and custom/non-flesh alien semantics through the local `ZLTestAlienRace` fixture.
- CameraPlus: zombie marker/color hooks.
- Dubs Performance Analyzer: current 1.6 overlay/profiler target family (`Analyzer.Profiling.H_ThingOverlaysOnGUI.GetPatchMethods`) plus legacy draw-name hook absence; direct profiler UI activation only if it becomes a named player-facing requirement.
- Save Our Ship 2: space zombie generation, hologram exclusion, floating zombie mesh altitude.
- Vehicle Framework: active type/member and direct-flee patch target resolution plus concrete `VehiclePawn` move speed/timestamp behavior through the local `ZLTestVehicle` fixture. Direct-flee behavior around zombies remains additive if we later need a player-facing vehicle-flee scenario.

Assertions:
- If mod assembly is installed: target types/methods resolve and runtime smoke passes.
- If mod assembly is not installed: integration cleanly disables without load errors.
- No integration patch creates hard dependency load errors.

Artifacts:
- Installed/unavailable matrix.
- Decompiler target member ids for each installed integration.
- Runtime log summary for each installed integration.

Current runtime evidence:
- Added 2026-05-31 from `EMPTY.rws` with the active mod configuration `Harmony`, `Core`, `Zombieland`, and `RimBridgeServer` only. Static/source audit covered CE, AlienRace, Vehicle Framework, Dubs Performance Analyzer, SoS2, RimConnect, Camera+, and generic `ZombielandSupport` custom integration gates; `scripts/coverage-inventory.sh --dynamic-patches` listed the seven optional dynamic patch rows. RimWorld 1.6 decompiler confirmed `Verse.Map.MapUpdate()` remains available for the SoS2 floating-zombie draw hook, while `RimWorld.ShipCombatManager.GenerateShip` is absent from the Core-only assembly and must remain dependency-gated. `rimworld/get_mod_configuration_status` reported four active mods with no ordering issues, unsatisfied dependencies, session mismatch, restart requirement, or version warnings. The expanded `zombieland/startup_support_state` optional integration snapshot reported CE, AlienRace, Vehicle Framework, Dubs Performance Analyzer, SoS2, and RimConnect as absent with zero optional patch targets installed; Camera+ support methods and marker textures resolved but Camera+ was not active; `Customization` found zero registered `ZombielandSupport` evaluators. A 2026-06-01 CE pass installed upstream `CombatExtended-Continued/CombatExtended` commit `6380aeedeb24c49d20309fe233f268003c5f436d` as the local Git-backed `Mods/CombatExtended` checkout with the playable CE assembly present, enabled it before Zombieland, and fixed the stale Zombieland CE targets for the current `CombatExtended.HarmonyCE` namespace and `CompAmmoUser.Notify_ShotFired(int)` ammo event. With CE active, `startup_support_state` reported CE `packageActive=true`, `runtimeState=active`, `missingMemberCount=0`, and `patchTargetCount=4`; `EMPTY` loaded, `zombieland/damage_tanky_armor` proved tanky shield reduction through the CE damage stack, and `zombieland/active_electrifier_bullet_absorption_contract` proved active electrifier bullet absorption still wins while disabled electrifiers take bullet damage. A focused `zombieland/area_workflow_state setupFixture:true actionMode:ce-compat` pass then proved the named CE behavior tails: SurgicalCut ArmorReroute skip returned amount `7` without error, human CE projectile launch changed `797` pheromone cells while spitter launch changed `0`, the current `ProjectileCE_Bursting.Launch` override also changed `797` cells, spitter CE after-armor `Bullet` amount `22` returned amount `2` with `armorReduced=true`, and CE turret ammo stayed `8 -> 8` under reduced consumption but consumed `8 -> 7` with the setting disabled. The CE log-clean follow-up showed that the earlier noise was from the old `EMPTY` fixture, not from CE startup or Zombieland behavior: `EMPTY.rws` had three stuffless flak apparel items and saved vanilla equipment verbs. The CE-specific `ZL_CE_EMPTY_00.rws` fixture was repaired, loaded under CE, saved once through RimWorld, then reloaded in a fresh session; the clean fixture reload and `actionMode:ce-compat` produced no warning-or-higher logs. A post-install verification against the Git-backed CE checkout then loaded `ZL_CE_EMPTY_00` with active load order Harmony/Core/CE/Zombieland/RimBridgeServer, confirmed CE rootDir `Mods/CombatExtended`, package version `16.7.2.0`, no mod-configuration issues, five active Zombieland CE patch targets including base and `ProjectileCE_Bursting` launch targets, successful `ce-compat`, tanky damage-stack, and active-electrifier bullet-absorption smokes, and empty warning-or-higher logs. A follow-up `actionMode:ce-ranged-projectiles` pass on the current active 10-mod stack loaded `ZL_CE_EMPTY_00`, ran a real `CombatExtended.Verb_ShootCE.TryCastShot` with `Gun_BoltActionRifle`, recorded that the vanilla Zombieland `Verb_LaunchProjectile` forced-miss helper returns false for the CE verb boundary, spawned `CombatExtended.BulletCE` with intended target equal to the zombie, consumed ammo `8 -> 7`, changed `797` pheromone cells through `CETools_Patch2`, and kept warning-or-higher logs empty; CE's own ShiftTarget/accuracy still owns projectile destination and this evidence does not claim Zombieland's vanilla forced-miss override applies to CE ballistics. A 2026-06-01 installed Dubs 1.6 source/decompiler pass found the current `Analyzer.Profiling.H_ThingOverlaysOnGUI.GetPatchMethods` overlay entry and confirmed the old `Analyzer.Fixes.H_DrawNamesFix:Prefix` hook is gone. A follow-up active-Dubs runtime smoke temporarily enabled `Dubwise.DubsPerformanceAnalyzer.steam`, loaded `EMPTY`, verified `startup_support_state` Dubs `targetFamily=current_overlay_entry` and `runtimeState=active_current_overlay_entry`, spawned `Thing_Zombie6898`, captured `zl_dubs_active_overlay_smoke_zombie__cell_rect.png`, advanced the zombie into `Stumble`, and kept warning-or-higher logs empty. Generic `rimworld/open_main_tab` could not open the Dubs tab because it hit a bridge-side `WindowStack.Add` null-reference; this is tracked only as an external profiler UI item, not an open Zombieland compatibility blocker. The active Camera+ pass temporarily enabled `brrainz.cameraplus`, loaded `EMPTY`, verified `CameraPlus.CameraDelegates`, `CameraPlus.Caches.GetCachedCameraDelegate`, `CameraPlus.DotTools.GetMarkerColors`, `CameraPlus.DotTools.GetMarkerTextures`, Zombieland support methods, and marker textures through `startup_support_state`, spawned `Thing_Zombie6898`, captured furthest-zoom marker screenshot `zl_cameraplus_active_marker_smoke_zombie__cell_rect.png`, advanced the zombie into `Stumble`, and kept warning-or-higher logs empty. The active HAR framework pass installed upstream `erdelf/AlienRaces` commit `3ef0a5f` as `Mods/AlienRaces`, enabled it before CE/Zombieland, confirmed six active mods with no ordering or dependency issues, resolved the AlienRace type/field/method surface through `startup_support_state`, loaded `ZL_CE_EMPTY_00` visual-ready, and kept warning-or-higher logs empty. The HAR semantic follow-up used HAR's own `HumansAreAliensToo.xml`, which patches `Human` to `AlienRace.ThingDef_AlienRace`, then ran the existing `zombieland/zombie_eats_downed_pawn_contract` from `ZL_CE_EMPTY_00`: the enabled human case returned `targetAlienFlesh=true`, ate on tick `301`, removed nine parts, and logs stayed clean; disabled human, animal, and non-flesh controls matched expectations. A local `ZLTestSupport` fixture loaded before Zombieland and registered one `CanBecomeZombie` plus one `AttractsZombies` evaluator; with that hook active, `ZL_CE_EMPTY_00` loaded visual-ready, the reusable `zombieland/position_side_effects_contract` proved custom false/null/true return semantics and the `CannotBecomeZombie` false override, and warning-or-higher logs were empty. A local `ZLTestAlienRace` fixture loaded before Zombieland and extended the existing corpse/downed-pawn eating contracts with `ZLTestNonFleshAlienKind`: both optional HAR cases returned `targetFlesh=true`, `targetHumanlike=true`, `targetAlienFlesh=false`, `expectEating=false`, `tickHit=-1`, `missingDelta=0`, and `finalEatTarget=null`, with warning-or-higher logs empty. A local `VehicleFramework` checkout at `release/1.6` commit `1e8ec34` was initialized with submodules and built into playable 1.6 assemblies. The first active launch exposed a Zombieland `WorkGiver_Scanner.JobOnThing` fanout crash on Vehicle Framework's open generic `WorkGiver_CarryToVehicle<T>`; after filtering broad scanner targets to concrete non-generic methods, the active stack Harmony/Core/HAR/CE/Vehicle Framework/ZLTestVehicle/ZLTestSupport/ZLTestAlienRace/Zombieland/RimBridgeServer reported no configuration issues, resolved the Vehicle Framework type/member surface and one Zombieland vehicle patch target, loaded `ZL_CE_EMPTY_00` visual-ready, and passed the reusable position side-effects contract with concrete `ZLTestScoutVehicle` evidence: `vehicleHook.success=true`, `vehicleDef=ZLTestScoutVehicle`, `moveSpeed=10`, `changedCount=709`, `timestampAfter=320039000`, and warning-or-higher logs empty. A 2026-06-01 SoS2 source pass installed local `Bqr1s/SaveOurShip2` commit `7c9e1ec3`, decompiled the 1.6 assembly, resolved current `ShipInteriorMod2.IsHologram(Pawn)`, `ShipInteriorMod2.GenerateShip(...)`, and `GenerateSpaceSubMesh.Prefix(SectionLayer, MeshParts)`, and updated Zombieland SoS patch lookups to current targets with legacy fallbacks. Active SoS2 runtime is now claimed through the local `ZLTestSoS2Content` fixture plus copied CE `SOS2Compat.dll`: `EV.RUNTIME.SOS2.SPACE_FLOATERS` covers the OuterSpaceBiome floater/render path, and `EV.RUNTIME.SOS2.SHIP_HOLOGRAM` covers current ship-generation pawn conversion/event-zombie generation plus hologram exclusion.
- The 2026-06-01 active RimConnect pass installed `Better-Scenes/RimConnect-mod` commit `6b9c0a0`, built `RimConnection.dll` plus `RestSharp.dll` and `0MultiplayerAPI.dll` into `Mods/RimConnect/1.6/Assemblies`, and decompiled `RimConnection.Settings.CommandOptionSettings.DoWindowContents`. Active load order Harmony/Core/HAR/CE/VF/ZLTestVehicle/ZLTestSupport/ZLTestAlienRace/Zombieland/RimConnect/RimBridgeServer had no mod configuration, ordering, dependency, session, or version issues. `startup_support_state` reported RimConnect `packageActive=true`, `runtimeState=active`, `missingMemberCount=0`, and two Zombieland patch targets installed: `ActionList.GenerateActionList` postfix and `CommandOptionSettings.DoWindowContents` transpiler. `ZL_CE_EMPTY_00` loaded visual-ready; `zombieland/rimconnect_special_type_queue_contract`, `zombieland/rimconnect_rage_kill_contract`, and `zombieland/rimconnect_super_drop_contract` all passed. The main RimConnect settings dialog opened and screenshot `zl_rimconnect_active_settings_smoke__clip.png` was captured.
- The connected RimConnect follow-up added reusable local backend script `scripts/rimconnect-local-backend.py`, rebuilt the external local RimConnect DLL temporarily with DEBUG so RimConnect used `http://localhost:8080/`, and restored the release DLL afterward. The stub accepted `auth/mod`, `command/valid`, `loyalty/config`, `mod/world`, and `command/list`; it recorded 643 valid commands and world `Diphda II`. With `ZL_CE_EMPTY_00` loaded, warning-or-higher logs stayed empty, RimConnect settings showed Connected, the Items button was available, clicking it opened `RimConnection.Settings.CommandOptionSettings`, and `get_ui_layout` saw the Loyalty Store command-option window with category tabs, shifted header row, scroll view, item labels, and numeric cost/cooldown fields. Screenshot `/Users/ap/Library/Application Support/RimWorld/Screenshots/rimbridge_20260601_063617.png` captured the connected command-option window. All three RimConnect contracts passed again in the connected setup.

Completion:
- Covered for the current local mod set when every installed optional integration is runtime-tested and every missing optional integration is explicitly classified unavailable with source audit evidence. Combat Extended is installed and active with target, damage-stack, named behavior, and clean-fixture log evidence. Humanoid Alien Races is installed and active at framework-member level, HAR-patched Human eating semantics are covered, and custom/non-flesh alien behavior is covered through the local `ZLTestAlienRace` fixture. Generic `ZombielandSupport` registration and return-value semantics are covered through the local `ZLTestSupport` fixture. Vehicle Framework is installed and active at framework/type/patch-target level, and concrete vehicle timestamp movement is covered through the local `ZLTestVehicle` fixture. RimConnect is installed and connected-runtime covered for Zombieland action generation/spawn/rage/kill/drop behavior plus settings target patching and command-option UI/log-clean evidence through the local backend harness. SoS2 is installed and source/decompiler-aligned, and the active local fixture stack covers the named space-map floater/render path, current ship-generation conversion/event-zombie generation, and hologram exclusion; broader SoS2 gameplay remains additive rather than an open current-local blocker.

## S-Uninstall-Hygiene

Goal: prove the Zombieland removal flow creates a copy save that can load without Zombieland and has no dangling zombie defs, components, hediffs, thoughts, filters, factions, tales, or battle-log references.

Prior evidence:
- No specific post-baseline coverage found in commit subjects.

Source checks:
- Zombieland: `Source/ZombieRemover.cs`, `Source/Dialog_SaveThenUninstall.cs`, `Source/ContaminationSerializer.cs`, settings/world/map component serializers.
- RimWorld 1.6 decompiler: `RimWorld.Dialog_SaveFileList`, `GameDataSaveLoader.SaveGame`, `LoadedModManager.RunningMods`, faction/world pawn containers, filters, tales, battle log.
- Decompiler fact already checked: `Dialog_SaveFileList` remains an abstract save-file dialog with protected `DoFileInteraction(string)`, protected `ShouldDoTypeInField`, and public `PostClose()`, matching the override pattern in `Dialog_SaveThenUninstall`.

Fixture:
- Generated disposable save: `ZL_Uninstall_Hygiene_source.rws`.
- Output copy: `ZL_Uninstall_Hygiene_no_zl.rws`.

Setup:
- Create a deliberately dirty save: zombies, special zombies, zombie corpses, zombie faction, contamination, bitten/infected pawns, zombie thoughts/memories, battle log entries, tales, filters containing zombie extract/serum/corpses, active map/world components, and optional Zombieland items/buildings.

Runtime:
- Run save-then-uninstall on the copy, not on source.
- Disable Zombieland or inspect the save XML/type refs before attempting load if disabling mods is not automatable.
- Load the output save without Zombieland if possible.

Assertions:
- No remaining `ZombieLand`, zombie defs, Zombieland component class names, zombie faction, zombie hediffs, zombie thoughts, zombie battle/tale refs, or filters requiring removed defs.
- Player pawns retain safe non-zombie state.
- Save loads without unresolved cross-reference/type errors.
- Source save remains intact.

Artifacts:
- Source and output save names.
- XML/type-ref scan summary.
- Load log summary without Zombieland if runtime path is possible.

Current source/decompiler evidence:
- Started 2026-05-31. RimWorld 1.6 `Dialog_SaveFileList` still exposes the protected save-file interaction pattern used by `Dialog_SaveThenUninstall`; `GameDataSaveLoader.SaveGame(string)` still writes `Current.Game` through `Scribe_Deep.Look`; `WorldPawns.ExposeData()` still persists `pawnsAlive`, `pawnsMothballed`, `pawnsDead`, and `pawnsForcefullyKeptAsWorldPawns`; `FactionManager.ExposeData()` still persists `allFactions`; and `BattleLog.Battles` remains the public battle-log list surface. The source pass found that `ZombieRemover.IsZombieDef()` was too narrow for no-mod save hygiene because many Zombieland defs do not match `Zombie_`/`_Zombie` and do not use Zombieland classes, including examples such as `ZombieScare`, `ZombieThought`, `ZombieZapA/B`, `Chainsaw`, `Thumper`, and several custom jobs/effects. It now uses the owning Zombieland mod package as the authoritative def test.
- The same pass tightened additional source-only cleanup gaps that a dirty fixture must verify next: Zombieland recipes are removed from work-table bill stacks rather than only cleaning their ingredient filters; Zombieland hediff defs/classes and memories such as `ZombieScare` are removed from pawns; Zombieland apparel/equipment/inventory/carrying contents are removed from pawns; and active Zombieland jobs or mental states are stopped/reset before the copy save is written. This is not runtime completion evidence; it defines the next destructive fixture assertions.
- Runtime evidence added 2026-05-31. A reusable scenario contract, `zombieland/uninstall_hygiene_state`, generated a deliberately dirty fixture from `EMPTY.rws` and saved it as `ZL_Uninstall_Hygiene_source.rws`. The fixture contained normal/special zombies, spitter, symbiant, zombie corpse, Zombieland map/world components, zombie faction, Zombieland hediffs, jobs, mental state, memory, held/inventory items, stockpile filter refs, recipe bill refs, battle-log refs, tale refs, thumper, sticky goo, tar smoke, zombie extract, and serum. Source save warning-or-higher logs were empty.
- Running `actionMode=remove` wrote `ZL_Uninstall_Hygiene_no_zl.rws`. The pre-remove counters recorded `totalZombieThings=10`, `mapComponents=4`, `worldComponents=3`, `zombieFactionCount=1`, `zombieHediffs=3`, `zombieJobs=3`, `zombieMentalStates=1`, `zombieMemories=1`, `heldZombieThings=3`, `zombieRecipes=1`, `zombieIngredientFilters=1`, `allowedZombieDefs=13`, `battleEntries=2`, and `tales=1`. Warning-or-higher logs after removal were empty. An XML scan of the output save for `ZombieLand`, `Zombieland`, `brrainz.zombieland`, zombie defs/jobs/needs/hediffs/thoughts/items/components, `battleActive>Battle`, and the zombie faction returned no matches. The source save still had 80 Zombieland matches, proving the copy path did not scrub the original.
- The runtime pass found and fixed real hygiene gaps: pawn Zombieland needs such as `ContaminationNeed` were removed from internal need lists; pawn `battleActive` records are cleared when their battle is removed; map-component cleanup now happens after other map cleanup so Zombieland components are not reintroduced during cleanup; and `ZombiePathing` tolerates pre-finalized or teardown-time room/pathing grids so transient `TickManager` construction during scene cleanup does not log an error.
- True no-Zombieland load proof added 2026-06-01. `ModsConfig.xml` was temporarily matched to the output save metadata: Harmony, Core, and RimBridgeServer only, with local and Steam Zombieland packages inactive. `rimworld/get_mod_configuration_status` reported activeModCount `3`, no ordering/dependency/session issues, and no restart requirement. `rimworld/load_game_ready saveName=ZL_Uninstall_Hygiene_no_zl readiness=visual` loaded a playable paused map; `rimworld/step_game_ticks ticks=30` advanced from tick `5039` to `5069`; `rimbridge/list_logs minimumLevel=warning` returned no entries before load, after load, and after ticks. The original CE/Zombieland/RimBridge `ModsConfig.xml` was restored byte-for-byte after stopping RimWorld.

Completion:
- Covered: dirty source save generation, copy-save removal, XML/type-ref scrub, source-save preservation, and true no-Zombieland load/tick/log proof are all present.

## S-Dense-Performance

Current regression owner: [`scripts/zombie-ticking-benchmark.lua`](scripts/zombie-ticking-benchmark.lua), compiled/executed through real RimBridge Lua as a thin wrapper around the async companion-owned benchmark. It reloads the selected save before each speed sample and uses `zombieland/zombie_ticking_test_mode` only to remove ordinary and zombie-free-event zero-threat cleanup from the measurement. The companion's non-cancelled C# `finally` restores the copied settings timeline after success, nested-call failure, or operation cancellation. Follow-up operation `op_8b8ac99fc4ad469faa2757a492d0c702` injected both failure and cancellation through that scope and restored the exact `False:True|False:True` fingerprint each time; lowered-Lua wrapper operation `op_45aa09b926cd40ad8af5c73a755b6f8a` completed through the same companion boundary. Final 25-zombie operation `op_317cd88ec6ad441580072bea64bbfe34` retained all 25 at Normal/Fast/Superfast/Ultrafast; Ultrafast entered `Emergency` but kept all remotes at `1/120`, oldest age 116. The all-DLC 1,500-zombie matrix `op_e6899f48667e404fae09a3ea003b0972` kept every zombie alive: Normal 147 ticks/2542 ms/age 2, Fast 267/2607 ms/age 121, Superfast 294/2647 ms/age 120, and Ultrafast 301/2622 ms/age 119. Its 45-second Superfast soak advanced 4,654 ticks, held the queue at 1,500 with zero stale entries/compactions, and ended at age 118 after a 145-tick dynamic-priority transition peak—bounded jitter, not cumulative starvation. Only known old-save migration/ritual warnings appeared; there was no Zombieland warning/error.

Player-speed evidence owner: async companion tools `zombieland/zombie_ticking_create_player_fixture` and `zombieland/zombie_ticking_run_player_evidence`. The four reusable all-DLC saves `ZL_Ticking_Player_100`, `_500`, `_1000`, and `_2000` use the same 250×250 base map, production-random default type chances, 10% initially visible, and the rest beyond camera protection. Every speed is a fresh load followed by 500 ms warm-up and 10 seconds of real `rimworld/play_for`; internal ticking and forced speed are disabled, RimBridge's private ultrafast debug boost is explicitly false, and actual `CurTimeSpeed` is counted on every update. Primary operations `op_a8ac51c805b846e7954c8d07ed14c628`, `op_32cf6eae956f4b2386518535a61b10ea`, `op_3ef982b0ab5948989e79af8611f9bff2`, and `op_a8beda5964654c23841acdfe684d1302` covered 19,189,791 candidate opportunities: all 1,978,096 exactly visible opportunities and all 2,832,519 priority opportunities were selected, 6,061,202 selections equaled 6,061,202 real `CustomTick` calls, no zombie was never selected, and the worst gap was 122 game ticks. Independent 1,000/2,000 repeats `op_bca6a66d816a4ce6b690f7b13891bacf` and `op_bccfd3db490e48a897e5fe2f81ed3b71` preserved 100% visible and priority selection, zero starvation, and the same 122-tick bound. The final error-level journal was empty; dense loads produced only the separate generated-child body-type warning class.

Slow-host evidence owner: async companion tool `zombieland/zombie_ticking_slow_host_evidence`. It dynamically injects and measures aggregate CPU cost from a `Priority.First` `Verse.TickManager.DoSingleTick` prefix, before vanilla and Zombieland work but inside the production update-feedback window, then disables and unpatches itself in `finally`. Operation `op_390a659bc31740ed91d5509579947b13` calibrated 7.8125 ms per native tick plus an 8 ms spike every 60 ticks against a zero-zombie target of 105 Fast TPS. The final zero-zombie matrix measured Normal 59.9 TPS, Fast 106.1, Superfast 103.5, and Ultrafast 107.6, proving the host was already unable to sustain practical 2× throughput without zombie work. The same fixed profile across the 100/500/1,000/2,000 saves covered 9,080,647 candidate opportunities: all 937,961 exactly visible, all 1,152,860 priority, and all 195,197 special-type opportunities were selected; 1,629,500 selections equaled 1,629,500 `CustomTick` calls; no zombie starved; and the worst gap was 123 game ticks. Cleanup returned `enabled=false` and `patchInstalled=false`; the final error journal was empty, with only the existing generated-child body-type warning class.

Goal: bound runtime cost after correctness scenarios stabilize.

Prior evidence:
- `c6f2b87 Optimize zombie tick sampling`
- `4e90c65 Trim zombie ticking hot path`
- `394e740 Optimize chainsaw ticking`

Source checks:
- Zombieland: `Source/TickManager.cs`, `Source/ZombieStateHandler.cs`, `Source/PheromoneGrid.cs`, `Source/ZombieWanderer.cs`, `Source/Chainsaw.cs`, contamination tick paths.
- RimWorld 1.6 decompiler: tick manager and map component tick dispatch if measured results look suspicious.

Fixture:
- Generated save: `ZL_Dense_Performance_base.rws`.
- Follow-up save after normal-speed window: `ZL_Dense_Performance_after_normal_00.rws`.

Setup:
- Use a fixed-size map with deterministic colonists, weapons, buildings, and a dense but bounded zombie count.
- Include at least one chainsaw, one shocker/thumper if they are expected to be common, and a small contaminated area.

Runtime:
- Warm up for a fixed number of ticks.
- Run paused single-tick stepping and normal-speed stepping for fixed windows.
- Capture tick duration, frame duration if available, zombie count, active jobs, and log summary.

Assertions:
- No runaway allocations/log spam.
- Tick duration remains within an agreed local baseline for the machine.
- Zombie count and job state remain stable enough to compare runs.

Artifacts:
- Timing sample JSON or compact text.
- Save name.
- Deduped log summary.

Current runtime evidence:
- Added 2026-05-31 after a static/decompiler pass over `Verse.TickManager.TickManagerUpdate()`, `Verse.TickManager.DoSingleTick()`, `Source/TickManager.cs`, and the Zombieland tick-manager patches. A generic fixture helper, `zombieland/spawn_zombie_group`, was added for bounded horde setup; it is not scenario-specific and can be reused by later dense/gauntlet rows. From `EMPTY.rws`, one disabled-work colonist bait was spawned at `(100,100)` and 80 normal zombies were spawned in a radius-25 group around the same cell. Initial `zombieland/get_status` showed `spawnedZombieCount=80`, `ordinaryZombies=80`, `tickManager.cachedZombieCount=80`, and a clean paused state before runtime. The fixture was saved as `ZL_Dense_Performance_base.rws`.
- The paused exact-step baseline ran `rimworld/step_game_ticks ticks:300 pauseFirst:true` and advanced `5038 -> 5338` in 5062 ms with no warning-or-higher logs. This deliberately remains a wall-clock exact-step baseline only: it mirrors paused Dev TickOnce behavior and does not run the normal `TickManagerUpdate` prefix that populates `ZombieTicker.managers`.
- The normal-speed window then ran for roughly 20 seconds and was paused for evidence capture. Post-run `zombieland/get_status` showed all 80 zombies still live/cached, `zombieGrid.liveOrdinaryWithGridCount=80`, `zombieGrid.nonZeroCells=80`, `tickManager.currentZombiesTickingLength=80`, `tickManager.currentZombiesTickingIndex=80`, and `zombieTicker.managersCount=1` with `currentTicking=80`, `maxTicking=80`, `zombiesTicked=80`, `percentTicking=1`, and `frameWatchElapsedMilliseconds=118`. A 20-zombie sample was alive, pathing, and in `Stumble`/`Tracking`; the bait colonist was alive in `FleeAndCower` at `(91,19)`. Warning-or-higher logs were empty after the normal-speed window. The post-window state was saved as `ZL_Dense_Performance_after_normal_00.rws`, then `EMPTY.rws` was reloaded and verified with zero live/cached zombies and no warning-or-higher logs.

Completion:
- Covered when there is a repeatable local baseline after correctness scenarios pass.

## S-Symbiant-Symbiosis

Status:
- The simplified Symbiant surface has current runtime evidence for settings cap behavior, dynamic severance ingredients, door slowdown, map-cache cleanup, create-event spawning, coherent room placement/migration/construction repair/exterior overflow, one-cell core selection with non-core click-through, smooth visual interaction, persisted core state, whole-body hostile targeting, selected UI semantics, no player-facing selected status gizmo, four developer-and-god-mode action gizmos, inspect text, and repeated load after restart. Its 2026-06-22 click-anywhere evidence is historical and superseded by the single visible selection-core design. Older placement evidence is likewise historical where it conflicts with the authoritative 2026-08-10 one-connected-patch-per-room, direct-founding, and all-indoor-space-before-overflow model. Intended behavior and the durable release gate live in `docs/features/symbiant.md`.

Fixture:
- Start from `SYMBIANT-TEST-MAP`, `EMPTY.rws`, `ZL_DPA_Profile_500_ProfileReady`, the persisted visual fixture `ZL_Symbiant_SelectionCore_00`, player regression saves `symbiant-test` and `symbiant-wall-problem`, or a small colony room fixture with enclosed used rooms, at least one door, constructed divider/perimeter-wall edges, a huge-but-indoor control, a no-room/outdoor-only variant, at least one eligible free adult colonist, ineligible host controls, valid feed corpses, zombie extract, industrial medicine, glitterworld medicine, and herbal medicine as the below-tier control.

Bridge contract:
- `zombieland/symbiant_infestation_state` supports `read`, `spawn`, `createEvent`, `expand`, `shrink`, `move`, `feedCorpse`, `removeHostHediff`, `killHost`, `stageRetreatSave`, `stress`, and `cleanup` modes. `zombieland/symbiant_feeding_contract` owns corpse grouping/exclusions, non-root visible-core menu routing through job target C, pulse strength, and cached placement-read probes. `zombieland/symbiant_expansion_contract` owns classifier, capacity, establishment-anchor, overflow, wall-commit rollback, deferred multi-pulse feeding across a wall-topology rebuild, and structural scan probes. `zombieland/symbiant_multi_room_contract` builds reversible separated-room fixtures and proves one cardinal patch per room, direct empty-room founding without divider damage, room balancing, interaction-core handoff, component rendering, and whole-body targeting across globally disconnected room patches. `zombieland/symbiant_relocation_contract` owns migration-queue normalization/revalidation, topology-event coalescing, largest-component retention, immediate one-cell/root indoor return when capacity exists, founding-state transfer during relocation-debt repayment, construction repair, exterior return, and authorized-overflow no-room grace/dormancy with further exterior growth blocked; its optional `availableRoomRootOnly` and `constructionRootOnly` modes isolate those canonical-root regressions from the larger fixture matrix. `zombieland/symbiant_selection_core_contract` proves the one-cell ordinary inspection surface, context-menu suppression, installed selector filter, all-logical-cell active manual targeting, rectangular-gap rejection, discovery handoff, occupied and outgoing-removed source hit-testing, stable feeding-job destination mapping, fallback rendering, and valid core repair through shrink. Render/read state reports the transient hover/selection/discovery blends for deterministic real-time easing probes. `zombieland/symbiant_cross_map_save_contract` supplies staged `stage`/`read`/`return`/`cleanup` modes for true multi-map save/load.
- `zombieland/symbiant_host_effect_isolation_contract` is the generic coupling firewall gate. It adversarially checks fixed inert marker state, real pregnancy, real labor removal and controlled-safe childbirth completion, anesthesia, host injury-worker classification despite contradictory `harmsHealth` metadata, effect-only damage with misleading health metadata, Symbiant-local control effects, and safe host detachment after direct Symbiant destruction.
- `zombieland/symbiant_combat_isolation_contract` supports `contract`, `setup-assault`, `read-assault`, and `cleanup`. The contract mode proves one real Pawn/attack target, one root `ThingGrid` registration, logical boundary cells, a bound ranged shoot line, melee target A plus stand/strike cells B/C, negative player/animal controls, and exactly one shared-health hit from a remote-cell explosion. A dedicated linked host stays in a same-map cryptosleep casket so the real attack scenario can verify active harmless echoes without offering the host as an alternative raid target. `setup-assault` removes other bystanders and gives two bolt-action-rifle hostile fixtures a real `LordJob_AssaultColony`; `read-assault` reports their lord, duty, stance/focus, last attacked target, melee B/C state, and the partitioned pool/source/echo/real-host-injury facade.
- Use `symb-under-attack.rws` unchanged as the natural repeated-melee fixture: its three knife raiders should attack the Symbiant while the host stays safe and healthy. Supplement it rather than editing it: the combat contract supplies two hostile bolt-action rifles with pistol fallback and CE ammunition setup, while controlled contract cases own Flame, EMP, and logical-cell explosion coverage.
- The bridge read state should report host identity, hediff presence, Moving capacity, Move Speed, movement ticks, growth state, current cells, selection-core cell/motion/discovery state, ordered benefit list, eligible room cells, relocation debt/timer, uprooted state, severance operation state, one-cell selector geometry, render metadata, and simplified settings.
- Current simplified runtime evidence, 2026-06-22 on `SYMBIANT-TEST-MAP`: settings (`op_b71ff072d53740beaa65492c17d6c68b`), severance (`op_62f6304761dd4856ab7b238e7f5249dc`), door path cost (`op_ad90e439d0144a768e06a3c51c14b606`), map cache (`op_cd933d6f1a0344c195cf85f0a2ca31c3`), event creation (`op_dcf73c3cdec44689b76436c9e2d85531`), expansion (`op_4ec9a17a81904fdd9cb510286ddc9440`), historical click-anywhere selection (`op_f89e3bf86b504f9eb4d011ab4926f4a6`), selection semantics (`op_66b3469fc8a84ff4aa5b342638c09143`), selected gizmos (`op_13ffd847c5a140439ea61c69bf3caf2e`), and repeated load (`op_bd1c67d8bdf242d0a7a8a493d3977f3d`). The click-anywhere result is retained only as historical evidence and is superseded by the visible selection core. The removed `SymbiantCoagulantPack` def produced expected old-save load errors and the map still loaded playable.
- Selection-core refresh, 2026-08-03 on the all-official-DLC Steam profile: `zombieland/symbiant_selection_core_contract` `op_10a0bfe29f25496fb71869718070a6b0` passed the complete one-cell/all-body/gap/discovery/ambient-move/shrink matrix. Actual map clicks passed through a non-core slime cell to Steel (`op_4cdd71bfe52f4365af4d3a178a63953a`) and selected the Symbiant from its core (`op_5e5fcbe45d5a489a9761970ead5c731b`). The persisted 21-cell `ZL_Symbiant_SelectionCore_00` fixture loaded with a valid one-cell selector; paused real-time probes recorded gradual hover `0.03 -> 0.49 -> 0.99`, selection `0.24 -> 0.64 -> 0.99`, and deselection `0.97 -> 0.57 -> 0.02`. Close screenshots `zl-symbiant-selection-core-final-hover-close__cell_rect.png` and `Zombie Symbiant 1x1 Final Smooth__cell_rect.png` cover the final alpha-feathered multi-cell and one-cell visuals; the one-cell core remained inside its underlying blob. Whole-body hostile targeting stayed green in combat contract `op_9aa7ee29efd940e6af6ad00903c0f873`, including non-root ranged/melee/range-gate/explosion cells. Both final contracts had empty warning-or-higher correlated logs (`op_229665365ce24f18a5946729dec7767e`, `op_e0771351b6234390b93de81847cc91f7`).
- Selection-core handoff review fix, 2026-08-04 on the same all-official-DLC Steam profile: `zombieland/symbiant_selection_core_contract` `op_e257d517b958497bb0570035d8474d69` sampled a deterministic nonadjacent core move at `0`, `0.1`, `0.25`, `0.5`, `0.75`, and `0.9` progress. Every visual center stayed inside its derived hit cell, the one-cell selector followed it, and generic targeting found the Symbiant there; correlated warning-or-higher logs were empty (`op_d49c8480f8b7492fb1d448f98223ec98`).
- Selection-core fallback review fix, 2026-08-04 on the same all-official-DLC Steam profile: the refreshed contract forcibly released every render resource and bypassed GPU-metaball preparation while the core was moving over non-root cell `(108,109)`. The independent transparent-alpha core resources rebuilt and drew successfully, the one-cell selector remained aligned and targetable there, and all five logical body cells stayed targetable (`op_2296e6123a57467e84b7fbc95c260248`); correlated warning-or-higher logs were empty (`op_de9dd5a21623491a88dc5d453fbc5f99`).
- Selection-core movement scaling review fix, 2026-08-04 on the same all-official-DLC Steam profile: after one real ambient move had removed and re-appended the core as the last ordered cell, the repeated-move probe examined one preferred target and performed exactly two connectivity checks: one for that core source and one final commit recheck. The preferred pool reported its 12-target cap, carried the core again, and the complete selection/targeting/handoff/fallback/shrink contract remained green (`op_86818dfeac754c1a9a37a2147cc94a96`); correlated warning-or-higher logs were empty (`op_b9a97bce32a64be0b5933b031b2d3574`).
- Selection-core initialization scaling review fix, 2026-08-04 on the same all-official-DLC Steam profile: the refreshed contract forced a missing-core reinitialization on a compact 25-cell body. It cheaply ranked all 24 eligible non-root cells, shortlisted exactly 12, performed exactly 12 whole-body connectivity checks, selected a valid logical core, and kept the complete selection contract green (`op_81de73c5a55f4b6987ed1386222ccaa4`); correlated warning-or-higher logs were empty (`op_8106625dc8d046fd8945ff391bdf4b2d`).
- Selection-core tooltip input review fix, 2026-08-04 on the same all-official-DLC Steam profile: RimWorld 1.6 source inspection confirmed the public window-obscured signal and the exact bottom main-button and alert-stack geometry. The refreshed contract rejected a window-covered point, bottom-button point, alert point, and off-screen point while accepting open map and map immediately beside the alert stack; the complete selection contract remained green (`op_b9c338581f584b42b3f0d11cf41ea17a`), with empty correlated warning-or-higher logs (`op_fc2a83f7d92c41eea257f009c4fbc33b`).
- Selected developer-gizmo validation, 2026-08-04 on `ZL_Symbiant_SelectionCore_00`: with both developer mode and god mode enabled, core selection exposed exactly the four requested action widgets (`op_90660245585d486a90b045c1c64d0753`, `op_9bfbd81b757e4f45b1b2f1a486c2f292`); disabling god mode hid all four. A transient enclosed-room setup proved one normal shrink (`21 -> 20`), one normal growth (`20 -> 21`), and one single-cell move with stable count. The assignment popup listed the three eligible fixture colonists, assignment to Sarai created the bond/hediff, and the second invocation removed both. Vanilla `Apply Damage -> Blunt -> Body` executed from the canonical main cell and reduced shared health by exactly 5. Reloading the fixture discarded all staging and action mutations (`op_dfff70e17b9f4a02a9996bff54b3ddb8`); no later warning-or-higher logs remained (`op_ec4b4c7736cf476db6d649036afa05b5`).
- Multi-room growth and relocation validation, 2026-08-04 on exact-profile save `ZL_DPA_Profile_500_ProfileReady`: `zombieland/symbiant_multi_room_contract` (`op_6629310b1876436487542640e44fd053`) kept the first 25-cell room local through six occupied cells, reached the 25% threshold at seven, crossed the adjacent room's door before considering its wall or a remote room, and handed the interaction core to the first cell inside that room. It kept the second room local to the same threshold, then founded a remote patch with two disconnected components and two render patches. An original-patch cell and the remote-patch cell both remained generic-targetable; the remote cell also remained in the hostile combat geometry and boundary. Removing the original room's roof relocated all seven invalid cells with stable total size, spread them across two valid rooms, and carried the core to a founding destination. The existing expansion (`op_bcdc1564983d4f10a39bb9f9723ba9dd`), relocation (`op_850e590b32cd47cfb57db6ce0a1bacf2`), and full selection-core (`op_7d8e30fd05ba45c986fa5d7b1edc66ab`) regressions also passed. Their four correlated warning-or-higher queries were empty (`op_bc151a3fee9445bea2d7b6a7ce713a17`, `op_439d438e351d4bffa42fcc60fc7e4c68`, `op_8185137374df4247b48ea25d71cd329f`, `op_78b04b04f3884f8dbe33e5e9ccd67596`).
- Authoritative placement-model validation, 2026-08-10 on the all-official-DLC Steam profile: expansion `op_4c1df5ccf7d441f888a5718ef2f2377e`, multi-room `op_8c1441d75023437bbb4ebed1022a4cf5`, relocation `op_5233230cf800406e9e662188b92f9522`, feeding `op_a27e539c86a2475b8eaa738ec0229213`, selection `op_2317937578a04239af282c57aed8fa04`, door path `op_36f8850fcdc2417a981cd06ceb59c187`, and settings `op_165ff1f4d13144e0becb034a5b8ac423` all passed in the final paired deploy. The expansion contract included free-door capacity, exact all-full authorization, a forced failed wall commit that restored the wall and left authorization false, one successful safe perimeter breach, cardinal outdoor continuation, and no second breach. Relocation passed three consecutive runs after the fixture explicitly removed its test roofs before taking down supports (`op_28bbb95522704f6dabeda97eec359049`, `op_67484faad8c84b8e852780ce2e15091b`, `op_5233230cf800406e9e662188b92f9522`). The focused current-profile root-construction rerun `op_f3719e1a64704809a90326636ea33efb` returned `deferredRootRecoveryResult=false`, then relocated and rebased the root through one repair plan while preserving all five cells, the wall, host link, and valid core. The final available-room root regression `op_9058f09f59e947b7a45356fb0af9025b` exposed a one-cell Symbiant beside another usable 25-cell room and proved it bypassed no-room grace, relocated the canonical root inward without count change or debt, and preserved its host; correlated warning-or-higher logs were empty (`op_8a0089a557e34140ac8140b9b62dfcbe`). Structural probes recorded no room work in hot `GrowthState` reads or topology callbacks and one cached 25-cell capacity evaluation across 64 same-tick feed checks. Correlated warning-or-higher logs were empty for every final contract.
- Player-save validation, 2026-08-10: `symbiant-wall-problem` loaded compatibly (`op_6df10524f4b340388a76948c0140ac9c`), showed the left room only 10/15 full and the right room empty (`op_52e12b7784914e69aa4892d5b7dedcb5`), then one expansion founded `(152,116)` in the right room without touching the divider (`op_0c6f5250f4534b84b31dec47b764ff47`). An independent cell read found the same five intact full-health player walls at x151/z112..116 (`op_8f66fb8263ce4f1db3fb357783e16411`). `symbiant-test` loaded compatibly (`op_2d18b796a6074609ae2f5d8b77e8a78e`), preserved a connected five-cell room patch across one normal movement (`op_ba3166366b8946f58b69646dc7772ef0`), and founded one cell in the right room on expansion (`op_cdf54ef551324767bd85eb7dd819ad6a`). This produced two intentional global components but exactly one connected patch per occupied room; `/tmp/zl-symbiant-final-two-rooms.png` is the RegionShot visual artifact. All eight operation-correlated warning-or-higher queries were empty.
- Feed-menu draft-state regression validation, 2026-08-11 through RimBridge: on `symbiant-wall-problem`, the eligibility-state probe showed that undrafted Emma's reachable and reservable corpses were all outside her `Home` restriction, while unrestricted Pyrrha received the same three Symbiant feed rows undrafted (`op_262f3592d1f84c169f3ede09d1c73a70`) and drafted (`op_a8c90f5a4f9044949d27ab52ad328834`). Emma received no feed rows in either state once item forbiddance and allowed-area policy were evaluated independently of drafting (`op_edd45e81b5b947e195a436278b76cc23`, `op_d66db3554c044ad7bfc63c69c304848d`). The real undrafted float-menu route exposed three grouped rows (`op_49bb16fcbb9e48eba95f8e4a263164cd`); choosing the fresh-rat row (`op_9e8e67d1e0874c2d94bf0e7fbcacafa5`) left Pyrrha undrafted and started `FeedZombieSymbiant` (`op_60d1a13e10034b5f909c436b7c0305bc`). The broader feeding contract remained green on compatible `symbiant-test` (`op_497d6e30586a4764a31f070bff45c469`), including human/animal rows, animal deduplication, forbidden/inaccessible/mechanoid exclusions, job targets, growth pulses, and cached-capacity performance checks. Warning-or-higher logs were empty for the menu, action, and contract (`op_68d85731b0284770ab7c4ae4c6d5b9c7`, `op_eace8d89d7a24451937f55605a58003c`, `op_8f9b5795cd0b487094f565624beee96e`).
- Historical 2026-06-15 contracts remain useful for broad behavior: feeding, unsafe damage, combat isolation, benefit, natural spawn, discovery letter, expansion, host availability, save/load, and cache switching. Rerun them when touching those behavior surfaces.
- Final 2026-07-12 Release evidence reran every current behavior contract, true two-map save/load/return, final-cell retreat save/load, render create/read/cleanup, direct spawn/cleanup, and a 760-pawn/4000-cell stress fixture. The final-cell fixture must remain present while paused after load and disappear on resumed game time without a draw-pass deregistration warning or `WorldPawns` residue.
- Symbiant hostile-combat evidence on 2026-07-12 used the Steam RimWorld 1.6 assembly and paired deployed mod/companion DLLs. Final vanilla contract `op_6f0c8505306b465d94837e47925d32e0` proved five logical cells, one Pawn, one root grid registration, two independent hostile rifle verbs with non-root lines to `(125,120)`, two reachable melee B/C pairs, negative player/animal controls, and one remote-cell explosion hit for exactly 5 shared health. Real assault setup `op_bea7140cd1164053b153f7614137ce08` immediately entered `Stance_Warmup` focused on the Symbiant; after 1500 ticks the raider recorded the Symbiant as its last attacked target and rifle fire reduced shared health `8940 -> 8868`. With installed Combat Extended 16.7.2 temporarily enabled before Zombieland, setup `op_1d0419fc25354621a65b734972c4e301` loaded eight rounds, entered a CE ranged warmup on the Symbiant, and retained the same logical-cell/explosion assertions. After 1200 ticks, CE fire reduced shared health `8940 -> 8897` and the raider transitioned into `AttackMelee` at B `(104,100)` against C `(105,100)`; another 300 ticks reduced it to `8887`, proving the CE melee boundary too. CE was disabled afterward and the normal Steam-profile loadout was restored. No Zombieland, Symbiant, CE, exception, or error warning entries were present in the final CE log query.
- Review follow-up on 2026-07-12: final normal-profile contract `op_ec618148359241e192eb6cee52f5f2a0` added explicit root-out-of-range/logical-in-range selection (`12`/`11`, max `11.5`), caller-validator rejection, all-cells-thick-roof rejection, and live melee B/C rebinding after removing the attacked cell. Vanilla assault step/read `op_0ae218d8117a4929b76212b32e89c3a1`/`op_b84ffe059cb64930a35e9a30ae973cca` left both rifle raiders focused on the Symbiant and recorded 108 gunshot pool damage with a healthy host. Final CE setup/step/read `op_f031ded58cb54907a2db26e0b9bcaed5`/`op_c8b50044e02545be9f00a761191cc5aa`/`op_60003e33fbef4c7e97b3b7e57a1c6cf4` proved CE's actual ordinary-ballistic `CheckCellForCollision` path: 15 logical bounds tests, last at non-root `(105,100)` while root was `(106,100)`, 120 gunshot pool damage, and no host injury. All correlated warning queries were empty, and the normal non-CE mod list was restored afterward.
- Review resolution on 2026-07-13 used the paired Release deployment in the isolated all-DLC profile. Normal combat `op_509f46f159ef4a08a35e41682cd37a46` passed deterministic five-cell geometry, two independent logical shoot lines, acquisition with the root outside range, validator/thick-roof rejection, non-mutating melee rebinding, scan memoization/gating, beam/spray exclusion, same-center blast-score parity, and `excludeRadius` damage suppression. Unsafe damage `op_e23742d7c3944de7a9d9607abd13c394` passed the exact shared-health recovery deadline and the full damage/echo/map-lifecycle matrix. Avoid-grid routing `op_0705f5962150485db124a83e7deb09b7` proved one 62,500-cell native snapshot shared by four request customizers, safe resolve/dispose/cancel release, route avoidance, and snapshot retirement when avoidance was disabled; in-process reload `op_7350c5d2cad440d68f197b628a8ef200` returned visual-ready with no errors. With Combat Extended 16.7.2 temporarily active and configuration-clean, combat `op_f405e3b67e564e0dbb83df9f01815e48` and unsafe damage `op_6e681c93057548cc803e6105e019b121` both passed, including CE ranged/melee/explosion selection and zero residual anatomy injuries after direct Cut and Burn damage. No errors appeared after the known non-CE-save migration messages, and the normal all-DLC mod list was restored.
- Host non-health damage follow-up, 2026-07-23 with Combat Extended `16.7.3.0`: full unsafe contract `op_cff17fc2098145398f632a0da4ffd315` recorded the real host packet sequence. Extinguish (`999999`, non-health) applied firefoam with zero pool drain, Stun (`5`, non-health) stunned with zero pool drain, and EMP emitted a harmless outer packet plus CE's separate harmful `Electrical 12` packet, producing exactly 12 expected and actual shared damage. The 40-cell organism, host, and exact bond survived; cleanup left no active or world-pawn residue, and the correlated warning query was empty (`op_69e7e4b61b554700aa82d8c6933685fb`). The 2026-07-29 behavior classifier supersedes this operation's old metadata-based expectation; the secondary packet is now classified independently by its worker.
- Generic coupling-firewall follow-up, 2026-07-29 on Steam RimWorld with all official DLCs and no Combat Extended: `zombieland/symbiant_host_effect_isolation_contract` (`op_befe2502c1954be79c239a20505a2a0a`) proved a hostile marker-severity write, real `PregnantHuman`, real labor removal through `ApplyBirthOutcome`, one newborn, postpartum exhaustion, lactation, real `Anesthetic`, a false-metadata Blunt injury worker, a true-metadata effect-only `SeismicWave`, Symbiant Stun, and direct Symbiant destruction cannot cross unintended coupling boundaries. The controlled birth disabled only vanilla's intentional random maternal-death roll; the host and exact bond survived with shared health unchanged. The full unsafe/lifecycle matrix (`op_e7a6e1a901184a52b49014b3f3b40401`) retained genuine injury sharing, Symbiant damage echoes, recovery, cross-map cleanup, valid safe-detaching corpse creation, and lethal host collapse only on actual shared-pool exhaustion. Its same-map and cross-map external host-death rows each produced exactly one funeral obligation, at most one matching funeral letter, and exactly one death letter before the surviving Symbiant severed and began retreat. Error-level logs were empty; warning-level entries were limited to RimBridge startup timing and the loaded fixture's existing hidden-ritual/zombie-faction repairs.

Release candidate checks:
- `scripts/build-quiet.sh`.
- XML and translation validation when XML/languages changed.
- One-cell inspection-core contract, real non-core/core player clicks, persisted save/load, smooth paused hover/select/deselect blends, and both one-cell and multi-cell screenshots. Re-run the whole-body hostile combat contract in the same final build so inspection filtering cannot narrow enemy targeting.
- Cold-load warning-or-higher logs clean except documented old-save missing-def compatibility errors.
- Natural or bridge spawn/createEvent creates one active Symbiant, one initial cell, and a linked eligible host when one exists.
- Natural spawn with no eligible host or no used indoor rooms is skipped cleanly; debug hostless slime is test-only and has no surgery/host trauma.
- Host selection rejects children, prisoners, slaves, guests, temporary joiners, quest lodgers, caravaning/unavailable pawns, holograms, non-flesh optional-mod pawns, existing symbiant hosts, and late/active zombie infection cases.
- Host selection is independent from spawn-room steering; moving one colonist into a disposable room must not deterministically choose that pawn.
- `SymbiantSymbiosis` is fixed-severity display/sync state: it remains at `0.001`, never encodes growth or benefit factor, and is explicitly nonlethal/non-tendable with zero pain, bleeding, and summary-health impact. External removal while the authoritative link remains active causes the Symbiant to recreate it; unrelated host hediff transitions remain local.
- Expansion pulse adds exactly one legal indoor/door cell, direct-founds one legal empty-room cell, or begins one authorized exterior transition; it never combines mutations in one pulse. Empty-room founding prefers bare floor but falls back to passable furniture when every otherwise valid floor cell is furnished, preserving the furniture.
- Door cells can be occupied while the door remains a door.
- Natural rock blocks expansion.
- Every eligible occupied room contains at most one cardinal component. A valid component outranks a larger wholly invalid component before size and root/core tie-breakers, so a valid canonical-root patch remains established while the invalid component drains. Existing disconnected cells are queued once, entries that become connected are pruned, and each movement pulse performs at most one hard repair relocation in addition to normal movement.
- A persisted establishment anchor, not the moving visible core, keeps the current room at 25% before founding another relevant room. Founding may cross distance but is an unanimated direct placement and never damages the divider.
- All relevant room capacity, including legal door cells, is exhausted before exterior overflow. Non-full-but-blocked rooms remain contained. An exact all-full audit authorizes at most one safe constructed perimeter-wall breach per episode, carries the connected source-side patch into authorization when the room rebuild makes it exterior, keeps unrelated damage outside that component, and continues every later outdoor cell from that patch instead of another occupied perimeter door; eligible-room dividers, junction/corner walls that would join an eligible room, and natural rock are never breached.
- Exterior/invalid cells move into newly available indoor placements before growth continues and do not increase total footprint. Exterior ambient moves are rejected before mutation when the destination would have no surviving cardinal neighbor after removing the source. Overflow authorization clears when the last exterior cell is gone; unauthorized damage/roof exposure cannot bootstrap outdoor growth. If every relevant room disappears after an authorized overflow, the organism starts no-room grace, blocks further exterior growth, and becomes dormant when no reseed room appears; dormant capacity scans occur only when the blocked retry deadline is due or a topology notification wakes it.
- Impassable construction may cover logical cells. The completed building remains, covered cells relocate through one shared destination plan where possible and are otherwise crushed in one batch without debt; root, core, active motion, load-time, and multiple-building cases preserve a valid shell or end it cleanly.
- Lowering the maximum below the current footprint or footprint-plus-debt does not delete cells/debt. It blocks net growth and breaching while allowing count-preserving repair.
- Feeding with a humanlike or non-humanlike non-Zombieland corpse adds the expected growth pulses and consumes the corpse.
- Drafted and undrafted selected colonists receive the same eligible feed choices when they right-click the visible interaction core. Forbidden corpses and corpses outside that colonist's allowed area remain excluded in both states; drafting is never required merely to expose an otherwise eligible feeding order. When the visible core differs from the canonical root, the order stores that core in target C and uses it for reachability, route destination, facing, waiting, and progress.
- Ordinary, fresh, humanlike, and non-humanlike corpse pulse strengths are verified for growth amount.
- Feed selection chooses the sensible total route for pawn-to-feed and feed-to-symbiant distance.
- Stockpiled corpses accelerate growth but do not directly remove or neutralize the Symbiant.
- Current acquired host benefits are visible in acquisition order on the host hediff tooltip and Symbiant info/inspect surface.
- Linked host receives the intended acquired benefits, including zombie infection immunity and the randomized fixed-50% mood benefit when that benefit has been obtained.
- Each acquired movement benefit increases the host's same-map Moving capacity by 25%, and each acquired manipulation benefit increases Manipulation by 25%; both stack. Health shows the increased capacities, the colonist info dialog's Move Speed worker returns the corresponding increased movement value, `GetStatValue` agrees with that worker, and real pathing uses fewer ticks per cell. Dormancy removes both capacity effects and returning to the Symbiant's effective map restores them.
- The host hediff retains the `Benefits:` acquisition list and places a localized `Combined:` heading immediately before RimWorld's aggregate Moving and Manipulation capacity rows, so stacked `+25%` entries and their combined `x150%` result are visually distinct.
- Each acquired skill benefit stacks and grants every enabled skill +4 below 200% Zombieland difficulty, +3 from 200% to below 300%, +2 from 300% to below 400%, and +1 at 400% or above. `SkillRecord.GetLevel` and `GetLevelForUI` agree at the exact 200%, 300%, and 400% boundaries, and disabled skills remain disabled. Vanilla Bio rows show base plus the actually applied Symbiant contribution (`10 + 4`, or `18 + 2` at the level cap); a fully capped skill stays `20`. The hover description adds one localized Symbiant-benefit row immediately below `Skill level`, including the nominal bonus when the cap reduces it. Dormancy restores the plain base label and removes the extra tooltip row; reuniting restores both. If another mod replaces the expected numeric-label anchor, the transpiler fails open and the semantic accessors remain authoritative.
- `SeverSymbiantSymbiosis` is available on the linked host, requires zombie extract and industrial-or-better medicine, accepts glitterworld medicine, rejects herbal medicine, removes the link and hediff without host trauma, and leaves the unbound Symbiant to retreat.
- Host death or severance causes retreat/disappear behavior without reward drops.
- Any external host `Pawn.Kill`, including vanilla's fatal childbirth outcome, completes before bond teardown. It produces exactly one death letter and no more than one funeral obligation/letter, removes the marker after clearing link authority, and starts the surviving Symbiant's retreat once.
- Symbiant shrink clears contamination on each removed cell once.
- The visible pulsing core cell selects the Symbiant; another slime cell clicks through to ordinary cell contents. The selected Symbiant brightens, exposes useful inspect/info text, and does not expose normal pawn tabs or a custom status gizmo. Generic targeting and hostile combat still resolve every logical slime cell, while empty rectangular-bounds gaps do not resolve the Symbiant.
- The exact host identity and single display hediff persist through raw despawn, containment, caravans, shuttles, gravship travel that preserves the origin map, and spawning on a second loaded map. All active benefits, immunity, protection, healing, shared damage, and surgery use effective-map identity: a host carried or contained by a holder on the Symbiant's map stays active, while no effective map or a different effective map is dormant. A real active-to-dormant transition creates one neutral right-edge letter with a concise localized label, the full conditional warning in its body, and both host and Symbiant as look targets; the health text shows the same warning. Sharing the map again reactivates the same bond without reassignment or duplicate hediffs, and same-map carrying or containment creates no letter. The Symbiant never boards an Odyssey gravship or enters another pawn holder: a non-relocation, non-kill despawn safely severs, destroys, removes it from `WorldPawns`, and discards it, while `Pawn.Kill` creates a valid Symbiant corpse and safely detaches a living host. A gravship launch or Metal Hell closure removes the Symbiant before vanilla's pawn-transfer/holder snapshot. Automatic or direct removal of a temporary site, camp, settlement, ordinary pocket map, or space map performs equivalent cleanup before `MapDeiniter` can transfer pawns; a linked host still spawned on that map survives and follows vanilla's normal `WorldPawns` transfer. Remote host death starts retreat, while remote-host Symbiant destruction or shared-pool failure also clears the bond safely.
- Symbiant-targeted damage runs through the real worker and drains the pool once by `DamageResult.totalDamageDealt`; it never creates a real host injury. Plain exact `Hediff_Injury` anatomy is pruned after the packet, effectful fire/stun/additional/custom conditions remain active, and the health facade keeps Symbiant part health full, pain zero, required capacities full, downed/dead false, and summary health equal to the pool fraction. Direct host-targeted injury damage retains the existing reduced ordinary white injury. Host packets share only when the live worker is `DamageWorker_AddInjury` or a subclass, or when `SymbiantSharedHealthDamageExtension` explicitly opts a custom def in; the extension may also opt one out. Unknown custom workers fail closed at the coupling boundary even when `harmsHealth=true`, while their native target-local effect still runs. Tests must retain contradictory metadata controls in both directions: an AddInjury worker with `harmsHealth=false` still shares, while an effect worker with `harmsHealth=true` does not. Firefoam, stun, the outer EMP packet, `SeismicWave`, pregnancy, anesthesia, smoke, and other local effects therefore cannot drain the pool or authorize a bond death. If a worker emits a real secondary injury packet, that nested packet is classified independently and shared.
- Shared-pool exhaustion is the sole lethal coupling transition. Any direct Symbiant `Kill`/`Destroy`, unexpected lifecycle removal, ordinary host hediff addition/removal, or unrelated health-state reevaluation must preserve the living counterpart and terminate or retain the bond according to its own lifecycle rule without manufacturing a coupling death.
- An active host receives inert whole-body grey damage-history rows whose cumulative amounts equal Symbiant-targeted pool loss. Visible labels use the compact localized form `Crack: 40 damage`; the surrounding bond group and detailed tooltip explain their Symbiant-echo semantics. Seven stable named categories plus a compact `Other` row is the hard maximum; echoes are not tendable, naturally healing, AutoHeal-eligible, lethal, or health-affecting. Different-map dormancy contains zero echo objects, same-map holders retain them, reunion reconstructs the persisted ledger without duplicates, and bond cleanup removes them. Exact grey is required in vanilla; alternate Health tabs must retain explicit text and tooltip semantics without exceptions.
- Any pool drain resets recovery. No healing occurs for the first quiet hour; at the boundary and each subsequent quiet hour, the Symbiant restores `max(1, missing * 0.05)` shared health without consuming growth, food, medicine, or colony work. Damage resets the delay, save/load preserves the timing, recovery works while the exact host is dormant, and history echoes remain cumulative instead of shrinking as health recovers.
- Pawns on goo have difficulty-scaled movement, work, and tend penalties without damage, hediffs, disease, filth, or item destruction. The linked host is exempt from its own Symbiant's penalties, and the current values are visible in the Symbiant info text.
- Focused save/load keeps ordered cells, host link/id, recreated hediff, acquired benefits, relocation/uprooted state, and renderer state. Same-process no-symbiant save switching proves stale static-cache prevention. Broader save/load still needs feed request, active motions, retreat state, and true multi-map coverage.
- Disabling symbiant events while a symbiant already exists stops future spawns without silently deleting the active symbiant.
- Lowering `symbiantMaxCells` below current cell count stops further growth without deleting existing cells.
- Multi-map behavior keeps one active symbiant per map and prevents cross-map host selection.
- Default 400-cell stress reaches the configured cap without growth beyond it.
- Bridge-only 4000-cell stress reaches the source/render ceiling without changing saved gameplay settings and without shader buffer or save/load errors.
- Ordinary hostile humanlike and mechanoid attackers may select the Symbiant without an artificial priority bonus; player/friendly pawns, animals, turrets, zombies, and Anomaly-specific attackers remain excluded. Ranged LOS/range/cover/projectiles use one exposed logical cell consistently, melee reaches B and strikes C while damaging target A, and an explosion overlapping multiple cells damages the organism at most once.

Follow-up coverage:
- Full save/load matrix: feed request, active motions, retreat state, and renderer resources, plus exact-host link preservation while separated across real map switches and while the host is held by a shuttle or world object.
- Host edge matrix: children, prisoners, slaves, guests, temporary joiners, quest lodgers, host deletion, optional-mod holograms, non-flesh pawns, existing hosts, and late/active zombie infection. Cross-map travel variants must prove dormant effects, remote damage safety, same-host return, remote death, and origin-map abandonment.
- Natural-base disruption: work-stat, tend-speed, path-cost, item-reach behavior, and multi-room relocation in a more organic colony layout.
- Feeding variants: humanlike/non-humanlike corpse freshness and body-size pulse strengths.
- Endurance/performance: bridge-only 4000-cell render ceiling, many-pawn pathing stress, and normal-play log cleanliness over longer runtime.

## S-Fleshmass-Collision

Status:
- Completed on 2026-07-16 against installed Steam RimWorld `1.6.4871 rev597`
  and Assembly-CSharp MVID `967ddb80559449f0a776dafa26a855d1`.

Fixtures:
- Clean immutable base: `ZL_Fleshmass_Base_00.rws`.
- Reusable save/load fixture: `ZL_Fleshmass_Collision_00.rws`.
- Active-Biotech category fixture: `ZL_Ticking_Player_100.rws`; its recorded
  mod identifiers were checked before loading it.

Bridge contracts:
- `zombieland/fleshmass_collision_contract` is the fast direct 36-case
  contract for required defs, category ordering, settings defaults/copy/
  timeline/import, patch registration, shared target gates, heart immunity,
  `OnlyColonists`, suicide arming, and response accounting.
- `zombieland/fleshmass_collision_scenario` is the reusable async scenario.
  Its `all` stage reloads the clean base before each of `attacks`,
  `response`, `sourceLoss`, `saveLoad`, and `dense` so one stage cannot
  contaminate the next. The ordinary `response` case uses a connected 7x7
  sourced field and requires a witnessed live `AttackStatic` to destroy its
  selected root plus at least four additional cells; isolated one-cell
  `attacks` cases are only the category and smash-mode matrix.

Runtime evidence:
- Normal Anomaly/Odyssey profile direct contract
  `op_d4dfd7ecacfe4136b8efdc6ce5176f36` passed 36/36. The same final DLL pair
  on the compatible all-official-DLC save passed 36/36 in
  `op_b3c165266582463596f697356e37e9cf`; with Biotech active it proved an
  ordinary child maps to `Ordinary`, while child former-colonist and child
  special zombies map to `FormerColonistAndSpecial`.
- Consolidated operation `op_a42ed6862ad642dda5d70266355eaa84`
  passed all five live stages. Attacks passed 12/12. Ordinary response
  destroyed 8 cells and advanced `1 -> 162`; suicide response observed real
  arming/explosion, destroyed 13 cells, and advanced `1 -> 174`. Both spawned
  an assault-lord fleshbeast and leaked zero response letters.
- The 2026-07-17 review follow-up made the live cascade criterion explicit.
  Focused response operation `op_806da6193fd54ff598b438b11dd0706b`
  witnessed an ordinary `AttackStatic`, destroyed its selected root and 5/49
  connected cells, and reported `liveCascadeObserved=true`; the suicide case
  destroyed 12/49. Focused attack operation
  `op_a13fdfced8d94539a89af85a4381ada0` passed 12/12, including valid tank
  route parents equal to both intended route destinations. Post-cleanup map
  inspection found zero named scenario things and zero matching tank corpses.
  Final direct operation `op_f721cb311edf482d987542057780b23b`
  passed 36/36 and identified the exact consolidated explosion prefix and
  finalizer. All three operation-scoped warning queries were empty.
- Source loss was captured during a real `AttackStatic` full-body strike. The
  stored heart source was destroyed/unspawned before zombie-attributed damage;
  response stayed at `1000` after the attack and after eight later
  `KillFinalize` decay cells, with no new letters.
- Save/load paused during an in-flight full-body `AttackStatic`. The job,
  stance, three settings, and two-keyframe timeline persisted; normal AI
  reacquired after load, destroyed the target, and left the observer colonist
  alive.
- The dense two-heart fortress used 120 zombies. It reached 62 concurrent
  actions, resumed 46 after the observation boundary, destroyed 118 flesh
  cells, preserved all 49 fortress pieces and both colonists, spawned six
  fleshbeasts including four assault-lord members, and leaked zero response
  letters.
- The live settings dialog showed all three default-on category checkboxes and
  the `Only colonists` faction note. The note was proved after a real
  temporary UI change to `OnlyColonists`, then `Everything` was restored.
- Final normal-profile warning query `op_f43f531aa38841fbb6510ffc1d24d423`
  returned no warning-or-higher entries. The active-Biotech child-contract
  warning query `op_9621fddbcfcf4a208a6b1c9709cf6939` was also empty after
  the startup cursor. After restoring the original mod profile,
  `op_d9bc174aa2bd4de79fa30807b3305071` confirmed `attackMode=Everything`
  and all three flesh settings enabled; restored-profile warning query
  `op_11406429912b47618e08527683d76f2e` was empty. The game was left paused
  on `ZL_Fleshmass_Base_00`.

Completion:
- Category gating, response accounting, touching fields, concurrent attacks,
  source loss, save/load, dense-horde behavior, settings persistence/timeline,
  live UI wording, exact Biotech child ordering, and clean logs are covered.
